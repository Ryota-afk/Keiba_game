# ARCHITECTURE — 生きた仕様参照

現行の**計算式・定数・「触ると何が壊れるか」**の正本。⚠️**履歴は書かない**
（履歴は`DEVLOG.md`の索引と`devlog/waveNN.md`）。仕様を変えたら**同じコミットで必ず更新する**。

⚠️**このファイルが仕様の正本。** `devlog/waveNN.md`の変数表は「その弾で何を決めたか」の
履歴であり、**後の弾で覆った決定は反映されない**。仕様を確認するときは必ずここを読む。

> ⚠️**このファイルが空だったことで実際に事故が起きかけた。** 第1弾-A/Bの設計中、
> 仕様の正本が無いまま`devlog/wave01.md`の変数表が代役を務め、6箇所が古いまま残った
> （落馬率の要因・判断カードの効き幅・開示の条件・乱数固定の粒度など）。
> 3回目の抜け漏れチェックで発見。**設計が決まった時点でここへ集約する。**

**状態**：第1弾（-A/-B/-C/-D/-E）の設計が確定。実装は`src/`の最小骨組みのみ。
**第2弾の範囲**：最小ループ（夢のダービー→卒業式→キャラ作成→デビュー→1年目の終わり）を
人が通せる形まで。⚠️**見た目は仮（四角と文字）。ドット絵は後の弾。**
計算式・定数の具体値は**実装の弾で置く**（下記の「暫定」表記を参照）。

---

## 1. キャリアと時間

| 項目 | 仕様 |
|---|---|
| デビュー | **18歳** |
| 上限 | **48歳（30年）** |
| 総プレイ時間 | ⚠️**確定値を置かない。プレイスタイル次第**（下記） |
| 時間の進み方 | **週次。基本は毎週止まる。** ⚠️流したいときは**条件設定とカレンダースキップ**をプレイヤーが能動的に使う |
| 1週の流れ | ⭐**月曜に依頼一覧** → 平日（行動枠）→ 金曜の騎乗確定（競馬場→鞍→**脚質の指定**・天候の予報を見る）→ 週末のレース（カード2回）→ 結果（§2） |
| カレンダースキップ | 指定日まで自動で進む。⚠️**「必ず知らせる6項目」で止まる**（早い方） |
| 終わり方 | 自主引退／クリア引退／オート消化／強制引退の4通り |
| クリアの定義 | **凱旋門賞を勝った時点でクリア**。以後いつでも引退を選べる（続けてもよい） |
| 引退勧告 | **評判が3本目の線を割った状態が続く**（§6）／または**40歳以降、毎年 成績次第** |
| オート消化 | **引退を宣言した後だけ**使える。⚠️キャリア途中では使えない |

### ⚠️ プレイ時間は結果であって前提ではない（第1弾-Eで訂正）

⚠️**当初「総プレイ時間25時間・約5,000判断」を確定値として書いていたが、誤りだった。**
プレイヤーが**乗る鞍数を能動的に選べる**以上、時間は結果である。

**目安の内訳**（毎週止まり、週1〜2鞍を操作した場合）：

| 内訳 | 年あたり |
|---|---|
| 週次（依頼一覧から場所と鞍を選ぶ＋平日の行動） | 52週 × 約3 ＝ **156** |
| レース中の判断カード | 78鞍 × 2枚 ＝ **156** |
| **合計** | **年312判断**。30年で**約9,400** |

1判断18秒で**約47時間**。⚠️**条件設定とカレンダースキップを使えば短くなる。**
⚠️⚠️**計測（§13 Q1）は「基準となるプレイスタイル」を決めてから測ること。**
基準が無いと「実測と設計値の乖離」が測れない。

### 画面の順序（第1弾-Eで確定）

**タイトル（開始年1974／1984／1994・難易度4段を選ぶ）→ 夢のダービー → 卒業式（キャラ作成・所属厩舎）→ 1年目**

⚠️**開始年は夢より前に決まっていなければならない**——夢のダービーの他の17頭は史実ローテで出る。

### ⚠️ 必ず知らせる6項目（飛んだ週でも通知する）

1. **主戦の座を失ったこと**（最優先）
2. 預けた鞍で自分の馬が勝ったこと
3. 怪我と落馬
4. 信頼が大きく動いたこと
5. 新しい依頼が来たこと
6. **疲労が危険水域に入ったこと**

⚠️**この6項目が無いと設計が壊れる。** 主戦の座を失う瞬間も、疲労が溜まる週も、
プレイヤーが操作していない週に起きる。飛ばし方が雑だと気付かない。

---

## 2. 騎乗の構造

| 項目 | 仕様 |
|---|---|
| 名目の騎乗数 | 地位で変動：新人80／中堅250／一線級500／トップ800鞍（**暫定**） |
| 自分で操作する鞍 | **週1〜2鞍＝年約78鞍**。⚠️**どのランクでも同じくらい**（トップでも増やさない） |
| 選び方 | ⭐**まず「どの競馬場へ行くか」を決め、その場の鞍から選ぶ**（下記C2） |
| 関わる馬 | **年約12頭**（78鞍 ÷ 1頭あたり年6.3走）。内訳は**主戦5頭の全レース約31鞍＋単発約47鞍** |
| 預けた鞍 | 騎手本人は乗っている。**騎乗回数に加算する**。⚠️**能力の開示は進まない** |
| 馬に任せる | レース中に判断を放棄できる。⚠️**その鞍では能力の開示が進まない** |

⭐**単発の47鞍が「これから主戦になるかもしれない馬との出会い」になる。**
⚠️年33鞍だった頃は主戦馬の全レースでほぼ埋まり、**新しい馬に乗る余地が無かった**——
出会いの4経路（§7）を作ったのに、乗る枠が存在しなかった。
⭐**12頭に薄く乗るか5頭に集中するかがプレイヤーの判断**になる（分散すれば主戦が取れない）。

⚠️**騎手はレースを選べない**（決めるのは調教師と馬主）。
**同じ週に違うクラスへの依頼が複数来る**形にすることで、どれに乗るかの選択が格の選択になる。

### ⚠️⚠️ 週末に乗れる競馬場は1つ（第1弾-Eで確定）

**JRAは同じ週に2〜3場で開催するが、騎手の身体は1つ。**
⚠️**海外・地方はこの限りでない**（地方は平日開催があるため）。

⭐**選択が階層になる**：**どの競馬場へ行くか → その場の鞍から選ぶ**。
競馬場を選んだ時点で他場の鞍は全部消える。

⭐⭐**新しい奪う側**：**主戦の馬が2頭、別々の競馬場で走る週が必ず来る。片方は諦めるしかない。**
諦めた側は他の騎手が乗り、勝てば主戦の座を失いうる。

### 週の流れ（第1弾-Eで更新）

**月曜に今週の騎乗依頼一覧が届く → 平日（行動枠）→ 金曜に騎乗確定 → 週末のレース → 結果**

⚠️**依頼一覧が平日より先に来ることが重要。** 従来は今週何に乗るか分からないまま
「体を作る／休む」を選んでおり、⚠️**判断材料が無い選択**だった（§0-1が禁じる形）。
⭐今は「今週は重い鞍があるから体力を回復しておく」という判断が生まれる。

| 項目 | 仕様 |
|---|---|
| 依頼の件数 | **乗れる鞍数より多く来る**。⚠️**新人でも選択が成立する数にする** |
| 断るコスト | **信頼は下がらない。乗れば上がる** |
| 所属厩舎の馬 | **優先的に回るが、断れる**。⭐所属の安定と外へ出る冒険の対立が新人期の中心 |
| 依頼の質 | **調教師の信頼 × ランク × 厩舎の強さ** |

⚠️**弱い厩舎で信頼を積んでも良い馬には乗れない。** ⭐どの厩舎に出入りするかが長期の賭けになり、
調教師の「見抜く力」（§8）が生きる。

### 代打騎乗

**空きが出たときに、信頼の高い順で回る。**

1. 先約の騎手が乗れなくなり、**鞍が空く**（落馬・怪我・別のレースと重複）＝**きっかけは運**
2. 空いた鞍が誰に回るかは、その厩舎・馬主への**信頼値の高い順**＝**行き先は積み上げ**

⭐これが**信頼値の3つ目の使い道**（他の2つは依頼の質・無理を通すときの消費）。
⭐**自分が動いていない時に効く**唯一の用途で、平日に人に会う枠の対価になる。

⚠️**実装の必須確認**：代打の空きは**週×馬シードとは別の乱数ストリーム**から引く
（`devlog/wave01.md`§15-4）。共有すると代打の有無で後続の乱数がずれ、C4が壊れる。

---

## 3. 馬

### 能力（9軸）

⚠️**表示形式はウイポ基準**：**スピードとスタミナのみ数値。残り7つは記号（G〜S）。**

| 軸 | 表示 | 着順を何着ぶん動かすか |
|---|---|---|
| スピード | **数値** | 直線で3〜4着分 |
| スタミナ | **数値** | 適距離を外したときだけ大きく垂れる（0〜8着分）。⚠️**非対称**：適距離より短いレースでは能力があまり落ちず、**長いレースでは直線で垂れる** |
| 瞬発力 | 記号 | 末脚。2〜4着分。**勝負根性との大小が脚質を決める** |
| 勝負根性 | 記号 | 最後の粘り。1〜3着分。**瞬発力との大小が脚質を決める** |
| 柔軟性 | 記号 | 単独では動かさない。**スタミナと組んで適距離の「幅」を決める** |
| 賢さ | 記号 | ⭐**着順を直接は動かさない。判断カードの効き幅を動かす** |
| 健康 | 記号 | ⭐**着順を動かさない。落馬確率・離脱期間・疲労を動かす** |
| **パワー** | 記号 | **坂と重馬場**。⭐中山・阪神の直線の坂で失速する |
| **精神力** | 記号 | **遠征**（地方・海外・東西の移動）で弱体化しない |

⚠️**脚質と適距離は導出値**。別枠で持たない。
⭐**パワーと精神力は第1弾-Eで足した**——B2で地方61レース・B8で海外20〜30を入れて遠征の
仕組みができ、F4でコース形状に坂が入ったため（`TODO.md` #2の条件が揃った）。

### ⭐ 適応能力（レース傾向への適応・第1弾-Eで確定）

**血統から導出し、レースで育つ。** ⚠️独立した軸として手で付けない（読む量を増やさない）。

| 適応 | 対応するレース傾向 | 血統の由来 |
|---|---|---|
| 瞬発 | **瞬発戦**（最終盤に一気にペースが上がる） | 日本型の系統 |
| 持久 | **持久戦**（中盤からずっとペースが上がる） | 欧州型の系統 |
| 消耗 | **消耗戦**（スタート直後から速い） | 米国型の系統 |

⭐**育つ経路：同じ傾向のレースに出る／判断カードでその傾向に対応する作戦を選ぶ。**
⚠️**育つ速さを間違えると全部の馬が全適性を持つ。** 計測すること。

**開示の対象外**（2026-09-02・ユーザー）：適応能力は**血統表を見れば分かる**。確率開示の2プール
（§3 開示）には入れない。血統表の系統（日本型／欧州型／米国型）を読み取る仕事をプレイヤーに残す。

**性別**（牡・牝・セン）を持ち、**レースにも効く**：牝馬限定戦があり、斤量も変わる。
⚠️牝馬の引退は**史実馬は史実通り、架空馬は早め**（繁殖に入る）。
⚠️**レース体系に牝馬限定の系統を1本足す必要がある**（B-1で決めた体系の更新）。

### 馬の一生（第1弾-Eで確定）

| 項目 | 仕様 |
|---|---|
| 成長型 | **4種類程度**。ピークの年齢が違う（早い型は2歳後半〜3歳前半、遅い型は4歳後半以降） |
| 能力の成長 | **後天的に伸びる。⚠️ただしスタミナは伸びない**（⭐長距離適性は生まれつきで決まる＝見抜く価値のある軸） |
| 引退 | **年齢と成績と怪我**。⚠️**史実馬は史実の引退年が上限**で、怪我で早まる |
| 出走数 | 1頭あたり**年6.3走**（実測の逆算）。キャリア2〜3年で**生涯15〜20走** |

⚠️⚠️**主戦の座との関係**：馬のキャリアが2〜3年なので、**1頭との関係は実質2〜3年**。
常時5頭が主戦なら**30年で延べ40〜50頭**と深く関わる（§2）。

### 出走馬の決定（H・第1弾-Eで確定）

**各馬が次走の目標を持ち、調教師が決める。**
馬ごとに「前走からの週数・クラス・適距離・芝ダート」があり、調教師が条件に合うレースを選ぶ。
1頭年6.3走＝**約8週に1回**。⚠️史実馬の**重賞**は史実ローテ（下記）が優先する。

⭐**調教師の「得意分野」（§6）がここで働く**——芝が得意な厩舎の馬は芝を多く使う。
⭐依頼一覧（§2）は、この決定の結果として「今週走る馬」から生成される。

### クラスの昇降（J・第1弾-Eで確定）

**勝利数で上がる。降級なし。** 未勝利→1勝→2勝→3勝→オープン。
⚠️オープン以上（G3・G2・G1）への出走条件は勝利数だけでは決まらない（実装の弾）。

### ⭐ 史実の拘束力

**史実は「どんな馬がいつ、どのレースにいたか」を決める。「何が起きたか」は決めない。**

| 史実が決める | ゲーム内で決まる |
|---|---|
| 生まれる年／引退する年（**上限**）／初期能力（⚠️戦績から**導く**）／**重賞のローテーション** | **着順**・獲得賞金・誰が乗るか |

⭐**矛盾が原理的に起きない。** プレイヤーがダービーで史実の2着馬に乗って勝っても、
史実の勝ち馬が2着になるだけで、どの馬も消えないし予定も狂わない。

⚠️**引退だけは例外で、怪我で早まる。** ⭐**自分が乗っているときの落馬で史実の名馬を
終わらせられる。** 「史実馬は怪我をしない」は採らない——**「史実馬に乗っていれば安全」という
歪んだ最適戦略**が生まれ、一番良い馬に乗っているときだけ緊張が消える。

### 馬の供給（第1弾-Eで確定）

| 項目 | 値 | 導出 |
|---|---|---|
| 現役馬 | **約7,600頭** | 190厩舎 × 平均40頭。競走回数からの逆算と一致 |
| 年間デビュー | **約2,400頭** | 現役7,600 ÷ 平均在籍3年 |
| **実名の馬** | **重賞に出走した馬**。年100〜200頭の桁（⚠️要実測）。常時**数百頭＝現役の数%** | 重賞出走表（事実） |
| **架空の馬** | 現役から実名を引いた残り（⚠️要実測。実名が数百頭なら**9割以上**） | 差分 |
| 馬主 | **100人前後**（⚠️実際のJRAは約2,000人） | 下記 |

**実名の線＝重賞に出走した馬**（重賞出走表で定義する）。
⚠️**ウイポの史実馬リストは使わない**——リストの「どの馬を選んだか」と能力値・成長型は
コーエーテクモの査定であり、データベースの著作物に当たる可能性がある（`design/rights-check.md`）。
⭐**生年・引退年・血統・戦績は事実**なので一次資料（JRA公式・Wikipedia）から取り、
**能力値と成長型は戦績から自分で導く**（何歳のどの重賞で何着だったか）。

⚠️**馬主を実数の1/20に減らした理由**：馬主の信頼が重賞の騎乗を決める（§6）ので、
2,000人だと30年で関わる馬主が分散しすぎ、**誰の信頼も積まれないまま終わる**
＝重賞への道が実質閉じる。⚠️代償として1人あたり約70頭と実態より大口になる。

### 血統（架空馬も全頭が父母を持つ）

⭐**父母は必ず繁殖馬である。走って終わった馬は誰の親にもならない。**

| 保存が要るもの | 数 |
|---|---|
| 繁殖牝馬 | 常時**約3,400頭** |
| 種牡馬 | 常時**約250頭** |
| 30年の延べ | **約1万頭**・1頭約30バイト → **約300KB** |

**保存の規則**：全頭が父母IDを持つが、**引退して繁殖に上がらなかった馬は世代が変わったら
捨てる**。⚠️**プレイヤーが乗った馬は別に残す。**

### 馬の名前

**大口馬主だけ冠名を持つ**（「冠名＋語幹」）。小口の馬は語の組み合わせ。

⭐**これで「今どの馬主の馬に乗っているか」を見せる専用UIが要らない。**
名前を見れば持ち主が分かり、⭐**分かるのは重賞に関わる馬主＝判断に必要な相手だけ**になる。
⚠️全馬主に冠名を与えると全部の馬名が同じ形式になり単調になる。

### レースカレンダー（第1弾-Eで確定）

⭐**1年分を持ち、月・週・曜日で固定して30年使い回す。** 30年分の日程を持たない。

| 対象 | 持ち方 | 量 |
|---|---|---|
| **一般競走**（年約3,325） | **実在の番組表を1年分**。競走名も実名 | 約170KB |
| **JRAの重賞**（年約130） | 雛形＋**創設年**でフィルタ。⚠️**重賞出走表（3,900レース×最大18頭）が実名の線・史実ローテ・成長型の推定・カレンダーの4つを兼ねる** | 1MB未満 |
| **地方のダートグレード** | ⚠️**61全部**（Jpn1〜Jpn3・JRA開催16を含む） | 小 |
| **海外** | **20〜30**：ドバイ(3月)→香港(4月・12月)→英仏の夏秋(6〜10月)→BC(11月) | 小 |

**週の定義**：**1年52週。月の第N週は実カレンダー通り**（月によって4〜5週）。

⚠️⚠️**地方を61全部にした理由**：中堅ランクで地方が解禁される（§8）のに、Jpn1だけだと
**解禁から頂点まで途中の目標が1つも無い**。芝には未勝利→条件→オープン→重賞の階段があるのに、
**ダートに階段が無い**状態になる。⭐**ダートで主戦になった馬を育てる先が要る。**
⚠️海外も同じ理由で20〜30にした（凱旋門賞だけだと海外解禁の報酬が空になる）。

**データ源**（実測で確認済み）：

| 用途 | 出典 | 範囲 |
|---|---|---|
| JRAの重賞（2002〜） | `jra.go.jp/datafile/seiseki/replay/YYYY/jyusyo.html`（**事実**） | ⚠️2001以前は403 |
| JRAの重賞（〜2001） | **Wikipedia各レース記事の歴代優勝馬・出走馬**（CC BY-SA 4.0・出典表示） | 未取得 |
| 地方のダートグレード | ⚠️NAR公式は転載が許諾制。**Wikipedia**（各競走の記事）を第一候補に | 未取得 |
| 史実馬の生年・引退年・血統・戦績 | ⚠️JBISは公開目的の利用を禁止（二次利用の申請窓口あり）。**Wikipedia**を第一候補に | 未取得 |
| 史実の馬主・調教師 | **JRA公式の騎手・調教師データ** `/datafile/`（**事実**） | 未取得 |
| 1年分の番組表 | JRA公式 `/keiba/program/YYYY/pdf/bangumi/*.pdf`（47ファイル）。⚠️**取得はできるが本文が読めない**（下記） | 未解決 |

⚠️⚠️**競馬ブック・netkeiba・アホヌラ館・JBISは使わない**——規約で公開目的の利用を明示的に禁止
（`design/rights-check.md`）。⭐**JRA公式（事実は対象外）とWikipedia（CC BY-SA 4.0）の2本に確定**（2026-09-02・ユーザー）。
⚠️Wikipediaのデータは別ファイルに分け、出典表示とライセンス表記を付ける。重要な値はJRA公式で突き合わせる。

⚠️**JRAのPDFの読み方**（2026-09-04に再調査して更新）：`poppler-utils`は入らないが**`pypdf` 6.16.2はプリインストール済み**で、
PDFの取得と構造の読み取りはできる。読めないのは**本文の文字**だけで、原因は
**Identity-H のCIDフォントに`ToUnicode`が無い**こと（`bangumi.pdf`・各開催の`bangumi/*.pdf`・
`datafile/seiseki/report/`の日別全成績PDF、いずれも同じ）。
⭐**glyph IDと文字の対応は1対1で固定されている**——数字は`̌̍̎̏̐̑̒̓̔̕`＝0〜9、
`ະউར`＝未勝利、`৽അ`＝新馬、`উΫϥε`＝勝クラス、`μ`＝ダ、というように読める。
**置換表を1回作れば全PDFを機械的に復号できる**見込み。⚠️ただし表の列構成は`extract_text()`で
失われるため、番組表の全数抽出には座標付き抽出が別途要る。
⚠️`www.jra.go.jp`はAkamaiが断続的に403を返す。同じURLでも数回リトライすれば200になる。
HTMLの経路（`datafile/seiseki/replay/YYYY/jyusyo.html`はShift_JIS）が読めるものはそちらを使う。

### ⭐ 重賞出走表（1つの表が4つの仕事をする）

**年約130重賞 × 30年 ＝ 約3,900レース、約54,600出走レコード。1MB未満。**

1. **史実ローテーション**——どの馬がどの重賞に出たか
2. **実名の線**——重賞に出た馬＝実名にする馬。⭐線の定義と表の中身が完全に一致する
3. **成長型と能力の推定材料**——何歳のどの重賞で何着だったか。⭐**ウイポを見ない**
4. **どの重賞がいつ行われたか**——カレンダー（B）の材料

⭐**1つの表が4つの仕事をする。** 余分なデータを一切持たない。

### 能力の開示（確率式）

⚠️**プールが2つに分かれている。**

| プール | 開く項目 | 開く条件 |
|---|---|---|
| **調教** | 賢さ・健康・脚質・**精神力**（4項目） | 朝の調教で乗る |
| **レース** | 適距離・スピード・瞬発力・勝負根性・底・**柔軟性**・**パワー**（7項目） | **プレイヤーが自分で判断した鞍** |

⭐**馬を知り切るには両方やる必要がある。** 調教に通い、かつレースで自分で乗る。
⚠️**預けた鞍・馬に任せた鞍では開示が進まない。**

**確率**（**暫定**・⚠️プールが2つに分かれたため再計算が必要）：
`p = 0.55 + 0.15 × 連続で開かなかった回数`（上限0.95）。初回のみ確率1.0。

### クラス（10段）

新馬／未勝利／1勝／2勝／3勝／**オープン特別／リステッド**／G3／G2／G1

⚠️**2026-09-04に9段→10段へ修正**（ユーザーの指摘で判明）。**リステッドが1段抜けていた。**
[JRA公式「レースのクラス分け」](https://www.jra.go.jp/keiba/rules/class.html)の原文：
> レースのクラスは上位からGⅠ、GⅡ、GⅢ、**リステッド、オープン特別**、3勝クラス、2勝クラス、1勝クラス、新馬・未勝利までのレースがあり

同ページの収得賞金の算入額でも別扱いになっている（3〈4〉歳以上：オープン特別1,200万円／
リステッド1,400万円。3歳：1,000万／1,200万。2歳：600万／800万）。

⭐⭐**ただしオープン特別とリステッドは実質ほぼ同じ**（2026-09-04にユーザーが明示）。
騎手から見た違いは収得賞金の算入額200万円だけで、プレイヤーには直接見えない。
⚠️**「任される線」（§6）を引くとき、この2段には必要ランクも必要信頼もほぼ同じ値を置く。**
1段違うだけで急に任されなくなる壁を作ると実態と合わない。

**画面に出す言葉（2026-09-04にユーザーが確定・`design/mocks/class-labels.html`で3案を比較）**

| 内部キー | 画面の表記 | 意味 |
|---|---|---|
| `shinba` | 新馬 | 初めて走る馬だけ |
| `maiden` | 未勝利 | まだ1勝もしていない馬 |
| `win1` | 1勝クラス | 1勝した馬 |
| `win2` | 2勝クラス | 2勝した馬 |
| `win3` | 3勝クラス | 3勝した馬 |
| `open` | オープン特別 | 条件を卒業した馬 |
| `listed` | リステッド | 条件を卒業した馬（収得賞金の算入額がやや上） |
| `g3` | GⅢ | 重賞 |
| `g2` | GⅡ | 重賞 |
| `g1` | GⅠ | 重賞の頂点 |

⚠️⚠️**これはCLAUDE.md §7の例外である。書き換えないこと。**
§7は「画面に出す言葉は、競馬を知らない人が読んで分かる言葉だけ」と定めており、
「未勝利」「1勝クラス」はその基準を満たさない。⭐**それでもJRAの呼び方をそのまま使うと
ユーザーが決めた**（2026-09-04）。却下した案は「初戦／0勝／1勝／2勝／3勝／オープン…」と
勝ち数で書くもの2種類。
**根拠**：`design/sakurai-ui.md` #01「分かりやすいUIと様式性の高いUIは相反するものかもしれない。
単純に分かりやすければ良いUIとは言い切れない」／`DESIGN_PRINCIPLES.md`要点10
「競馬の様式（枠番・馬柱・出馬表・勝負服）は強い記号。最初からこれを活かす」。
⚠️**クラス名以外の画面文言に§7の例外は無い**——この例外はクラスの呼び方だけに適用する。
⚠️`src/data/classes.js`の`CLASS_LADDER`は**まだ9段**（`listed`が無い）。実装で揃える。

---

## 4. 騎手

| 種類 | 内訳 | 表示 |
|---|---|---|
| **適性**（10個） | 戦法4（逃げ／先行／差し／追込）・距離4（〜1400／1401-1800／1801-2400／2401〜）・馬場2（芝／ダート） | G〜S |

⭐⭐**距離帯の画面表記は「メートルのみ」**（2026-09-04にユーザーが確定・**全画面共通のルール**）：
`〜1400m` ／ `1400〜1800m` ／ `1800〜2400m` ／ `2400m〜`。
⚠️**「マイル」を画面に出さない**——CLAUDE.md §7「競馬を知らない人が読んで分かる言葉だけを
画面に出す」に照らすと、`mile`（1401-1800m）だけが初見に意味の伝わらない競馬用語だった。
「短距離」「中距離」「長距離」は日常語として通じるが、4つのうち1つだけ用語が混ざる形を
避け、4項目すべてをメートルで統一した。⚠️内部のキー（`sprint`/`mile`/`intermediate`/`long`。
`data/aptitudeCategories.js`）は変えない——**表記だけの規則**である。
⚠️境界の重複（1400が2つの帯に見える等）は画面上は許容する。
| **基礎能力**（2つ） | **度胸**（判断カードの効き幅）・**体力**（落馬しにくさ・疲労の溜まりにくさ） | — |
| **スキル** | 条件を満たすと候補が出て、**選んで習得**。⚠️**枠を消費し、枠はランクで増える** | — |

### 適性の成長

**乗った分だけ伸びるが、総量に上限**。⚠️上限は**ランクで上がる**（Sにできる数：新人2→トップ5）。

⭐**乗る鞍を選ぶことが自分の適性を作る。** 主戦の馬の脚質に自分の適性が引っ張られる。
⚠️**上限を外すと万能騎手になり、「騎手と馬の相性」が消える**（前作の均質化と同型）。

### スキルが動かすもの（4系統）

| 系統 | 動かすもの | ⚠️注意 |
|---|---|---|
| **適性の穴を埋める** | 苦手な距離・馬場・戦法での不利を減らす | ⚠️強すぎると万能騎手ができ、適性の総量上限（L4）が壊れる |
| **判断カードを強くする** | 効き幅・択の数・特定の状況でだけ強い | ⭐プレイヤーの判断そのものが伸びる |
| **馬との関係を強くする** | 開示が速い・主戦に必要な騎乗回数が減る | ⭐主戦の座に直接効く |
| **身体を守る** | 落馬しにくい・疲れにくい・復帰が早い | ⚠️§0-2の「奪う側」を弱めるので、効き幅を小さくする |

⚠️**一覧はウイニングポストを`design/`に参考ノートとして起こしてから作る。**

### ⚠️ スキルの枠（実装時の必須確認）

前作の実測バグ：`hasAbility`が`r.abilities`しか見ず、`maxSlots`が`sim/`から参照されておらず、
**枠の数が効果の発動と無関係だった**（枠を奪うだけで効果は据え置き）。
**枠がちゃんと効果を絞っていることを実装時に確かめる。**

---

## 5. レースsim

### 実力の決まり方

**そのレースでの実力 ＝ 素の能力 × 適性の噛み合い**

⭐**最大の逆転要因は適性**（距離・馬場・戦法）。騎手の適性10個と馬の適性が同じレースに掛かる。

### 斤量（負担重量）と見習騎手の減量（2026-09-04で確定）

⚠️**未実装。** `domain/raceOutcome.js`の`horseStrengthScore`は斤量を一切見ていない。
出典はJRA公式（https://www.jra.go.jp/keiba/rules/weight.html ／ .../rules/index.html ）。

**負担重量は4種類。全部持つ**（ユーザーが確定）。

| 種類 | 決まり方 |
|---|---|
| 馬齢重量 | 2歳・3歳の同一年齢限定戦のみ。2歳9月まで牡55・牝55／2歳10〜12月 牡56・牝55／3歳 牡57・牝55 |
| 別定 | 基本は3歳以上戦で5歳以上の牡・せんが平地58kg・障害60kg（いずれも牝2kg減）。3歳・4歳は下表で減量 |
| 定量 | 別定のうち年齢と性別だけで決まるもの。⭐**GⅠは定量か馬齢重量** |
| ハンデキャップ | 1頭ずつ個別に決める |

⚠️**別定の限界**：JRA公式は「オープンクラスなどの一部のレースは個別に基本となる重量が
設定されています」と書いている。その個別基準はレースごとの番組表にしか無く、番組表PDFは
未復号（TODO #15）。⭐**別定は標準の基準で置き、個別基準は仮とする。**

**別定の年齢減量（平地・kg。括弧内はオープンクラス以外に適用）**

| 距離 | 年齢 | 1月 | 2月 | 3月 | 4月 | 5月 | 6月 | 7月 | 8月 | 9月 | 10月 | 11月 | 12月 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1400m未満 | 3歳 | 7(6) | 6(5) | 5(4) | 4(3) | 4(3) | 3 | 3 | 2 | 2 | 1 | 1 | 1 |
| 1400〜1600m | 3歳 | 8(7) | 7(6) | 6(5) | 5(4) | 4(3) | 4(3) | 3 | 3 | 2 | 2 | 1 | 1 |
| | 4歳 | 1 | | | | | | | | | | | |
| 1600m超〜2200m未満 | 3歳 | 9(8) | 8(7) | 7(6) | 6(5) | 5(4) | 4(3) | 4(3) | 3 | 3 | 2 | 2 | 1 |
| | 4歳 | 1 | 1 | | | | | | | | | | |
| 2200m以上 | 3歳 | 10(9) | 9(8) | 8(7) | 7(6) | 6(5) | 5(4) | 4(3) | 4(3) | 3 | 3 | 2 | 2 |
| | 4歳 | 1 | 1 | 1 | | | | | | | | | |

⚠️南半球産馬の減量表も公式にあるが、本作は史実馬以外を国内生産として扱うため当面持たない。

**見習騎手の減量**

| 男性騎手 | 平地の勝利数 | 減量 | 記号 |
|---|---|---|---|
| 見習騎手 | 30回以下 | 3kg | ▲ |
| | 31〜50回 | 2kg | △ |
| | 51〜100回 | 1kg | ☆ |

| 女性騎手 | 平地の勝利数 | 減量 | 記号 |
|---|---|---|---|
| 見習騎手 | 50回以下 | 4kg | ★ |
| | 51〜100回 | 3kg | ▲ |
| 見習でなくなった後 | — | **2kg** | ◇ |

「見習騎手」＝免許の通算取得期間5年未満かつ平地100勝以下（または障害20勝以下）。
⚠️⚠️**特別競走とハンデキャップ競走では減量しない。**

**⭐ この仕組みがゲームの中で果たす役割**

⭐**減量は「なぜ新人の自分に依頼が来るのか」の答えである。** 実際の競馬でも見習騎手に
乗せる理由は軽くなること。100勝でその理由が消え、それまでに腕で選ばれる騎手に
なっていなければ依頼が途絶える。⭐**CLAUDE.md §0-2「奪う側の設計」が、史実の仕組みとして
完成している。** ⚠️前作の「均す・和らげる・保証する」癖の逆をJRAの規則が提供している。

**ゲームでの扱い（すべて2026-09-04にユーザーが確定）**

| 項目 | 決定 |
|---|---|
| 効き方 | ⭐**距離が長いほど1kgの不利が大きい。** 根拠は上の年齢減量表そのもの——3歳1月の減量が1400m未満7kg・2200m以上10kg。JRAが「距離が長いほど斤量が効く」前提で表を作っている |
| 1kgあたりの不利 | ⚠️**未定。仮値を置き、§10で計測して決める** |
| 減量の適用範囲 | ⭐**特別競走とハンデキャップ競走では減量しない**（史実どおり）。⚠️番組表のレースに`isSpecial`が要る。重賞も特別競走なので重賞では効かない |
| 騎手の性別 | ⭐**選べる。** 女性の恒久2kgは有利だが、⭐**上へ行くほど特別競走が増えて減量が効く場面が減り、差が自然に消える。追加の仕掛けは要らない** |
| 減量が切れる告知 | ⭐**切れた後にだけ告げる。** 手前で予告すると「勝ちたくない」が生まれる。勝った嬉しさの直後に失うものが来る |
| 画面表記 | ⭐**一覧では数字だけ（`57.0kg`）。ラベルを置かない。** 説明が要る場面でだけ「斤量」と書く |

⚠️⚠️**確認していないこと**：「上位クラスはほぼ全部が特別競走」かどうかは一次資料で
確認していない。重賞に名前があること・「オープン特別」がその名のとおりであることは確かだが、
3勝クラス以下で特別競走が占める割合は調べていない。番組表の`isSpecial`は仮値を置いて§10で計測する。

### ⭐⭐ 能力差が着順に変わる場所（第1弾-Eで確定）

**道中は消耗の差として溜まり、直線で開く。**

道中は隊列で流れて速度差が出ないが、⭐**同じ速度を保つのに使うスタミナが馬ごとに違う。**
差は道中に溜まり、**直線で一気に開く**。
⭐**だから「差し返しは無いが、相手が垂れると差し返す」になる——決着は加速ではなく消耗で決まる。**

⚠️⚠️**ここが前作が10年かけて見落とした場所。** 「集団内では全員が同速で能力が捨てられる」
という罠は、⭐**同速で走ること自体のコストを馬ごとに変える**ことで解ける。
**レース傾向（下記）と合わせて2段構えで解いている。**

### ⭐⭐ レース傾向（前作の罠への答え・2段目）

**レース傾向＝そのレースのペース展開。4種類**：瞬発戦／持久戦／消耗戦／総合戦。
強度は**究極 ＞ 超 ＞ 無印**。

⚠️⚠️**傾向は競馬場・レースごとの「なりやすさ」を土台に、
出走頭数・逃げ馬の頭数・強い逃げ馬の存在で揺らぐ。**
⭐**馬群の中の相互作用が1つの変数に集約される**ので、集団内にいても能力が効く。

⭐**出走表を見ればレース傾向が予想できる**ので、金曜の騎乗確定が判断になる。

**大レース傾向の表**は`design/winning-post-race-model.md`にある。
⭐**凱旋門賞＝持久戦●、日本のG1は瞬発戦が中心**（ダービー瞬発戦●・天皇賞秋 瞬発戦●）。
持久戦は皐月賞・天皇賞春・**宝塚記念**・菊花賞・有馬記念。

⭐⭐**P4（凱旋門賞は宝塚記念またはダービーを勝った馬）に後から意味が生まれた**——
**宝塚記念は持久戦◎、ダービーは瞬発戦●**。⚠️**どちらを経由したかで凱旋門賞での強さが変わる。**

### コースと出走頭数

**コースごとに持つ**：距離・直線長・**坂**・回り（右／左）・小回り。
**馬場状態**（良・稍重・重・不良）を持つ。**週×競馬場の乱数で天候が決まり、金曜に予報が見える**。
⭐「重馬場ならこの馬」と選べる。⚠️予報が外れる確率を置く（外れなければ判断ではなくなる）。
**出走頭数は実態どおり**（最大18頭・レースごとに変動）。⭐頭数がレース傾向を揺らす。

### ⚠️⚠️ 集団の見た目の動き（演出）は着順計算の乱数と別のハッシュを使う（前作の教訓）

`Roadrace_Game/ARCHITECTURE.md`に記録された実測の失敗：着順計算に使う`riderHash01`
（`(id*2654435761+salt*40503)%100000`）を集団描画の揺らぎ（演出）に流用したところ、
**`2654435761 mod 100000 = 35761`という数値の偶然で線形写像に退化し、集団が規則的に
動いて見える**（本作の2026-09-03の人間の目視でも「規則的な動きに見える・楕円状の動きが
わかる」という同種の指摘があった）。

⭐**対策**：着順計算用の乱数（C4準拠・`core/rng.js`の`RNG_STREAMS.SIM`等）と、
**集団描画の揺らぎなど演出専用の乱数は必ず別のストリームにする**。前作は演出専用の
`viewHash01`/`viewWander`を新設して解決した。本作でも⑦（レースsim本実装）で
馬群の見た目の位置を計算する際、`RNG_STREAMS`に演出専用の項目（例：`VIEW_WANDER`）を
追加し、**着順に効く乱数を絶対に流用しない**こと。
⚠️**数式が「見た目に十分ランダムに見えるか」は目視でしか確認できない**——前作も
今回も、実装者の計算上の確認だけでは気付けず、実際に動かして人の目で見て気付いた。
⑦で集団の動きを実装したら、**必ず人間が動きを見て「規則的に見えないか」を確認する**こと
（CLAUDE.md §0-4「人間の通しプレイに勝る検証はない」と同じ理由）。

#### 参考：すごろくダービーの実測値（2026-09-03・Opusサブエージェントによる画像解析、1回目）

`design/sugoroku-derby-race-screen.md`のUI構造調査とは別に、**個々の馬の動き方**
（前作の罠と同種の問題が無いか）を、動画から足元の影を色抽出→多物体追跡→カメラの
ズーム/パンを最小二乗で分離、という方法で数値的に確認した（⚠️あくまで**参考動画側の
実測値**。本作の実装が同じ値を採るべき、という意味ではない）。
- **画面更新は20Hz**（0.05秒ごと。60fps記録でも中身は秒間20コマ）。
- **胴体の上下動（走行アニメのループ）は周期約4コマ≒0.2秒・振幅は体高の5〜10%で、
  自己相関に明確な周期性がある**——ただし馬ごとに位相がずれており、全馬が同期して
  跳ねる「行進」にはなっていない。これは正常なループアニメで、前作の罠とは別物。
- 馬同士の距離（詰まる/開く）の変化は非周期的（自己相関|r|≦0.3）で、前作のような
  線形退化パターンは見当たらなかった。
- ⚠️「左右の揺れは実質ゼロ」という1回目の結論は、**2回目の検証（下記）で半分だけ
  正しいと訂正された**——全馬の追跡点への最小二乗カメラ推定という手法自体に
  問題があった。

#### ⚠️⚠️ 訂正：カメラは特定の1頭に固定され、背景（世界）の方がスクロールする
（2026-09-03・ユーザーの指摘を受けたOpusサブエージェントの2回目の検証）

1回目の検証は「全馬の動きの平均」からカメラの動きを推定していたため、**「カメラが
特定の1頭に追従し、その馬は画面上でほぼ動かず、背景と他馬の方が動いて見える」という、
この種の2Dサイドスクロールでは定番の作り方**を正しく切り分けられていなかった。
ユーザーの指摘（「各馬ではなく背景が動いている」）を受けて再検証し、次を確認した：

- **画面上でほぼ固定されている馬が実在する**（t=40〜48秒の区間で、画面x座標が
  8秒間で正味2pxしか動かない）。これはUIの「先頭」ボタンが示すとおり**先頭馬**だった
  （この区間ではカメラが先頭馬に追従する設定になっていた）。
- ⭐**プレイヤーの馬は固定されておらず、この区間では画面を左へ流れて画面外に押し出されて
  いた**（先頭に立てていない場面のため）。
- **背景（柵の支柱・生垣）は実際に水平スクロールしている**（支柱の格子位相解析で
  平均約900px/秒・8秒で画面幅5.3枚分に相当する移動を確認）。
- ⚠️**芝の縞模様は動いていない**——ただしこれは芝が背景と別レイヤーだからではなく、
  **縞が進行方向と平行な横帯（横方向の模様を持たない）ため、水平スクロールしても
  視覚的な変化が出ない**という説明が付いた（本作のモックは芝を垂直の縞
  `repeating-linear-gradient(90deg, ...)`で表現しており、参考とは縞の向きが異なる）。
- **1回目の「左右の揺れ実質ゼロ」は半分だけ正しい**：カメラに追従されている馬（先頭馬）は
  見かけ上ほぼ動かないが、**それ以外の馬は画面上でも確実に動いている**（毎秒18〜21px、
  後退方向）。この「後退の速さの違い」こそが、着順・隊列の差を見た目で表現する仕組み。
  ⚠️つまり実装すべきは「各馬をそれぞれ独立に規則性の無い乱数で揺らす」ことではなく、
  **1頭（カメラの追従対象）を画面上の基準点とし、他の馬と背景をその基準点との相対速度で
  動かす**という描画モデルである。

⭐**本作⑦への示唆**：2026-09-03の追加指摘（「全く動いていない」「ゴール板が右端に
固定されているのはおかしい」）を受け、**モック（`dream-derby-mock2.html`）を上記の
参考の技法どおりに作り直した**——自分/先頭のどちらか1頭を画面上の基準点（自分＝34%、
先頭＝72%。実測値ではなくデザイン上の割り切り）に固定し、他馬・芝の縞・柵（ゲート／
ハロン棒／ゴール標識）をその馬との相対距離(m)からスクロールさせる連続描画にした
（`VIEW_SPAN=220m`を画面の可視範囲とし、それを超えると要素は非表示になる。ゴール標識が
レース序盤は見えず、残り約145m＝ゴールまで一定距離に近づいて初めて画面に入ってくるのは
このモデルの直接の帰結）。⑦で本実装する際は、この連続描画の考え方（1頭をカメラ基準に
固定し、他は相対距離で動かす）を土台にすることが確認された——ただしこのモックの
`VIEW_SPAN`・カメラ基準位置（34%/72%）・芝の縞の流れる速さは実測に基づかない仮の値であり、
⑦で改めて調整が要る。

#### ⚠️⚠️ 3回目の訂正：カーブ・地面・芝は別レイヤーではなく1つの剛体として動く
（2026-09-03・Opusサブエージェントによる3回目の画像解析。ユーザーの「カーブの表現が変・
レイヤーが全体的におかしい」という指摘を受けたもの）

参考動画のコーナー区間（f_40等）をレイヤーごとにy座標計測したところ、**茶色帯の上端・
生垣の上端・芝の上端・芝の横縞のすべてが、同じ量（実測+53px、モック換算+16px）だけ
一体で下へ沈んでいた**。直線区間（f_109等）では同じ計測がどの列でも完全に同値になる。
⚠️つまり「柵と生垣の上端だけが弧を描き、茶色の地面と芝(CSS背景)は水平のまま」という
2回目の訂正後のモックの実装は、まだ**カーブ表現としては不完全**だった
（境界レイヤーだけを別扱いにしていた）。

正しいモデル：**画面上のカーブは、地面に貼り付いた全レイヤー（茶・柵・生垣・芝の上端・
芝の縞）を1つの変位関数`dy(u) = amp * sin(π*u)`（u=画面の横位置0..1）で共通に沈める、
剛体としての平行移動**。奥行きによる遠近圧縮は無い（同じdyが芝の全深さで一定）。
振幅は実測で最大53px（モック換算16px）、コーナーの進入・脱出で0→最大→0に変化する
（方向は今回確認できた範囲ではすべて「中央が沈む」向きで統一——逆向きは未確認）。

加えて、**カメラにズーム（寄り引き）がある**ことも判明した（芝の縞の周期比較で
0.66〜1.00倍の幅を確認）。拡大の中心はレース表示部の縦中央。馬群がばらけると引く
傾向はあるが、正確な閾値は未確認（TODO.md #10）。

**画面幅に相当する距離（4回目の再確認・2026-09-03）**：参考動画ではスタートゲートが
発走から約1秒で画面を横切って左へ消え、支柱位相解析でも背景は毎秒画面幅の約0.67倍
流れる。馬の速さ（2400m/111秒≒21.6m/s）から逆算すると**画面幅≒32m**。モックが
当初置いていた160〜220mは10分の1の遅さで、「背景が動いていない」と評された原因だった。
⚠️距離標識「20」の見え方はこの値と整合しない点が残る（`TODO.md` #10）。
⚠️カメラを「追う対象の位置へ一次遅れで追従」させると、連続して走る対象に定常的な遅れ
（速度×時定数。×4では画面幅を超える）が残り、対象が基準位置に来ない／画面外へ出る
（モックで実際に起きたバグ）。**レース進行（全馬共通の基準距離）で説明できない跳び**
（発走時の視点移動・自分/先頭の切り替え・先頭馬の入れ替わり）だけをオフセットに吸収して
減衰（時定数≒0.45秒）させ、連続した走行は遅れなく追うこと。カーブは`sin`の1山ではなく
画面幅より広い弧（放物線で近似）で、画面の端でも傾きが残る。
支柱・生垣の房・距離標識・ゲート・ゴール標識は**コース上の距離に固定された物**として
全部をカメラ距離から画面座標へ写す（芝の横縞だけは流れても見えない）。

⭐**本作⑦への追加の示唆**：境界（茶・柵・生垣）と芝を別々のレイヤー・別々の座標系
（46px角の専用SVG＋CSS背景）として実装するのはこの意味で不十分——**1つの座標系
（1枚のSVG、またはそれに相当する統一された変位関数）で全レイヤーの湾曲を共有させる**
必要がある。ズームも同様に、境界・芝・馬・ゲート・ゴール標識のすべてに同じ拡縮を
掛けること（馬だけ別スケールにしない）。`dream-derby-mock2.html`はこの回で
`#worldZoom`（CSS transform: scaleを共有）と1枚の`#trackBoundary`SVGへ作り直した。

#### 実装移植版（第2弾・`dream-derby-mock2.html`から実コードへ）

上記の設計は`src/data/dreamDerbyCourse.js`（距離・時刻・SVG境界の定数）・
`src/view/dreamDerbyRace.js`（distanceAtTime／gapMetersAt／カメラの一次遅れ／
curveY・curveRow・bandPath等の純関数）・`src/view/dreamDerbySprite.js`（馬＋騎手の
SVGマークアップ）・`src/view/dreamDerbyCommentary.js`（実況の文組み立て）・
`src/screens/dreamDerbyEngine.js`（rAFループ＋DOM書き込みのエンジン本体）・
`src/screens/DreamDerbyScreen.jsx`（Reactの薄い殻）へそのまま移植済み（第2弾）。

⚠️**移植時にモックのバグを1件直した**：モックは`gapMetersAt`が「残り1200m地点」から
最終着差（`finishGap`。自分は常に0＝常に勝つ前提の飾り）へ収束を始めていたが、その
瞬間はまだ直線の判断カード（結果に影響する選択）が選ばれていない。実装では
`domain/dreamDerby.js`の`runDreamDerbyRace`を**直線カードで選んだ瞬間**（結果が必要に
なる直前）にだけ呼び、それ以前の隊列の見え方は結果と無関係な演出専用の揺らぎ
（`viewHash01`。着順を決める`core/rng.js`の乱数ストリームとは別）だけで動かす。
これにより`## 11. 導入`の「勝つ。ただし選択によっては負ける」が文字どおり成立する
（自分が2着以下になるケースをPlaywrightで実測確認済み）。

⚠️**着差(m)・ゴールタイム・上がり3F/4Fの算出式は「仮」のまま**（`domain/dreamDerby.js`の
`PAR_SCORE`・`SECONDS_PER_SCORE_POINT`・`METERS_PER_SCORE_POINT`等）。`horseStrengthScore`
（仮simの強さ指標）の差を線形にmへ・秒へ変換しているだけで、消耗式・レース傾向（本節）とは
まだ繋がっていない。⑦のレースsim本実装で正式な値へ差し替える対象として明示しておく。



**逃げ／先行／差し／追込を宣言する。** 馬の得意脚質から外してもよい。
⭐騎手の戦法適性4つ（§4）がここで使われ、⭐**逃げ馬の数がレース傾向を揺らす**ので自分の作戦が
レース全体のペースに効く。NPC騎手も同じ規則で選ぶので、出走表から傾向が予想できる。

### 判断カード

| 項目 | 仕様 |
|---|---|
| 回数 | **1鞍に2回**（道中の位置取り・直線の仕掛けどころ） |
| 択の数 | **状況で2〜4**。状況の類型は**5種類**（下記）。⚠️これ以上増やさない（非支配性を測りきるため） |
| 動かすもの | **適性の不利を部分的に覆せる** |
| 覆せる量 | **基準幅 × 馬の「賢さ」× 騎手の「度胸」× 疲労** |
| 上限 | ⚠️**適性1段ぶんの差を超えないこと**（超えるとM1の「適性が主役」が崩れる） |

⚠️スタートはプレイヤーの判断にせず、馬の能力と運で決まる。

**出す時期は距離で決める**（2026-09-03・ユーザー指示・夢のダービーのモックで確定）：
道中のカードは**レースのちょうど半分の地点**（2400mなら残り1200m）、直線のカードは
**最終直線に入った瞬間**（4コーナー出口。以前のモックの「直線入りの16秒後」は遅い）。
時間ではなく距離で節目を取る（`timeAtDistance`）。択には「何を得て何を失うか」を1行添える
（例：内で我慢して進路を待つ＝脚は溜まる／前が開かなければそのまま終わる）。
実況（下記）が選択を見て反応する。

**実況**（同日・ユーザー指示）：レース中のテキストはプレイヤー一人称の地の文ではなく、
テレビ実況のようなアナウンサーの声にする。局面ごとの定型文をその時点の実際の隊列
（先頭〜5番手・自分の順位・1000m通過・着差）で穴埋めする。⚠️実在の実況の有名な文句は
一言一句使わない（放送の著作物。口調・情景の切り取り方だけを手本にしたオリジナル）。
モック（`dream-derby-mock2.html`の`COMMENTARY`・`say()`）が定型と穴埋めの雛形。

### ⭐⭐ 判断カードは馬を育てる（B-5のriskの答え）

⚠️B-5は**「適性が着順の大部分を決めるので、判断カードが飾りになりかねない」**という
risk を抱えたままだった。

⭐**答え：カードは着順だけでなく、馬の適応能力を育てる。**
**その鞍で勝てなくても、選んだ作戦が馬を作る。**
⭐**「今日は勝てないが、この馬を持久戦向きに育てておく」という長期の判断が生まれる。**

### 状況の5類型（自分の馬の位置と周りの馬で決まる）

**包まれた／前が開いた／前が遠い／馬が引っ張る／隊列が落ち着いた**

⭐**前作が10年かけて見落とした「馬群の中では前の馬が速度を決める」と同じ軸なので、
simの中身と判断カードが同じ変数を見ることになる。** 択が飾りにならない。

---

## 6. 奪う器官

### 主戦の座

**同じ馬に5回乗り、その間に1勝すると主戦。**
**5回は累積。間に他騎手が乗ってもリセットしない。失うのは他騎手が乗って勝ったときだけ。**
⚠️得やすく失いやすい非対称が意図どおり緊張を生むかは計測（§13 Q3）。

| 層 | 何が奪われるか | 頻度 | リロードで回避 |
|---|---|---|---|
| 毎週 | 選ばなかった馬に乗る機会 | 毎回 | **できない** |
| 数ヶ月 | 主戦の座（他騎手が勝った） | 年2〜3回 | 選択次第 |
| 数年 | 落馬による全ての主戦の座 | 30年で5〜8回 | **できない**（下記C4） |

⭐**主戦に「1勝」を課したことで、レースの格を選ぶことが主戦の座に直結する。**
勝てる条件戦で確実に取るか、格上に挑んで賞金と評価を取るか。

### 落馬・怪我

**基準 p=0.008/鞍**（**暫定**）を、**馬の「健康」・騎手の「体力」・疲労**で増減させる。
離脱は**骨折13週／打撲2週**（**暫定**）。⚠️離脱中の週は画面を出さず飛ばす。

### 疲労

**その週に何鞍乗ったかで決まる**（名目の鞍数が直接効く）。奪うものは3つ：
**判断カードの効きが落ちる／騎手の能力が落ちる／落馬率が上がる**。

⚠️**トップになるほど疲れる**（週15鞍）。支えるのは体力・体づくり（ショップ）・平日3枠。
⚠️**支えきれないと「トップになることが罰になる」逆転が起きる。** 計測で確認すること。

### 信頼（第1弾-Eで確定）

⚠️**必ず画面に出す。** 見えない閾値で干すことだけは禁止（前作の失敗）。

**信頼は3種類ある。**

| 種類 | 相手 | 何を決めるか |
|---|---|---|
| **調教師への信頼** | 厩舎ごと（**190件**） | **日常の鞍**——依頼が来るか、主戦の座を取れるか、調教に出入りできるか |
| **馬主への信頼** | 馬主ごと（**100人前後**・§3） | ⭐**大きな鞍**——**重賞に乗るには馬主の信頼が要る**。上げ方は**その馬主の馬で結果を出す（主）＋人に会う（副）** |
| **評判** | 全体に1つ | **信頼の上位数件の平均**（件数は実装の弾）。**3本の線はここに引く**（下記） |

⭐**育てた馬で重賞に行けない、という痛みが生まれる。** 主戦の座は調教師の信頼で取れるが、
その馬が重賞に出るとき馬主が別の騎手を指名しうる。
⚠️**自分が5回乗って勝たせた馬の、一番大きなレースだけ他人が乗る。**

⭐**史実の騎手の壁（§10）は、これで新しい機構を使わずに表せる**——
「馬主がそちらを信用している」だけでよい。

⚠️**信頼値の一覧に出すのは、関わったことのある相手だけ。** 190件を並べたら読めない。

⚠️**評判を合計にしてはいけない**——190件に広く浅く出入りするだけで上がり、
主戦の座（1頭に5回乗る）と**逆のインセンティブ**になる。上位数件の平均なら
⭐**深い関係を数本持つことが評価され、主戦の座と同じ方向を向く**。

⭐**日常の鞍が大きな鞍に繋がる**：未勝利戦でどの馬主の馬に乗るかが、
何年か先の重賞の椅子を決める。⭐持ち主は**大口馬主の冠名**で分かる（§3・専用UIは不要）。

**評判に3本の線**：

| 線 | 起きること |
|---|---|
| 1本目を割る | **難易度が1段下がる**（救済） |
| 2本目を割る | **依頼が来なくなる**（干される） |
| 3本目を割った状態が続く | **引退勧告** |

**使い道**：**「無理を通す」ときに消費する**。⚠️**消費して線を割れる**（警告は出す）。
「無理」は2種類：**断った依頼を後から取りに行く**／⭐**主戦でない馬の重賞に乗せてもらう**
（馬主の信頼が足りない重賞に、調教師の信頼を消費して割って入る＝**2系統を跨ぐ唯一の手段**）。

**悪事イベント**：⚠️**第2弾に入れる**。**小口馬主から、所持金が一定以下の週に「このレースで手を抜けば
金を出す」と持ちかけられる。** 受けたらそのレースは必ず負け、評判が大きく下がる。**断ってもコストは無い。**
⭐金に困っているときだけ来るので、断るだけの操作（§0-1）にならない。
⭐**プレイヤーが自分の判断で信頼を捨てる道**——これが無いと信頼が下がる経路は「負け続ける」
だけになる。⚠️**3本の線が実装済みでないと結果が見えず、「金が増えるだけのボタン」になる。**

### 調教師（第1弾-Eで確定）

| 項目 | 仕様 |
|---|---|
| 数 | **190件**（JRAの実数。貸付馬房3,838÷1人約20馬房） |
| 管理頭数 | **平均40頭**（上限は貸付馬房数の2.5倍）。⭐190×40＝**約7,600頭**が現役の実頭数 |
| 能力 | **4軸＋特性**：育てる力（馬の能力が伸びる速さ）／見抜く力（良い素質馬を持ってくる）／仕上げ（レース当日の調子）／得意分野（**芝ダート2×距離4＝8種類**。⚠️2026-09-04に6→8へ増やし、騎手の適性の距離帯（`sprint`/`mile`/`intermediate`/`long`）と同じ軸に揃えた。以前は`intermediate`に対応する厩舎が無く、日本ダービー（2400m）の距離帯の受け皿が存在しなかった）。⚠️調教師モードでは軸を足す |
| 見せ方 | **信頼値の閾値3段**。1段目＝得意分野／2段目＝育てる力／3段目＝見抜く力・仕上げ・特性 |
| 名前 | **もじる**（騎手・馬主と揃える） |
| 世代交代 | **引退と新規開業がある**。⭐信頼を積んだ調教師の引退は奪う側になる |

⚠️**馬の確率開示（§3）とは別の仕組み。** 馬は騎乗回数に応じた確率、調教師は信頼値の閾値。
⭐考え方は共通（関わるほど分かる）だが、**実装は別々の関数になる**。

### NPC騎手（I・第1弾-Eで確定）

| 項目 | 仕様 |
|---|---|
| 人数 | **約160人**（JRAの実数）。引退とデビューがある |
| 能力 | ⭐**プレイヤーと同じ型**：適性10個＋基礎能力2つ（§4）。⚠️簡略化するとプレイヤーにだけ適性の逆転が起きる非対称になる |
| 名前 | **もじる**。史実の騎手は実在をモデルにする |
| 鞍の得方 | ⭐**プレイヤーと同じ規則——信頼で鞍を得る**。⚠️プレイヤー専用の分岐を作らない |

⭐**主戦の座の争奪が対称になる**：プレイヤーが主戦の座を失ったとき、**誰が取ったかに理由がある**
（その騎手がその厩舎に通っていた）。奪う側に顔がついた。

⚠️**実装の必須確認**：160人が毎週190厩舎の全鞍を信頼順で取り合う計算は重い。
**「所属厩舎＋主戦馬のリスト」を持ち、空いた鞍だけ信頼順で埋める**形にする。

### 所属厩舎の決まり方

**卒業式で3件提示され、簡単な特徴だけ見て選ぶ。**
⭐開示の1段目（得意分野）が、そのまま選ぶときの材料になる。⚠️不完全な情報での選択（§0-1）。
見せるのは**調教師名・得意分野・一言**の3つだけで、一言は**得意分野の言い換え**にとどめる
（`data/stableRemarks.js`）——育てる力・見抜く力は信頼を積むまで見せない開示規定を破らない。

⭐⭐**候補の選び方（2026-09-04に確定）**：成績表の得意（最も高い距離帯×高い方の馬場）と
**馬場も距離も一致する厩舎を候補から除外**し、残りから3件引く。新人には選択権が無く、
声をかけてくれた厩舎しか選べない、という筋を通した設計。
⚠️これにより**どの厩舎を選んでも最初は不利な鞍に乗ることになる**が、§4「乗る鞍が自分の
適性を作る」が働くので、選択の意味が「今の自分を活かす」から
**「これからどんな騎手になるか」**に変わる（§0-1の「正解が表示されない」を満たす）。
実測：8種類が190厩舎にほぼ均等分布するため、3件とも「馬場も距離も合わない」になる確率は
(3/7)³≈**7.9%**。約92%の回で半分だけ噛み合う厩舎が1件以上出る。
⚠️候補が2件以下になったら除外条件を無視して補充する。

### プレイヤーの所属（第1弾-Eで確定）

**新人は厩舎に所属し、実績でフリーになれる。**

⚠️⚠️**これは第1弾-Aから空いていた穴を埋めるもの**——「新人が最初の鞍をどう得るか」が
一度も書かれていなかった。信頼ゼロの新人に依頼が来る理由が無く、売り込みも断られうる。
⭐**フリーになること自体がランク昇格の報酬になる。**

⚠️⚠️**売り込みに信頼を要求してはいけない。** 信頼を消費するのは「無理を通す」ときだけ。
E4の「自分から売り込む」は**行動枠を使うだけ**で、これが**干されたときの出口**（§7・調教に通うと2本）。
混同すると**詰みが発生する。**

### ⚠️ セーブとやり直し（C4）

**乱数の目は「週 × 馬」から導出して固定する。** リロードしても同じ馬に乗れば同じ結果。
**運はやり直せず、判断だけやり直せる。**

⚠️**実装で外すと機能しない点（3つ）**：
1. **種は「週」ではなく「週 × 馬」から導出する。** 週だけで決めるとどの馬に乗っても落馬する
2. **乱数ストリームを用途ごとに分ける**（落馬判定／開示判定／他騎手の動き／レースsim）。
   1本の列を順に消費すると、**プレイヤーが違う行動をした時点で順序がずれ、運まで変わる**
3. **週の種をセーブに含める**

⚠️**落馬の判定は「目を固定して、閾値を疲労で動かす」形にする。**
「落馬するかどうか」自体を先に決めるとC4が壊れる。

---

## 7. 得る側の環

**信頼 → 出会い → 主戦の座 →（失う）**

### 出会いの4経路

| 経路 | 平日の枠 | 性格 |
|---|---|---|
| 朝の調教で乗る | 使う | 信頼の高い厩舎に出入りし、若い馬を先に見抜く。⭐**通うこと自体で信頼が少し上がる**（⚠️「乗らずに通うだけ」が最適にならない幅に） |
| 騎乗依頼が向こうから来る | 使わない | 信頼値とランクが高いほど良い馬 |
| 自分から売り込む | 使う | ⚠️**干されたときの出口**（調教に通うと並ぶ2本のうち1本）。**成功するとその厩舎から来週の依頼が1件来る**（依頼一覧に乗る）。成功率はランクと評判。断られることがある |
| 牧場・セリを見に行く | 使う | デビュー前の馬を押さえる。配合の産駒を先に押さえる経路にもなる |

### 平日の行動（7種類）

朝の調教で乗る／自分から売り込む／牧場・セリを見に行く／人に会う／体を作る／休む／買い物に行く

⚠️**新人は1枠なので毎週6つ捨てる。**

**⭐「何も選べない週」は構造的に存在しない。** 7種類のうち**体を作る／休む／買い物に行く**は
相手が要らないため常に選べる。⚠️干されている時期でも手が止まらない
（売り込みが「出口を開ける」装置なのに対し、こちらは「出口が無い週でも進める」装置）。

⚠️**ただし退屈が無いことは意味しない。** 相手が要る行動が1つも選べない週が続けば§0-4の
退屈そのもの。**その連続長を計測する**（§13）。

---

## 8. 騎手ランク（6段）と報酬

昇格は**実績のみ**（ライバル戦はゲートにしない）。

| ランク | 昇格条件 | 平日の枠 | 増える行動 | 行ける場所 | ショップ | スキル枠 | 適性の上限 |
|---|---|---|---|---|---|---|---|
| 新人 | — | 1 | 調教／売り込む／休む | 中央 | 騎乗用具 | 2 | S 2個 |
| 若手 | 初勝利 | 1 | ＋買い物 | 中央 | ＋体づくり | 3 | 2 |
| 中堅 | 重賞勝利 | 2 | ＋人に会う | ＋地方 | ＋情報 | 4 | 3 |
| 実力派 | G1勝利 | 2 | ＋体を作る | **＋海外** | ＋移動と拠点 | 5 | 4 |
| 一流 | 最多勝利騎手 | 3 | ＋牧場・セリ | 全部 | 上級品 | 6 | 4 |
| トップ | 凱旋門賞勝利 | 3 | 全部 | 全部 | 全品 | 8 | 5 |

（枠・スキル枠・適性上限の具体値は**暫定**）

⚠️**売り込みは新人から使える。** 中盤で解禁すると、新人期に信頼を落としたプレイヤーに
出口が無くなる。**この制約は動かさない。**

### 騎手の収入（第1弾-Eで確定・実際の仕組みどおり）

| 配分先 | 割合 |
|---|---|
| 馬主 | 本賞金の**80%** |
| 調教師 | 10% |
| **騎手（進上金）** | **5%** |
| 厩務員 | 5% |

**騎乗手当**（着順に関係なく乗れば出る）：G1 **64,500円**／その他重賞 **44,500円**／
その他 **27,500円**（⚠️現在の額。1974・1984年は別）。

⭐**2本立てが対立を作る**：**騎乗手当は乗った鞍数に比例し、進上金は勝った時だけ跳ねる。**
「数を乗る」と「大きいのを勝つ」が別々の収入源になる。

**⚠️ プレイヤーは馬を持てない。** 所有せず、乗せてもらう立場を最後まで貫く。

### 継続的な支出（金の抜き場）

**遠征費／拠点の維持費／身体のケア／人付き合い。**

⚠️**騎乗手当だけで週15鞍のトップは年2,100万円**入る。前作は**恒久的な使い道が
キャリアの1/3で枯渇した**。⭐**継続的な支出は一度買って終わりにならない**ので枯渇しない。

⚠️**額が判断に繋がる形にすること**（定期支出は「承認するだけの操作」になりやすい・§0-1）。
⭐例：**地方の安い鞍に遠征すると赤字になる**ので、行くかどうかに金が絡む。

⚠️⚠️**人付き合いの費用は「信頼を買う」のではなく「信頼を積む行動を取るための必要経費」。**
平日に「人に会う」を選ぶと金がかかり、金が無ければその行動が取れない。
⚠️**しかし金を払っただけでは信頼は上がらない**——行動枠を使うことが必須。
⭐これで**悪事で得た金では信頼が買えず**、悪魔の釜が働き続ける（K1の決定と両立する）。

### ⭐ 配合への関わり方（馬を持たないまま）

**自分が主戦だった馬について、馬主や生産者に「この馬と配してほしい」と進言できる。
信頼が高いほど通る。**

⭐**信頼の4つ目の使い道**（他3つ：依頼の質・無理を通す消費・代打の順番）。
⚠️**通らないことがある。それ自体が奪う側**——自分が育てた馬の血を残せないまま終わる道が実在する。

### ショップ（4系統）

騎乗用具（能力・落馬率）／体づくり（騎手ステータス・怪我の復帰）／
**情報**（⚠️開示が1段だけ進む・**暫定**）／移動と拠点（行ける場所）

⚠️**人脈は採らない**——信頼が金で買えると、悪事イベント（金は入るが信頼が極めて下がる）の
悪魔の釜が働かなくなる。
⚠️**情報に上限を置かないと**、E2の緊張が消えて判断が「金を払うか」だけになる。

---

## 9. 難易度（悪魔の釜）

| 項目 | 仕様 |
|---|---|
| 粒度 | ゲーム全体の設定。**4段** |
| 変更 | **年の区切りで年1回**。⚠️いつでも変更できると「勝てそうな時だけ上げる」が最適解になる |
| 効く対象 | **相手の強さ** |
| リターン | **賞金・信頼の倍率＋良い馬の依頼が増える** |
| 前払いのコスト | 段を上げた瞬間から相手が強くなる。1年戻せない |
| 自動低下 | **評判が1本目の線を割ると自動で1段下がる**（救済・§6） |

⚠️**相手が強くなる一方で良い馬にも乗れるため、差し引きが見えにくい。**
4段それぞれで勝率と累積賞金の両方を測り、段に対して単調かを確認すること。

---

## 10. 世界

| 項目 | 仕様 |
|---|---|
| 実在競馬 | **レース名・コース名は実名。史実馬は重賞に出走した馬まで実名**（§3）。⚠️権利関係は`design/rights-check.md` |
| ゲーム内で生まれた馬 | **架空**。⭐史実と原理的に矛盾しない |
| ライバル騎手 | **実名をもじる**。⚠️馬にパブリシティ権が無いという判断は生身の人間には及ばないため、馬とは分けた |
| 時代 | **過去から始めて史実をなぞる。開始年は選択式** |
| 開始年 | **1974／1984／1994の3本**（終点は2004／2014／2024） |
| 開始年の制約 | ⚠️**開始年＋30 ≦ 現在**、かつ**終点が史実の固まっている年**であること。⭐史実が尽きる状態を作らないので、架空馬への移行の仕組みが要らない |
| 年間のレース数 | **3,455**（2025年実測。1990年代以降3,400〜3,450で安定） |
| 行ける場所 | 地方は**ダートグレード61を開催する全場**、海外は**主要G1のある地域**（フランス／イギリス／アイルランド／ドバイ／香港／アメリカ／豪州。§3のカレンダー参照） |
| 海外の馬 | ⚠️**乗れない。日本の馬で遠征するだけ。** 海外の厩舎・馬主・騎手は作らない（相手馬としてだけ存在） |
| 凱旋門賞 | **最高ランクへの昇格条件＝キャリアの頂点**（勝った後も続けられる） |

### ⭐ 史実の名馬に乗れるか

**乗れる。ただし史実でその馬に乗った騎手が壁になる。**

⚠️**新しい機構は要らない。** 史実でその馬に乗った騎手（NPC・§6）を、**その馬の主戦として
NPC騎手の「主戦馬リスト」に初期設定し、馬主がその騎手を信用している状態**にするだけでよい。
§2の代打騎乗（空きが出たら信頼の高い順）と§6の主戦の座（他騎手が乗って勝ったら失う）がそのまま働く。

⭐**史実の騎手を退けるには、その馬主への信頼で上回るしかない**（重賞は馬主の信頼で決まる・§6）——
つまり**その馬主の馬で結果を出すこと＋平日に人に会う枠が、名馬に乗るための道になる**。
⚠️新しい定数は「史実の騎手が初期に持つ主戦馬の重み」1つだけ。

### 凱旋門賞への道

1. **乗れるのは自分が主戦の馬だけ**
2. **その馬が日本のG1を2つ以上勝つ**
3. **その年の宝塚記念またはダービーを勝つ**
4. → **調教師から打診される**（プレイヤーからは申し込めない）

⭐**ダービーが条件に入っているので、導入（夢のダービー）の再現がそのまま頂点への切符になる。**
⚠️**壁が1本立つ**：良い馬に出会えないまま30年が終わる道が実在する。
救済（評判の1本目→難易度低下→良い馬の依頼）が効くかを計測で確認すること。

---

## 11. 導入（最初の3分）

**夢の中でトップジョッキーとして日本ダービーに騎乗 → ゴールすると夢から覚めて
競馬学校の卒業式 → そこからキャラメイク。**

**勝つ。ただし選択によっては負ける**（⚠️負けてもすぐ覚めて先へ進む）。

⭐**奪う体験が導入そのものに入っている**——夢から覚めた瞬間、トップジョッキーの力を全部失う。

### 夢の中の馬

**架空の馬。後にプレイヤーの主戦馬の父になる。**

⚠️**配合（TODO #3）が実装されるまで意味が発生しない。** 第2弾では導入としてだけ働く。
⚠️⚠️**父になる馬のIDを第2弾のセーブに残すこと。** 残さないと後の弾で回収できない。

### キャラ作成（卒業式）

⚠️⚠️**2026-09-04に全面的に作り直した。** 旧版は「適性10個にポイントを振る。夢の騎乗内容に
応じた推奨配分が最初から入っている（変更可）」だったが、次の理由で**撤回した**（経緯と
MAGIシステムでの検討は`devlog/wave02.md`）：
- **夢→推奨配分の写像規則が作れない**——夢は東京・芝・2400m固定なので、距離4・馬場2の
  6項目は夢から導けない（情報がゼロ）。
- 適性は`growAptitude`で後から伸びる（§4「⭐乗る鞍を選ぶことが自分の適性を作る」）ため、
  初期配分の影響は1〜2年で消える。10項目を振らせる操作は費用に見合わない。
- **推奨配分が入っていて変更可**は、初見にとって承認するだけの操作になり、§0-1
  「正解が表示される選択は判断ではない」に抵触する。

**画面の流れ**：①名前入力 →②式典（「卒業証書授与、〇〇」と呼ばれる）→③成績表と夢の記録
→④所属厩舎を選ぶ。上半分＝絵、下半分＝メッセージと選択肢（夢のダービーと同じ2分割）。

**プレイヤーの判断は2つだけ**：
1. **夢の記録を受け入れるか、決別するか**。夢の判断カード2回から**道中×直線の2×2**
   （前へ行く/動かない × 早く仕掛ける/溜める）で戦法1つを導き、受け入れるとその戦法が
   D・他3つがG〜E（尖る）、決別すると4つともE（尖らないが穴もない）。
2. **声をかけてきた厩舎3件から1件**（下記§6）。

**適性の初期値の由来**：
| 適性 | 由来 |
|---|---|
| 距離4・馬場2 | **競馬学校の成績表**（`saveSeed`から生成・プレイヤーは選べない）。距離4のうち1つがD・残り3つがG〜E、馬場2のうち1つがD・もう1つがG〜E。⚠️最高値を各グループで1つだけ立て、同点処理を不要にする |
| 戦法4 | **夢の判断カード2回**（上記） |
| 度胸・体力 | 乱数。⚠️**新人ランクは下限40**（判断カードが機能する最低ラインの保証。NPCの新人にも同じ下限を適用し、プレイヤー専用の分岐を作らない） |

⚠️**全部Gスタートにはしない**——何にも乗れず成立しない（最高がDなので満たしている）。

**成績表の見せ方**（2026-09-04にモックを3案比べて確定。経緯は`devlog/wave02.md`）：
- 項目ごとに**細い棒**（高さ6px）＋**棒の右端に記号**（G〜S）。棒の長さは**`位置 / 7`**
  （G=0%・F=14.3%・E=28.6%・D=42.9%・S=100%）。⚠️`(位置+1)/8`にするとDが50%まで伸び、
  「8段階の下から4番目」が「そこそこある」と読めてしまう
- ⚠️**記号は棒の右端に付ける**（トラックの右端に固定しない）。記号が棒と一緒に動くことで
  「記号が全部左に寄っている＝全部低い」が一目で伝わる。これがこの画面の第一の仕事
- ⚠️**最高値の2項目（得意な距離・得意な馬場）だけ`accent`**、残り4項目は無彩色＋`muted`の記号。
  6項目すべてを`accent`で塗ると§9「アクセントは少数の値に限る」に反し、階層が消える
- **スケール（`G ——— S`）は見出し直下に1度だけ**置く。6本のトラックは左右が揃うので各行に
  置くとうるさい。⚠️縦線は引かない
- ⚠️**実装の注意**：S（100%）の項目が出ると記号がパネル右端からはみ出す。右マージンを確保すること

---

## 12. ファイル構成と検証

→ **`DEVLOG.md`§1・§2**が正本。

---

## 13. 計測（実装の弾で必ず回す）

→ **`devlog/wave01b.md`のB-6**に11項目＋測り方。⚠️**加えて面白さ指標Q1〜Q13**（下表）。

⚠️**CLAUDE.md §0-3が「バランス指標だけでなく面白さ指標を、設計と計測の両方に含める」と
要求している。** 当初の11項目は全てバランス指標（勝率・金額・到達時期・確率）で、
面白さ指標が1つも無かった。

| ID | 測る対象 |
|---|---|
| Q1 | **意味のある判断の密度**（年ごとの分布）。⚠️**基準プレイスタイルを決めてから測る**——第1弾-Eで「総プレイ時間25時間」を撤回したため、比較対象が消えた |
| Q8 | **実際に何頭に乗り、何頭で主戦を取れたか**。⚠️薄く広く乗るのが常に有利／不利なら、この判断は実質1択（Q2） |
| Q9 | **年ごとの所持金の推移**。⚠️単調増加なら抜き場が足りていない（前作はキャリアの1/3で使い道が枯渇した） |
| Q10 | **適応能力が育つ速さ**。⚠️速すぎると全部の馬が全適性を持ち、レース傾向の意味が消える |
| Q11 | **9軸それぞれが着順を何着ぶん動かしたか**。⚠️前作は軸を足しても着順を動かさない「飾りの軸」が生まれた |
| Q12 | **調教に通うだけで信頼がどこまで上がるか**。⚠️「乗らずに通うだけ」が最適なら幅を下げる |
| Q13 | **天候予報が外れる割合**。⚠️外れなければ予報を見ることが判断でなくなる |
| Q2 | **選択肢に本当の対立があるか**（判断カードだけでなく鞍選び・平日の行動・難易度も） |
| Q3 | **危機を感じる瞬間があるか**——**回数と間隔の両方**。⚠️回数だけだと「30年で十分に起きているが全部前半に集中していた」を見逃す |
| Q6 | **代打騎乗が年に何回来るか／うち重賞は何回か**。0に張り付けば信頼への投資が空振りし、多すぎれば「乗る馬を自分で選ぶ」（§6）が薄まる |
| Q7 | **相手が要る平日の行動が1つも選べない週の連続長**。⚠️3ヶ月続くなら干され状態の設計を見直す（§7） |

⚠️**測り方の必須手順**：`Math.random`・`Date.now`・乱数ストリームを**3点固定**。
A/B比較では各アームの直前に**全アーム共通の副シードへ張り直す**。
**固定しないと得られる数字はすべて無意味**（前作は2回踏んでから気付いた）。

---

## 14. localStorage キー一覧

（実装の弾で確定する。⚠️**セーブに週の種を含めること**）

---

## 15. ⚠️ 未定義のまま実装に入ると詰まる箇所

| 項目 | 状態 |
|---|---|
| 牧場・セリを見に行く効果 | ⚠️「デビュー前の馬を押さえる」が何を起こすか未定（一流で解禁・最小ループ外） |
| 斤量がsimにどう効くか | ⚠️性別で変わると決めたが、効き方が無い |
| 地方遠征が平日の行動枠を消費するか | ⚠️地方は平日開催。未定（中堅で解禁・最小ループ外） |
| 年の区切りの処理順と時点 | 難易度変更・引退勧告・馬の引退／デビュー・世代交代・ランク判定。実装の弾 |
| オープン以上（G3・G2・G1）への出走条件 | ⚠️勝利数だけでは決まらない（§3 J） |
| NPC騎手160人の初期の所属と主戦馬 | ⚠️史実の騎手はモデルから、架空は生成。未定 |
| ~~キャラ作成の初期適性~~ | ⭐2026-09-04に確定（§11）。成績表＝距離4・馬場2、夢の判断カード＝戦法4、度胸・体力＝乱数＋下限40 |
| **スキルの一覧** | 4系統は確定（§4）。⚠️個々のスキルは`design/`の参考ノートを起こしてから作る |
| **騎手側にレース傾向への適性を持たせるか** | ⚠️**未定**。騎手の適性10個は戦法4・距離4・馬場2で、レース傾向への適性が無い |
| **9軸を画面にどう出すか** | ⚠️CLAUDE.md §7「その情報は本当に要るか」。⚠️**9軸すべて出すのか** |
| 各競馬場のコースデータ（距離・直線長・坂・回り・小回り） | JRA10場・主要距離のスタート位置／コーナー構成／直線長／坂は`design/jra-course-geometry.md`（2026-09-03収蔵）。⚠️回り・小回りの分類や全距離の網羅は未完で、⑦で使う前に要再確認 |
| **クラスごとのレース数の配分** | 実装の弾で置く。⚠️下位クラスほど多い（JRAの分布）ことだけ確か |
| 疲労の増減・各クラスの賞金・ショップの価格・夢のカード数 | 実装の弾で置く |
| **夢の中の馬** | 性格は確定（架空・後に主戦馬の父／§11）。⚠️**どんな馬か**は実装の弾で置く |
| 確率開示の速度 | ⚠️**プールが2つに分かれたため再計算が必要**。初回の確率1.0をどちらに適用するかも決める |

### ⚠️ 数値を置くときの制約（数値そのものではなく、満たすべき条件）

- **ショップは品目ごとにメリットとデメリットを極端に振る**（`DESIGN_PRINCIPLES.md` #21）。
  ⚠️どれを買っても少しずつ良くなるだけなら、買い物が判断にならない（§0-1）
- **キャラ作成の初期適性を全部Gにしない**——何にも乗れず成立しない
- **判断カードで覆せる幅は適性1段ぶんを超えない**（§5）
- **史実の騎手の初期信頼値**は、平日に人に会う枠を何年か積まないと届かない高さに置く（§10）

### ⚠️⚠️ 仕組みが無いもの

⚠️**「仕組みが無い項目はゼロ」と3回目のチェックで結論したが、間違っていた。**
5回目のチェックで7件（A〜G）が見つかり、**第1弾-Eで7件とも設計した**（`devlog/wave01e.md`）。
⚠️**6回目のチェック（同ファイル末尾）でさらに見つかったものは、下の表が正本。**

⚠️⚠️**§15は「気付いた未定義」を集めた表である。気付いていないものは入らない。**
この表が空に近づいたことは、設計が完成した証拠にならない。
**弾を締める前に「この書類だけを渡されて実装できるか」を1回通すこと**（`TODO.md`）。
