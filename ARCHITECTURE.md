# ARCHITECTURE — 生きた仕様参照

現行の**計算式・定数・「触ると何が壊れるか」**の正本。⚠️**履歴は書かない**
（履歴は`DEVLOG.md`の索引と`devlog/waveNN.md`）。仕様を変えたら**同じコミットで必ず更新する**。

⚠️**このファイルが仕様の正本。** `devlog/waveNN.md`の変数表は「その弾で何を決めたか」の
履歴であり、**後の弾で覆った決定は反映されない**。仕様を確認するときは必ずここを読む。

> ⚠️**このファイルが空だったことで実際に事故が起きかけた。** 第1弾-A/Bの設計中、
> 仕様の正本が無いまま`devlog/wave01.md`の変数表が代役を務め、6箇所が古いまま残った
> （落馬率の要因・判断カードの効き幅・開示の条件・乱数固定の粒度など）。
> 3回目の抜け漏れチェックで発見。**設計が決まった時点でここへ集約する。**

**状態**：第1弾（-A/-B/-C/-D/-E）の設計が確定。実装は`src/`の最小骨組みのみ。
**第2弾の範囲**：最小ループ（夢のダービー→卒業式→キャラ作成→デビュー→1年目の終わり）を
人が通せる形まで。⚠️**見た目は仮（四角と文字）。ドット絵は後の弾。**
計算式・定数の具体値は**実装の弾で置く**（下記の「暫定」表記を参照）。

**切り出したファイル**：`arch/horse.md`（§3 馬）／`arch/race-sim.md`（§5 レースsim）／
`arch/race-program.md`（番組表＝1年のレースをどう並べるか。決定A・質問8〜11の結果）／
`arch/historical-horses.md`（史実馬の追加手順）／`arch/taking.md`（§6 奪う器官）／`arch/report-check.md`（ユーザーへ送る文の
検査手順。CLAUDE.md §15の実体）／`arch/ui-fable.md`（デザイン・UIはFableで行う。§2・§8の実体）。⭐**どれもこのファイルと同じく正本。**

---

## 1. キャリアと時間

| 項目 | 仕様 |
|---|---|
| デビュー | **18歳** |
| 上限 | **48歳（30年）** |
| 総プレイ時間 | ⚠️**確定値を置かない。プレイスタイル次第**（下記） |
| 時間の進み方 | **週次。基本は毎週止まる。** ⚠️流したいときは**条件設定とカレンダースキップ**をプレイヤーが能動的に使う |
| 週カウンタ | ⭐**`player.currentWeek`は折り返さない絶対値**（1〜約1,560＝30年×52週）。年は52週ごとに
  `currentYear`が繰り上がる（`domain/weekLoop.js`）。⚠️2026-09-04に「52で1へ折り返す」実装を
  一度書いて自分で壊した——`isDueForNextRace`が`currentWeek - lastRaceWeek`の差分で出走間隔を
  判定するため、年境界で1へ戻すと差分が負に転落し、年をまたいだ馬が二度と出走候補にならなく
  なった。**折り返すのは番組表の週（1〜52）を引くときの`weekOfYear()`（`data/calendar.js`）
  だけ**にする |
| 週の起点 | ⭐**本編は週1ではなく週105から始まる**（`data/calendar.js`の`MAIN_TIMELINE_START_WEEK`
  ＝`BOOTSTRAP_WEEKS`(104)+1。2026-09-20のユーザー決定・`devlog/wave10.md`§13）。
  ⚠️**理由**：`domain/bootstrap.js`が事前シミュレーションで立てた馬の出走計画
  （`horse.plan.targetWeek`）は事前の絶対週なので、本編を週1から始めると計画が指すレースと
  本編の同じ週のレースが**別物**になる（`domain/weeklyCard.js`の番組表は絶対週から作られ、
  実測で中身の一致は4.8%だけだった）。続きの週から始めれば計画がそのまま通用する。
  ⚠️**`resetPlansForMainTimeline`は削除済み**（計画を`null`に戻すと第1〜3週の依頼が0件になる。
  `ROTATION_MIN_LEAD_WEEKS`(3)より先を目標にするため）。⚠️定数の正本は`data/calendar.js`
  （`domain`同士のimportを増やさないため）。表示は`weekOfYear`が折り返すので週105は「1月1週」 |
| 1週の流れ | ⭐**月曜に依頼一覧**（⭐依頼のレース詳細に**出走予定の馬**が並ぶ・枠番と人気は無し。質問25＝(ア)・2026-09-15）→ 平日（行動枠）→ 金曜の騎乗確定（競馬場→鞍→**脚質の指定**・天候の予報を見る。⭐出馬表に枠番と人気が付く）→ 週末のレース（カード2回。⭐直前に同じ出馬表）→ 結果（§2）。⚠️月曜から金曜で出走予定は変わらない（棚上げ#71） |
| カレンダースキップ | 指定日まで自動で進む。⚠️**「必ず知らせる6項目」で止まる**（早い方） |
| 終わり方 | 自主引退／クリア引退／オート消化／強制引退の4通り |
| クリアの定義 | **凱旋門賞を勝った時点でクリア**。以後いつでも引退を選べる（続けてもよい） |
| 引退勧告 | **評判が3本目の線を割った状態が続く**（§6）／または**40歳以降、毎年 成績次第** |
| オート消化 | **引退を宣言した後だけ**使える。⚠️キャリア途中では使えない |

### ⚠️ プレイ時間は結果であって前提ではない（第1弾-Eで訂正）

⚠️**当初「総プレイ時間25時間・約5,000判断」を確定値として書いていたが、誤りだった。**
プレイヤーが**乗る鞍数を能動的に選べる**以上、時間は結果である。

**目安の内訳**（毎週止まり、週1〜2鞍を操作した場合）：

| 内訳 | 年あたり |
|---|---|
| 週次（依頼一覧から場所と鞍を選ぶ＋平日の行動） | 52週 × 約3 ＝ **156** |
| レース中の判断カード | 78鞍 × 2枚 ＝ **156** |
| **合計** | **年312判断**。30年で**約9,400** |

1判断18秒で**約47時間**。⚠️**条件設定とカレンダースキップを使えば短くなる。**
⚠️⚠️**計測（§13 Q1）は「基準となるプレイスタイル」を決めてから測ること。**
基準が無いと「実測と設計値の乖離」が測れない。

### 画面の順序（第1弾-Eで確定）

**タイトル（開始年1974／1984／1994・難易度4段を選ぶ）→ 夢のダービー → 卒業式（キャラ作成・所属厩舎）→ 1年目**

⚠️**開始年は夢より前に決まっていなければならない**——夢のダービーの他の17頭は史実ローテで出る。

### ⚠️ 必ず知らせる6項目（飛んだ週でも通知する）

1. **主戦の座を失ったこと**（最優先）
2. 預けた鞍で自分の馬が勝ったこと
3. 怪我と落馬
4. 信頼が大きく動いたこと
5. 新しい依頼が来たこと
6. **疲労が危険水域に入ったこと**

⚠️**この6項目が無いと設計が壊れる。** 主戦の座を失う瞬間も、疲労が溜まる週も、
プレイヤーが操作していない週に起きる。飛ばし方が雑だと気付かない。

---

## 2. 騎乗の構造

| 項目 | 仕様 |
|---|---|
| 名目の騎乗数 | 地位で変動：新人80／中堅250／一線級500／トップ800鞍（**暫定**） |
| 自分で操作する鞍 | **週1〜2鞍＝年約78鞍**。⚠️**どのランクでも同じくらい**（トップでも増やさない） |
| 選び方 | ⭐**まず「どの競馬場へ行くか」を決め、その場の鞍から選ぶ**（下記C2） |
| 関わる馬 | **年約12頭**（78鞍 ÷ 1頭あたり年6.3走）。内訳は**主戦5頭の全レース約31鞍＋単発約47鞍** |
| 預けた鞍 | 騎手本人は乗っている。**騎乗回数に加算する**。⚠️**能力の開示は進まない** |
| 馬に任せる | レース中に判断を放棄できる。⚠️**その鞍では能力の開示が進まない** |

⭐**単発の47鞍が「これから主戦になるかもしれない馬との出会い」になる。**
⚠️年33鞍だった頃は主戦馬の全レースでほぼ埋まり、**新しい馬に乗る余地が無かった**——
出会いの4経路（§7）を作ったのに、乗る枠が存在しなかった。
⭐**12頭に薄く乗るか5頭に集中するかがプレイヤーの判断**になる（分散すれば主戦が取れない）。

⚠️**騎手はレースを選べない**（決めるのは調教師と馬主）。
**同じ週に違うクラスへの依頼が複数来る**形にすることで、どれに乗るかの選択が格の選択になる。

### ⚠️⚠️ 週末に乗れる競馬場は1つ（第1弾-Eで確定）

**JRAは同じ週に2〜3場で開催するが、騎手の身体は1つ。**
⚠️**海外・地方はこの限りでない**（地方は平日開催があるため）。

⭐**選択が階層になる**：**どの競馬場へ行くか → その場の鞍から選ぶ**。
競馬場を選んだ時点で他場の鞍は全部消える。

⭐⭐**土曜と日曜で別々の競馬場へ行ける（1日1場）**（2026-09-16にユーザーが決定）。
⚠️**2026-09-04の「週末に1つ」から変わった。** `design/mocks/riding-offers-v2.html`の案C
（土日タブ＋競馬場タブ）がそのまま使える形になる。
⚠️`src/domain/fridayConfirmation.js`の`confirmMounts`は**週に1つの`courseId`しか受け取らない**
ので、土日で2つ受け取る形に直す必要がある。

⭐⭐**新しい奪う側**：**主戦の馬が2頭、同じ日に別々の競馬場で走る週が来る。片方は諦めるしかない。**
諦めた側は他の騎手が乗り、勝てば主戦の座を失いうる。
⚠️**1日1場にしたぶん、この締め付けは「週末に1場」より弱い**（2頭が土日に分かれていれば両方
乗れる）。⭐その代わり、断るコスト（上の行）を「主戦の馬を断ったときだけ下がる」に置いたので、
締め付けは**同じ日に主戦が2頭重なった週**へ集まる。⚠️実際に何週に1回起きるかは未計測。

### 週の流れ（第1弾-Eで更新）

**月曜に今週の騎乗依頼一覧が届く → 平日（行動枠）→ 金曜に騎乗確定 → 週末のレース → 結果**

⚠️**依頼一覧が平日より先に来ることが重要。** 従来は今週何に乗るか分からないまま
「体を作る／休む」を選んでおり、⚠️**判断材料が無い選択**だった（§0-1が禁じる形）。
⭐今は「今週は重い鞍があるから体力を回復しておく」という判断が生まれる。

| 項目 | 仕様 |
|---|---|
| 依頼の件数 | **乗れる鞍数より多く来る**。⚠️**新人でも選択が成立する数にする** |
| 断るコスト | ⭐**主戦を張っている馬の依頼を断ったときだけ、その厩舎の信頼が下がる**（2026-09-16にユーザーが決定）。それ以外の依頼は断っても下がらない。⚠️週6件で3件しか乗れず**毎週3件は必ず断る**ので、全件で下げると信頼が一方的に減り続ける。⭐断る痛みを「どの馬の主戦を続けるか」という1つの判断に集める。⭐**実装は`domain/weekLoop.js`の`DECLINE_MAIN_MOUNT_TRUST_LOSS`(2・根拠は無い暫定値)**。52週の通しで信頼が負に落ちる厩舎は実測0件（`devlog/wave09.md`§9） |
| 所属厩舎の馬 | **優先的に回るが、断れる**。⭐所属の安定と外へ出る冒険の対立が新人期の中心。⭐**件数の上限は置かない**（2026-09-17にユーザーが決定・#94を締めた）。実測：条件5通り×52週＝260週で依頼の**74.8%**が所属厩舎、依頼が全部所属厩舎だった週が**36.3%**、1週に出てくる厩舎は平均**2.22軒**（`devlog/wave10.md`§7） |
| 依頼の来る競馬場 | **馬の目標レースがある競馬場から素直に来る。行ける範囲へ寄せることはしない**（2026-09-17にユーザーが決定・#89を締めた）。1日1場なので、実測で**98.8%**の週は同じ日の依頼が2か所以上に散り、どこへ行くかを選ぶと他の鞍は捨てることになる（1日あたり平均2.57か所・同§7）。⭐**捨てる判断が毎週起きるのは意図どおり**——画面は行けない競馬場を理由付きで見せる（合意済みのモック） |
| 依頼の質 | **調教師の信頼 × ランク × 厩舎の強さ** |
| 乗る馬を決めるのは | ⭐**プレイヤー**（2026-09-17・`advanceWeek`の`options.chooseMounts`。`devlog/wave09.md`§12・`devlog/wave10.md`§9）。渡さなければ`defaultChooseMounts`（質の高い順の自動選択）——ヘッドレスの計測はこちら。同じレースは1頭・上限件数の2つは画面で押せなくするのが先で、`confirmMounts`側は安全網（先勝ちで1頭・超過は切り捨て） |
| 月曜の画面 | ⭐**`src/screens/WeekScreen.jsx`＋`WeekScreen.css`＋`src/view/weekOffersView.js`**（2026-09-17・Fable・`devlog/wave10.md`§10）。見本は`design/mocks/week-offers-v3.html`案2（レースが主役・1レース＝1つの表）＋`week-offers-v4.html`案B（行を押すとその行の下に「乗る」と「出馬表」／「この週を進める」は一覧の末尾）。画面の決まり：あと◯件＝`RIDABLE_SLOTS_PER_WEEK`−決めた数／決めた表の残りは薄い／その日に別の場で決めると他の場のタブは薄く・押すと理由（`lockedCourseMessage`）／薄い行は押しても開かない／開く行は1つ／週を進めたら先頭へ戻す。⚠️**見本に無いのに置いたもの**：先週の出来事（怪我・主戦の喪失・信頼の大きな動き）を見出しの下に補足の段で出す（要否はユーザー確認待ち・§10） |

⚠️**弱い厩舎で信頼を積んでも良い馬には乗れない。** ⭐どの厩舎に出入りするかが長期の賭けになり、
調教師の「見抜く力」（§8）が生きる。

### 代打騎乗

**空きが出たときに、信頼の高い順で回る。**

1. 先約の騎手が乗れなくなり、**鞍が空く**（落馬・怪我・別のレースと重複）＝**きっかけは運**
2. 空いた鞍が誰に回るかは、その厩舎・馬主への**信頼値の高い順**＝**行き先は積み上げ**

⭐これが**信頼値の3つ目の使い道**（他の2つは依頼の質・無理を通すときの消費）。
⭐**自分が動いていない時に効く**唯一の用途で、平日に人に会う枠の対価になる。

⚠️**実装の必須確認**：代打の空きは**週×馬シードとは別の乱数ストリーム**から引く
（`devlog/wave01.md`§15-4）。共有すると代打の有無で後続の乱数がずれ、C4が壊れる。

---

### 人気（`domain/popularity.js`・2026-09-20）

出馬表と戦績に出る「1番人気・2番人気…」の並び。⚠️**表示専用で、着順には一切入らない**
（`RNG_STREAMS.POPULARITY`という人気専用の乱数を使い、simの乱数とは独立している）。

⚠️⚠️**馬の本当の能力（`horse.abilities`）は使わない**——人気が「正解表示」になると、
プレイヤーは上位人気の馬に乗るだけでよくなり、CLAUDE.md §0-①が壊れる。
⭐**ユーザー決定（2026-09-20）：公開されている情報だけから作る。**

出走馬ごとに0〜1の材料を重み付きで足し、台形分布の揺らぎ（±0.05）を足して降順に並べる。

| 材料 | 重み | 中身 |
|---|---|---|
| 戦績 | 0.50 | 勝率0.4＋連対率0.3＋前走着順0.3（0戦なら0固定） |
| 騎手ランク | 0.14 | 6段を0〜1へ（騎手不明なら0.5） |
| クラス | 0.12 | 10段をそのまま0〜1へ |
| 収得賞金 | 0.08 | 対数スケール（`DERBY_PURSE_1974`を目安に0〜1へ） |
| 厩舎の強さ | 0.08 | 育てる力・見抜く力・仕上げの平均（厩舎不明なら0.5） |
| 血統（父） | 0.08 | 父の収得賞金（対数）と重賞勝ち数を半々（`sireId`不明なら0.5） |

⚠️**新馬戦は全馬0戦なので、戦績・クラス・賞金が全馬で並ぶ。** 実質「騎手・厩舎・血統＋揺らぎ」
（重み0.30ぶん）だけで並びが決まり、1番人気の勝率は11.5%しかない（実測）。これは意図した形。

**実測（`saveSeed`3通り・1974年開始・助走104週＋本編52週・全98,108走）**：
1番人気の勝率15.4%→**30.4%**（目標は実データの32.3%。`data-src/ahonoora/`1972〜1980の重賞899本）。
頭数帯別は〜8頭34.4%／9〜12頭29.5%／13頭以上24.1%。
⭐**着順は直す前と完全に一致**（98,107走の`{classId, position, fieldSize}`をdiffして差分0行）。

⚠️**`stable.abilities`は画面のどこにも出ていない**（`grep`で確認）。
「公開されている情報だけ」という決定に対して、ここだけは厳密には外れている（`TODO.md` #109）。


## 3. 馬

⭐**本体は`arch/horse.md`**（2026-09-04に切り出した。`TODO.md` #25）。

能力9軸（スピードとスタミナのみ数値・残り7つは記号16段〈G〜S+〉）／適応能力（血統から導出しレースで育つ）／
馬の一生／出走馬の決定（H）／クラスの昇降（J）／史実の拘束力／馬の供給（厩舎数×40頭・190厩舎を下限）／
血統／馬の名前／レースカレンダー／重賞出走表（1つの表が4つの仕事をする）／能力の開示（確率式）／
クラス10段と画面表記。

⚠️**クラスの画面表記はCLAUDE.md §7の例外**（競馬の名前をそのまま使う）。書き換えないこと。
詳細は`arch/horse.md`。

### 3.1 史実馬の取り込み

⭐**本体は`arch/historical-horses.md`**（2026-09-07に作成）。⚠️**今後、何百頭・何千頭と
追加するための手順の正本。** データ源と文字コード／⚠️記号7軸の並び（柔軟性は4番目）／
スピードとスタミナの変換式と較正の根拠／適正距離の式／⚠️較正が有効な範囲（スタミナ47〜81。
外はすべて外挿）／**追加のたびに走らせる検査7項目**（`tools/check-historical.mjs`）。

## 4. 騎手

| 種類 | 内訳 | 表示 |
|---|---|---|
| **適性**（10個） | 戦法4（逃げ／先行／差し／追込）・距離4（〜1400／1401-1800／1801-2400／2401〜）・馬場2（芝／ダート） | 記号16段（G〜S+） |

⭐⭐**距離帯の画面表記は「メートルのみ」**（2026-09-04にユーザーが確定・**全画面共通のルール**）：
`〜1400m` ／ `1400〜1800m` ／ `1800〜2400m` ／ `2400m〜`。
⚠️**「マイル」を画面に出さない**——CLAUDE.md §7「競馬を知らない人が読んで分かる言葉だけを
画面に出す」に照らすと、`mile`（1401-1800m）だけが初見に意味の伝わらない競馬用語だった。
「短距離」「中距離」「長距離」は日常語として通じるが、4つのうち1つだけ用語が混ざる形を
避け、4項目すべてをメートルで統一した。⚠️内部のキー（`sprint`/`mile`/`intermediate`/`long`。
`data/aptitudeCategories.js`）は変えない——**表記だけの規則**である。
⚠️境界の重複（1400が2つの帯に見える等）は画面上は許容する。
| **基礎能力**（2つ） | **度胸**（判断カードの効き幅）・**体力**（落馬しにくさ・疲労の溜まりにくさ） | — |
| **スキル** | 条件を満たすと候補が出て、**選んで習得**。⚠️**枠を消費し、枠はランクで増える** | — |

### NPC騎手と厩舎の結びつき（2026-09-15・質問23＝(ウ)）

**厩舎ごとに主戦騎手1人**。条件戦の馬にはその主戦が乗り、空いていなければ①厩舎が2番目に信頼する騎手
②空いている騎手の勝率順。**オープン以上の馬は馬ごとの主戦騎手を持つ**（オープンに上がった時点で付く）。
1人の騎手の1日の騎乗上限は仮に**8**（根拠なし・§10で測る）。詳細`devlog/wave07.md`。

### 適性の成長

**乗った分だけ伸びるが、総量に上限**。⚠️上限は**ランクで上がる**（Sにできる数：新人2→トップ5）。

⭐**乗る鞍を選ぶことが自分の適性を作る。** 主戦の馬の脚質に自分の適性が引っ張られる。
⚠️**上限を外すと万能騎手になり、「騎手と馬の相性」が消える**（前作の均質化と同型）。

### スキルが動かすもの（4系統）

| 系統 | 動かすもの | ⚠️注意 |
|---|---|---|
| **適性の穴を埋める** | 苦手な距離・馬場・戦法での不利を減らす | ⚠️強すぎると万能騎手ができ、適性の総量上限（L4）が壊れる |
| **判断カードを強くする** | 効き幅・択の数・特定の状況でだけ強い | ⭐プレイヤーの判断そのものが伸びる |
| **馬との関係を強くする** | 開示が速い・主戦に必要な騎乗回数が減る | ⭐主戦の座に直接効く |
| **身体を守る** | 落馬しにくい・疲れにくい・復帰が早い | ⚠️§0-2の「奪う側」を弱めるので、効き幅を小さくする |

⚠️**一覧はウイニングポストを`design/`に参考ノートとして起こしてから作る。**

### ⚠️ スキルの枠（実装時の必須確認）

前作の実測バグ：`hasAbility`が`r.abilities`しか見ず、`maxSlots`が`sim/`から参照されておらず、
**枠の数が効果の発動と無関係だった**（枠を奪うだけで効果は据え置き）。
**枠がちゃんと効果を絞っていることを実装時に確かめる。**

---

## 5. レースsim

⭐**本体は`arch/race-sim.md`**（2026-09-04に切り出した。`TODO.md` #25）。

実力の決まり方（素の能力 × 適性の噛み合い）／⭐⭐能力差が着順に変わる場所（道中は消耗差として
溜まり直線で開く——⚠️前作が10年見落とした罠の答え）／⭐⭐レース傾向（罠への答えの2段目）／
斤量（負担重量）と見習騎手の減量／コースと出走頭数／⚠️⚠️集団の見た目の動きは着順計算の乱数と
別のハッシュを使う／判断カード／⭐⭐判断カードは馬を育てる／状況の5類型。

⚠️**レースsimの実装と計測だけは`claude-opus-5`で行う**（CLAUDE.md §2）。詳細は`arch/race-sim.md`。

⭐**実装は`src/sim/`**（2026-09-06）：`pace.js`（レース傾向）／`stamina.js`（消耗式）／
`raceSim.js`（本体・4段のモデルとフォーク）／`index.js`（窓口）。計算式・定数の正本は
`arch/race-sim.md`「実装（`src/sim/`）」。⚠️`sim/`はDOM・JSX・`domain/`をimportしない。

## 6. 奪う器官

⭐**本体は`arch/taking.md`**（2026-09-20に切り出した。`TODO.md` #103）。

主戦の座（同じ馬に5回乗り、その間に1勝。失うのは他騎手が乗って勝ったときだけ）／
落馬・怪我（基準 p=0.008/鞍・暫定）／疲労／信頼（厩舎・馬主）／調教師／NPC騎手（I）／
所属厩舎の決まり方／プレイヤーの所属／⚠️セーブとやり直し（C4）。

⚠️**この節はCLAUDE.md §0-2「奪う側の設計」の実体。** 数字を緩める方向へ動かす前に、
`arch/taking.md`の該当箇所と、そこに書かれた「なぜその厳しさなのか」を読むこと。

---

## 7. 得る側の環

**信頼 → 出会い → 主戦の座 →（失う）**

### 出会いの4経路

| 経路 | 平日の枠 | 性格 |
|---|---|---|
| 朝の調教で乗る | 使う | 信頼の高い厩舎に出入りし、若い馬を先に見抜く。⭐**通うこと自体で信頼が少し上がる**（⚠️「乗らずに通うだけ」が最適にならない幅に）。⭐**2026-09-12に追加**：通った厩舎の馬の「出来」が言葉で見える（`arch/horse.md`「出来」・ユーザー決定(ア)）。⚠️通う先を選べるようにする工事が要る |
| 騎乗依頼が向こうから来る | 使わない | 信頼値とランクが高いほど良い馬 |
| 自分から売り込む | 使う | ⚠️**干されたときの出口**（調教に通うと並ぶ2本のうち1本）。**成功するとその厩舎から来週の依頼が1件来る**（依頼一覧に乗る）。成功率はランクと評判。断られることがある |
| 牧場・セリを見に行く | 使う | デビュー前の馬を押さえる。配合の産駒を先に押さえる経路にもなる |

### 平日の行動（7種類）

朝の調教で乗る／自分から売り込む／牧場・セリを見に行く／人に会う／体を作る／休む／買い物に行く

⚠️**新人は1枠なので毎週6つ捨てる。**

**⭐「何も選べない週」は構造的に存在しない。** 7種類のうち**体を作る／休む／買い物に行く**は
相手が要らないため常に選べる。⚠️干されている時期でも手が止まらない
（売り込みが「出口を開ける」装置なのに対し、こちらは「出口が無い週でも進める」装置）。

⚠️**ただし退屈が無いことは意味しない。** 相手が要る行動が1つも選べない週が続けば§0-4の
退屈そのもの。**その連続長を計測する**（§13）。

---

## 8. 騎手ランク（6段）と報酬

昇格は**実績のみ**（ライバル戦はゲートにしない）。

| ランク | 昇格条件 | 平日の枠 | 増える行動 | 行ける場所 | ショップ | スキル枠 | 適性の上限 |
|---|---|---|---|---|---|---|---|
| 新人 | — | 1 | 調教／売り込む／休む | 中央 | 騎乗用具 | 2 | S 2個 |
| 若手 | 初勝利 | 1 | ＋買い物 | 中央 | ＋体づくり | 3 | 2 |
| 中堅 | 重賞勝利 | 2 | ＋人に会う | ＋地方 | ＋情報 | 4 | 3 |
| 実力派 | G1勝利 | 2 | ＋体を作る | **＋海外** | ＋移動と拠点 | 5 | 4 |
| 一流 | 最多勝利騎手 | 3 | ＋牧場・セリ | 全部 | 上級品 | 6 | 4 |
| トップ | 凱旋門賞勝利 | 3 | 全部 | 全部 | 全品 | 8 | 5 |

（枠・スキル枠・適性上限の具体値は**暫定**）

⚠️**売り込みは新人から使える。** 中盤で解禁すると、新人期に信頼を落としたプレイヤーに
出口が無くなる。**この制約は動かさない。**

### 騎手の収入（第1弾-Eで確定・実際の仕組みどおり）

| 配分先 | 割合 |
|---|---|
| 馬主 | 本賞金の**80%** |
| 調教師 | 10% |
| **騎手（進上金）** | **5%** |
| 厩務員 | 5% |

**騎乗手当**（着順に関係なく乗れば出る）：G1 **64,500円**／その他重賞 **44,500円**／
その他 **27,500円**（⚠️現在の額。1974・1984年は別）。

⭐**2本立てが対立を作る**：**騎乗手当は乗った鞍数に比例し、進上金は勝った時だけ跳ねる。**
「数を乗る」と「大きいのを勝つ」が別々の収入源になる。

**⚠️ プレイヤーは馬を持てない。** 所有せず、乗せてもらう立場を最後まで貫く。

### 継続的な支出（金の抜き場）

**遠征費／拠点の維持費／身体のケア／人付き合い。**

⚠️**騎乗手当だけで週15鞍のトップは年2,100万円**入る。前作は**恒久的な使い道が
キャリアの1/3で枯渇した**。⭐**継続的な支出は一度買って終わりにならない**ので枯渇しない。

⚠️**額が判断に繋がる形にすること**（定期支出は「承認するだけの操作」になりやすい・§0-1）。
⭐例：**地方の安い鞍に遠征すると赤字になる**ので、行くかどうかに金が絡む。

⚠️⚠️**人付き合いの費用は「信頼を買う」のではなく「信頼を積む行動を取るための必要経費」。**
平日に「人に会う」を選ぶと金がかかり、金が無ければその行動が取れない。
⚠️**しかし金を払っただけでは信頼は上がらない**——行動枠を使うことが必須。
⭐これで**悪事で得た金では信頼が買えず**、悪魔の釜が働き続ける（K1の決定と両立する）。

### ⭐ 配合への関わり方（馬を持たないまま）

**自分が主戦だった馬について、馬主や生産者に「この馬と配してほしい」と進言できる。
信頼が高いほど通る。**

⭐**信頼の4つ目の使い道**（他3つ：依頼の質・無理を通す消費・代打の順番）。
⚠️**通らないことがある。それ自体が奪う側**——自分が育てた馬の血を残せないまま終わる道が実在する。

### ショップ（4系統）

騎乗用具（能力・落馬率）／体づくり（騎手ステータス・怪我の復帰）／
**情報**（⚠️開示が1段だけ進む・**暫定**）／移動と拠点（行ける場所）

⚠️**人脈は採らない**——信頼が金で買えると、悪事イベント（金は入るが信頼が極めて下がる）の
悪魔の釜が働かなくなる。
⚠️**情報に上限を置かないと**、E2の緊張が消えて判断が「金を払うか」だけになる。

---

## 9. 難易度（悪魔の釜）

| 項目 | 仕様 |
|---|---|
| 粒度 | ゲーム全体の設定。**4段** |
| 変更 | **年の区切りで年1回**。⚠️いつでも変更できると「勝てそうな時だけ上げる」が最適解になる |
| 効く対象 | **相手の強さ** |
| リターン | **賞金・信頼の倍率＋良い馬の依頼が増える** |
| 前払いのコスト | 段を上げた瞬間から相手が強くなる。1年戻せない |
| 自動低下 | **評判が1本目の線を割ると自動で1段下がる**（救済・§6） |

⚠️**相手が強くなる一方で良い馬にも乗れるため、差し引きが見えにくい。**
4段それぞれで勝率と累積賞金の両方を測り、段に対して単調かを確認すること。

---

## 10. 世界

| 項目 | 仕様 |
|---|---|
| 実在競馬 | **レース名・コース名は実名。史実馬は重賞に出走した馬まで実名**（§3）。⚠️権利関係は`design/rights-check.md` |
| ゲーム内で生まれた馬 | **架空**。⭐史実と原理的に矛盾しない |
| ライバル騎手 | **実名をもじる**。⚠️馬にパブリシティ権が無いという判断は生身の人間には及ばないため、馬とは分けた |
| 時代 | **過去から始めて史実をなぞる。開始年は選択式** |
| 開始年 | **1974／1984／1994の3本**（終点は2004／2014／2024） |
| 開始年の制約 | ⚠️**開始年＋30 ≦ 現在**、かつ**終点が史実の固まっている年**であること。⭐史実が尽きる状態を作らないので、架空馬への移行の仕組みが要らない |
| 年間のレース数 | **3,455**（2025年実測。1990年代以降3,400〜3,450で安定） |
| 行ける場所 | 地方は**ダートグレード61を開催する全場**、海外は**主要G1のある地域**（フランス／イギリス／アイルランド／ドバイ／香港／アメリカ／豪州。§3のカレンダー参照） |
| 海外の馬 | ⚠️**乗れない。日本の馬で遠征するだけ。** 海外の厩舎・馬主・騎手は作らない（相手馬としてだけ存在） |
| 凱旋門賞 | **最高ランクへの昇格条件＝キャリアの頂点**（勝った後も続けられる） |

### ⭐ 史実の名馬に乗れるか

**乗れる。ただし史実でその馬に乗った騎手が壁になる。**

⚠️**新しい機構は要らない。** 史実でその馬に乗った騎手（NPC・§6）を、**その馬の主戦として
NPC騎手の「主戦馬リスト」に初期設定し、馬主がその騎手を信用している状態**にするだけでよい。
§2の代打騎乗（空きが出たら信頼の高い順）と§6の主戦の座（他騎手が乗って勝ったら失う）がそのまま働く。

⭐**史実の騎手を退けるには、その馬主への信頼で上回るしかない**（重賞は馬主の信頼で決まる・§6）——
つまり**その馬主の馬で結果を出すこと＋平日に人に会う枠が、名馬に乗るための道になる**。
⚠️新しい定数は「史実の騎手が初期に持つ主戦馬の重み」1つだけ。

### 凱旋門賞への道

1. **乗れるのは自分が主戦の馬だけ**
2. **その馬が日本のG1を2つ以上勝つ**
3. **その年の宝塚記念またはダービーを勝つ**
4. → **調教師から打診される**（プレイヤーからは申し込めない）

⭐**ダービーが条件に入っているので、導入（夢のダービー）の再現がそのまま頂点への切符になる。**
⚠️**壁が1本立つ**：良い馬に出会えないまま30年が終わる道が実在する。
救済（評判の1本目→難易度低下→良い馬の依頼）が効くかを計測で確認すること。

---

## 11. 導入（最初の3分）

**夢の中でトップジョッキーとして日本ダービーに騎乗 → ゴールすると夢から覚めて
競馬学校の卒業式 → そこからキャラメイク。**

**勝つ。ただし選択によっては負ける**（⚠️負けてもすぐ覚めて先へ進む）。

⭐**奪う体験が導入そのものに入っている**——夢から覚めた瞬間、トップジョッキーの力を全部失う。

### 夢の中の馬

**架空の馬。後にプレイヤーの主戦馬の父になる。**

⚠️**配合（TODO #3）が実装されるまで意味が発生しない。** 第2弾では導入としてだけ働く。
⚠️⚠️**父になる馬のIDを第2弾のセーブに残すこと。** 残さないと後の弾で回収できない。

### 相手馬17頭（2026-09-06に実名化）

**史実の日本ダービー優勝馬（実名）に差し替えた。** JRA顕彰馬9頭（ミスターシービー〜
コントレイル）は毎回固定、残り8頭は`saveSeed`ごとに抽選する（`domain/dreamDerby.js`の
`pickDreamRivalRecords`）。⚠️**抽選条件は「騎手が既に選ばれた馬と重ならない」ことだけ**
（同じ騎手の馬を引かない。騎手の差し替えはしない）——武豊のようにJRA顕彰馬の騎手と
重なる馬（5頭）は原理的に一度も引かれない。データは`data/derbyWinners.js`
（Wikipedia「東京優駿」・CC BY-SA 4.0。**このファイルだけ**が対象ライセンス）。
騎手名・調教師名は実在の人物のため実名を画面・実況に出さず、`data/derbyPeopleNames.js`の
もじり変換（`displayJockeyName`/`displayTrainerName`。対応表が空なら「？」）を経由する。
⚠️能力値は`generateHorse`のまま「仮」——史実馬の能力を戦績から導くのはレースsimの弾で行う。

### キャラ作成（卒業式）

⚠️⚠️**2026-09-04に全面的に作り直した。** 旧版は「適性10個にポイントを振る。夢の騎乗内容に
応じた推奨配分が最初から入っている（変更可）」だったが、次の理由で**撤回した**（経緯と
MAGIシステムでの検討は`devlog/wave02.md`）：
- **夢→推奨配分の写像規則が作れない**——夢は東京・芝・2400m固定なので、距離4・馬場2の
  6項目は夢から導けない（情報がゼロ）。
- 適性は`growAptitude`で後から伸びる（§4「⭐乗る鞍を選ぶことが自分の適性を作る」）ため、
  初期配分の影響は1〜2年で消える。10項目を振らせる操作は費用に見合わない。
- **推奨配分が入っていて変更可**は、初見にとって承認するだけの操作になり、§0-1
  「正解が表示される選択は判断ではない」に抵触する。

**画面の流れ**：①名前入力 →②式典（「卒業証書授与、〇〇」と呼ばれる）→③成績表と夢の記録
→④所属厩舎を選ぶ。上半分＝絵、下半分＝メッセージと選択肢（夢のダービーと同じ2分割）。

**プレイヤーの判断は2つだけ**：
1. **夢の記録を受け入れるか、決別するか**。夢の判断カード2回から**道中×直線の2×2**
   （前へ行く/動かない × 早く仕掛ける/溜める）で戦法1つを導き、受け入れるとその戦法が
   D・他3つがG〜E（尖る）、決別すると4つともE（尖らないが穴もない）。
2. **声をかけてきた厩舎3件から1件**（下記§6）。

**適性の初期値の由来**：
| 適性 | 由来 |
|---|---|
| 距離4・馬場2 | **競馬学校の成績表**（`saveSeed`から生成・プレイヤーは選べない）。距離4のうち1つがD・残り3つがG〜E、馬場2のうち1つがD・もう1つがG〜E。⚠️最高値を各グループで1つだけ立て、同点処理を不要にする |
| 戦法4 | **夢の判断カード2回**（上記） |
| 度胸・体力 | 乱数。⚠️**新人ランクは下限40**（判断カードが機能する最低ラインの保証。NPCの新人にも同じ下限を適用し、プレイヤー専用の分岐を作らない） |

⚠️**全部Gスタートにはしない**——何にも乗れず成立しない（最高がDなので満たしている）。

**成績表の見せ方**（2026-09-04にモックを3案比べて確定。経緯は`devlog/wave02.md`）：
- 項目ごとに**細い棒**（高さ6px）＋**棒の右端に記号**（G〜S+）。棒の長さは**`位置 / 15`**
  （16段。G=0%・F=13.3%・E=26.7%・D=40%・S+=100%）。⚠️2026-09-07に記号8段→16段化した
  （`design/rights-check.md`§9）際、`位置 / (段数-1)`という比率のままなので式自体は
  変わらない（`gradePercent`＝`GRADE_SCALE.indexOf(grade) / (GRADE_SCALE.length - 1)`が
  分母を自動で16に切り替える）。⚠️`(位置+1)/段数`にすると最高評価未満の項目でも棒が
  詰まって見え、「実際より高く見える」ため使わない
- ⚠️**記号は棒の右端に付ける**（トラックの右端に固定しない）。記号が棒と一緒に動くことで
  「記号が全部左に寄っている＝全部低い」が一目で伝わる。これがこの画面の第一の仕事
- ⚠️**最高値の2項目（得意な距離・得意な馬場）だけ`accent`**、残り4項目は無彩色＋`muted`の記号。
  6項目すべてを`accent`で塗ると§9「アクセントは少数の値に限る」に反し、階層が消える
- **スケール（`G ——— S`）は見出し直下に1度だけ**置く。6本のトラックは左右が揃うので各行に
  置くとうるさい。⚠️縦線は引かない
- ⚠️**実装の注意**：S（100%）の項目が出ると記号がパネル右端からはみ出す。右マージンを確保すること

---

## 12. ファイル構成と検証

→ **`DEVLOG.md`§1・§2**が正本。

### 判断カードは位置で分ける（2026-09-04にユーザー合意・2026-09-06に実装・詳細は`devlog/wave03.md`§10）

**位置の区切り（頭数Nに対する順位）**：先頭＝1番手／前＝2〜4番手／
中団＝5番手〜後ろから5頭目／後方＝それより後ろ。
⚠️**10頭未満は狭める**——前＝2〜3番手、後方＝最後の2頭。
判定は`view/dreamDerbyCommentary.js`の`positionBandOf(rank, fieldSize)`（純関数・戻り値
`"lead"|"front"|"mid"|"rear"`）。
⚠️**状況の文は書かない。事実（順位）だけを出す。** 表示文言は同ファイルの
`positionLabelFor(band, rank)`——先頭は「先頭」、それ以外は「N番手」。
⚠️**内外は出していない**——`view/dreamDerbyRace.js`の`laneY`は着順と無関係な演出専用の
乱数で、自分の馬は常に画面中央固定（0.47）のため、内外を区別する材料が無い
（レーン値に意味付けするための情報源が実装時点で無かった。捏造していない）。

**択の組**：位置4×局面2（道中・直線）＝8組。`data/judgmentSituations.js`に
`dreamMidLead`/`dreamMidFront`/`dreamMidPack`/`dreamMidRear`／
`dreamStretchLead`/`dreamStretchFront`/`dreamStretchPack`/`dreamStretchRear`として持つ
（中団は状況キー内でのみ"Pack"と表記——"dreamMidMid"のような重複を避けるため。位置区分
そのものの値は"mid"のまま）。`screens/dreamDerbyEngine.js`が`domain/judgmentCard.js`の
`dreamSituationId(phase, band)`でキーを組み立て、`choicesFor(situationId)`で択を引く。
どちらも道中カードは`SITUATIONS`（通常レースの抽選プール）には含めない。

**脚質の決め方**（`domain/graduation.js`の`strategyFromDreamChoices`）：残り1200m時点の
位置（`choiceIds.midBand`）を基準段（先頭0＝逃げ／前1＝先行／中団2＝差し／後方3＝追込）とし、
道中の選択の`forward`タグがtrueなら−1・falseなら+1、直線の選択の`early`タグがtrueなら
−0.5・falseなら+0.5して`Math.round`で四捨五入し、0〜3にクランプする
（⚠️2026-09-06にユーザーが「直線は±0.5」と決定。道中の選択が脚質を決め、直線は境目でだけ効く）。
⚠️**選択肢のデータ（`data/judgmentSituations.js`）に`forward`/`early`の2軸タグを持たせている。**
持たせないと、どの状況のカードが出たかに関わらず脚質を決められない。
`screens/dreamDerbyEngine.js`の`pickCardChoice`が選んだ択から`choiceIds.midForward`/
`choiceIds.stretchEarly`を詰め、`choiceIds.midSituationId`/`stretchSituationId`は
`domain/dreamDerby.js`の`runDreamDerbyRace`が`resolveChoice`で効果量を引くのに使う
（位置によって択の組が違うため、択IDだけでは効果量を引けない）。

### 実況は条件（`when`）付きで選ぶ（2026-09-07・devlog/wave04.md §32）

`data/dreamDerbyCommentary.js`の`COMMENTARY`の各行は`{ text, when? }`。`when`が無ければ常に候補。
`view/dreamDerbyCommentary.js`の`pickCommentaryLine(slot, vars, sayCount)`は`when(vars)`が
true（またはwhen無し）の行だけの中から選ぶ。**条件を満たす行が1つも無ければ`null`を返し、
呼び出し元（`screens/dreamDerbyEngine.js`の`say()`）は実況欄に何も足さない**（`sayCount`も
増やさない）。同じスロットで直前と同じ文言が続かないよう、当たったら次の候補へずらす仕組みを
`pickCommentaryLine`内に持つ（`resetCommentaryHistory()`をレース開始のたびに呼んで記憶を消す）。

`when(vars)`が見る主な変数（`commentaryVars`が返す。すべて`vars`という1つのオブジェクトの中）：
- **`selfBand`**：`positionBandOf(selfRank, fieldSize)`の結果（"lead"|"front"|"mid"|"rear"）。
- **`moverName`/`moverNum`/`moverJockey`/`moverIsSelf`**：直近5秒（`t`から`t-5`まで）で
  順位を3つ以上上げた馬（無ければ最も上げた馬。閾値未満なら全部null）。simの時系列
  （`sim.distanceOf(num, t)`をtを変えて2回呼ぶ）から出す——フレームごとの差分ではない。
  `screens/dreamDerbyEngine.js`の`say()`が`ctx.distanceOfNumAt`として`distanceOf`関数
  そのものを渡している。⚠️**これを渡し忘れるとmover系は常にnullになる**（エラーにはならない）。
- **`leaderJockey`**：現在の先頭馬の`jockeyName`（先頭が自分の馬ならnull）。
- **`leadGap2nd`**：先頭と2番手の距離差(m)。`homage`の「独走」判定（3馬身＝7.2m以上）に使う。
- **`split1000Seconds`**：先頭が1000mを通過した時刻(秒)。未通過はnull（フォーマット済み文字列の
  `split1000`とは別に、条件判定用の生の値をそのまま持たせている）。

### ⚠️ 触ると壊れる：夢のダービーのエンジン（`screens/dreamDerbyEngine.js`）

- **スキップは無い。** 2026-09-04にユーザーが「外す」と決めて`skip()`ごと削除した。
  ⚠️**復活させるなら`clearPendingTimers()`で予約を消してやり直す形にしないこと**——
  以前はゴール後に連打すると`doFinish()`が予約した着順表と暗転が消え、ゴール実況だけが増え続けた。
  `doFinish()`は`finished`フラグで2回目を早期returnさせている。
- チュートリアルの発火はレース時計ではなく前のチュートリアルの完了から連鎖する（`devlog/wave03.md`§13）。
  `T_TUT_*`は「何秒あたりに出す想定か」の記録として残っているだけで、コードは参照していない
  （2026-09-06に`GAP_T`を削除した際、参照元が無くなった）。
- ⚠️⚠️**局面の節目は「自分の馬のsim上の通過距離」で判定する**（`milestones`の`atD`）。
  ⚠️**固定の秒に戻さないこと**——simのペースはレースごとに変わり、勝ちタイムは142〜147秒で動く。
  ゴールの節目だけは`sim.winnerTime`と比べる（`isFinish`）。
- ⚠️**エンジンは各馬の距離を自分で作らない。** `sim.distanceOf(馬番, 秒)`だけを読む。
  判断カードを選んだ瞬間に`forkDreamDerbySim`でsimを差し替える（フォーク前の数値は変わらない）。
- **`.screen-stack`（`styles/motion.css`）は黒地。** 自分の背景色を持たない画面をここに置くと黒地に
  黒文字になる（週の画面で実際に起きた・`devlog/wave03.md`§26）。画面を足すときは必ず背景色を持たせる。
- **画面の枠の高さは`min(720px, 100dvh)`**（レース画面は縦マージン64pxを引く）。
  ⚠️**720px固定に戻すとスマホで下が切れる。** `TRACK_W = 390`等の走路の座標定数とは別物なので、
  枠の高さを変えても座標定数は触らないこと。

---

## 13. 計測（実装の弾で必ず回す）

→ **`devlog/wave01b.md`のB-6**に11項目＋測り方。⚠️**加えて面白さ指標Q1〜Q13**（下表）。

⚠️**CLAUDE.md §0-3が「バランス指標だけでなく面白さ指標を、設計と計測の両方に含める」と
要求している。** 当初の11項目は全てバランス指標（勝率・金額・到達時期・確率）で、
面白さ指標が1つも無かった。

| ID | 測る対象 |
|---|---|
| Q1 | **意味のある判断の密度**（年ごとの分布）。⚠️**基準プレイスタイルを決めてから測る**——第1弾-Eで「総プレイ時間25時間」を撤回したため、比較対象が消えた |
| Q8 | **実際に何頭に乗り、何頭で主戦を取れたか**。⚠️薄く広く乗るのが常に有利／不利なら、この判断は実質1択（Q2） |
| Q9 | **年ごとの所持金の推移**。⚠️単調増加なら抜き場が足りていない（前作はキャリアの1/3で使い道が枯渇した） |
| Q10 | **適応能力が育つ速さ**。⚠️速すぎると全部の馬が全適性を持ち、レース傾向の意味が消える |
| Q11 | **9軸それぞれが着順を何着ぶん動かしたか**。⚠️前作は軸を足しても着順を動かさない「飾りの軸」が生まれた |
| Q12 | **調教に通うだけで信頼がどこまで上がるか**。⚠️「乗らずに通うだけ」が最適なら幅を下げる |
| Q13 | **天候予報が外れる割合**。⚠️外れなければ予報を見ることが判断でなくなる |
| Q2 | **選択肢に本当の対立があるか**（判断カードだけでなく鞍選び・平日の行動・難易度も） |
| Q3 | **危機を感じる瞬間があるか**——**回数と間隔の両方**。⚠️回数だけだと「30年で十分に起きているが全部前半に集中していた」を見逃す |
| Q6 | **代打騎乗が年に何回来るか／うち重賞は何回か**。0に張り付けば信頼への投資が空振りし、多すぎれば「乗る馬を自分で選ぶ」（§6）が薄まる |
| Q7 | **相手が要る平日の行動が1つも選べない週の連続長**。⚠️3ヶ月続くなら干され状態の設計を見直す（§7） |

⚠️**測り方の必須手順**：`Math.random`・`Date.now`・乱数ストリームを**3点固定**。
A/B比較では各アームの直前に**全アーム共通の副シードへ張り直す**。
**固定しないと得られる数字はすべて無意味**（前作は2回踏んでから気付いた）。

---

## 14. セーブ（IndexedDB・本筋4で確定・`devlog/wave10.md`§11）

⚠️**localStorageではなくIndexedDBを使う**。実測（150厩舎×5,090〜5,113頭の現行ロースター）で
開始時点5.77MB・52週後6.85MBあり、localStorageの上限（ブラウザにより5〜10MB）に迫る／超えうる。

- **1人1件の自動セーブ**（2026-09-19にユーザー決定。複数キャリアの並行セーブは持たない）。
- 保存タイミングは「卒業式が終わって週の画面に入った瞬間」と「毎週`この週を進める`の直後」
  （同じくユーザー決定）。
- 実装：`src/state/saveGame.js`。DB名`keiba-game`・ストア名`saves`・固定キー`"current"`。
  ペイロードは`{ schemaVersion, savedAt, saveSeed, startYear, roster, player }`
  （`SAVE_SCHEMA_VERSION`と一致しない・必須フィールドが欠けている場合は`readSave()`が`null`を返す
  ＝セーブ無し扱い）。`writeSave`/`readSave`/`clearSave`は例外を投げない
  （IndexedDBが使えない環境でもプレイを止めない）。
- **`週の種`（C4）は`saveSeed`のみで足りる**——全RNGストリームが`(saveSeed, week, ...)`から
  導出されるため、週ごとの種を別途保存する必要はない。
- **夢の馬のID**（`dreamHorseId`）は`generateDreamHorse(saveSeed)`が`saveSeed`だけから決定的に
  導出する純関数なので、`saveSeed`さえ保存されていれば`generateDreamHorse(saveSeed).id`で
  いつでも再現できる。専用フィールドは不要（配合の弾で回収するときも同じ経路を使う）。
- タイトル画面からの再開（「つづきから」）：`src/app.jsx`が起動時に`readSave()`を呼んで
  `existingSave`に持ち、`TitleScreen`へ4つのpropsを渡す——`onStart`・`hasSave`（真偽値）・
  `onContinue`（`handleContinue`。保存の`roster`/`player`をそのまま`gameState`へ入れ
  `phase`を`"week"`にする）・`saveSummary`（`{ name, weekLabel, money } | null`。
  `weekLabel`は`formatWeekLabel(player.currentYear, player.currentWeek)`で作る）。
- 見た目は**案B**（2026-09-19にユーザーが3案から選択。`devlog/wave10.md`§12）。
  「騎手になる」の下に記録の1枚を置き、**青く塗らない**（主ボタンは「騎手になる」のまま＝
  §9「主ボタンは1画面に1つ」）。所持金は`toLocaleString("ja-JP")+"円"`の形。
  ⚠️保存がある状態で「騎手になる」を押すと**上書きの確認**を1回挟む
  （`.title-screen__modal`。`.title-screen`直下に置く——`__content`の中だと画面の下を走る馬を
  覆えない）。保存が無いときは確認を出さずすぐ`onStart`を呼ぶ。
- ⚠️**触ると壊れる**：記録の1枚の名前と日付は、それぞれ`white-space: nowrap`の`<span>`で、
  折り返してよい場所を両者の間だけに限っている。外すと320px・名前8文字（上限）のとき
  「1974年 4」「月4週」と週の途中で割れる。
- ⚠️**既知**：iPhone SE（320×568）では記録の1枚のぶん画面が縦に52px伸び、画面の下を走る馬の
  一部が最初の画面より下に出る（押せるものは全部収まっている。2026-09-19に直さないと決定）。

---

## 15. ⚠️ 未定義のまま実装に入ると詰まる箇所

| 項目 | 状態 |
|---|---|
| 牧場・セリを見に行く効果 | ⚠️「デビュー前の馬を押さえる」が何を起こすか未定（一流で解禁・最小ループ外） |
| 斤量がsimにどう効くか | ⭐**2026-09-04に§5で確定**（4種類・距離が長いほど効く・見習の減量）。⚠️残るのは**1kgあたりの不利の大きさ**だけで、§10で計測して決める。⚠️実装は第4弾（`TODO.md` #24） |
| 地方遠征が平日の行動枠を消費するか | ⚠️地方は平日開催。未定（中堅で解禁・最小ループ外） |
| ~~年の区切りの処理順と時点~~ | ⭐2026-09-15に確定（`arch/race-program.md`§9。①引退→②繁殖入り→③捨てる→④新2歳→⑤調教師→⑥騎手→⑦主戦→⑧既存の年次処理） |
| オープン以上（G3・G2・G1）への出走条件 | ⚠️勝利数だけでは決まらない（§3 J） |
| NPC騎手160人の初期の所属と主戦馬 | ⚠️史実の騎手はモデルから、架空は生成。未定 |
| ~~キャラ作成の初期適性~~ | ⭐2026-09-04に確定（§11）。成績表＝距離4・馬場2、夢の判断カード＝戦法4、度胸・体力＝乱数＋下限40 |
| **スキルの一覧** | 4系統は確定（§4）。⚠️個々のスキルは`design/`の参考ノートを起こしてから作る |
| **騎手側にレース傾向への適性を持たせるか** | ⚠️**未定**。騎手の適性10個は戦法4・距離4・馬場2で、レース傾向への適性が無い |
| **9軸を画面にどう出すか** | ⚠️CLAUDE.md §7「その情報は本当に要るか」。⚠️**9軸すべて出すのか** |
| 各競馬場のコースデータ（距離・直線長・坂・回り・小回り） | JRA10場・主要距離のスタート位置／コーナー構成／直線長／坂は`design/jra-course-geometry.md`（2026-09-03収蔵）。⚠️回り・小回りの分類や全距離の網羅は未完で、⑦で使う前に要再確認 |
| ~~クラスごとのレース数の配分~~ | ⭐2026-09-14に確定（`arch/race-program.md`§3）。新馬1.06・未勝利3.95・1勝3.09・2勝1.56・3勝0.74・オープン以上0.60／日 |
| 疲労の増減・各クラスの賞金・ショップの価格・夢のカード数 | 実装の弾で置く |
| **夢の中の馬** | 性格は確定（架空・後に主戦馬の父／§11）。⚠️**どんな馬か**は実装の弾で置く |
| 条件戦の1着賞金（1974年） | ⚠️仮の値を置いた（`arch/race-program.md`§8。2026年の中央値÷7.5） |
| 確率開示の速度 | ⚠️**プールが2つに分かれたため再計算が必要**。初回の確率1.0をどちらに適用するかも決める |

### ⚠️ 数値を置くときの制約（数値そのものではなく、満たすべき条件）

- **ショップは品目ごとにメリットとデメリットを極端に振る**（`DESIGN_PRINCIPLES.md` #21）。
  ⚠️どれを買っても少しずつ良くなるだけなら、買い物が判断にならない（§0-1）
- **キャラ作成の初期適性を全部Gにしない**——何にも乗れず成立しない
- **判断カードで覆せる幅は適性1段ぶんを超えない**（§5）
- **史実の騎手の初期信頼値**は、平日に人に会う枠を何年か積まないと届かない高さに置く（§10）

### ⚠️⚠️ 仕組みが無いもの

⚠️**「仕組みが無い項目はゼロ」と3回目のチェックで結論したが、間違っていた。**
5回目のチェックで7件（A〜G）が見つかり、**第1弾-Eで7件とも設計した**（`devlog/wave01e.md`）。
⚠️**6回目のチェック（同ファイル末尾）でさらに見つかったものは、下の表が正本。**

⚠️⚠️**§15は「気付いた未定義」を集めた表である。気付いていないものは入らない。**
この表が空に近づいたことは、設計が完成した証拠にならない。
**弾を締める前に「この書類だけを渡されて実装できるか」を1回通すこと**（`TODO.md`）。
