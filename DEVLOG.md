# DEVLOG — 競馬ゲーム（開発引き継ぎ）

新しいチャットはこのファイルを最初に読めば、巨大な本体ファイルを全読みしなくても文脈を
安く復元できます。**使い方**：新チャット冒頭で「`DEVLOG.md`を読んで続きから」と指示 →
必要な箇所だけ本体を部分読みする。

**サイズ規律**：運用ルールの正本は**CLAUDE.md §4**（閾値60,000B検討／90,000B必須、
Read上限25,000トークン、換算2.5〜2.6バイト/トークン）。
- 圧縮対象は**各弾の詳細プローズ**（`devlog/waveNN.md`へ移す）。
- **生きた参照情報は残す**：実装済み機能一覧（§3）。
  未着手TODOは**`TODO.md`へ分離済み**（Read切り捨てが末尾から起きるため）。

> ⚠️**仕様の正本は`ARCHITECTURE.md`。** 第1弾の設計確定に伴い記入済み。
> `devlog/waveNN.md`の変数表は**履歴**であって仕様ではない——後の弾で覆った決定は
> 反映されない。⚠️実際、正本が空だったために変数表が代役を務め、6箇所が古いまま残った
> （3回目の抜け漏れチェックで発見）。**仕様を変えたら`ARCHITECTURE.md`を同じコミットで直す。**

---

## 0. これは何か

競馬ゲーム。React + Vite + 単一HTML出力（GitHub Pages配信）。
**前作`Roadrace_Game`（自転車ロードレース経営・95弾）の後継**で、開発プロセス（CLAUDE.md）と
`design/`の知見を引き継いでいる。⚠️**コードは流用していない**が、設計パターンは下敷きにする。

- **主モード＝騎手**（最初はこれだけを作る）。調教師モードは後から載せる。
  オーナーブリーダーは未定。
- 見た目はカイロソフト風の斜め見下ろし。歴代名馬をもじった馬。配合要素を強めに、血統表も作る。
- 馬の能力モデルはウイニングポストの**枠組み**を参考（⚠️データの丸写しはしない。
  一度削除→権利調査を経て同日復活。`design/rights-check.md`）。

> **作業ブランチはその時のタスク指定に従う。** 既定ブランチは`main`。

> **デプロイの規律（第1弾-Aで確定）**
> **配信元ブランチは`main`。GitHub Actionsがビルドして配信する。**
> リポジトリに`index.html`を置かない（`.gitignore`の`!index.html`は削除する）。
> ⚠️前作で26弾分が未反映だった事故の真因は「`npm run build`ではなく`npx vite build`を叩くと
> 直下の`index.html`が更新されない」＝**人間が手順を1つ忘れること**だった。Actions方式は
> 手順そのものを消すので、この事故が構造的に起きない。
> ⚠️`.gitignore`はallowlist方式のため、`!.github/`と`!.github/**`を書かないと
> ワークフローがコミットされない。
> ⚠️PagesのSourceを「GitHub Actions」に変更する作業がユーザー側に1回だけ必要。
> 詳細は`devlog/wave01.md`§9。

---

## 1. ファイル構成と開発フロー（第1弾-Cで確定）

- **`src/`** … 唯一の編集対象・真実の源。`root: "src"`でVite管理（`src/index.html`が入口）
  - `src/data/` … 静的データ定数のみ（ロジック禁止・JSX/domain import禁止）
  - `src/core/` … 乱数・共通の小関数（⚠️C4の週×馬シード方式はここに置く。`devlog/wave01.md`§15-4）
  - `src/sim/` … レースシミュレーション
  - `src/domain/` … 純粋なドメインロジック（JSX無し）。馬・騎手・信頼・週の進行など
  - `src/view/` … 文字列生成の純関数（JSX無し）
  - `src/state/` … セーブ・ロード
  - `src/components/` … 共通UI部品
  - `src/screens/` … 画面（今週／騎乗確定／レース／結果 など）
  - `src/app.jsx` … 画面遷移の起点
  - `src/main.jsx` … Reactのマウント処理
- **`index.html`はリポジトリに置かない**（ビルド成果物。`.gitignore`でも除外済み）。
  `npm run build`が`dist/index.html`を生成し、GitHub Actionsがそのまま配信する
- `tools/` … ヘッドレスsim・計測ツール（ビルドには不要。第1弾ではまだ無い）

依存は下向き一方通行：`data → core/sim → domain/state/view → components/screens → app`。
`data/`・`view/`はJSXをimportしない（＝常にNode単体テストが可能な状態を保つ）。

---

## 2. 検証（第1弾-Cで確定）

- **ビルド確認**：`npm run build` → `dist/index.html`が単一ファイルとして生成されることを確認
- **描画確認**：`npx http-server dist -p <port> -s` → Playwright
  （`executablePath: "/opt/pw-browsers/chromium"`）でpageerror・console errorがゼロであることを確認
- 純ロジック（`domain/`・`sim/`・`core/`）はNode単体テスト。JSXを含まないため軽量に回せる
- **配信の確認**：`main`へpush → Actionsの`deploy.yml`が緑になり、Pagesが更新されることを確認
  （初回のみ。以降は自動）

⚠️**CLAUDE.md §0-4より：simは退屈を検知できない。第3弾までに人間が最小ループ
（本編の最初の30分＝デビューから1年目の終わりまで。`devlog/wave01.md`§2）を1回通す。**

⚠️**simを実測するときの必須手順**（前作`Roadrace_Game/ARCHITECTURE.md`より継承・
`devlog/wave01.md`§10）：`Math.random`・`Date.now`・乱数ストリームの状態をすべて固定する。
A/B比較では各アームの直前に共通副シードへ張り直す。固定しないと得られる数字は無意味。

---

## 3. 実装済み機能インベントリ

※ **ここにある物は「もうある」前提で扱うこと。**

- **`src/data/`**（第2弾①）：`courses.js`（JRA10場・地方10場・海外4）／`classes.js`
  （クラス9段のラダー）／`names.js`（冠名12語・語幹20語＋`generateHorseName`）／
  `calendar.js`（52週の雛形`buildCalendarSkeleton`）／`gradedRaces.js`（重賞出走表の
  スキーマ`GradedRaceEntry`。⚠️中身は空——実データ未取得。詳細`devlog/wave02.md`）。
- **`src/core/rng.js`**（第2弾②）：C4の乱数実装。`streamRandom(saveSeed, stream, ...keyParts)`
  で用途ごとに独立した生成器を作る（「1本の列を順に消費する」方式は採らず、呼び出し順序に
  一切依存しない設計。詳細`devlog/wave02.md`）。
- **`src/domain/horse.js`**（第2弾③の1/4）：`generateHorse`（9軸能力・性別・成長型4種・
  血統の系統・名前を持つ架空馬の生成）／`growAdaptability`／クラス昇降（J）／出走間隔の
  判定（H）。厩舎・馬主・NPC騎手の生成は未着手（詳細`devlog/wave02.md`）。
- **`src/data/grades.js`**／**`src/data/humanNames.js`**／**`src/domain/stable.js`**／
  **`src/domain/owner.js`**（第2弾③の2/4）：G〜S記号スケール・もじった人名の生成を共有部品に
  切り出し、厩舎190件（得意分野・4軸のうち3軸・管理頭数）と馬主（大口のみ冠名）の生成を
  実装。NPC騎手の生成・世代管理は未着手（詳細`devlog/wave02.md`）。
- **`src/data/ranks.js`**／**`src/data/aptitudeCategories.js`**／**`src/domain/jockey.js`**
  （第2弾③の3/4・4/4）：騎手ランク6段・適性10個の内訳・NPC騎手/プレイヤー共通の
  `generateJockey`／スキル枠とS上限を機械的に絞る`learnSkill`・`growAptitude`
  （前作の「枠と効果が無関係だった」バグの再発防止）。馬・厩舎・騎手に`isActive`/
  `isRetired`フラグと引退の純関数を追加（世代管理は最小形。実際の引退判定は④に
  持ち越し）。**これで③（馬・厩舎・馬主・NPC騎手の生成）完了**（詳細`devlog/wave02.md`）。
- **`src/domain/career.js`**（第2弾④の1/n）：`createInitialRoster(saveSeed)`——厩舎190・
  馬主100・馬7,600・NPC騎手160をJRAの実数規模で生成し所属関係を結ぶ（7,600頭で約30ms）。
  ⚠️大口馬主の閾値を実測し固定値5→平均持ち馬数(76)へ変更したが、馬の78.7%が冠名付きに
  なる残課題を`TODO.md` #9へ記録（詳細`devlog/wave02.md`）。
- **`src/domain/player.js`**／**`src/domain/weeklyRequests.js`**（第2弾④の2/n）：
  プレイヤー状態（所持金・信頼マップ・評判・主戦の座）と、月曜の騎乗依頼一覧生成
  （依頼の質＝信頼×ランク×厩舎の強さ）。⚠️実測で「初週に全馬が一斉に出走候補になる」
  不具合を発見し、`career.js`の初期`lastRaceWeek`を散らして修正済み（詳細`devlog/wave02.md`）。
- **`src/data/weekdayActions.js`**／**`src/domain/weekdayActions.js`**（第2弾④の3/n）：
  平日7行動のランク別解禁表と効果。朝の調教（信頼+1）・自分から売り込む（成功率＝
  基準0.3＋ランク×0.1＋評判/100、上限0.9）・人に会う（所持金が無いと不成立）は仕様どおり。
  体を作る／買い物／牧場・セリは効果が未定（ARCHITECTURE.md §15）のため明示的な
  プレースホルダー（詳細`devlog/wave02.md`）。
- **`src/domain/weather.js`**／**`src/domain/strategy.js`**／**`src/domain/fridayConfirmation.js`**
  （第2弾④の4/n）：金曜の確定（競馬場→鞍→脚質→予報）。天候予報は的中率85%（外れ率実測
  14.9%）。脚質は4軸から導く暫定ヒューリスティック。週末に乗れる競馬場は1つという制約を
  `confirmMounts`で実装（詳細`devlog/wave02.md`）。
- **`src/domain/raceOutcome.js`**／**`src/domain/mainMount.js`**／**`src/domain/income.js`**
  （第2弾④の5/n）：仮sim（着順は⑦で本実装に差し替える）・主戦の座の獲得/喪失・
  騎乗手当（実額）＋進上金5%（賞金は暫定）。⚠️主戦の座の再獲得条件はARCHITECTURE.md
  未記載のため解釈で実装（要ユーザー確認・詳細`devlog/wave02.md`）。
- **`src/domain/disclosure.js`**／**`src/domain/fatigue.js`**／**`src/domain/fall.js`**／
  **`src/domain/reputation.js`**／**`src/domain/notifications.js`**／**`src/domain/weekResults.js`**
  （第2弾④の6/n・**④完了**）：能力の確率開示・疲労・落馬（「目を固定して閾値を疲労で
  動かす」を実装）・評判・通知を`weekResults.js`で束ねた。必ず知らせる6項目のうち
  3つ（主戦喪失・預けた鞍・新規依頼）は依存する仕組みが無く未実装（理由明記・詳細
  `devlog/wave02.md`）。
- **`src/domain/weekLoop.js`**（第2弾④総仕上げ）：`advanceWeek`——月曜の依頼一覧から
  結果処理までを1週分としてまとめる主ループ。主戦喪失・新規依頼の通知も解消し、
  6項目中5件が実装済み（残る1件は「預ける」機能待ち）。104週のヘッドレス実行で
  信頼急変通知の閾値ミスキャリブレーション（5→4）を実測で発見・修正（詳細`devlog/wave02.md`）。
- **`src/screens/TitleScreen.jsx`**（第2弾⑤）：タイトル画面「本馬場」デザイン実装済み。
  タイトル文字列「ファーストジョッキー」・開始年3択・難易度4段の選択→「騎手になる」。
  `data/startYears.js`・`data/difficulty.js`を新設。CLAUDE.md §8の手順（候補提示×2回・
  タイトル練り直し6回）を経て確定（詳細`devlog/wave02.md`）。
- **`data/judgmentSituations.js`**／**`domain/judgmentCard.js`**／**`domain/dreamDerby.js`**
  （第2弾⑤）：判断カード（状況5類型・仮の効果量）と夢のダービーのバックエンド。
  ⚠️正式な効果量はレースsim本実装（⑦・`claude-opus-5`）に残す判断。最善/最悪の選択で
  勝率85.0%/74.8%を実測（詳細`devlog/wave02.md`）。夢のダービーの**画面**は
  CLAUDE.md §8の候補提示段階（`dream-derby-mock2.html`、まだ実装ではない）。
  ⚠️レース画面は当初`design/sugoroku-derby-race-screen.md`の文章だけを見て自分の解釈で
  作り、ユーザーから「参考動画を本当に確認したのか」と指摘された。動画を1秒間隔で見直し
  Opusサブエージェント（CLAUDE.md §2のサブエージェント併用ルールの初運用）で独立に
  二重チェックして作り直した（コーナー・スタートゲート・ゴール標識・ハロン棒などを追加。
  詳細`devlog/wave02.md`）。フォントはM+フォントへ**ゲーム全体を**統一する方針が決まり、
  タイトル画面（`TitleScreen.jsx`）の書体差し替えが今後のタスクとして残っている。
  ⚠️続けて、WebSearchで東京競馬場・芝2400mの実コース（発走位置・コーナー・直線長・
  好走タイム帯）を調べ、レース全体を実測に近い144秒（2:24.0）に作り直し、判断カード以外の
  丸ボタン3つ（自分/先頭・馬名/馬番/非表示・×1/×2/×4）とスキップのチュートリアル、
  紙吹雪・掲示板演出（着順は5着まで表示・全馬ぶんはデータとして保持）を追加。個々の馬の
  動きの規則性はOpusサブエージェントに画像解析で数値検証させ、前作の罠は無いことを確認した
  （詳細`devlog/wave02.md`、動き方の実測値は`ARCHITECTURE.md`§5）。
  `domain/dreamDerby.js`の着差/上がり算出への拡張が⑦向けの宿題として`TODO.md`に残っている。
  ⚠️続けて、ユーザーから「カーブが変・レイヤーが全体的におかしい・馬の動きがワープしている・
  カメラ遷移が急・文言がクサい」の5点を指摘され、Opusサブエージェント（3回目の画像解析）と
  Fableサブエージェント（文言の全面リライト）に依頼して作り直した。境界（茶・柵・生垣）と
  芝が別レイヤーだった構造を1枚のSVGへ統合し全レイヤーが同じ湾曲を共有するようにした・
  ズーム(0.66〜1.00)を追加・馬のシルエットを正方形に近い比率へ拡大・レース中の全テキストを
  熱血調から具体的で淡々とした文体へ書き直した（詳細`devlog/wave02.md`）。ズームの引き金の
  正確な閾値・上端マーカー帯のx写像規則は未確認のまま`TODO.md`へ残した。
  ⚠️その版はユーザーから「悪化しています」と評価され、Fable 5.1でやり直した（ユーザー指示・
  §2の例外として記録）。悪化の実体は「馬がCSSの塊のまま拡大され、隊列が縦一列の塔になった」
  こと。馬＋騎手をドット絵風SVG（帽子＝枠番色・勝負服・ゼッケン・2コマの脚）に描き直し、
  隊列を横に広げ、縦位置を芝の上半分に限定、ズーム時の外周の黒を解消した（詳細`devlog/wave02.md`）。
  ⚠️さらに本家を再確認し（画面幅≒32m・ゲートは左へ流れる・距離標識は存在する・カメラは
  跳びだけを吸収して追う・カーブは端まで傾く放物線）、「メッセージ」を実況エンジン（局面別
  定型160本超を実際の隊列で穴埋め。実在の実況の文句は写さない）に置き換え、判断カードを
  残り1200m／直線入りの瞬間に距離で出す形へ、選択肢を騎手の判断＋損得1行へ改めた
  （詳細`devlog/wave02.md`）。
- **夢のダービー画面（実装）**（第2弾・`dream-derby-mock2.html`をReactへ移植）：
  `data/dreamDerbyCourse.js`・`data/raceMargins.js`・`data/dreamDerbyVisuals.js`・
  `data/dreamDerbyCommentary.js`（COMMENTARY／WAKE_LINESを転記後diff一致確認）・
  `view/dreamDerbyRace.js`・`view/dreamDerbySprite.js`・`view/dreamDerbyCommentary.js`
  （純関数のみ）・`screens/dreamDerbyEngine.js`（rAFループ＋DOM書き込み）・
  `screens/DreamDerbyScreen.jsx`＋`.css`を新設し`app.jsx`へ配線。⚠️移植時にモックのバグを
  1件修正——`domain/dreamDerby.js`の`resolveDreamDerby`/`drawDreamDerbySituations`を削除し
  `assignPostPositions`/`runDreamDerbyRace`に置き換え、最終着差への収束を**直線カードで
  選んだ瞬間**からにした（モックは自分が常に勝つ前提だった。実装では選択次第で負ける。
  Playwrightで実測確認）。着差・タイム換算式は「仮」（⑦で正式化）。ビルド確認・
  Playwrightでの通しシナリオ（実況→チュートリアル3種→判断カード2回→ゴール→掲示板→
  暗転→目覚め）をJSエラー0件で確認済み。⚠️**CLAUDE.md §0-4の人間の通しプレイは
  未実施のまま**（詳細`devlog/wave02.md`）。

---

## 4. ゲーム開発の指針 → **`DESIGN_PRINCIPLES.md`**

外部知見（現在：桜井政博「ゲーム作るには」6カテゴリ・130テーマ）の収蔵庫。本体は薄い索引で、
全文級の詳細は`design/*.md`。設計（CLAUDE.md §1）・バランス（§10）・UI/デザイン（§7/§8）に
入る前に索引から関係ノートをReadする。取り込みルールはCLAUDE.md §11。

## 5. 未着手TODO → **`TODO.md`**

未着手TODO・「やらないと決めたこと」・恒常タスクはルートの**`TODO.md`**が正本。
このファイルには置かない（Read切り捨てが末尾から起き、末尾のTODOが真っ先に読めなくなるため）。
弾を締めるときの先送り項目も`TODO.md`へ直接書く。

## 6. 前作からの参照

`Roadrace_Game`をセッションに接続している場合、以下を実装済みの設計パターンとして参照できる
（⚠️コードのコピーはしない）：
- `Roadrace_Game/ARCHITECTURE.md` — レースsim・配合・マスク能力・絆の設計
- `Roadrace_Game/devlog/wave95.md` — 面白さの総点検（**同じ穴に落ちないための必読**）
- `Roadrace_Game/tools/` — ヘッドレスsim／判断カードEV計測／テレメトリの設計思想

---

## 7. 実装履歴（弾ごとの索引）

- **第2弾｜実装**（進行中）。作る順序は`TODO.md` #1（①〜⑦）。
  ①`data/`実装済み（詳細`devlog/wave02.md`）：JRA10場・クラス9段・架空の名前語彙・
  52週の暦の雛形・重賞出走表のスキーマ。⚠️重賞出走表とカレンダーの中身は実データ
  （JRA公式＋Wikipedia）を取得するまで空のまま——記憶からの穴埋めはしていない。
  ②`core/rng.js`実装済み：週×馬などのキーからハッシュ種を作り直す方式でC4
  （乱数ストリームの分離・順序非依存・セーブは基底シード1つで足りる）を満たした。
  ③は範囲が広いため段階を分け、まず`domain/horse.js`（馬の生成・適応能力・クラス昇降・
  出走間隔）を実装済み。厩舎・馬主・NPC騎手の生成は③の続きとして残っている。
  続けて厩舎190件・馬主の生成を実装済み（`domain/stable.js`・`domain/owner.js`）。
  NPC騎手の生成・世代管理が③の残り。
  NPC騎手の生成（`domain/jockey.js`）と世代管理の最小形も実装し、**③完了**。
  ④週の進行（仮sim）が次。
  ④は初期ロースター生成（`domain/career.js`）から着手。厩舎190・馬主100・馬7,600・
  NPC騎手160を実数規模で生成し所属を結んだ。月曜の依頼一覧生成が次。
  続けて月曜の依頼一覧生成（`domain/player.js`・`domain/weeklyRequests.js`）を実装。
  平日7行動が次。
  続けて平日7行動（`domain/weekdayActions.js`）を実装。金曜の確定が次。
  続けて金曜の確定（`domain/weather.js`・`domain/strategy.js`・
  `domain/fridayConfirmation.js`）を実装。仮simと結果処理が次。
  続けて仮sim・主戦判定・収入（`raceOutcome.js`・`mainMount.js`・`income.js`）を実装。
  残る結果処理（信頼変化・開示・疲労・落馬・6項目の通知）が次。
  続けて結果処理（信頼・開示・疲労・落馬・評判・通知）を実装し、**④（週の進行・
  仮sim）完了**。⑤導入と、週の進行の主ループ（オーケストレーター）が次。
  続けて週の進行の主ループ（`domain/weekLoop.js`）を実装し、**④を主ループまで含めて
  完了**。104週のヘッドレス実行で決定的に動作することを確認。⑤導入が次。
  ⑤タイトル画面を実装（「本馬場」デザイン・タイトル「ファーストジョッキー」）。
  続けて判断カード・夢のダービーのバックエンドを実装（正式な効果量は⑦へ残す判断）。
  続けて夢のダービー画面をモック（`dream-derby-mock2.html`）からReactへ実装し、
  `app.jsx`から到達可能にした（自分が負けることもある形に修正済み。人間の通しプレイは
  未実施）。⚠️**卒業式の画面（キャラ作成）がまだ無い**——これが次。

- **第1弾｜騎手モードの設計図と土台** — **設計完了**（-A〜-E。監査6回）。実装は第2弾。
  奪う器官の中核は**主戦の座**（同じ馬に5回乗りその間に1勝すると主戦。他の馬を選んだ週に失う
  ＝選択の結果なのでリロードで取り返せない）。**週ごとに乱数を固定**し運はやり直せない。
  馬の能力は**確率で開示**され、進むのは**自分で判断した鞍だけ**。
  騎手は**適性10個＋基礎能力2つ＋スキル**で、⭐**乗る鞍を選ぶことが自分の適性を作る**。
  レースの実力＝**素の能力 × 適性の噛み合い**で、判断カードは**適性の不利を部分的に覆す**。
  最初の3分は**夢のダービー→卒業式→キャラメイク**（⭐奪う体験が導入に入っている）。
  キャリア18〜48歳・総プレイ25時間・騎手ランク6段・**凱旋門賞は自分が主戦の馬だけ**。
  ⚠️-Aで**独断で決めていた11件の監査**を実施（`wave01.md`§15。H3は丸ごと機能していなかった）。
  以後は確定表に「誰が決めたか」を記録する運用にし、-Bの17件は全てユーザーが決めた。
  -Cで配信ワークフローと`src/`の最小骨組みを実装し、実機で確認済み。
  ⚠️**抜け漏れチェックを3回実施**。3回目で根本原因（本ファイルの正本`ARCHITECTURE.md`が
  空だったこと）を特定し、仕様を集約。**未定義の項目は「実装で数値を置けば済むもの」だけになり、
  仕組みが無い項目はゼロ**（代打騎乗＝空きが出たとき信頼の高い順／空の週は構造的に存在しない）。
  -Dで**世界の中身13件**を確定：史実の名馬は実名・ゲーム内で生まれた馬は架空、
  **過去から始めて史実をなぞる（開始年は選択式3〜4本・史実が尽きない範囲に限定）**、
  ⭐**史実の名馬には乗れるが史実の騎手が壁**（その馬への初期信頼を高く置くだけで実装できる）、
  馬に性別、スキル4系統、悪事イベント、判断カードの状況5類型。
  ⚠️-Dの調査で**節から節への送りで6件が消えていた**ことが判明（`wave01d.md`§0。締め前grepを追加）。
  ⚠️⚠️**-Eで、4回の監査が構造的に見つけられなかった7件が判明**——A馬の生成／Bレース
  カレンダー／C依頼の生成／D調教師と馬主／E騎手の収入／Fレースsimの中身／G馬の一生。
  ⚠️1〜4回目は**書類と書類自身**を照合しており、**一度も誰も書かなかったもの**は原理的に
  出ない。5回目は**「この書類だけで実装できるか」**で照合した。
  ⭐**-Eは完了（A〜G 7件＋6回目で見つけたH・I・J＋小4件）。** 最大の成果は**前作が10年見落とした罠の解決**——
  ①同速で走るコストを馬ごとに変える（道中に消耗差が溜まり直線で開く）
  ②集団の相互作用を**レース傾向**1変数に集約する、の2段構え。
  ⭐**B-5の「判断カードが飾りになりかねない」riskも解けた**（カードが馬の適応能力を育てる）。
  → `devlog/wave01.md`（-A/-C）・`devlog/wave01b.md`（-B）・`devlog/wave01d.md`（-D）・
  `devlog/wave01e.md`（-E）
