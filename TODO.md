# TODO — 次のアクション（未着手のみ）

`DEVLOG.md`から分離（CLAUDE.md §4）。**分離の理由**：DEVLOGへのReadは上限超過時に
**末尾から切り捨てられる**ため、末尾に置いたTODOが真っ先に読めなくなる事故が構造的に起きる
（前作`Roadrace_Game`で実際に発生した）。TODOは変更頻度も性質も履歴と異なるため独立させる。

運用：
- ユーザーから明示的に指示された、今後着手すべき項目のメモ。優先度・着手順は未確定。
- **完了項目はここから削除する**（詳細は該当する弾の`devlog/waveNN.md`に残る。
  番号の欠番は削除済みの完了項目の跡）。
- ⚠️**弾を締めるとき、その弾で先送りにしたものは`devlog/waveNN.md`に「上げる」と書かずに
  このファイルへ直接書く。** 締め前に
  `grep -rn "「次のアクション」へ上げる\|TODO.mdへ上げる\|TODOへ上げる" devlog/` が
  **0件**であることを確認する（前作で2度再発した失敗の再発防止）。
- ⚠️⚠️**同じ締め前に、devlog内の「節から節への送り」も確認する**：
  `grep -rn "へ送る\|に残っている" devlog/` の各項目が、送り先で実際に扱われたかを追う。
  ⚠️**2026-09-02にここで6件が消えていたことが判明した**（`devlog/wave01d.md`§0）。
  上のgrepは`TODO.md`への先送りだけを見張っており、節から節への送りは素通りしていた。
- ⚠️⚠️**grepの前に、「この書類だけを渡されて実装できるか」を1回通す。**
  ⚠️上の2本のgrepは**書かれたが落ちたもの**しか捕まえない。
  **一度も誰も書かなかった仕組み**は、実装者の視点で読み直すまで見つからない
  （2026-09-02に7件見つかった。`devlog/wave01e.md`§0）。

## やらないと決めたこと（再提案しないこと）

第1弾-Aで検討し、理由付きで却下したもの。詳細は`devlog/wave01.md`の該当節。

1. **本編と検証用でプレイ時間の尺を分ける**（検証専用の短縮モードを別に持つ）。短縮モードで
   測った数字が本編に当てはまるかを毎回確かめる作業が増える。第3弾で人間が通すのは
   **本編そのものの最初の30分**にした（§2）。
2. **月次で時間を進める**（前作と同じ方式）。6年で72回の「今月何をするか」が発生し、判断回数の
   予算を3倍超える。収めるには「何も選べない月」を多く作ることになり、空打ちになる（§3）。
3. **難易度をいつでも変更できるようにする**。⚠️「勝てそうな時だけ上げ、危なくなったら下げる」が
   最適戦略になり、悪魔の釜の前払いコストが払われない。**年1回**に限定して塞いだ（§8）。
   ⚠️要望が再燃したら、この穴が再び開くことを先に伝えること。
4. **判断カードごと全自動にする手段を用意する**（D4）。飛ばしたいなら最初から預ければよく、
   その仕組みは既にある。「乗るが判断はしない」状態を作る理由がない（§6-4）。
5. **オート消化をキャリア途中で使えるようにする**。使えるのは引退宣言後だけ。途中で使いたく
   なったら、それは飛ばしたい区間の設計が悪いという計測結果として読む（§6-1）。
6. **配信で`main`直下に`index.html`をコミットする／`gh-pages`専用ブランチを使う／開発ブランチ
   から直接配信する**。いずれも「人間が手順を1つ忘れる」余地を残し、前作の26弾未反映と同型の
   事故を招く（§9）。
7. **騎乗回数による固定の開示ラダー**（0/1/2/4/6/8で決まった項目が開く形）。
   ⚠️3回目・5回目・7回目が空振りになり、さらに主戦の座（8回）と完全な理解（8回）が一致して
   8回目で馬への興味が切れる。確率開示に置き換えた（`devlog/wave01.md`§13-3）。
8. **オートに預けた鞍を騎乗回数に加算しない**。⚠️預けた鞍でも騎手本人は乗っており、
   乗ったのに馬を知らないままなのは筋が通らない。「預ける」と「引退後のオート消化」を
   混同した設計だった（`devlog/wave01.md`§13-2）。
9. **馬の能力を数値ではなく言葉だけで返す**（乗った感触のみを見せる案）。⚠️言葉しか無いものの
   期待値は測れず、CLAUDE.md §10の計測が原理的にできない。内部に数値を持ち、
   **騎乗回数という明示的な条件で開示段階を切り替える**形にした（§5-3）。

## 未着手のTODO

1. **第2弾：実装**（⚠️実装なので`/model sonnet`へ切り替える。⚠️Fable等の他モデルを使う場合は
   着手前にユーザーへ確認する）。**仕様の正本は`ARCHITECTURE.md`**。経緯は`devlog/wave01*.md`。
   **範囲**：最小ループ（夢のダービー→卒業式→キャラ作成→デビュー→1年目の終わり）を人が通せる形まで。
   見た目は仮（四角と文字）。
   - ⚠️⚠️**着手直前に「この書類だけで実装できるか」をもう1回通す**（6回目までで24件出た。
     `devlog/wave01e.md`末尾）。⚠️**「18頭のうちプレイヤー以外の17頭はどこから来て誰が乗るか」を
     最初に問う**
   - ⚠️⚠️**計測を同じ弾で回す**：B-6の11項目（`devlog/wave01b.md`）＋面白さ指標**Q1〜Q12**
     （`ARCHITECTURE.md`§13）。⚠️**Q1は基準プレイスタイルを先に決める**（25時間を撤回したため）。
     測り方の必須手順（3点固定・共通副シード）は§13
   - ⚠️**暫定・未定の数値を置く**：`ARCHITECTURE.md`§15の表が正本（キャラ作成の初期適性／
     疲労の増減／賞金／クラスごとのレース数／ショップの価格／夢のカード数と夢の馬／確率開示の速度／
     引退勧告までの年数／評判の上位件数／調教師の開示閾値／世代交代の割合／依頼の件数／
     消耗の計算式／適応能力が育つ速さ／レース傾向の揺らぎ方／次走を決める重み／NPC騎手の初期配置／
     オープン以上の出走条件）。⚠️§15の「数値を置くときの制約」4つを満たすこと
   - ⚠️**データ取得**（⚠️利用規約の確認はユーザー作業）：史実馬7,884頭（アホヌラ館）／
     JRA重賞2002-2026（JRA公式HTML）／ダートグレード（競馬ブック）／史実の馬主・調教師（競馬ブック）／
     ⚠️**競馬ブック・netkeiba・アホヌラ館は使わない**（規約・査定データ）。NAR公式・JBIS・JRA公式から／
     ⚠️**1年分の番組表は未解決**（JRAのPDFは文字化け）／⚠️**2001年以前の重賞出走表は未取得**／
     ⚠️**史実馬の能力値・成長型は戦績から自分で導く**（規則は実装の弾）／
     ⚠️**各重賞の創設年は未取得**／⚠️**各競馬場のコースデータ**——JRA全10場・主要距離の
     スタート位置／コーナー構成／直線長／坂は`design/jra-course-geometry.md`へ2026-09-03に
     収蔵済み（出典はJRA公式）。⚠️ただし小回り・回り（右/左）を含む全項目・全距離の
     完全な網羅ではなく「要確認」「未確認」の穴が残っている。レースsim本実装（⑦）の
     コースデータ源として使う前に、必要な項目が揃っているか改めて確認すること
   - ⚠️**外すと壊れる点**：C4の乱数を「週×馬」から導出し用途別ストリームに分ける（代打の空きは別ストリーム）／
     落馬は「目を固定して閾値を疲労で動かす」／売り込みに信頼を要求しない／
     ⚠️**夢の馬のIDをセーブに残す**（配合の弾で回収）／⚠️**信頼の3本の線を悪事より先に実装する**／
     NPC騎手にプレイヤー専用の分岐を作らない／スキルの枠が効果を絞っていることを確認
   - **見せ方の宿題**（CLAUDE.md §8の手順で）：B4 飛ばした週の見せ方／クラスの表示語／
     ⚠️**9軸を画面にどう出すか**（§7「その情報は本当に要るか」）
   - **作る順序**（提案・依存の低い方から）：
     ①`data/`——カレンダーの雛形（1年分・週×競馬場）・コース表・重賞出走表の索引・語彙表（冠名／語幹）
     ②`core/`——週×馬シードと用途別ストリーム（⚠️代打・天候は別ストリーム）
     ③`domain/`——馬・厩舎・馬主・NPC騎手の生成と世代管理／次走の決定（H）／クラス昇降（J）
     ④週の進行——月曜の依頼一覧（C）→平日7行動→金曜（競馬場→鞍→脚質→予報）→**仮のsim**→結果
       （信頼・金・主戦判定・開示・疲労・落馬・6項目の通知）
     ⑤導入——タイトル（開始年・難易度）→夢のダービー→卒業式（ポイント振り・所属選び）。
       ⚠️**タイトル画面の書体をM+フォント（M PLUS 1p／M PLUS 1 Code）へ差し替える**
       （2026-09-03にユーザーがゲーム全体の方針として明示。現状はRampart One／
       Zen Maru Gothic／DotGothic16のまま。§8の手順で候補提示から）
       ⚠️**画面と画面の間の遷移アニメーション（例：タイトル→夢のダービー→卒業式）は、
       2026-09-03時点でTODO化されていなかった**（ユーザーからの確認を受けて追加）。
       `design/sakurai-specification.md`§17「画面遷移は素早く」（遅いフェードアウト・
       長い暗転を避ける）が唯一の関連する既存の指針。実装方針・具体的な演出は未検討——
       §8の手順（候補提示→選択→合意→実装）で別途進める
     ⑥`tools/`——ヘッドレスsim・計測（B-6 11項目＋Q1〜Q13・3点固定）
     ⑦**レースsim本実装（Fの消耗式・レース傾向・適応能力の成長）→ ここだけ`claude-opus-5`**。
       ⚠️**`domain/dreamDerby.js`の`runDreamDerbyRace`（第2弾で新設。全18頭ぶんの着順・
       着差・ゴールタイム・上がり4F/3Fを返す形は実装済み）が使っている換算式
       （`PAR_SCORE`・`SECONDS_PER_SCORE_POINT`・`METERS_PER_SCORE_POINT`等、
       `horseStrengthScore`の差をm・秒へ線形変換しているだけの「仮」実装）を、
       正式な消耗式・レース傾向（F・ARCHITECTURE.md）に基づく値へ差し替える**。
       ゴールタイムは現状すでに勝ち馬の強さで変動する（144秒固定ではない）が、
       **馬場状態は考慮していない**——強い馬・良馬場なら速いタイム、力の劣る馬・重い馬場
       なら遅いタイム、という対応を消耗式と一体で設計すること。
     ⚠️④で仮のsimを先に置くのは、週の進行と計測を先に回すため。⑦は仮を差し替える形にする

3. **配合システムの設計**（企画で「要素を強めに」と明示）。血統表・系統・ニック・インブリード。
   ⚠️**E3（担当馬の引退後、プレイヤーとの関係がどう残るか）をここで決める。**
   TODO #3が成立するかはE3次第（`devlog/wave01.md`§11）。
   キャリア上限30年で**4回転する**前提（1回転7年・`devlog/wave01.md`§2）。
   ⚠️前作の`Roadrace_Game/ARCHITECTURE.md`の配合節が下敷きになる。
   ⚠️⚠️**プレイヤーは馬を持てない**（第1弾-E E3）。⭐**主戦騎手として馬主・生産者に
   「この馬と配してほしい」と進言する形**で成立させる（E4）。信頼が高いほど通る。
   ⚠️**所有を前提にした設計にしないこと。**

4. **調教師モード**（騎手モードの仕組みの上に後から載せる）。着手前に騎手モードの
   最小ループが人間の通しプレイで検証済みであること。

5. **オーナーブリーダーモード**（未定）。⚠️調教師と道筋が被るため、実装するかどうかから決める。

7. **B4：飛ばした週の「見せ方」**。必ず知らせる6項目は確定済み（`devlog/wave01.md`§13-8）。
   残るのはレイアウト・級数・配置で、CLAUDE.md §8のUI手順（候補を複数提示→選択→合意→実装）で決める。
   ⚠️口頭で決めると§8が禁じる「良さそうなので作ってみました」になる。

8. ⚠️**前作の第86〜95弾を参照したくなったら**、そのブランチかコミットをセッションに接続する。
   現在接続されている`Roadrace_Game`は**第85弾時点**で、CLAUDE.md §12が指定する
   `devlog/wave95.md`と、TODO #1が指定していた`tools/decisioncard_ev.mjs`は**存在しない**
   （`devlog/wave01.md`§0）。結論は本リポジトリのCLAUDE.md・DESIGN_PRINCIPLES.md・design/に
   引用の形で残っているが、実測値そのものは失われている。

9. ⚠️**大口馬主の閾値と、冠名を持つ馬の割合を測って調整する**（`domain/career.js`の
   `createInitialRoster`実装時に判明。詳細`devlog/wave02.md`④）。馬主100人・馬7,600頭の
   持ち馬数に偏りを持たせたところ、閾値を「持ち馬5頭以上」（旧暫定値）にすると馬主の
   **91%**が大口になり、閾値を平均持ち馬数（76頭）に上げても馬主の42%・**馬の78.7%**が
   冠名付きの名前になった。⚠️§3「全馬主に冠名を与えると全部の馬名が同じ形式になり単調に
   なる」という設計意図に対し、**馬主の人数比を均しても、大口馬主が持ち馬を多く抱える
   構造そのものが、馬の名前ベースでは単調さを再び生む**——動かなかった数値として記録する。
   対策候補：①大口馬主1人あたりの語幹をもっと増やす②「持ち馬数」以外の閾値（例：獲得賞金・
   重賞勝利数）を使う③冠名の使用を大口馬主の一部の馬（主戦級）だけに絞る。実装の弾で決める。

10. ⚠️**夢のダービー・モック（`devlog/wave02.md`）の完コピ化で残った視覚的な未確定項目**
    （2026-09-03、Opusサブエージェントの参考動画実測で判明・本実装(⑦)に着手する前に
    再検証すること）：
    - **カメラのズーム（寄り引き）の発生条件**：「馬群がばらけると引く」という大まかな傾向
      までしか実測できておらず、正確な閾値は未確認。モックでは仮の一次関数（距離の広がり
      15〜135mで1.00→0.66）で近似している。
    - **上端の馬番マーカー帯のx座標マッピング**：距離の単純比例でも走路の画面座標でもなく、
      2フレームの実測で両仮説とも矛盾した。モックでは「本線と同じ画面座標」で近似している。
    - **左下の丸ボタン3つの状態表示**：実物は分割された弧のリング（灰＝未選択・水色＝現在の
      状態）。モックは全周リングのまま簡易表示。優先度は低いとOpusサブエージェントも判定。
    - **右下HUDのコースミニマップ**：実物は残り距離のテキストだけでなく、コース形状の
      ミニマップ（現在位置・ゴール位置つき）が背後にある。今回のモックでは未着手。
    - **枠番（8枠）の色配分**：18頭を8枠へ割る配分は実際のJRA運用ではなく簡易な均等割り
      （[2,2,2,3,3,2,2,2]）。
    - **距離標識**：赤白ポールは参考動画に無いが、白丸に赤数字（残り距離÷100）の標識は
      200mごとにコース上に固定されて流れる（4回目の再確認で確定）。⚠️残り1957mの画面で
      「20」が先頭の少し後ろに見える点が「画面幅≒32m」と整合しない——残り表示が誰の距離か、
      標識の配置規則かが未確認。⑦の実装前に確かめること。

14. ⚠️**人名の生成を漢字（姓＋名）へ作り直す**（2026-09-04・卒業式のモック作成中に判明）。
    現状の`data/humanNames.js`はカタカナの音節16種類を2つ繋げるだけで**256通りしかなく**、
    実測で**厩舎190件のうち51件の調教師名が重複**していた（「ミヤヤマ」×4・「スギスギ」×4）。
    ⚠️プレイヤーの騎手名は漢字で入力させるため、このままだと**プレイヤーだけ漢字・NPCは
    全員カタカナ**になる。Fableに作らせた姓40×名40の語彙表（1600通り）が
    `devlog/wave02.md`にある。⚠️**姓・名を独立に引く方式では同名が完全には消えない**
    （実測：350人でユニーク311・同名35組）ため、ロースター生成時に使用済みの名前を避ける
    処理も一緒に入れること。⚠️馬主100人・NPC騎手160人も同じ関数を使う。

12. ⚠️**NPC騎手にも年齢・経験に応じた適性の低さを持たせる**（2026-09-04・卒業式の設計中に判明）。
    `generateJockey`は適性を`pickGrade`（G〜Sの均等乱数）で引くため、NPC160人の適性平均は
    8段階の中央（C相当）になる。一方プレイヤーの卒業時は**最高D・残りG〜E**（ARCHITECTURE.md
    §11）なので、**デビュー2年目のNPCが卒業したてのプレイヤーより最初から上手い**状態が
    構造的に起きる。⚠️「NPC騎手にプレイヤー専用の分岐を作らない」（§6）を守るなら、
    **新人ランクのNPCにもプレイヤーと同じ生成規則を適用する**のが筋。⑦のsim実装か、
    1年目のバランス計測で数値が出た時点で対応する。
    ⚠️度胸・体力の下限40も同じ扱い（新人ランク全員に適用する）。

13. ⚠️**セーブ機能が未実装**（`src/state/`は空・ARCHITECTURE.md §14のlocalStorageキー一覧も
    「実装の弾で確定する」のまま）。⚠️**最小ループ（夢のダービー→卒業式→デビュー→1年目の
    終わり）を人間が通すには必須**——途中でリロードすると最初からになるため。卒業式の出力は
    `{ saveSeed, year, difficulty, dreamHorseId, jockey, stableId }`の1オブジェクトにまとめて
    あるので、それを保存するところから始められる。⚠️**夢の馬のIDをセーブに残す**
    （ARCHITECTURE.md §11・配合の弾で回収する）のはこの作業に含まれる。

11. ⚠️**夢のダービー画面（`screens/DreamDerbyScreen.jsx`）の人間の通しプレイが未実施**
    （CLAUDE.md §0-4「simは退屈を検知できない。人間の通しプレイに勝る検証はない」）。
    実装後の確認はビルド（`npm run build`）とPlaywrightの自動シナリオ（実況→チュートリアル
    3種→判断カード2回（道中・直線）→ゴール→掲示板→暗転→目覚め→「卒業式へ」）に留まり、
    JSエラー0件・想定どおりの遷移は確認済みだが、**これは人間の検証の代わりにはならない**。
    第3弾までに必ず1回、人間が実際にブラウザで触って通すこと。

15. ⚠️**JRA番組表PDFの復号ルートを作る**（2026-09-04の調査で道筋が見えた・`devlog/wave02.md`）。
    ARCHITECTURE.md §3が「1年分の番組表＝未解決」としている件。**`pypdf` 6.16.2はプリインストール済み**で
    PDFの取得と構造の読み取りはできる。読めないのは本文の文字だけで、原因は
    **Identity-HのCIDフォントに`ToUnicode`が無い**こと。⭐glyph IDと文字の対応は固定で、
    数字`̌̍̎̏̐̑̒̓̔̕`＝0〜9・`ະউར`＝未勝利・`৽അ`＝新馬・`উΫϥε`＝勝クラス・`μ`＝ダ、
    のように部分的に読めている。**置換表を1回作れば機械的に復号できる**見込み。
    - 対象：`/keiba/program/YYYY/pdf/bangumi/*.pdf`（開催ごと47ファイル）
    - ⚠️`extract_text()`は列の構造を失う。番組表の全数抽出には**座標付き抽出**（各文字の
      x/y座標を取って列に割り付ける）が別途要る。
    - ⚠️`www.jra.go.jp`はAkamaiが断続的に403を返す。同じURLを数回リトライすれば200になる。
    - これができるまでの間、番組表の分布は`devlog/wave02.md`に記録した**二次資料の仮値**で置く。

16. ⚠️**騎乗依頼の生成が設計どおりに動いていない**（2026-09-04・モック着手前の実データ確認で判明・
    `devlog/wave02.md`）。実測で**毎週40件・全部が所属厩舎・他厩舎は0件**だった。
    - `domain/weeklyRequests.js`：所属厩舎の馬に上限が無く、`fillCount = max(0, 6 - own.length)`が
      常に0になるため他厩舎が締め出される。⭐**所属にも上限を設ける**必要がある。
    - `domain/horse.js`の`lastRaceWeek`は**プレイヤーが乗ったときしか更新されない**
      （`weekResults.js`のみが書く）。乗らなかった馬・NPC騎手が乗った馬も更新しないと、
      `isDueForNextRace`が真のまま積み上がる。
    - クラスの昇降が動いておらず、全馬が`shinba`のまま。
    ⚠️**この弾の「指名して1件を断る」設計は、他厩舎の依頼が来ることを前提にしている。**

17. ⚠️**馬名にも同名が出る**（2026-09-04）。1画面に「ヤマトインパクト」が2頭並んだ。
    TODO #14（人名の漢字化）と同じ問題。ロースター生成時に同名回避が要る。

18. ⚠️**馬に年齢のフィールドが無い**（2026-09-04）。`bornYear`から導出するのか、
    フィールドとして持つのかを決める。馬柱・出馬表の表示に要る。

19. ⚠️**クラスの階段にリステッドを足す**（2026-09-04・ユーザーの指摘で判明・JRA公式で確認済み）。
    `src/data/classes.js`の`CLASS_LADDER`が**9段でリステッドが抜けている**。JRA公式は
    「上位からGⅠ、GⅡ、GⅢ、**リステッド、オープン特別**、3勝クラス…」と定めている
    （https://www.jra.go.jp/keiba/rules/class.html ）。
    - `CLASS_LADDER`を`open`と`g3`の間に`listed`を挟んで10段にする
    - `GRADED_CLASSES`（重賞＝g3/g2/g1）は変えない。リステッドは重賞ではない
    - ⚠️`classIndex`の値が`g3`以上でずれる。参照箇所を確認する
    - ⭐**任される線（`devlog/wave02.md`案ア）では、オープン特別とリステッドに
      ほぼ同じ必要ランク・必要信頼を置く。** 実質ほぼ同じクラスなので、1段違うだけで
      急に任されなくなる壁を作らない
    - 画面に出す言葉は別に決める（モックの「上級」はこの2段を1つに潰していた）

20. ⚠️⚠️**見習騎手の減量が丸ごと抜けている**（2026-09-04・クラスの調査中に判明）。
    `grep -rn "減量\|見習\|apprentice" src/ ARCHITECTURE.md TODO.md`が**0件**だった。
    JRA公式「馬やレースに付く記号」（https://www.jra.go.jp/keiba/rules/index.html ）に定義がある：
    - **男性騎手**：平地の勝利数30回以下＝▲3kg／31〜50回＝△2kg／51〜100回＝☆1kg
    - **女性騎手**：50回以下＝★4kg／51〜100回＝▲3kg／それ以降も◇2kg（女性は勝利数によらず継続）
    - ⚠️**特別競走とハンデキャップ競走では減量しない**
    - 「見習騎手」＝免許の通算取得期間5年未満かつ平地100勝以下（または障害20勝以下）
    ⭐**新人騎手のゲームとして本質的な機構**——最初に与えられた武器（斤量の有利）が、
    **勝てば勝つほど奪われる**。CLAUDE.md §0-2「奪う側の設計」そのもので、
    ⚠️前作の「均す・和らげる・保証する」癖の逆を、史実の仕組みがそのまま提供している。
    - 併せて**斤量（負担重量）そのものが未実装**。`domain/raceOutcome.js`の
      `horseStrengthScore`は斤量を一切見ていない。減量を入れるなら斤量が先に要る
    - ⚠️女性騎手を選べるようにするかは未決定。減量の条件が男女で違うため、選択の意味が生まれる

## ユーザー作業（未完了）

- ⚠️**Wikipedia由来データのライセンス表記を公開前に確認する。** 2026-09-02にデータ源を
  **JRA公式＋Wikipedia（CC BY-SA 4.0）**に確定した（`design/rights-check.md`§6）。
  実装ではデータを別ファイルに分けて出典とライセンスを付ける。公開前にその表記が
  CC BY-SA 4.0の要件（出典・ライセンス名・変更の明記）を満たしているかを確認する。
- ⚠️（経緯の記録）**データ経路の権利関係を判断する。** Claudeが各サイトの規約本文・判例・法の条文を調べて
  `design/rights-check.md`に記録した（2026-09-02）。**Claudeは法律判断をしない。** 事実として：
  - **競馬ブック**は「プログラムでのデータ取り込み」と「個人的な利用の範囲を超える転載・蓄積」を
    規約で明示的に禁止している
  - **netkeiba**は第14条で「事実データも著作物とみなす」「公開はいかなる目的でも私的利用を超える」と定めている
  - **ウイポの史実馬データ**（能力値・成長型）はコーエーテクモの査定値で、著作権法12条の2
    （データベースの著作物）に当たる可能性がある。公式のデータ再利用ガイドラインは無い
  - **JRA公式HTMLの事実データ**（日付・名前・距離・優勝馬）と**馬名**（ギャロップレーサー判例）はリスクが低い
  ⚠️現在の設計は競馬ブック・netkeiba・ウイポの3経路に依存している。**代替経路は同ファイル§3。**

## 恒常タスク（終わりが無い）

- **UIテキスト・ビジュアルの作り込み**（CLAUDE.md §7・§8）。新画面・新機能を作るたびに点検する。
  ⚠️競馬は用語が難しいジャンルなので、**競馬を知らない人が読んで分かる言葉だけ**を画面に出す。
- **バランス計測**（CLAUDE.md §10）。機能を足したら同じ弾の中でbefore/afterを測る。
  第1弾-Aで立てた計測計画は`devlog/wave01.md`§10。⚠️「後で測る」は禁止。
