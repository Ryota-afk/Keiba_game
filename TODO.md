# TODO — 次のアクション（未着手のみ）

`DEVLOG.md`から分離（CLAUDE.md §4）。**分離の理由**：DEVLOGへのReadは上限超過時に
**末尾から切り捨てられる**ため、末尾に置いたTODOが真っ先に読めなくなる事故が構造的に起きる
（前作`Roadrace_Game`で実際に発生した）。TODOは変更頻度も性質も履歴と異なるため独立させる。

運用：
- ⭐**「本筋」だけが今やること。それ以外は全部「棚上げ」**（2026-09-06・CLAUDE.md §16）。
- ユーザーから明示的に指示された、今後着手すべき項目のメモ。
- **完了項目はここから削除する**（詳細は該当する弾の`devlog/waveNN.md`に残る。
  番号の欠番は削除済みの完了項目の跡）。
- ⚠️**弾を締めるとき、その弾で先送りにしたものは`devlog/waveNN.md`に「上げる」と書かずに
  このファイルへ直接書く。** 締め前に
  `grep -rn "「次のアクション」へ上げる\|TODO.mdへ上げる\|TODOへ上げる" devlog/` が
  **0件**であることを確認する（前作で2度再発した失敗の再発防止）。
- ⚠️⚠️**同じ締め前に、devlog内の「節から節への送り」も確認する**：
  `grep -rn "へ送る\|に残っている" devlog/` の各項目が、送り先で実際に扱われたかを追う。
  ⚠️**2026-09-02にここで6件が消えていたことが判明した**（`devlog/wave01d.md`§0）。
  上のgrepは`TODO.md`への先送りだけを見張っており、節から節への送りは素通りしていた。
- ⚠️⚠️**grepの前に、「この書類だけを渡されて実装できるか」を1回通す。**
  ⚠️上の2本のgrepは**書かれたが落ちたもの**しか捕まえない。
  **一度も誰も書かなかった仕組み**は、実装者の視点で読み直すまで見つからない
  （2026-09-02に7件見つかった。`devlog/wave01e.md`§0）。

## ⭐ 本筋（この順に進める。2026-09-06にユーザーが承認）

⚠️**この5項目だけが「今やること」。** 1つ終えるたびに、次の項目へ進む前に棚上げを1回見直す。

1. ⭐**ユーザーが通しプレイ**（タイトル→夢のダービー→卒業式）。レースsimと実況の体験を確認する。
   ⭐**2026-09-07にアーティファクトを公開して依頼済み**（史実馬51頭の実データ版）。
   ⚠️CLAUDE.md §0-4「simは退屈を検知できない」。**この確認が済むまで先へ進まない。**
   ⭐**2026-09-08にユーザーが通しプレイし8件の指摘。うち2件は対応済み**（タイトル画面の馬の
   足＝`devlog/wave05.md`§50／カード選択後に位置が一瞬で動く＝§51-52）。
   ⚠️⚠️**残り6件を直し、もう一度通してもらうまでが本筋1**（2026-09-08にユーザーが決定。
   「指摘が出た＝確認が済んだ」ではない）。⚠️直す作業は次の3つ。実測値は`devlog/wave05.md`§54。
   - ~~1-A：道中で18頭が独立して動くようにする（指摘②③）~~ ⭐**2026-09-08に完了**
     （`devlog/wave05.md`§56）。部品1〜5を実装。③は部品1〜5と目標差の圧縮だけで消えたため、
     ユーザーが選んだ案(a)（垂れの境目を上げる）は**入れていない**——判断カードが
     合わせても外しても18.3%で同じになるため。夢の馬の記号を6→7段へ。
   - ~~1-B：道中の4択を作り直す（指摘④⑤⑥）~~ ⭐**2026-09-08に完了**（`devlog/wave05.md`§57）。
     案Aで実装。`CARD_MOVE_METERS`を62→22にして、16通り全部の1着率を
     一番良い68〜70%／一番悪い21.0%（目安の内側）にした。
   - ~~1-C：「第92回」を消す（指摘⑧）~~ ⭐**2026-09-08に完了**（`devlog/wave05.md`§58-59）。
     案A＋夢の入りの浮かび上がり（D2）。レース番号も10R→11Rに訂正。
   ⭐**2026-09-08に2回目の通しプレイ。新たに3件の指摘**（`devlog/wave05.md`§61）。
   ⚠️⚠️**1-Aで目標差を0〜24mへ圧縮した案(c)が裏目に出た**——全頭が画面に収まって迫力が消え、
   馬名が重なった。「追込が画面外」を画面に合わせて小さくすることで解いたのが誤り。
   - ~~1-D/1-E：道中で差がつくようにする・直線の攻防~~ ⭐**2026-09-08に完了**
     （`devlog/wave05.md`§63）。`TARGET_GAP_BAND`を3倍（0〜72m）、`STRETCH_ABILITY_SCALE`
     0.30と`STRETCH_VMAX_FLOOR`0.825を新設、`GASSED_THRESHOLD`0.25→0.49。
     道中の幅18.9→56.1m（画面の1.8倍）、上り上位5頭0.5→0.25秒、2着差4.0→2.5m、
     カード69/19→79/11%。⚠️ゴール幅は38mのまま（棚上げ#67）。
   - ~~1-F：画面~~ ⭐**2026-09-09に完了**（`devlog/wave05.md`§64）。
     `zoomForSpread`（隊列15m超で最大0.66倍に縮める）を撤去。発走直後の馬名の重なり
     （#66）は隊列の実際の幅が15m未満の間だけラベルを消す形で解消。
2. **レースを実体にする**（#23）。⭐**2026-09-16に完了**（相手馬・出馬表・番組表・馬の年齢・
   着順を本物のsimで決める・勝ちタイム・馬場適性・競馬場ごとの施行距離）。残りはIndexedDBセーブ。
   レースsimと夢のダービーの画面を流用する。
3. **週の画面を本番の見た目にする**。
   ⭐**着手前に決めるはずだった#24の5件は2026-09-16に全部決まった**（`devlog/wave09.md`§1）。
   ⭐**2026-09-17に見た目が決まった＝`design/mocks/week-offers-v3.html`の案2**
   （レースが主役・1レース＝1つの表。`devlog/wave09.md`§6）。案1・案3は却下。
   ⚠️**画面の見た目・文言の仕事なのでFableで行う**（CLAUDE.md §8・`arch/ui-fable.md`）。
   ⭐**背後のロジック（土日の割り当て・1日1場・断るコスト・重賞に乗れるかの判定）は
   2026-09-17に実装・実測済み**（`devlog/wave09.md`§10）。⚠️**残る前提は#89（行けない
   競馬場の依頼が混ざる週の見せ方）——画面の実装と合わせて決める。**
4. **セーブを作る**（#13）。⭐**ロジックは2026-09-19に完了**（IndexedDBへ1人1件の自動セーブ・
   `devlog/wave10.md`§11）。残るのはタイトル画面「つづきから」ボタンの見た目（#13）。
5. **ユーザーが1年目を通しプレイ**。

## やらないと決めたこと（再提案しないこと）

第1弾-Aで検討し、理由付きで却下したもの。詳細は`devlog/wave01.md`の該当節。

1. **本編と検証用でプレイ時間の尺を分ける**（検証専用の短縮モードを別に持つ）。短縮モードで
   測った数字が本編に当てはまるかを毎回確かめる作業が増える。第3弾で人間が通すのは
   **本編そのものの最初の30分**にした（§2）。
2. **月次で時間を進める**（前作と同じ方式）。6年で72回の「今月何をするか」が発生し、判断回数の
   予算を3倍超える。収めるには「何も選べない月」を多く作ることになり、空打ちになる（§3）。
3. **難易度をいつでも変更できるようにする**。⚠️「勝てそうな時だけ上げ、危なくなったら下げる」が
   最適戦略になり、悪魔の釜の前払いコストが払われない。**年1回**に限定して塞いだ（§8）。
   ⚠️要望が再燃したら、この穴が再び開くことを先に伝えること。
4. **判断カードごと全自動にする手段を用意する**（D4）。飛ばしたいなら最初から預ければよく、
   その仕組みは既にある。「乗るが判断はしない」状態を作る理由がない（§6-4）。
5. **オート消化をキャリア途中で使えるようにする**。使えるのは引退宣言後だけ。途中で使いたく
   なったら、それは飛ばしたい区間の設計が悪いという計測結果として読む（§6-1）。
6. **配信で`main`直下に`index.html`をコミットする／`gh-pages`専用ブランチを使う／開発ブランチ
   から直接配信する**。いずれも「人間が手順を1つ忘れる」余地を残し、前作の26弾未反映と同型の
   事故を招く（§9）。
7. **騎乗回数による固定の開示ラダー**（0/1/2/4/6/8で決まった項目が開く形）。
   ⚠️3回目・5回目・7回目が空振りになり、さらに主戦の座（8回）と完全な理解（8回）が一致して
   8回目で馬への興味が切れる。確率開示に置き換えた（`devlog/wave01.md`§13-3）。
8. **オートに預けた鞍を騎乗回数に加算しない**。⚠️預けた鞍でも騎手本人は乗っており、
   乗ったのに馬を知らないままなのは筋が通らない。「預ける」と「引退後のオート消化」を
   混同した設計だった（`devlog/wave01.md`§13-2）。
9. **馬の能力を数値ではなく言葉だけで返す**（乗った感触のみを見せる案）。⚠️言葉しか無いものの
   期待値は測れず、CLAUDE.md §10の計測が原理的にできない。内部に数値を持ち、
   **騎乗回数という明示的な条件で開示段階を切り替える**形にした（§5-3）。

## 棚上げ（⚠️本筋の7項目が終わるまで着手しない）

⚠️**ここの項目は、本筋を止めているとき以外は触らない。**「ついでに直す」をしない
（2026-09-06にユーザーが「設計だけして後回しにすると本筋を見失う」と指摘したことへの対処）。

67. **ゴールでの1着→最後方の幅が38mで、実データの53mに届かない**（2026-09-08・§62/§63）。
    伸ばすには直線の速さの下限（`STRETCH_VMAX_FLOOR` 0.825）を下げる必要があるが、そうすると
    上り3Fが実データの3.9秒を超え、判断カードの一番悪い選び方の1着率が1桁%まで落ちる。
    ⚠️同じく「2着との差が1馬身以内」が48%（実データ70%）も未達。

68. **実況1行が「府中」を含んだまま競馬場の出し分けが付いていない**（2026-09-09）。
    `homage`の「あなたの、そして私たちの夢が、いま府中を走っています。」。ユーザーが承認した
    6行の出し分け（`devlog/wave06.md`§65）には入っていなかったため据え置いた。競馬場を増やす
    弾で、「府中」を含む他の4行（`TRACK_NAME === "東京"`の条件付き。`corner4`・`stretchEntry`・
    `homage`・`choiceReact.goNow`に1行ずつ）と揃える。
69. **ファン投票（有馬記念）とレーティング上位10頭による優先出走権を作る**（2026-09-10）。
    ⭐トライアル→本番の優先出走権は2026-09-15に本筋2へ入った（質問15＝(イ)・`devlog/wave06.md`）。
    残るのはファン投票とレーティングの2つ。制度は`design/jra-entry-rules.md`§2。


45. **レースsimで着順を動かさない軸が3つある**（2026-09-06の計測。`devlog/wave04.md`§30-1）。
    **精神力**は発走の`dash`にしか入っておらず1段では0.000着／**健康**はレース中に使い道が無い
    （落馬・故障の軸）／**柔軟性**は距離適性の幅を決めるが夢の馬では効かない（±0.02着）。
    ⚠️CLAUDE.md Q11の「飾りの軸」。遠征・故障・多様な距離のレースを入れる弾で見直す。

49. **`domain/strategy.js`の`deriveFavoredStrategy`が4脚質をほぼ均等（各25%）に配る。**
    実際の競馬の逃げ馬は18頭中1〜3頭。⚠️`sim/pace.js`の`paceScoreOf`はこの25%を基準値に
    置いて辻褄を合わせているので、脚質の導出を実態に寄せたら基準値も一緒に直すこと。

23. **本筋2「レースを実体にする」の残りの穴**（2026-09-16現在）。
    ⭐**相手馬・出馬表・番組表・毎週のNPC出走・馬の年齢は本筋2で実装済み**（`devlog/wave07.md`）——
    `raceOutcome.js`のsyntheticRivalScoreは廃止し実在の馬から出走枠を組むようになった。
    `domain/bootstrap.js`が開始年の2年前から104週を回し、生年・戦績・クラスを付ける。
    残っているのはこの1つ：
    - ~~**コースデータ**~~ ⭐2026-09-16に完了（`devlog/wave08.md`§9）。
      ⚠️距離の一覧は今のJRAの表＋史実の重賞データの和集合で、**当時は行われていない距離が混じる**。
      ⚠️**福島のダートだけ出典から取れておらず仮の値。**
      ⚠️直線長・坂・小回りといったコースの形はまだ持っていない（simが使うときに足す）。

24. **第4弾：指名・任される線・斤量と減量**（2026-09-04に第2弾から切り出した）。
    ⭐**着手前に決め直す5件は2026-09-16に全部決まった**（`devlog/wave09.md`§1）。
    1. ⭐**ランクの循環**——重賞に乗れるかの判定に**ランクを使わない**。調教師への信頼と
       馬主への信頼で決める（`src/data/ranks.js`のコメントが正本）。
    2. ⭐**馬主の信頼を判定に入れる**（`ARCHITECTURE.md`§6）。
    3. ⭐**1日1場**——土曜と日曜で別々の競馬場へ行ける（`ARCHITECTURE.md`§2）。
       ⚠️`domain/fridayConfirmation.js`の`confirmMounts`は週に1つの`courseId`しか
       受け取らないので、土日で2つ受け取る形に直す。
    4. ⭐**断るコスト**——**主戦を張っている馬の依頼を断ったときだけ**その厩舎の信頼が下がる。
    5. ⭐**得意分野**——出走頻度ではなく馬の強さに効く。どの馬場に使うかは馬自身の芝ダ適性
       （`preferSuitedRunners`）が決める（`arch/horse.md`）。
    ⚠️**まだ決まっていないもの**：得意分野から外れたときの扱い（普通か弱いか）／
    見習の減量がいつ切れるか（5年条項を採るか・名目の鞍の勝ちを数えるか）／
    判定表の「上位」というランクは存在しない（6段は新人・若手・中堅・実力派・一流・トップ）
    ⚠️**モックの直し3件**：適性A→卒業直後は最高D／クラス表記が却下済みの「初戦・0勝・上級」のまま／
    選択中タブの色が`accent`（§9違反・`action`にする）

3. **配合システムの設計**（企画で「要素を強めに」と明示）。血統表・系統・ニック・インブリード。
   ⚠️**E3（担当馬の引退後、プレイヤーとの関係がどう残るか）をここで決める。**
   TODO #3が成立するかはE3次第（`devlog/wave01.md`§11）。
   キャリア上限30年で**4回転する**前提（1回転7年・`devlog/wave01.md`§2）。
   ⚠️前作の`Roadrace_Game/ARCHITECTURE.md`の配合節が下敷きになる。
   ⚠️⚠️**プレイヤーは馬を持てない**（第1弾-E E3）。⭐**主戦騎手として馬主・生産者に
   「この馬と配してほしい」と進言する形**で成立させる（E4）。信頼が高いほど通る。
   ⚠️**所有を前提にした設計にしないこと。**

4. **調教師モード**（騎手モードの仕組みの上に後から載せる）。着手前に騎手モードの
   最小ループが人間の通しプレイで検証済みであること。

5. **オーナーブリーダーモード**（未定）。⚠️調教師と道筋が被るため、実装するかどうかから決める。

7. **B4：飛ばした週の「見せ方」**。必ず知らせる6項目は確定済み（`devlog/wave01.md`§13-8）。
   残るのはレイアウト・級数・配置で、CLAUDE.md §8のUI手順（候補を複数提示→選択→合意→実装）で決める。
   ⚠️口頭で決めると§8が禁じる「良さそうなので作ってみました」になる。

8. ⚠️**前作の第86〜95弾を参照したくなったら**、そのブランチかコミットをセッションに接続する。
   現在接続されている`Roadrace_Game`は**第85弾時点**で、CLAUDE.md §12が指定する
   `devlog/wave95.md`と、TODO #1が指定していた`tools/decisioncard_ev.mjs`は**存在しない**
   （`devlog/wave01.md`§0）。結論は本リポジトリのCLAUDE.md・DESIGN_PRINCIPLES.md・design/に
   引用の形で残っているが、実測値そのものは失われている。

9. ⚠️**大口馬主の閾値と、冠名を持つ馬の割合を測って調整する**（`domain/career.js`の
   `createInitialRoster`実装時に判明。詳細`devlog/wave02.md`④）。馬主100人・馬7,600頭の
   持ち馬数に偏りを持たせたところ、閾値を「持ち馬5頭以上」（旧暫定値）にすると馬主の
   **91%**が大口になり、閾値を平均持ち馬数（76頭）に上げても馬主の42%・**馬の78.7%**が
   冠名付きの名前になった。⚠️§3「全馬主に冠名を与えると全部の馬名が同じ形式になり単調に
   なる」という設計意図に対し、**馬主の人数比を均しても、大口馬主が持ち馬を多く抱える
   構造そのものが、馬の名前ベースでは単調さを再び生む**——動かなかった数値として記録する。
   対策候補：①大口馬主1人あたりの語幹をもっと増やす②「持ち馬数」以外の閾値（例：獲得賞金・
   重賞勝利数）を使う③冠名の使用を大口馬主の一部の馬（主戦級）だけに絞る。実装の弾で決める。

10. ⚠️**夢のダービー・モック（`devlog/wave02.md`）の完コピ化で残った視覚的な未確定項目**
    （2026-09-03、Opusサブエージェントの参考動画実測で判明・本実装(⑦)に着手する前に
    再検証すること）：
    - **カメラのズーム（寄り引き）の発生条件**：「馬群がばらけると引く」という大まかな傾向
      までしか実測できておらず、正確な閾値は未確認。モックでは仮の一次関数（距離の広がり
      15〜135mで1.00→0.66）で近似している。
    - **上端の馬番マーカー帯のx座標マッピング**：距離の単純比例でも走路の画面座標でもなく、
      2フレームの実測で両仮説とも矛盾した。モックでは「本線と同じ画面座標」で近似している。
    - **左下の丸ボタン3つの状態表示**：実物は分割された弧のリング（灰＝未選択・水色＝現在の
      状態）。モックは全周リングのまま簡易表示。優先度は低いとOpusサブエージェントも判定。
    - **右下HUDのコースミニマップ**：実物は残り距離のテキストだけでなく、コース形状の
      ミニマップ（現在位置・ゴール位置つき）が背後にある。今回のモックでは未着手。
    - **枠番（8枠）の色配分**：18頭を8枠へ割る配分は実際のJRA運用ではなく簡易な均等割り
      （[2,2,2,3,3,2,2,2]）。
    - **距離標識**：赤白ポールは参考動画に無いが、白丸に赤数字（残り距離÷100）の標識は
      200mごとにコース上に固定されて流れる（4回目の再確認で確定）。⚠️残り1957mの画面で
      「20」が先頭の少し後ろに見える点が「画面幅≒32m」と整合しない——残り表示が誰の距離か、
      標識の配置規則かが未確認。⑦の実装前に確かめること。

12. ⚠️**NPC騎手の適性分布が卒業したてのプレイヤーより有利なまま**（2026-09-04・卒業式の
    設計中に判明。⭐**度胸・体力の下限40は2026-09-04の第2弾実装で新人ランク全員に適用済み
    ——解決済み**）。`generateJockey`の適性（aptitudes）は`pickGrade`（G〜Sの均等乱数）で
    引くため、NPC160人の適性平均は8段階の中央（C相当）になる。一方プレイヤーの卒業時は
    **最高D・残りG〜E**（`domain/graduation.js`の`buildGraduatedAptitudes`）なので、
    **デビュー2年目のNPCが卒業したてのプレイヤーより最初から上手い**状態が構造的に残っている。
    ⚠️「NPC騎手にプレイヤー専用の分岐を作らない」（§6）を守るなら、**新人ランクのNPCにも
    プレイヤーと同じ生成規則を適用する**のが筋。⑦のsim実装か、1年目のバランス計測で
    数値が出た時点で対応する。

13. ⭐**セーブのロジックは2026-09-19に完了**（IndexedDBへ1人1件の自動セーブ。仕様は
    `ARCHITECTURE.md`§14・経緯は`devlog/wave10.md`§11）。**残るのはタイトル画面
    「つづきから」ボタンの見た目だけ**——`TitleScreen.jsx`は2026-09-06にFableの§8手順で
    確定済みの画面なので、新しい要素を足すには同じ手順（候補提示→選択→合意→実装）を
    もう一度通す必要がある。ロジック側（`hasSave`/`onContinue`のprops）は`app.jsx`に
    配線済みなので、Fableの手順②は見た目だけ作ればよい。

15. ⚠️**JRA番組表PDFの復号ルートを作る**（2026-09-04の調査で道筋が見えた・`devlog/wave02.md`）。
    ARCHITECTURE.md §3が「1年分の番組表＝未解決」としている件。**`pypdf` 6.16.2はプリインストール済み**で
    PDFの取得と構造の読み取りはできる。読めないのは本文の文字だけで、原因は
    **Identity-HのCIDフォントに`ToUnicode`が無い**こと。⭐glyph IDと文字の対応は固定で、
    数字`̌̍̎̏̐̑̒̓̔̕`＝0〜9・`ະউར`＝未勝利・`৽അ`＝新馬・`উΫϥε`＝勝クラス・`μ`＝ダ、
    のように部分的に読めている。**置換表を1回作れば機械的に復号できる**見込み。
    - 対象：`/keiba/program/YYYY/pdf/bangumi/*.pdf`（開催ごと47ファイル）
    - ⚠️`extract_text()`は列の構造を失う。番組表の全数抽出には**座標付き抽出**（各文字の
      x/y座標を取って列に割り付ける）が別途要る。
    - ⚠️`www.jra.go.jp`はAkamaiが断続的に403を返す。同じURLを数回リトライすれば200になる。
    - これができるまでの間、番組表の分布は`devlog/wave02.md`に記録した**二次資料の仮値**で置く。

17. ⚠️**馬名にも同名が出る**（2026-09-04）。1画面に「ヤマトインパクト」が2頭並んだ。
    TODO #14（人名の漢字化）と同じ問題。ロースター生成時に同名回避が要る。

20. ⚠️**斤量（負担重量）と見習騎手の減量を実装する**（2026-09-04に設計確定・`ARCHITECTURE.md`§5）。
    現状`domain/raceOutcome.js`の`horseStrengthScore`は斤量を一切見ていない。
    - `data/weights.js`（新規）：4種類の負担重量・3歳4歳の年齢減量表・見習減量表（男女）
    - 番組表のレースに`weightType`（馬齢/別定/定量/ハンデ）と`isSpecial`（特別競走か）を持つ
    - `horseStrengthScore`に斤量の項を足す。⭐**距離が長いほど1kgが効く**
    - プレイヤーに性別と、平地の勝利数を持たせる
    - ⚠️**別定の個別基準は仮**（番組表PDF未復号・TODO #15）
    - ⚠️**1kgあたりの不利は仮値。§10で計測**（1年目の勝ち数・減量が切れた前後の勝率）
    - 減量が切れたことは切れた後にだけ告げる。一覧の表記は数字だけ（`57.0kg`）

21. ⭐**アイデア：騎手の体重管理を平日の行動に組み込む**（2026-09-04・ユーザー）。
    ⚠️まだ設計していない。着想の記録。
    - **斤量49.0kgの鞍には、自分の体重がそれを超えていたら乗れない。** 太ると乗れる鞍が
      減る＝依頼が減る（CLAUDE.md §0-2の奪う側）
    - **平日に「人に会う」を重ねると太る。「体を作る」で絞る。** ⭐この2つは
      `ARCHITECTURE.md`§7「平日の行動（7種類）」に**既にある**。新しい行動を足す必要がない。
      ⭐**信頼を積む行動と体を絞る行動が、同じ1枠を奪い合う**（§0-1の意味のある判断）
    - **無理に絞ると能力が下がる（減量苦）。** 軽い鞍を取りに行くほど、その鞍で弱くなる
    - ⚠️**言葉の衝突**：「減量」が2つの意味を持つ（見習いの斤量が軽くなる／体重を落とす）。
      ただし斤量は数字だけ出すと決めた（`ARCHITECTURE.md`§5）ので、画面に「減量」と書く場面は
      体重の方だけになり衝突しない
    - ⚠️新人は平日の枠が1つしかない（§7）。体重管理を入れると、その1枠の重みがさらに増す。
      **枠の奪い合いが厳しくなりすぎないかを§10で計測する**

22. ⚠️**馬の能力の定義に穴が2つある**（2026-09-04・詳細画面のモック作成中に判明）。
    - ⚠️**「底」が定義されていない。** `ARCHITECTURE.md`§3の開示表（レースで開く7項目）に
      1度出てくるだけで、能力9軸にも入っていない。何を表す値か決まっていない
    - ⚠️**「脚質」が調教で分かる側にあるが、9軸では「瞬発力と勝負根性の大小が脚質を決める」
      導出値**。導出値なのに、元になる勝負根性より先に分かることになる

25. ⚠️**`devlog/wave02.md`が185,770バイト＝約72,800トークンで読み切れない**（2026-09-04）。
    ⭐**次の弾（第3弾）は`devlog/wave03.md`に分ける。** 過去の記録は動かさない。
    ⚠️`ARCHITECTURE.md`が読み切れなかった件と`DEVLOG.md`に索引が無かった件は2026-09-04に解決済み
    （§3と§5を`arch/horse.md`・`arch/race-sim.md`へ切り出し、CLAUDE.md §4のサイズ規律を
    4ファイル全部へ拡張した）。

26. ⚠️**悪事イベントが未実装のまま残っている**（`ARCHITECTURE.md`§6「信頼」は「悪事イベント：
    ⚠️第2弾に入れる」と明記していたが、2026-09-04の第2弾範囲の切り直し（`devlog/wave02.md`末尾）
    でも今回の9項目の実装でも扱わずに漏れた）。小口馬主から所持金が一定以下の週に
    「このレースで手を抜けば金を出す」と持ちかけられ、受けると評判が大きく下がる仕組み。
    ⚠️評判の3本の線自体は`domain/reputation.js`に実装済み（`computeReputation`・
    `reputationLineCrossed`）だが、**プレイヤーが自分の判断で信頼を捨てる経路が無いと、
    信頼が下がるのは「負け続ける」場合だけになる**（ARCHITECTURE.md原文）。

27. ⚠️**`domain/weekdayActions.js`がどこからも呼ばれていない**（2026-09-04・第2弾実装中に判明。
    grep実測で参照0件）。平日の行動7種類（人に会う・体を作る等）のロジックは実装済みだが、
    `weekLoop.js`にもどの画面にも配線されていない。⚠️これにより**第2弾の計測項目4番目
    「平日に相手が要る行動を1つも選べない週の連続長」が今回測定不能だった**。ゲーム性の
    柱の1つ（信頼を積む行動と体を絞る行動が同じ1枠を奪い合う設計）が未稼働のままでもある。

28. ⚠️**`growAptitude`／`growAdaptability`が実装のどこからも呼ばれていない**（2026-09-04・
    第2弾実装中にgrepで確認）。`src/domain/jockey.js`の`growAptitude`と`src/domain/horse.js`の
    `growAdaptability`は関数として存在するが呼び出し元が無く、**騎手の適性・馬の適応能力は
    レースを重ねても一切成長しない**状態が続いている。

29. ⚠️**プレースホルダーsim（`raceOutcome.js`）が騎手の適性を一切読まない**（2026-09-04・
    第2弾の計測で判明。CLAUDE.md §10「動かなかった数値も記録する」に基づく記録）。
    `horseStrengthScore`の宣言戦略は常に`deriveFavoredStrategy(horse)`（馬自身の適性）で、
    `player.jockey.aptitudes`を参照しないため、**卒業式で「受け入れる」を選んでも「決別する」
    を選んでも、1年目の勝ち数・騎乗数が完全に同じ数値になる**ことを15シードで確認した
    （`devlog/wave02.md`）。⚠️バグではなく、騎手適性がレース結果に効くのは本番sim（⑦・
    `claude-opus-5`で実装）に明示的に予定されている範囲。それまでは卒業式の選択が
    数値上は無意味であることを認識して扱うこと。

30. ⚠️**卒業式の戦略マッピングはClaudeの独自解釈で、正典が無い**（`domain/graduation.js`の
    `MID_CHOICE_AXIS`／`STRETCH_CHOICE_AXIS`／`STRATEGY_BY_AXES`）。夢のダービーの道中・
    直線の判断カード選択を2×2グリッドに写して脚質適性（逃げ／先行／差し／追込）を導く
    仕組みは、2026-09-04の卒業式実装時にその場で決めたもので、史実のルールや既存の
    設計ノートに根拠が無い。ユーザーの確認・承認がまだ済んでいない。

31. ⚠️**9軸を画面にどう出すかが未解決のまま**（`ARCHITECTURE.md`§15「未定義のまま実装に
    入ると詰まる箇所」に元から記載・2026-09-04の卒業式実装でも先送りにした）。卒業式の
    成績表は距離帯4種・馬場2種の**適性のみ**を見せており、能力9軸のうち残り7軸
    （瞬発力・勝負根性・柔軟性・賢さ・健康・パワー・精神力）はどの画面にも出していない。
    ⚠️CLAUDE.md §7「その情報は本当に要るか」からやり直す必要がある（全部出す前提にしない）。

33. ⚠️**画面と画面の間の遷移アニメーションが未設計**（`ARCHITECTURE.md`§15に記載）。
    `design/sakurai-specification.md`§17「画面遷移は素早く」が唯一の関連指針で、
    実装方針そのものが未検討。タイトル→夢のダービー→卒業式→週の進行、いずれの遷移も
    現状は瞬間切り替え。

34. ⚠️**判断カードのヒントを消したので、4択の選択率が偏らないかを測る**（2026-09-04・
    CLAUDE.md §10）。ユーザーの判断で選択肢の説明文を画面から消した（「内で待つ」等の
    ラベルだけになった）。⚠️**何を賭けているか分からないまま押す状態になったので、
    どれか1つに偏るなら選択になっていない**という意味になる。⭐測り方：夢のダービーの
    道中・直線それぞれで、4つの選択率を多数のseedで集計する。`hint`のデータは
    `data/judgmentSituations.js`に残してあるので、戻すのは容易。

35. ⚠️**タイトル画面の級数がトークン化されていない**（2026-09-04）。`TitleScreen.css`は
    比較ページでユーザーに見せておらず合意が無いため、今回の対象から外した。
    ⚠️現状は20/14/13/12pxの4種類が生のpxで書かれている。CLAUDE.md §8の手順
    （候補を見せる→選ぶ→合意→実装）でやり直すこと。

36. ⚠️**`.race-top`の`calc(100dvh - 330px)`の330は、判断カードの中身の高さに依存している**
    （2026-09-04）。選択肢の文言を長くしたり個数を増やすと、縦の狭い機種で再び
    枠からはみ出す。⭐**判断カードの中身を変えたら、iPhone SE（320×568）で
    4つ目の選択肢が枠内に収まるかを必ず測り直すこと。**

38. ⚠️**余白が8pxグリッドに乗っていない**（2026-09-05実測）。CSSの余白に**22種類**の値があり、
    8の倍数でないものが多数（**14pxが12箇所で最多**、ほかに2/5/6/7/9/10/11/13/18/22/26/30px）。
    ⭐**文字サイズを5段に畳んだのと同じ整理を余白にもやる。** 8pxグリッド（4pxをグループ内の
    最小刻みとする）へ寄せ、`src/styles/`のトークンにして生のpxを書けなくする。

39. ⚠️**中央揃えが9箇所・左揃えが2箇所**（2026-09-05実測。タイトル画面の
    `.title-screen__content`は2026-09-06の案Aで左揃えに直した）。東美スタディは
    「迷ったら左揃え。中央揃えの多用は注意」としている。残りの箇所（タイトル画面以外の
    画面）はどこを左揃えに変えるかを§8の手順で決める。

40. ⚠️**行間が本文の目安（160〜190%）を下回る箇所がある**（2026-09-05実測）。
    `1.35 / 1.4 / 1.5`を使用。⚠️見出しなら120〜140%でよいので、**どれが本文でどれが見出しか**を
    仕分けてから直す。

41. ⚠️**面積比70:25:5とコントラスト比を測っていない**（2026-09-05）。
    ⭐**ユーザーが「面積比は教科書通りを目指す」と判断した**（`CLAUDE.md`§9の
    「額面どおり満たすと階層が壊れる」という前作の記録とは逆の方針）。
    ピクセルを分類して面積比を測り、本文のコントラストが4.5:1以上かも測る。

44. **判断カードの状況欄に内外が出ない**（同上）。`laneY`は演出用の乱数で自分の馬は0.47固定。
    レースsimが走路上の位置を持つようになったら「7番手 内」の形にする。

52. **タイトル画面の馬に足が無く、浮いて見える**（2026-09-06にユーザーが指摘）。
    `src/screens/TitleScreen.jsx`の`TitleHorse`（Fable作のSVG）。棚上げ。

53. **週の進行の仮simで記号の比重が上がった**（2026-09-07・記号16段化の副作用）。
    ⚠️`domain/raceOutcome.js`の`horseStrengthScore`が使う記号は**7つのうち4つだけ**
    （瞬発力・勝負根性・柔軟性・パワー。賢さ・健康・精神力は着順に関与していない）。
    式は`speed*0.5 + stamina*0.3 + その4つの合計*2`。
    4つの合計が0〜28から0〜60になったため、**記号の比重が41%→60%へ動いた**。
    プレイヤーも相手も同じ式なので勝率の期待値は動かないが、どの能力が着順を動かすかは変わった。
    ⚠️この仮simは本筋4でレースsimに置き換える予定なので、そのときに一緒に直す。
54. **卒業式の成績表で最高評価の記号が右端で切れる**（2026-09-07に確認）。
    `screens/GraduationScreen.css`の`.score-grade`の右余白が足りず、`S+`だとパネル右端を
    約12px超える。⚠️今の成績表はG〜Dしか出さない仕様なので実害は出ていない。
    `ARCHITECTURE.md`§11に元々「Sが出るとはみ出す」と書かれていた既存の問題で、16段化で悪化した。
55. **騎手の適性の上限判定が1段ずれた**（2026-09-07・記号16段化）。
    `domain/jockey.js`の`growAptitude`は「最高評価にできる数」をランクで制限するが、
    16段化で最高評価がSからS+になったため、**Sまでは無制限・S+だけ制限**という形になった。
    設計判断として通したが、旧仕様（Sの時点で制限）と意図がずれていないか確認が要る。
56. **スピードがレースsimの着順にあまり効いていない**（2026-09-07・変換式の再設計で計測）。
    18頭の架空馬で2400mを400レース走らせたところ、着順との相関はスタミナ−0.658に対し
    **スピードは−0.244**。⚠️架空馬の分布を釣鐘型にすると−0.129までさらに落ちる。
    `sim/raceSim.js`の`vMaxMul`の`0.045 * n.speed`と`dash`の`0.5 * ab.speed`を
    上げるかを別途決める必要がある。⚠️計測しただけで、変更はまだ提案していない。
57. **配合で生まれる馬に架空馬のスピード上限を適用しないこと**（2026-09-07）。
    架空馬のスピード上限を史実馬の最低値より下に置く決定をした場合、その上限を配合馬にも
    かけると「配合で史実級の馬を作る」という到達点が原理的に消える。配合の実装時に注意する。
58. **ウイニングポストの3つの適性上限を`adaptability`に取り込むか決める**（2026-09-07）。
    取得済みのデータに「瞬適上限4/8」「持適上限4/7」「消適上限2/4」の3項目がある。
    今のsimの`adaptability`（瞬発戦・持久戦・消耗戦）と対応しそうだが、変換式の設計では
    全部1に固定して測っており、取り込みは検討していない。
59. **記号7軸の母集団分布を取得していない**（2026-09-07）。
    架空馬の記号の引き方を釣鐘型にする案は、「ダービー馬51頭の実測（平均10.10＝B）より
    下になる」ことだけを満たすように決めたもので、全16,570頭の実測に合わせたものではない。
    ⚠️取得済みの`wp-all-horses.json`にはスピードとスタミナしか入っていない。
62. **史実馬の戦績の抽出が6頭で戦数と合わない**（2026-09-07・`tools/check-historical.mjs`の
    検査8が不合格）。ラッキールーラ25/24・シリウスシンボリ25/26・ウオッカ27/26・
    ディープブリランテ6/7・シャフリヤール15/18・ドウデュース17/16。海外遠征の書式差
    （人気が「―」・馬場がAW・着順が取消）を直して11頭から6頭へ減らしたが残っている。
    ⚠️差は±1〜3本で、距離適性の判定（検査2〜4）は全部合格しているため取り込みは止めていない。
    ⚠️**何百頭と足すときに効いてくるので、その前に直すこと。**
60. **1600m以下で追込馬がスタミナに関係なく平均16.0〜16.6着に固定される**
    （2026-09-07・距離適性の設計中に発見。⚠️今回の変更以前から存在する）。
    18頭・180レースの実測で、最適1900mの馬も最適2540mの馬も、追込を選ぶと16着前後で並ぶ。
    `sim/raceSim.js`の`STRETCH_METERS = 600`が距離に関係なく固定であることが疑わしいが、
    ⚠️**そこが原因かは測っていない。**
61. **2400mでは柔軟性が着順をほとんど動かさない**（2026-09-07・実測）。
    18頭・150レースで柔軟性をGからS+まで振っても平均着順の差は0.32着（変更前も0.33着）。
    最適2300mの馬にとって2400mは適正帯のど真ん中で、適性値が0.970〜0.997しか動かないため。
    ⚠️夢のダービー（2400m）だけを遊ぶ間、9つの評価項目のうち柔軟性は画面に出ているのに
    着順に現れない。本筋4で他の距離のレースが入れば解消する見込み。
70. **史実の海外馬を作り、来日と海外遠征の仕組みを作る**（2026-09-15・ユーザーが指摘）。
    ⚠️**仕組みは無い。** 決まっているのは`ARCHITECTURE.md`§10の「海外の馬は乗れない・相手馬として
    だけ存在」だけで、**ジャパンカップに外国馬が来る仕組みも、海外レースに史実の海外馬が出る
    仕組みも作っていない。** ⭐**史実で来た馬は、史実で来た年にほぼ必ず来るようにしたい**（ユーザー）。
    要るもの：①史実の海外馬の能力値（`arch/historical-horses.md`は日本の馬だけ）②JCの招待枠の数
    ③どの馬が来るかの決め方（史実優先＋残りは架空）。⚠️出典が取れるかは未調査。
71. **出走予定の回避・追加（月曜の出走予定が金曜までに変わる仕組み）**（2026-09-15）。
    質問25＝(ア)で出馬表を月曜から見せると決めた際、実装を軽くするため「変わらない」と置いた。
    JRAでは木曜の出馬投票で確定し、枠順確定前の取消は出馬表に載らない（`design/jra-entry-rules.md`§4）。
72. **馬の出来（調子）を入れる**（2026-09-15・質問26＝(イ)で本筋2から外した）。設計は`arch/horse.md`「出来」
    （1本の数値・非単調・朝の調教で乗った厩舎の馬だけ言葉で見える）。数値は全部未定。前提工事の
    ②通う先を選べるようにする ③調教の開示を配線する もここ。①`RACE_INTERVAL_WEEKS`は本筋2で直した。

73. **NPC週次レースの1年あたり出走数が目標に届かない**（2026-09-15・`domain/npcWeeklyRace.js`実装時に
    実測）。実測3.68走／年（5年平均）——`arch/horse.md`の目標6.3走の58%。⚠️原因は出走枠（週あたりの
    レース数×出走頭数）に対し「次走間隔2〜7週」で出走待ちになる頭数が上回り、収得賞金順の選抜で
    待たされる馬が生じるため。出走枠の見直し（レース数を増やす／間隔を長くする等）で近づける。
74. **NPC週次レースの処理が`tools/bench-weekloop.mjs`（型付き配列）の約20倍遅い**（2026-09-15実測
    37.77ms/週 vs 0.98ms/週→`domain/weeklyCard.js`化した後の再実測で20.15ms/週 vs 0.98ms/週。
    週の番組表をレースごとに毎回作り直す無駄が減った副次効果で、狙って直したわけではない）。
    実際の馬オブジェクトの複製コストが主因のまま残っている。週1回のUI操作の裏では問題にならないが、
    設計③「開始前に104週を無表示で回す」が約2秒（20.15ms×104週）かかる計算になる。
    型付き配列化などの追加の最適化が要るかを判断すること。
76. **新2歳馬の能力が親から遺伝しない**（2026-09-15・`domain/yearBoundary.js`実装時に確認）。
    `generateHorse`は毎回能力を新規に振るだけで、`sireId`/`damId`は記録に残るが配合としては
    使われていない。配合（ニック・インブリード・系統確立・因子・レシピ）は前作`Roadrace_Game`の
    設計パターンを借りる想定（`ARCHITECTURE.md`§12）だが、本筋2ではまだ未着手。

78. **夢のダービー〜卒業式の絵5件**（2026-09-15・通しプレイ①の「その他」）。①目覚めの部屋で寝ている
    人物が枕の上の生首に見える ②壁の絵が馬に見えない ③窓の外が殺風景 ④名前入力の建物が殺風景
    ⑤卒業式の場所がどこか分からない。`DreamDerbyScreen`の目覚めと`GraduationScreen`。
    ⚠️§8の手順で候補を見せてから直す。本筋2が終わったら1回のまとまりとして着手。

80. ⚠️**史実のダービー馬51頭の芝ダ適性がランダムに振られている**（2026-09-16・`devlog/wave08.md`§4）。
    `data/derbyHorseAbilities.js`は9軸しか持たず、芝ダ適性は`generateHorse`のランダム生成の
    ままなので、**シンボリルドルフが「芝×」**になる（85頭中13頭が芝×）。
    ⚠️そのため夢のダービーだけ適性の係数を切っている（`aptitudeFactors: false`）。
    ⭐史実馬の能力値なのでCLAUDE.md §17によりユーザーの確認を通してから入れる。
    入れた後は、判断カードの1着率を`tools/measure-card-winrate.mjs`で測り直すこと
    （2026-09-16の実測は一番良い選び方69.5%／一番悪い27.5%・シード200通り）。
    ⚠️道悪適性も同じ（史実馬は未設定＝割引なしで動いている）。

79. **タイトル画面で、重賞データが無い開始年を選べなくする**（2026-09-16・`domain/bootstrap.js`
    実装時に追加）。判定そのもの（`assertStartYearData(startYear)`）は実装済み。画面側
    （`screens/TitleScreen.jsx`の該当年ボタンを押せなくする・理由を伝える見た目）は未着手。
    ⚠️画面の見た目・文言の仕事なので`arch/ui-fable.md`によりFableで行う。

81. **クラスが馬の能力をほとんど分けていない**（2026-09-16に実測で発見・`devlog/wave08.md`§6-3）。
    現役7,494頭で測ると1勝クラスの能力平均0.4615が未勝利の0.4841より**低い**。勝ち上がりの
    選別圧が弱い（スピード上位20%の勝率13.4%・下位20%7.1%＝1.9倍）ため、数戦ではクラスが
    能力順に並ばない。⚠️**勝ちタイムはクラスではなく出走馬の能力平均で決めているので、
    タイムのほうは実データどおり（重賞と新馬・未勝利で4.18秒差）になっている。**
    ⭐直すなら、出走レースの選び方（弱い馬が強いレースに出ない）か選別圧そのもの。

82. **レースの勝ちタイムを戦績に残していない**（2026-09-16）。`appendRaceResult`が保存するのは
    着順・頭数・人気・週・年・競馬場・馬場・距離・馬場状態だけで、タイムと上り3Fが無い。
    馬柱を作るときに要る。⚠️`runRaceSim`の戻り値には`winnerTime`・`last3F`・`last4F`がある。

83. **`arch/horse.md`の9軸の表と、simの実際の効き目が2か所食い違っている**（2026-09-16に実測・
    `devlog/wave08.md`§7-1）。`tools/measure-ability-axes.mjs`で、1頭の1軸だけをG→S+に
    入れ替えて着順の動きを測った（芝1800m・良・12頭立て・400レース）。

    | 軸 | `arch/horse.md`の記述 | 実測（芝1800m） | 実測（芝2400m） |
    |---|---|---|---|
    | 柔軟性 | **単独では動かさない**。スタミナと組んで適距離の幅 | **3.27着** | 2.31着 |
    | 瞬発力 | 末脚。2〜4着分 | 2.54着 ✓ | 1.47着 |
    | スピード | — | 2.09着 | 0.93着 |
    | パワー | 坂と重馬場 | 1.68着 | 1.03着 |
    | 勝負根性 | 最後の粘り。1〜3着分 | 1.00着 ✓ | 0.56着 |
    | **精神力** | **遠征で弱体化しない**（＝着順は動かさないはず） | **0.96着** | 1.39着 |
    | スタミナ | 後天的に伸びない | −0.56着 | **5.36着** |
    | 賢さ | ⭐着順を直接は動かさない | −0.09着 ✓ | 0.02着 ✓ |
    | 健康 | ⭐着順を動かさない | 0.00着 ✓ | 0.00着 ✓ |

    ⚠️食い違いは2つ。**①柔軟性が9軸で一番大きい**（表では「単独では動かさない」）。
    適距離の幅を広げる＝どの距離でも距離適性が高くなるので、距離適性が着順の主因である以上
    一番効いてしまう。**②精神力が約1着動く**（表では遠征専用）。`sim/raceSim.js`の
    発走の加速`dash = 0.5×speed + 0.3×power + 0.2×精神力`に入っているため。
    ⭐**どちらを直すか（表の記述か、simか）を決める。** 賢さ・健康が0なのは設計どおりで正常。

84. **騎手が馬の芝ダ適性の0.16倍になった**（2026-09-16・`devlog/wave08.md`§8-7）。
    ウイポの実測に馬場適性を合わせた結果、馬の芝ダ適性が1.44着→**5.05着**になり、
    騎手は0.83着のまま。⚠️2026-09-16のユーザー決定は「騎手＝馬の芝ダ適性の1.5倍（0.9着）」
    だったので、比では逆転している。⭐**選択肢に書かれていた絶対値0.9着のほうを保った**
    ——1.5倍を保つと騎手は7.6着になり、脚の総量に掛ける幅が馬の能力より大きくなる。
    ⚠️ユーザーに上げ直すか確認する。

85. **競馬場ごとの施行距離が「今のJRAの表」になっている**（2026-09-16・`devlog/wave08.md`§9）。
    1970〜80年代の年ごとの施行距離は承認済みの出典から取れていないため、今の表と史実の
    重賞データ（1972〜1989年）の和集合を置いた。⚠️東京の芝2300m・中京の芝1600mなど、
    改修後にしか無い距離が混じる。⚠️**福島のダートは出典から取れておらず仮の値。**

91. **重賞に乗れる判定は、1年目（52週）では一度も画面に現れない**（2026-09-17に実測・
    `devlog/wave09.md`§11）。初めて重賞の依頼が出るのは平均89週（1年8か月）で、52週では
    5通り全部で0件。⭐本筋5（ユーザーが1年目を通しプレイ）では確かめられない機能である。
    ⚠️新人騎手として筋は通るので「直す」とは限らない。2年目まで通すか、閾値を下げるか、
    1年目に別の形で門の存在を見せるかを決める。
    ⭐**2026-09-17（第10弾の後）に測り直しても同じ**：条件5通り×52週＝260週の依頼2,940件のうち
    重賞は0件（`devlog/wave10.md`§7）。⚠️**ただし「初出は平均89週」の方は第9弾の値で、
    ローテーションを作り直す前の世界（新馬戦しか行われない状態）で測っている**——
    この89週は測り直しが要る。複数年の通しで測ること（52週の通しは今なら約5秒）。

98. **ローテーションの計画立てが1週あたり104msかかる**（2026-09-17に修正・`devlog/wave10.md`§6）。
    ⭐**当初の悪化（事前シミュレーション104週が2秒→23.8秒・1週平均430ms）は直した**
    ——`sim/stamina.js`に`aptitudeParamsOf`／`distanceAptitudeFrom`を足し、1頭の距離適性の
    もとを候補レースごとに計算し直すのをやめた。実測で事前シミュレーション6.4秒
    （3.7倍速い）・1週平均104ms（4.1倍速い）。計画の中身は1件も変わらないことをハッシュで確認済み。
    ⚠️**残りは構造的な費用**：毎週1,840頭（現役の36%）が約700件の候補を見ている。
    さらに速くするには「目標を探す範囲52週を短くする」か「出走枠から外れた馬が毎週
    立て直すのをやめる」のどちらかで、**どちらもユーザー決定が要る**（§6に選択肢を記載）。
    今の104msは週を進めるたびの待ちとしては許容範囲なので、体感で気になったときに再検討する。

100. **断るコストの減点（-2）が、通知の閾値（4）に届かず画面に出ない**（2026-09-17に発見・
     `devlog/wave10.md`§9）。#90の測り直しで、断るコスト自体は正しく発動している
     （52週×5通りで合計91件）と分かったが、`BIG_TRUST_CHANGE_THRESHOLD`(4)が
     `DECLINE_MAIN_MOUNT_TRUST_LOSS`(2)より大きいため、`bigTrustChange`通知が一度も出ない。
     ⚠️**プレイヤーは「主戦を断ると信頼が下がる」という仕組みの手応えを、今のままでは
     一度も画面で見られない**（CLAUDE.md §0-2「奪う側の設計」が伝わらない）。
     ⭐直し方の候補（どちらもユーザー決定が要る）：①断るコストの通知だけ閾値を下げる／
     ②本筋3の週の画面（合意済みのモック）に信頼の増減を毎週素直に見せ、通知の仕組み自体に
     頼らない形にする。⚠️後者なら本筋3の実装と同時に解決する可能性がある。

101. **開始から3週は騎乗依頼が1件も来ない**（2026-09-17に実測・`devlog/wave10.md`§10）。
     `harness-0`で週1〜3が0件・週4から12件。第10弾の計画立て（`domain/rotation.js`）が
     `ROTATION_MIN_LEAD_WEEKS`(3)より先のレースを目標にするので、本編の第1週に立てた計画の
     最初のレースが週4になる。⚠️ゲームの最初の3回の「この週を進める」が空振りになる——
     本筋5（1年目の通しプレイ）で最初に触れる場所。直し方の候補：①事前シミュレーションの
     末尾の計画をリセットせず引き継ぐ（`bootstrap.js`の`resetPlansForMainTimeline`を、週番号を
     ずらして引き継ぐ形に変える）／②最初の3週だけ最短の目標を許す。どちらも設計判断なのでユーザー決定。

102. **所持金が増えるだけで、減る経路が稼働していない**（2026-09-19・「つづきから」に出す
     実データを測る途中で判明）。⚠️実測：開始100,000円→16週後**3,395,000円**（約34倍。
     seed `mock-data-1`・開始年1974・実際に`advanceWeek`を16回）。
     grepで確認した経路は2本だけ——増えるのは`domain/weekResults.js`の`rideIncome`
     （騎乗ごと）、減るのは`domain/weekdayActions.js`の`MEET_PEOPLE_COST`のみで、
     ⚠️**後者のモジュールはどこからも呼ばれていない**（#27の状態が継続）。
     ⚠️`ARCHITECTURE.md`§8の「継続的な支出（金の抜き場）」とショップ4系統は未実装。
     ⭐**本筋4で「つづきから」に所持金を出すと決めたため、増えるだけの数字を
     タイトル画面に出すことになる。** CLAUDE.md §0-2「奪う側の設計」に関わる。
     #27（平日の行動の配線）と同時に見ること。

## ユーザー作業（未完了）

- ⚠️**Wikipedia由来データのライセンス表記を公開前に確認する。** 2026-09-02にデータ源を
  **JRA公式＋Wikipedia（CC BY-SA 4.0）**に確定した（`design/rights-check.md`§6）。
  実装ではデータを別ファイルに分けて出典とライセンスを付ける。公開前にその表記が
  CC BY-SA 4.0の要件（出典・ライセンス名・変更の明記）を満たしているかを確認する。
- ⚠️（経緯の記録）**データ経路の権利関係を判断する。** Claudeが各サイトの規約本文・判例・法の条文を調べて
  `design/rights-check.md`に記録した（2026-09-02）。**Claudeは法律判断をしない。** 事実として：
  - **競馬ブック**は「プログラムでのデータ取り込み」と「個人的な利用の範囲を超える転載・蓄積」を
    規約で明示的に禁止している
  - **netkeiba**は第14条で「事実データも著作物とみなす」「公開はいかなる目的でも私的利用を超える」と定めている
  - **ウイポの史実馬データ**（能力値・成長型）はコーエーテクモの査定値で、著作権法12条の2
    （データベースの著作物）に当たる可能性がある。公式のデータ再利用ガイドラインは無い
  - **JRA公式HTMLの事実データ**（日付・名前・距離・優勝馬）と**馬名**（ギャロップレーサー判例）はリスクが低い
  ⚠️現在の設計は競馬ブック・netkeiba・ウイポの3経路に依存している。**代替経路は同ファイル§3。**

## 恒常タスク（終わりが無い）

- **UIテキスト・ビジュアルの作り込み**（CLAUDE.md §7・§8）。新画面・新機能を作るたびに点検する。
  ⚠️競馬は用語が難しいジャンルなので、**競馬を知らない人が読んで分かる言葉だけ**を画面に出す。
- **バランス計測**（CLAUDE.md §10）。機能を足したら同じ弾の中でbefore/afterを測る。
  第1弾-Aで立てた計測計画は`devlog/wave01.md`§10。⚠️「後で測る」は禁止。
