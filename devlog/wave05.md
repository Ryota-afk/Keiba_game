# 第5弾：史実馬の取り込みルールと較正

⚠️`devlog/wave04.md`が64,599バイトになりRead上限を超えたため、ここから分けた
（CLAUDE.md §4）。第4弾の§27〜§42は`devlog/wave04.md`にある。

## §43 倍率を1頭に合わせていたのをやめ、51頭の戦績で較正し直した（2026-09-07）

### ユーザーの指摘（原文）

> 史実馬の追加ってこれから何百頭何千頭とするので、そこで何かおかしなことが起きないように
> きっちりとルール決めをしておきましょう。
> 倍率に関してはディープインパクトだけに固執してて変ですよ。例えば他の馬も現実の勝ち鞍と
> 照らし合わせて、上手い具合になるように設定すべきじゃない？

⚠️**そのとおりだった。** 51頭745レースの戦績が手元にあるのに、`APT_UNDER_SCALE`を
「ディープインパクトの下端が2000mになる値」から逆算していた。全頭で測り直した。

### ⭐測って分かった、より根の深い誤り

745レースを「馬ごとに中心化した適性」と「馬ごとに中心化した成績」の相関で見たところ、
**短い側と長い側で符号が逆**だった：

| | 相関 | 本数 |
|---|---|---|
| 適正より**長い**レース | **+0.3045** | 164 |
| 適正より**短い**レース | **−0.1305** | 562 |

745本のうち562本（75%）が「適正より短い」に分類されていた。⭐**これは偏りではなく、
最適距離が実際より長く出ていたということ。** 検算：

**「最適距離 − その馬の好走スコアで重み付けした平均距離」は、51頭中51頭が正。中央値+202m。**

⚠️⚠️**原因**：`最適距離 = 1000 + 20 × スタミナ`は最初に置いた仮の値で、**一度も戦績と
突き合わせていなかった**。しかも私は`wd ≈ 905.4 + 17.176 × ウイポのスタミナ`という回帰を
自分で出しておきながら、**それを補正の計算にしか使わず、最適距離そのものには反映していなかった**。

較正：ゲームのスタミナ→実際の重心の回帰は`912.0 + 17.79 × スタミナ`。丸めて
**`900 + 18 × スタミナ`**（スタミナ47→1746m・81→2358m。0〜100で900〜2700m）。

較正の効果（相関）：全体 +0.0476→**+0.1587**／短い側 −0.1254→**−0.0420**（ほぼ0）／
長い側 +0.3045→+0.1759。⭐長短の分割も164対562から**335対395**へ均衡した。

### 短い側の倍率は「勝ったかどうか」からは決められない

較正後も短い側の相関は−0.042でほぼ0。⭐**史実には「適正より短い距離で負けた」証拠が無い**
（陣営が持つと判断した距離しか使わない）。別の基準——**51頭の勝ち鞍303鞍のうち何鞍が
適正帯に入るか**——で決めた：

| 倍率 | 勝ち鞍が帯の中 | 備考 |
|---|---|---|
| 1.1（私が入れた値） | **85.5%** | ディープインパクト1頭から逆算した値 |
| **1.6（採用・元の値）** | **89.1%** | ⭐ここで伸びが止まる |
| 2.6 | 90.1% | ディープインパクトの帯が下限1000mに張り付く |

⚠️**1.1への変更は改悪だった**（3.6ポイント下げていた）。1.6に戻した。

### 決めたこと

- `optimalDistance`を`900 + 18 × スタミナ`へ（100m単位に丸めるのは維持）
- `APT_UNDER_SCALE`を1.1→**1.6**へ戻す
- `APT_A`のコメントから「ディープインパクトで2000〜3200mに一致させる値」という根拠を削除。
  ⚠️**実測では0.25〜2.0を振っても相関は0.18前後でほとんど動かず、この値は決まっていない。**
- **`arch/historical-horses.md`を新設**（何百頭・何千頭のための手順の正本）。
  ⚠️§3に**較正が有効な範囲はスタミナ47〜81だけ**という外挿の警告を明記。
  スプリンター・ステイヤーをまとめて足すときは、そのグループで較正をやり直す。
- **`tools/check-historical.mjs`**（検査7項目）。⚠️全項目合格まで`src/data/`へ入れない。

### 較正後の51頭（全項目合格）

ずれ中央値 **−6m**（+202mから）／勝ち鞍が帯の中 **91.4%**（85.5%から）／
長い側の相関 +0.1759／スピードの逆転0組／範囲外0頭。

⚠️ディープインパクトの適正距離は**2400m・帯1300〜3100m**になった。以前ユーザーが挙げた
「2000〜3200」からは外れるが、⭐**1頭に合わせないという今回の指摘に従った結果である。**

## §44 較正の目標を間違えていた（2026-09-07・同日中に自分で発見して差し戻し）

⚠️⚠️**§43の較正（`900 + 18 × スタミナ`）は誤りだった。** ユーザーに一覧を見せる直前に
各馬の値を並べて気付いた：**ダービー馬なのに2400mが適正帯の外に出ている馬が10頭いた**
（ウオッカ1000〜2300、ドゥラメンテ1300〜2300など）。**51頭全員が2400mを勝っている**のに。

帯の外に出た勝ち鞍26鞍の内訳：**2400mが12鞍**・3000mが6鞍・3200mが5鞍・1000mが2鞍・1200mが1鞍。

### 何を間違えたか

⭐**較正の目標に「全レースの重み付き平均距離」を使ったこと。** この指標は
**2〜3歳時の1600〜2000m戦の本数に引っ張られるだけで、適性を測っていない。**
ダービー馬は若い頃に短い距離を多く走るので、平均を取ると必ず短く出る。
§43で「51頭中51頭で+202m」を「系統的な偏り」と読んだが、**それは偏りではなく正しい差**だった。

### 測り直し（合わせる先を勝ち鞍にした）

| 対応 | 勝ち鞍が帯の中 | 2400mが帯の外 |
|---|---|---|
| 900 + 18（§43の較正） | 91.4% | **10頭** |
| 950 + 19 | 97.0% | 1頭 |
| **1000 + 20（元の値・差し戻し）** | **96.4%** | **0頭** |
| 1100 + 22 | 92.7% | 0頭 |

⭐**元の`1000 + 20 × スタミナ`が最良だった。** 差し戻した。

### 検査項目のほうを直した

⚠️**間違った指標を検査に入れていたのが最大の問題。** `arch/historical-horses.md`§4の
検査2を差し替えた：

- 旧：「最適距離 − 全レースの重み付き平均距離」の中央値が±50m以内 ← **この目標が誤り**
- 新：⭐**そのグループの全頭が勝っている距離があるなら、それが適正帯の外に出る馬は0頭**
  （ダービー馬なら2400m。`COMMON_WIN_DIST`で指定）

検査3の合格ラインも85%→**95%**へ上げた（現状96.4%）。

### 差し戻し後の7項目（全合格）

逆転0組／2400mが帯の外0頭／勝ち鞍が帯の中 **96.4%**（292/303）／長い側の相関 **+0.3045**／
範囲外0頭／較正範囲スタミナ47〜81。

⭐**§43で「較正した」と報告した数字（ずれ−6m・勝ち鞍91.4%）は、誤った目標に対する
合格だった。** 指標が間違っていれば、その指標での合格は何も保証しない。

## §45 短距離まで届きすぎていたのを直した（2026-09-07・ユーザー指摘）

### ユーザーの指摘

> これでもまだ全体的にちょっと短い気はするけど。スプリントまで対応できる馬がちょっと多い
> 気はする。特に昔の馬。柔軟性も同時に調整していい感じにできないかしら。

実測：**51頭中33頭が適正距離の下限1400m以下**（12頭が1200m以下）。年代別では
1974-89が16頭中14頭・1990-04が15頭中10頭・2005-24が20頭中9頭で、**指摘のとおり昔の馬に偏る**。

### ⭐なぜ昔の馬か——2歳戦だった

史実では昔の馬は確かに短距離で勝っている（1974-89は16頭中12頭が1400m以下で勝利。
2005-24は0/20）。⚠️**だが年齢を調べたところ、51頭の1400m以下の勝ち鞍18鞍のうち16鞍が
2歳時**で、その大半が新馬・未勝利戦だった。⭐**当時のデビュー戦が短かっただけで、
完成後の距離適性の証拠にならない**（能力値はウイポの完成時の値）。
3歳以上に限ると最短の勝ち鞍は1400m（バンブーアトラス・カツラノハイセイコの2鞍）、次が1600m。

### 直した値（案A）

| | 前 | 後 |
|---|---|---|
| 得意距離 | `1000 + 20 × スタミナ` | **`1100 + 20 × スタミナ`** |
| 短い側の倍率 | 1.6 | **1.2** |
| 幅（柔軟性） | `400 + 1200 × 柔軟性` | **変えない** |

⚠️**柔軟性の幅の式を動かしても、この問題は直らなかった**（`500+1000`や`300+1000`を試した結果、
勝ち鞍が適正距離に入る割合が97.7%・70.7%へ下がるだけだった）。**原因は短い側の倍率のほう。**

結果：下限1400m以下が**33頭→2頭**（コーネルランサーとカツトップエースのみ）、
下限の中央値1400m→**1700m**、上限の中央値2700m→**2800m**、
**3000m級の勝ち鞍が適正距離に入る割合が93%→100%**。

### ⚠️抽出のバグを2つ見つけた

1. **日付・レース名・格をまとめて拾う正規表現に変えたら、745本→713本に減った。**
   ロジユニヴァースの2009年（ダービー勝ちを含む）が丸ごと落ち、ディープスカイの着順がずれた。
   ⭐**「人気・着順・斤量・馬場・距離」だけを拾う短い正規表現へ戻し、年は別に持ち回った。**
2. **海外遠征の行を落としていた**（人気が「―」・馬場が`AW`・着順が`取消`）。
   ⚠️これを検査項目8「抽出した戦数がページ記載の戦数と一致するか」として追加したところ、
   **11/51頭で不一致**が判明。書式差を直して6頭まで減らしたが残っている（`TODO.md` #62）。

### 検査（8項目中7項目合格・8だけ不合格）

逆転0組／2400mが帯の外0頭／勝ち鞍が適正距離の中 **96.7%**（208/215・3歳以上）／
長い側の相関 **+0.3190**／範囲外0頭／較正範囲47〜81／⚠️抽出の取りこぼし6頭。

## §46 柔軟性を実際の勝ち鞍の幅で補正した（2026-09-07・ユーザー指摘）

### ユーザーの指摘

> 柔軟性を変えて欲しいと言ったのは、必要以上に適正距離の幅を広くする必要はないという意味です。
> 例えば現実で勝ち鞍が2000〜2400mしかない馬の適正距離を1600〜3200mに設定するのは正しくありません。

⚠️**§45で「柔軟性の幅の式を動かしても直らない」と報告したのは、別の問題（短距離まで届く）に
対する答えで、ユーザーの意図とずれていた。** ⭐**スピードとスタミナには戦績の補正を入れたのに、
柔軟性だけウイポの記号をそのまま使っていた。**

### 実測

- ウイポの柔軟性から出した幅（平均**1161m**）に対し、**実際の3歳以上の勝ち鞍の幅は平均628m・
  中央値600m**。⭐**ほぼ2倍広い。**
- 記号と実際の幅の相関は**0.6798**（記号自体は情報を持っている。捨てる必要はない）。
- 極端な例：ワンアンドオンリーは2400mで2勝しかしていないのに帯が1700〜2800m（+1100m）。
- ⚠️**勝ち鞍数の影響が混じる**：1鞍4頭は幅0m・2鞍11頭は309m・6鞍以上13頭は1092m。
  ⭐**幅0mはデータ不足であって、適性が狭い証拠ではない。**
- 柔軟性の段ごとの実際の幅（4鞍以上に限る）：D 400m／C 400m／C+ 743m／B+ 700m／A+ 1055m／S 1800m。

### 直した2点

1. **`aptitudeWidth`を`400 + 1200×柔軟性`→`300 + 1000×柔軟性`**（上限1600m→1300m）
2. **柔軟性の記号自体を実際の勝ち鞍の幅で補正**（`arch/historical-horses.md`§2に手順）
   ```
   段 = ウイポの段 + clamp(round(15 × 信頼度 × (実際の幅 − 予測) / 1000), −3, +3)
   予測   = −140 + 1411 × (段/15)          ※4鞍以上の26頭で回帰
   信頼度 = min(1, (勝ち鞍数 − 1) / 4)      ※1鞍なら0・5鞍以上で1.0
   ```
   ⚠️**上限±3段が要る**——付けないとキズナがA+→D（7段）・カツラノハイセイコがS→S+（9段）
   まで動き、**ウイポの値をほぼ捨てる**。51頭中39頭が動いた。

### 結果

| | 前 | 後 |
|---|---|---|
| 帯が実際の勝ち鞍の幅を超える量（中央値／最大） | +300m／**+900m** | +200m／**+400m** |
| 勝ち鞍での距離適性0.8以上 | 100.0% | **100.0%**（最小0.801） |
| ダービーが帯の外 | 0頭 | 0頭 |

### ⚠️検査3の測り方を変えた

**「勝ち鞍が適正帯（適性0.9以上）の中に入るか」で測ってはいけない。** 帯を実際の幅に
合わせて狭めると、適性0.85で勝った鞍が外に出て不合格になる。⭐**「勝ち鞍での距離適性が
0.8以上か」で測る**（現状100%・最小0.801）。あわせて検査3-b「帯が実際の勝ち鞍の幅を
超える量」を追加（中央+300m以内・最大+500m以内）。

⚠️検査6の照合用の表示が補正後の柔軟性を出していたので、補正前（`flex_wp`）に直した。

## §47 ユーザーが51頭の能力値を承認した（2026-09-07・CLAUDE.md §17）

⭐**2026-09-07、ユーザーが一覧を見て「いい感じ」と承認。** §17（史実馬の能力値は実装前に
ユーザーの確認を通す）の確認が済んだ。承認された内容は`arch/historical-huses.md`§2の変換式と、
それが出す51頭の値（アーティファクト`0c4c68f7-bd85-4371-bedb-fbb80c0d06c5`）。

**承認時点の検査結果**（`tools/check-historical.mjs`・8項目中7項目合格）：
スピードの逆転0組／2400mが帯の外0頭／勝ち鞍での距離適性0.8以上が100%（最小0.801）／
帯が実際の幅を超える量 中央+200m・最大+400m／長い側の相関+0.3236／値の範囲外0頭／
較正範囲スタミナ47〜81／⚠️抽出の取りこぼし6頭（`TODO.md` #62・未解決）。

### ⚠️次にやる実装（`claude-sonnet-5`へ切り替えてから）

1. **51頭の値を`src/data/`へ入れる**（名前・9軸・脚質・適応・年・顕彰馬か）
2. **`domain/dreamDerby.js`を実データに繋ぐ**。⚠️`RIVAL_SPEED_MIN`／`RIVAL_STAMINA_MIN`／
   `RIVAL_GRADE_STEPS`（相手馬を手続き的に底上げする定数）は**不要になるので削除**
3. **`BOOSTED_SPEED_MIN`の再調整**。⚠️今の80は「旧式でのディープインパクトと同値」で、
   相手17頭に自分より速い馬が1頭もいなくなる値だった（`devlog/wave04.md`§43）
4. **`generateHorse`の分布を案(b)へ**（ユーザーが選択済み）。
   スピード`round(6 + 66 × (r1+r2+r3)/3)`・記号7軸も同じ3回平均。⚠️スタミナは対象外
5. **計測**（CLAUDE.md §10）：夢のダービーの勝率・勝ちタイム・着差のbefore/after

## §48 51頭の実データを実装した（2026-09-07・`claude-sonnet-5`）

§47の5項目を実装した。

### 1. `src/data/derbyHorseAbilities.js`を新規作成

`arch/historical-horses.md`§2の変換式が出す51頭の確定値（承認済み）を、`data/derbyWinners.js`の
`horse`名をキーにしたオブジェクトで持つ。柔軟性は実際の勝ち鞍の幅で補正した後の値
（`flexibility`。補正前のウイポ値は保存していない——`arch/historical-horses.md`側に
手順として残っている）。脚質は参考値として4種へ対応させたものを`legStyle`に持つが、
⚠️**simにはまだ接続していない**（`domain/strategy.js`の`deriveFavoredStrategy`が能力値から
導く既存のヒューリスティックのまま。史実の脚質をsimへ反映するかは未決定・別途相談）。

### 2. `domain/dreamDerby.js`の`generateDreamRivals`を実データに接続

`RIVAL_GRADE_STEPS`・`RIVAL_SPEED_MIN`・`RIVAL_STAMINA_MIN`（手続き的な底上げ）を削除し、
`DERBY_HORSE_ABILITIES[record.horse]`をそのまま使うよう差し替えた。

### 3. `BOOSTED_SPEED_MIN`を測り直した

旧80は「旧式の変換でのディープインパクトと同値」という理由のない値だった
（`devlog/wave04.md`§43。相手17頭に自分より速い馬が1頭もいなくなっていた）。
devlog §28-3と同じ方法（持久力＝スタミナ×距離適性×傾向への適応が18頭の中央値以上なら
「前で運んで早く仕掛ける」型として、判断カードを実際にその型へ合わせる／外す）で測った。

| speedMin | 両方合わせる | 道中だけ | 直線だけ | 両方外す |
|---|---|---|---|---|
| 80 | 69.7% | 67.0% | 43.3% | 14.3% |
| **85（採用）** | **74.3%** | 70.3% | 44.3% | **14.0%** |
| 88 | 72.3% | 70.7% | 44.3% | 17.3% |
| 92 | 70.3% | 67.7% | 46.7% | 17.3% |

各300本（`saveSeed`固定の擬似乱数列）。⭐**85を採用**——目安（両方合わせて7〜8割）の上側に
最も近い。⚠️**「両方外す」の勝率は80〜92のどこでも14〜17%で、目安の2〜3割まで
上げられなかった**。相手が旧式の手続き生成（弱め）から実データの51頭（史実の名馬・
最大speed92）に変わったことで、外したときの下限がそのぶん下がったまま動かない。
⚠️`BOOSTED_SPEED_MIN`を上げても下限はほぼ動かないので、これ以上はスピードでは調整できない
（`TODO.md` #63）。

### 4. `generateHorse`の分布を案(b)へ

`src/data/grades.js`に`pickGradeBellCurve`を追加（一様乱数を3回引いて平均）。
`domain/horse.js`の`generateHorse`のspeedと記号7軸に適用。⚠️**スタミナは対象外**
（強さの軸ではなく距離の位置を決める軸）。⚠️**騎手の適性・厩舎の能力は対象外**
（`pickGrade`のまま。この決定は架空馬の記号7軸だけに限る）。

実測（1000頭）：speed 11〜72・平均39.06（史実51頭は66〜92）。
記号平均7.47/15・S以上0.80%・S+0.10%（一様のときS以上12.5%・史実51頭はS以上5.9%）。

### 5. 計測（カード未選択・200本）

勝率48.0%・勝ちタイム中央値144.6秒・**2着着差の中央値2.7m**（旧式7.8mより現実的な着差に
縮んだ——相手が実データの強豪ぞろいで拮抗するため）・勝ち馬の上がり3F中央値34.3秒。

`npm run build`成功。Playwright（iPhone 13・外部ネットワーク遮断）でタイトル→夢のダービー→
判断カード2回→「卒業式へ」到達までJSエラー0件を確認。

**棚上げに上げたもの**：`TODO.md` #63（「両方外す」の勝率が目安の2〜3割まで下げられない）。

## §49 本筋1が終わり、棚上げを見直した（2026-09-07・CLAUDE.md §16）

本筋1（レースsim）と2（実況の書き直し）が完了したので、次の項目へ進む前に棚上げを1回見直した。

### ⭐解消していた2件（棚上げから削除）

**#51 着差が実際のGⅠより広い**。⭐**史実馬51頭の実データ接続で解消した**（300本の実測）：

| | 前 | 後 |
|---|---|---|
| 2着の着差（中央値） | 7.8m | **2.4m**（本来1〜3m） |
| 3着 | 14.3m | 4.3m |
| 5着 | 21.8m | 8.4m |
| 最後方と勝ち馬の差 | 60.4m | 33.7m |
| 掲示板で「大差」と出る回（3着／5着） | 10%／32% | **0.0%／0.0%** |

⚠️#51には「**夢の馬を強くすることと着差を詰めることは逆を向く**」と書いていたが、
⭐**両立した**——勝率（型に合わせて74.3%）を保ったまま着差が縮んだ。相手が手続き生成の
弱い馬から実データの強豪ぞろいに変わり、18頭が拮抗したため。

**#43 判断カードの反応実況が中団の8択にしか無い**。⭐**本筋2の実況書き直しで解消済み**。
実測：`dream*`の**全32択すべてに`choiceReact`のキーがある**（無い択0）。

### 新しく棚上げに足した1件

**#65 週の画面に開発語彙がそのまま出ている**。通しプレイ用のビルドで確認した実測：
「騎手：佐々木悠真（**rookie**）」「札幌 **dirtsprint**」「小倉 **turflong**」
「函館 **turfintermediate**」。⚠️CLAUDE.md §7の違反。本筋3（週の画面）で直す。
⚠️通しプレイの対象（タイトル→夢のダービー→卒業式）の外なので、通しプレイは止めない。

### 通しプレイ用のビルドを公開した（本筋1へ）

Playwright（iPhone 13・外部ネットワーク遮断）で**タイトル→夢のダービー→判断カード2回→
掲示板→目覚め→卒業式→名前入力→式典→成績表→厩舎選択→1年目1週目**まで通し、
**JSエラー0件**を確認。アーティファクト形式（`<head>`を剥がした形）に変換したものでも
同じ経路を通し、やはりJSエラー0件だった上で公開した。

`https://claude.ai/code/artifact/d14419ad-f9a6-4823-a916-a66f403f9a21`

⚠️**このアーティファクトの更新通知はこのセッションでは受け取れない**（購読が403で拒否される）。

### TODO.mdの本筋を5項目に詰め直した

完了した本筋1・2を削除し、通しプレイが新しい本筋1になった。

## §50 通しプレイの指摘8件（2026-09-07・本筋1・ユーザーの実プレイ）

CLAUDE.md §0-4の「人間の通しプレイ」を実施していただき、8件の指摘を受けた。
⭐**ヘッドレスの計測とPlaywrightの自動確認では1件も検出できていなかった。**

### ① タイトル画面の馬の足が消えている（⭐3回目の指摘・直した）

⚠️⚠️**棚上げ#52として置いたままだった。3回言われた時点で棚上げの判断が誤りだった。**

**原因**：`TitleScreen.css`の
`animation: 0.3s steps(1, end) infinite;`——**名前を持たない一括指定**。
仕様上これは`animation-name: none`と同義なので、ビルドのCSS圧縮が`animation: none`へ畳む。
すると`animation-duration`が0秒に戻り、後続の`animation-name: th-f1`だけでは戻せず、
⭐**3コマとも`opacity: 0`のまま＝足が1本も出ない**。

⭐**レース画面のスプライトが無事だった理由**：`animation: var(--gallop,.3s) steps(1,end) infinite`
と`var()`を使っており、圧縮ツールが畳めなかった。**同じ書き方をした2箇所で、片方だけ壊れていた。**

⚠️⚠️**開発サーバーでは動き、ビルドしたときだけ壊れる。**

**直した**：一括指定をやめ、`animation-duration`/`animation-timing-function`/
`animation-iteration-count`の個別指定にした。ビルド後のCSSに`animation-duration:.3s`が
残ることと、実機サイズ（iPhone 13）のスクリーンショットで**足が接地して見えること**を確認した。

### ⚠️なぜ3回も見逃したか

⭐**私のPlaywright確認が「JSエラー0件」と「最後まで通るか」しか見ておらず、
一度も絵を見ていなかった。** タイトル画面は毎回通過していたのに、
スクリーンショットを撮っていなかった。→ CLAUDE.md §14に1行追加した。

### ②③⑤ 追い込み馬が固まる／前有利すぎる／内で待つと下がりすぎ（同じ原因）

`domain/strategy.js`の`deriveFavoredStrategy`は「スピード ≥ スタミナなら前に行きたい」と
判定するが、⚠️**史実51頭はスピード66〜92・スタミナ47〜81なので50/51頭が該当する**。

| | 逃げ | 先行 | 差し | 追込 |
|---|---|---|---|---|
| simが決めた脚質（51頭） | 21 | **1** | 29 | **0** |
| ⭐史実の実際の脚質（`legStyle`） | 5 | 17 | 23 | 6 |
| 300レース・5400頭での勝率 | 11.3% | 22.9% | **0.5%** | 0.0% |

⭐**18頭が「逃げ8頭・差し10頭」の2グループに割れ、同じ脚質は`TARGET_GAP_BY_STRATEGY`が
同じ値（逃げ3m・差し19m）なので塊になる。** 差しの2967頭のうち勝ったのは0.5%。

⚠️⚠️**これは私が今日「スコープ外」として棚上げした#64（史実の脚質がsimに未接続）そのもの。**
データを入れておきながら繋がないと、こういう形で体験に出る。

### ④⑤⑦ 判断カードの択（目標位置の移動量を実測）

`targetGap`は`-swing × 62`で動く（`swing = (積極性 − 0.35) × 賢さ倍率`）。賢さB＝倍率1.0のとき：

| 択 | 積極性 | 目標位置の移動 |
|---|---|---|
| 間を割る | 0.79 | **−27.3m（前へ）** |
| ペースを上げる／外から上がる | 0.71 | −22.3m |
| 並びかける／外に出す／外へ出す／早めに動く | 0.63 | −17.4m |
| 下げて外へ | 0.47 | −7.4m（⚠️名前は「下げて」なのに前へ出る＝#50） |
| **このまま行く／このまま** | 0.15 | **+12.4m（後ろへ）** ⚠️ |
| 内で待つ | 0.15 | +12.4m |
| 少し緩める／最後まで待つ | 0.07 | +17.4m |

⚠️⚠️**「このまま」を選んでも12.4m下がる**——中立値が0.35なのに据え置き系の択が0.15のため。
**日本語と挙動が矛盾している。** 差し（19m）から「内で待つ」で31.4m＝**追込（29m）より後ろ**。

**⑦が「一瞬」に見える理由**：12〜27mの移動を速度の上限（隊列の±13%＝約±2.2m/s）いっぱいで
詰めるので、27mでも約12秒（レース内）。⭐**×4再生だと3秒で完了する。**
しかも`resumeRaceSim`は選んだ瞬間から新しい目標位置を使うので、目標が階段状に変わる。

### ⑧ 「第92回日本ダービー」と言い切っている

`data/dreamDerbyCommentary.js`に2箇所、`screens/DreamDerbyScreen.jsx`に1箇所。

### 直す案（ユーザー確認待ち）

1. 脚質を`legStyle`（史実）から取る
2. 同じ脚質でも目標位置に幅を持たせる（今は±3mの乱数だけ）
3. 中立の択の積極性を0.35に揃える（「このまま」で位置が動かなくなる）
4. 道中の選択肢を「まくっていく」「内で待つ」「外に出す」「位置を下げる」に（ユーザー提案）。
   ⚠️「間を割る」は直線側へ移す
5. 目標位置を時間をかけて動かす（秒数は実測で決める）
6. 「第92回」を消す（文案を出してから決める）

## §51 判断カードを選んでから位置が動くまでの時間（実測・2026-09-08）

ユーザーの通しプレイ⑦「判断カードを選択したら即反映されて指定位置までほぼ一瞬で下がる」に対し、
「実測して案を出す」と指示された。計測スクリプトは
`/tmp/.../scratchpad/measure-card-lag.mjs`・`measure-ramp.mjs`・`measure-timeline.mjs`（使い捨て）。
seed 1〜12の中央値。`leaderAt - selfD` の、カード未選択のままの値との差を「動く量」とした。

### 現状（目標位置は段差で切り替わる）

⚠️測ったのは**中団（`dreamMidPack`）の4択**。「間を割る」と「内で待つ」が同時に出るのは
中団だけなので、他の3バンド（先頭・前・後方）の値は未計測。
⚠️動く量が10m未満だったseedは除いた（小さい移動は90%到達が短く出て平均を歪めるため）。

| 択 | 動く量（中央値／幅） | 90%到達（中央値／最速） | 最大の寄り速度 |
|---|---|---|---|
| 間を割る | 20.1m前へ（13.0〜30.5） | 11.6s ／ 7.8s | 2.61m/s（32px/s） |
| 外へ出す | 19.9m前へ（13.0〜23.2） | 11.6s ／ 8.1s | ― |
| 内で待つ | 16.4m後ろへ（12.8〜17.8） | 11.0s ／ 8.5s | 2.60m/s（32px/s） |
| 下げて外へ | 10.6m前へ（10.0〜11.1） | 18.9s ／ 11.6s | ― |

⚠️「90%到達」＝カードを選ばなかった場合との差が、最終的な差の90%に達した時刻。
残り10%はその後も動き続ける（動き終わりではない）。

⭐**一瞬ではない**（90%まで11.6秒）。にもかかわらず一瞬に見える理由を3つ特定した：

- **(a) 速度ボタンのチュートリアル（40秒・`requireButton: true`）が、カードより前に必ず1回
  押させる**（押すまで時計が止まる。飛ばす手段は無い）。ボタンは押すたび
  ×1→×2→×4→×1と循環する（`DreamDerbyScreen.jsx:14,138`）ので、1回押した状態＝×2。
  道中カードはレース時計74秒（範囲71.9〜75.5）に出る。⭐**×2なら11.6秒は実時間5.8秒・
  最速seedは3.9秒。×4なら2.9秒・2.0秒。2秒なら「ほぼ一瞬」という体感で正しい。**
  ⚠️「全員が×2以上」とは言えない（3回押せば×1へ戻る）。言えるのは「全員が一度は押す」まで。
- (b) カード表示中は`pauseClock()`で止まっており、選択の0.5秒後に動き出す（助走が無い）。
- (c) `VIEW_SPAN = 32`m を`TRACK_W = 390`px に映す＝1mが12.2px。16〜20mの移動は
  見えている馬群の5〜6割の幅を横切る。

道中カードから最終直線（115.7秒）までは41.8秒。11.6秒はその27.8%。

### 目標位置を秒数をかけて動かす場合（`swing`に`clamp((t-midMoveAt)/RAMP,0,1)`を掛ける）

| RAMP | 間を割る 90% / 最大 | 内で待つ 90% / 最大 |
|---|---|---|
| 0（現状） | 11.6s / 32px/s | 11.0s / 32px/s |
| 4 | 11.6s / 32px/s | 11.5s / 28px/s |
| 8 | 11.7s / 32px/s | 13.8s / 22px/s |
| 12 | 15.7s / 30px/s | 16.6s / 15px/s |
| 15 | 15.9s / 25px/s | 19.0s / 13px/s |
| 20 | 17.7s / 20px/s | 23.1s / 10px/s |
| 25 | 19.0s / 17px/s | 27.3s / 8px/s |

⚠️**動く量（19.9m／16.4m）はRAMPを変えても1cmも動かない。変わるのはペースだけ。**
⚠️⚠️**前へ行く択はRAMP 10以下では寄る速さが1px/sも変わらない。** 道中の速度は
`vField * 1.13`＝周りより毎秒2.13mまでという上限があり、20m前へ詰めるには9.4秒その上限の
まま走る必要がある。RAMPが短いと目標のほうが速く動くので、馬は上限のまま＝現状と同じ絵。
RAMP 12から上限を割り始め、15で25px/s・20で20px/sになる（12〜20は連続的に下がる。
「20で初めて」ではない）。

**案：RAMP = 20秒（`CARD_RAMP_SECONDS`）。** 間を割るは90%まで17.7秒（41.8秒のうち24.1秒
残る）、内で待つは23.1秒（18.7秒残る）。実時間は×2で8.9秒と11.6秒。
⚠️(a)が主因なので、RAMPを入れても×4では17.7秒→4.4秒に縮む（今の×4の2.9秒より1.5秒長い
だけ）。(a)そのものを直す別案として「カードの前後だけ×1に戻す」も提示した。ユーザーの判断待ち。

## §52 道中の判断カードに「動ききるまでの秒数」を入れた（実装・2026-09-08）

§51の実測を受けてユーザーが**20秒**で合意。`src/sim/raceSim.js`に`CARD_RAMP_SECONDS = 20`を
足し、道中の`swing`に`clamp((t - midMoveAt) / CARD_RAMP_SECONDS, 0, 1)`を掛けた。
⚠️直線の択には掛けていない（直線は`delay`＝追い出すまでの待ち時間が同じ役目を持つため）。

### 動く量は変わらず、寄る速さだけが変わった（12通りの乱数の中央値・中団の4択）

| 択 | 動く量（前／後） | 90%到達 前 | 90%到達 後 |
|---|---|---|---|
| 間を割る | 20.1m → 20.1m | 11.6s | 17.7s |
| 外へ出す | 19.9m → 19.9m | 11.6s | 22.2s |
| 内で待つ | 16.4m → 16.4m | 11.0s | 23.1s |
| 下げて外へ | 10.6m → 10.6m | 18.9s | 35.6s |

⚠️「下げて外へ」の35.6sは、カードから直線までの41.8秒のうち残り6.2秒しかない。
今の中団の4択のままだと、この択だけ道中でほぼ動ききらない。
⚠️合意済みの選択肢差し替え（「まくっていく／内で待つ／外に出す／位置を下げる」）で
`effect`を決め直すときに、この数字を見て決めること。

### ⚠️勝率が動いた（§10の計測）

`cardRamp`は`effortMul = 1 + 1.2 * swing`にも掛かる＝まだ動いていないうちは余分な消耗も
払わない。そのぶん前へ行く択が安くなる。200通りの乱数（`scratchpad/winrate.mjs`。
持久力が18頭の中央値以上なら前で運んで早く仕掛ける／外すときはその逆）：

| | 型に合わせる | 型を外す |
|---|---|---|
| 段差（前） | 66.0% | 16.0% |
| 20秒（後） | **72.5%** | 16.5% |
| 20秒だが消耗だけ満額（採らなかった案） | 68.5% | 17.0% |

⭐**採ったのは`effortMul`にも掛ける形**。まだ速く走っていない馬に満額の消耗を課すのは
おかしいため。72.5%は§48で決めた目安（7〜8割）の内側に収まっている。
⚠️この66.0%／16.0%は§48の74.3%／14.0%とは**別の測り方**（§48は択を1つずつ指定、
こちらは`forward`/`early`のタグで機械的に選ぶ）。§48の数字とは比べられない。
比べてよいのはこの表の中の前後だけ。

### 画面上での確認（Playwright・iPhone 13・×1・カメラ「先頭」）

カードで「外から上がる」を選んでから25秒間、自分の馬の`.horse-sprite.is-self`の
`getBoundingClientRect().left`を1秒ごとに読んだ（レース時計も1秒ずつ進む＝×1）：

```
 0s x=-78 |  5s x=-16 | 10s x= 15 | 15s x=134 | 20s x=216 | 25s x=243
 1s x=-72 |  6s x=-13 | 11s x= 28 | 16s x=161 | 21s x=221 |
 2s x=-54 |  7s x= -8 | 12s x= 49 | 17s x=184 | 22s x=223 |
 3s x=-35 |  8s x=  1 | 13s x= 75 | 18s x=200 | 23s x=227 |
 4s x=-19 |  9s x=  9 | 14s x=104 | 19s x=210 | 24s x=233 |
```

25秒かけて321px滑る（平均13px/s・最大は13〜17秒あたりで30px/s）。止まる瞬間が無い。
JSエラー0件。スクリーンショットも目視した（`scratchpad/ramp-*.png`）。
⚠️スクリプトは`scratchpad/ramp-xpos.mjs`。カメラを「自分」のままにすると自分の馬が
画面に固定されて動かないので、「先頭」に切り替えてから測ること。

## §53 ユーザーへ送る文の検査を仕組みにした（2026-09-08）

ユーザーの指摘：**「わからない用語が多いので是正したい。安くなる、満額。今後の説明で、
このゲーム自体を遊んだことがなく、競馬についてもあまり知らない人でも理解できる説明を
することを仕組み化して欲しい。」**

⚠️§15は2026-09-04から「どんな人間が見ても理解できる説明」と書いていた。**基準が言葉
だけだったので守れなかった**——§8の「ルールで守れなかったものは、書けない形にする」と
同じ型の失敗。⭐**機械の検査（言葉の一覧）と、固定した依頼文のサブエージェント検査という
2つの形に落とした。**

### 作ったもの

1. **`tools/check-words.mjs`** — 下書きのファイルを渡すと行番号つきで並べる。
   - **A：使わない言い方**（7組の正規表現）。1件でも出たら終了コード1。
     `安く|割安|割高|高くつ|お得|コストが` ／ `満額|全額|フルに|目一杯|全開で|上限いっぱい` ／
     `効く|効き|効か|効け|効果的|刺さ` ／ `レバレッジ|解像度|構造的|次元|土台|地盤|肝|落とし込|
     寄り添|手触り|温度感|世界観|立て付け|親和性` ／ `最適化|チューニング|ブラッシュアップ` ／
     `ちゃんと|きちんと|うまく|いい感じ|適切に|しっかり|きれいに` ／ `など|等|といった|系`
   - **B：開発の言葉22語**／**C：競馬の言葉28語** — 数えるだけ。初出の行に1行の説明が
     付いているかは自分の目で確かめる。
   - ⭐**「」で囲んだ部分は検査しない。** その言葉自体を話題にする文（このルールの説明・
     過去の自分の文の引用）で必ず引っかかるため。⚠️囲めば禁止語を書けてしまうが、
     囲むこと自体が「引用である」という表明なので抜け道として扱わない。
2. **`arch/report-check.md`** — いつ検査するか・3分類の扱い・書き換えの実例6組・
   **サブエージェントへの依頼文（毎回そのまま使う固定文面）**。依頼文の冒頭に
   「読み手はこのゲームを一度も遊んだことがなく、競馬もあまり知らない人」を置いた。
3. **CLAUDE.md §15を書き換え** — 読み手の前提と、上の2つを通す手順を3行で。
   `ARCHITECTURE.md`冒頭の切り出し一覧にも`arch/report-check.md`を足した。

### 動作の確認

前回ユーザーへ送った文をそのまま入れたところ、**A項目で「安く」と「満額」の2件が止まった**
（＝指摘された2語をこの検査が実際に捕まえる）。今回の返信の下書きも通し、A0件にしてから送った。
⚠️最初に書いた版はA12件だった——**そのほとんどが「禁止語の一覧を説明している行」**。
これが「」除外を足した理由。

⚠️**同日、ユーザーが「競馬用語に関しては俺がわかるから制限しなくていい」と明示。**
C（競馬の言葉28語）を`tools/check-words.mjs`・`arch/report-check.md`・CLAUDE.md §15から外した。
残るのはA（使わない言い方）とB（開発の言葉）。⚠️ゲーム画面に出す言葉（CLAUDE.md §7）は別で、
そちらは従来どおり「競馬を知らない人が読んで分かる言葉だけ」。

## §54 通しプレイの指摘のうち残り6件を実測で具体化した（2026-09-08）

ユーザーが**本筋1に6件を書き足す案（a）を選択**。⭐「指摘が出た＝確認が済んだ」ではなく、
**直してもう一度通してもらうまでが本筋1**（ユーザーの言葉：本筋の定義の確認から）。
⚠️このとき、Claudeが本筋を誤って記憶していたことも判明した（「残り5件」と書いたが指摘は
6件残り・作業は3つ）。計測スクリプトは`scratchpad/legstyle.mjs`・`frontbias.mjs`。

### 指摘②③（脚質の偏りと前有利）

200レース・3600頭ぶん。⚠️`domain/strategy.js`の`deriveFavoredStrategy`が割り当てた脚質：

| 脚質 | 今のsim | 1着率 | 同脚質どうしの前後の開き（道中の中央値） | 実在のダービー馬51頭 |
|---|---|---|---|---|
| 逃げ | 1517頭 42.1% | 11.9% | 5.5m（約7.6頭） | 5頭 9.8% |
| 先行 | 74頭 2.1% | 17.6% | 1.3m（約0.4頭） | 17頭 33.3% |
| 差し | 1993頭 55.4% | 0.4% | 5.9m（約10.0頭） | 23頭 45.1% |
| 追込 | 16頭 0.4% | 0.0% | ― | 6頭 11.8% |

⭐**18頭が2つの塊になる。** `TARGET_GAP_BY_STRATEGY`が脚質ごとに1つの値で、散らばりは
`gapJitter`の±3mだけ。

道中（残り1200m）の順位ごとの最終着順（200レース）：

| 道中の順位 | 平均着順 | 1着率 |
|---|---|---|
| 1番手 | 7.28着 | 20.5% |
| 2〜4番手 | 7.43〜8.07着 | 12.5〜15.5% |
| 5〜9番手 | 9.04〜9.67着 | 4.5〜9.5% |
| 10〜18番手 | 9.68〜10.90着 | 0〜1.0%（200レースで計7回） |

⚠️**②を直せば③も動くはずだが、それで足りるかは未確認。** 逃げの有利は
`positionFactor`（前にいると風よけで脚が減りにくい）からも来ている可能性がある。

### 指摘⑤（「内で待つ」で下がりすぎる）

中団（7〜12番手）で`holdInside`を選んだ71回：**選ぶ前 平均9.0番手 → 400m先で平均17.1番手。
8.1番手ぶん後ろへ。71回すべてで下がり、最大は11番手ぶん。**
⭐原因は`midAggressionOf`が`forward: false`に0.15を返すこと。`NEUTRAL_MID_AGGRESSION`は0.35
なので、**選ばないほうが前にいられる**状態になっている。

### 作業の分け方（`TODO.md`本筋1へ書いた）

- **1-A**：脚質を実データから＋位置の散らばり（②③）
- **1-B**：道中の4択を作り直す（④⑤⑥）
- **1-C**：「第92回」を消す（⑧）

⚠️棚上げ#64（史実馬の`legStyle`がsimに未接続）は本筋1-Aに合流したので削除した。

### ⚠️⚠️§54の②の読み違い（同日に訂正）

ユーザー：**「②について、違う。そもそもの馬の動き自体に違和感があるということ。
それぞれの馬が独立して動いているように見えない。」**
⭐こちらは「脚質の偏り」「同脚質が塊になる」の話だと解釈していたが、指摘は
**1頭1頭の動きそのもの**だった。測り直した（`scratchpad/motion.mjs`・`motion2.mjs`）。

50レース・道中の20〜100秒（80秒間）：

| 測ったこと | 値 |
|---|---|
| 18頭の平均速度の差（最速の馬と最遅の馬） | 毎秒0.059m（0.36%） |
| 1頭が80秒で先頭からの差を動かした幅 | 4.3m |
| 20秒→100秒で順位が1頭あたり何番ずれたか | 1.33番 |
| 順位の並びが1つでも変わった秒数 | 80秒のうち79.3秒 |

⭐**その場で毎秒震えているだけで、80秒走ってもどこへも行かない。**
1レースぶんの実測（seed 3・先頭からの差m。20/40/60/80/100秒）：
6番逃げ 0.0/0.4/0.0/0.2/0.0 ／ 16番逃げ 6.1/3.5/5.1/7.0/6.0 ／
12番差し 16.2/15.4/17.3/15.1/16.6 ／ 1番差し 22.8/19.3/22.5/20.5/20.7。
18頭の道中の平均速度は毎秒16.408〜16.435mの中に全部入る。

⭐**原因**：道中の式は18頭とも同じ形。(1)全馬が同じ基準線`dRef`（毎秒16.4m）を追う
(2)目標の差は`TARGET_GAP_BY_STRATEGY`（脚質ごとに1値）＋`gapJitter`（馬ごとに固定の±3m）
(3)寄る強さは全馬`POSITION_GAIN = 0.3` (4)速度の上下限も全馬同じ（0.86〜1.13倍）。
⚠️**時間で変わる項は`wander`＝2.5×sin(2π(t/周期＋位相))の1本だけ**（周期11〜17秒）。
画面上では30px幅の往復＝ユーザーの言う「楕円状の動き」。

⚠️**③（前有利）は②の結果**だった。平均速度の差が0.36%では道中で順位が動かないので、
道中の順位がそのまま着順になる。§54で「②を直せば③も動くはずだが足りるか未確認」と
書いたのは、原因の切り分けを間違えたまま書いたもの。

## §55 本筋1-Aの設計（2026-09-08・ユーザーの承認待ち）

### ③の原因を切り分けた

⭐**同じ18頭・同じ馬で、自分の脚質だけ4通りに変えた150レース**（`scratchpad/posvalue.mjs`）：

| 自分の脚質 | 平均着順 | 1着率 |
|---|---|---|
| 逃げ（3m後ろ） | 1.95着 | 70.0% |
| 先行（9m後ろ） | 2.91着 | 53.3% |
| 差し（19m後ろ） | 5.50着 | 14.0% |
| 追込（29m後ろ） | 9.19着 | **0.0%** |

⚠️**馬の中身は同じ。道中の位置だけで1着率が70%と0%に分かれる。**

なぜ届かないか（100レース・`scratchpad/stretchclose.mjs`）：

| 脚質 | 残り600mの差 | ゴールの差 | 詰めた量 | 残りの脚 | 直線の平均速度 |
|---|---|---|---|---|---|
| 逃げ | 2.8m | 1.8m | 1.0m | 0.547 | 17.54 m/s |
| 先行 | 8.6m | 3.4m | 5.2m | 0.570 | 17.59 m/s |
| 差し | 18.6m | 8.4m | 10.2m | 0.608 | 17.69 m/s |
| 追込 | 28.7m | 15.7m | **13.0m** | 0.646 | 17.78 m/s |

⭐**追込は13.0m詰めている（動けている）。26m後ろから出るので半分しか返せないだけ。**
⚠️直線の平均速度の差が**1.4%しかない**。脚は追込のほうが0.10多いのに、`vMaxMul`の
`0.2 × e`という係数では2%にしかならない。⚠️`gassed`（0.25を切ると急落）は、逃げでも
0.547残るので**一度も発動していない**＝前で行った馬が最後に止まる、が起きていない。

### ②の設計（5部品）

| 部品 | 今 | 案 | 測る数字（今の値） |
|---|---|---|---|
| 1 脚質を実データから | `deriveFavoredStrategy`で42%が逃げ | 相手17頭は`legStyle`、自分と架空馬は式を書き直し | 内訳（42.1/2.1/55.4/0.4%） |
| 2 目標位置を帯に | 脚質ごとに1値＋固定±3m | 帯（逃げ0〜5／先行5〜13／差し13〜24／追込24〜34m）。帯内の位置は精神力＋固定乱数 | 同脚質の前後の開き（5.5m/5.9m） |
| 3 位置を守る強さ | `POSITION_GAIN = 0.3`全馬同じ | `0.15 + 0.3 × 賢さ`（0.15〜0.45） | 2頭ずつの動きの似かた（中央0.41） |
| 4 正弦波をやめる | `2.5×sin(2π(t/周期+位相))` | 0.25秒ごとに前の値を9割残す不規則な揺れ（相関3秒）。振幅は同じ±2.5m | 位置の記録に周期が残るか |
| 5 道中の仕掛け | 無い | 各馬0〜2回、残り1400〜700mの固定地点で目標位置を変える。量と向きは脚質＋精神力。`CARD_RAMP_SECONDS`の仕組みを流用 | 20秒→100秒の順位のずれ（1.33番） |

⭐**順位を動かすのは部品5だけ。** 2〜4は揺れ方を変えるだけ。

### ③の直し方（3案・推しは(a)+(c)）

- **(a)** `gassed`の境目を0.25→0.55前後へ。逃げ（0.547）だけが垂れ、追込（0.646）は垂れない
- **(b)** `vMaxMul`の`0.2 × e`の0.2を上げる（全体の底上げになる）
- **(c)** `TARGET_GAP_BY_STRATEGY`を3/9/19/29→2/6/12/18mへ圧縮。⭐画面に映るのは32m
  （`VIEW_SPAN`）なので、追込の29mが画面外に出ている問題も直る

### 進め方

1. 部品1〜5 → ②の4つの数字を測る
2. その状態で③を測り直す（②だけで届く可能性がある）
3. 足りなければ(a)(c) → 測る
4. 判断カードの勝率を測り直す（今：型に合わせて72.5%・外して16.5%）
⚠️③を直すと自分が逃げ・先行のときの勝率が下がる。`BOOSTED_SPEED_MIN = 85`も測り直し。

## §56 本筋1-A を実装した（2026-09-08）

ユーザー決定：**部品1〜5を全部入れる／③は(a)と(c)**。
⚠️**実測の結果、(a)は入れなかった**（下記「(a)を採らなかった理由」）。

### 部品1：脚質を実在の記録から

実在51頭の`legStyle`と9軸の相関を測った（`scratchpad/fitleg.mjs`）。
⭐**瞬発力 +0.625／勝負根性 −0.606**（「後ろで走る度合い」との相関）が突出。
スピード−スタミナは−0.223で従。逃げvs先行はスピード−スタミナが24.4 vs 14.6で分かれる。

- 相手17頭 → `derbyHorseAbilities.js`の`legStyle`を直接使う（`startDreamDerbySim`）
- 架空馬 → `deriveFavoredStrategy`を1本の点数へ書き直した：
  `(瞬発 − 根性) − 10×(スピード − スタミナ)÷100`、境目 −5.5／0／7
  ⚠️境目は**架空馬4000頭の内訳が実在51頭の割合に近づく**ように決めた
  （9.5/32.9/46.1/11.6% 対 実在9.8/33.3/45.1/11.8%）。実在51頭への的中は56.9%→64.7%
- **結果**：夢のダービー18頭の内訳 逃げ42.1/先行2.1/差し55.4/追込0.4% → **7.8/37.0/40.3/14.9%**

### 部品2〜5（`sim/raceSim.js`）

| 部品 | 入れたもの |
|---|---|
| 2 | `TARGET_GAP_BAND`（逃げ0〜4／先行4〜10／差し10〜17／追込17〜24m）＋精神力で帯内に配置。⭐案(c)の圧縮も同時に達成 |
| 3 | `posGain = 0.15 + 0.3 × 賢さ`（旧`POSITION_GAIN = 0.3`の全馬固定を廃止） |
| 4 | 正弦波を廃止。`wanderNow = 0.92×前 + 1.4×(一様乱数3つの和−1.5)` |
| 5 | 各馬0〜2回の仕掛け。残り1400〜700mの固定地点から15秒かけて目標を動かす |

### ②の4つの数字（50レース・道中20〜100秒）

| 測ったこと | 前 | 後 |
|---|---|---|
| 18頭の平均速度の差 | 毎秒0.059m | **毎秒0.181m**（3.1倍） |
| 1頭が80秒で先頭からの差を動かした幅 | 4.3m | **7.2m** |
| 20秒→100秒で順位が1頭あたり何番ずれたか | 1.33番 | **3.11番**（2.3倍） |
| 2頭ずつの動き方の似かた（中央／最大） | 0.41／0.97 | **0.33／0.67** |
| 同脚質どうしの前後の開き（逃げ／差し） | 5.5m／5.9m | 2.1m／5.4m |

⭐実際の1レースでは、10番（差し）が12.0m後ろから0.0mまで上がりきる、7番が9.4→14.3→5.6と
出入りする、といった動きが出た。**80秒間ずっと同じ場所にいる馬がいなくなった。**

### ③（前有利）は部品1〜5と(c)だけで消えた

全18頭を対象にした計測（150レース・`scratchpad/frontbias.mjs`）：

| 道中（残り1200m）の位置 | 前の平均着順 | 後の平均着順 |
|---|---|---|
| 1〜3番手 | 7.6着相当 | 10.08着 |
| 10〜12番手 | 10.0着相当 | 7.90着 |
| 16〜18番手 | 10.6着 | 10.42着 |

⚠️**能力が着順を予測する力は落ちていない**（`scratchpad/abilitypredict.mjs`）——
スピード上位3頭の平均着順は前3.78着→後3.62着、下位3頭は15.74着→15.74着。
⭐**運だけのレースにはなっていない。位置だけが効かなくなった。**

### ⚠️(a)（垂れの境目を上げる）を採らなかった理由

`GASSED_THRESHOLD`を0.25→0.70へ掃引した（`scratchpad/sweep.mjs`）：

| 垂れの境目 | 自分の脚質別1着率（逃/先/差/追） | カード 合わせる / 外す |
|---|---|---|
| 0.25 | 50/35/32/11% | 60.8% / 9.2% |
| 0.40 | 45/34/31/13% | 47.5% / 1.7% |
| 0.50 | 35/31/32/24% | 25.0% / 3.3% |
| 0.70 | 29/27/35/34% | **18.3% / 18.3%** |

⭐**0.70では合わせても外しても同じ**＝判断カードが意味を失う。理由は`vMaxMul`の中で
残りの脚の傾きが0.2→0.5になり、瞬発力0.075・スピード0.045・パワー0.05を飲み込むため。
⚠️代わりに`positionFactor`（前を走る消耗）を0.28→0.60へ steepen する案も測ったが、
③が既に直っているため不要と判断し、これも入れていない。

### 夢の馬の強さを測り直した

道中の位置だけで勝てなくなったので、`BOOSTED_GRADE_STEPS`を**6→7**へ（`domain/dreamDerby.js`）。
200レースで**型に合わせる69.5%／外す11.0%**（変更前72.5%／16.5%、7段化前60.0%／9.0%）。
⚠️8段にすると記号7軸の62.9%がS+になりseedごとの違いが消える（7段は49.6%）。
⚠️`BOOSTED_SPEED_MIN`(85)と`BOOSTED_STAMINA_MIN`(60)は据え置き。

### 確認

`npm run build`が通る。Playwright（iPhone 13・×1）でJSエラー0件、道中の18頭の画面上のx座標を
2秒ごとに13回読み、全頭が別々に動くことを確認。スクリーンショットも目視した。

## §57 道中の4択を作り直した（本筋1-B・2026-09-08）

ユーザー決定：**案A**（前・中団・後方は同じ4択、先頭だけ別）。文言もユーザーが4箇所直した。

### 選択肢

| 位置 | 4択 |
|---|---|
| 前・中団・後方（`DREAM_MID_CHOICES`） | まくっていく／外に出す／内で待つ／位置を下げる |
| 先頭 | 後続を離す／このまま行く／息を入れる／控える |
| 直線・前 | **早めに仕掛ける**（旧「今並びかける」）／外へ出す／内から抜く／もう少し待つ |

⚠️「間を割る」は削除した（ユーザーが直線・前の4択に「内から抜く」を残したため行き場が無くなった）。
実況4行も削除。

### 積極性の持ち方を変えた

⭐`data/judgmentSituations.js`の各択に**`aggression`（0〜1）を直接書く**形にし、
`midAggressionOf`がそれを読む（`aggression`が無い択は従来の`forward`/`effect`の式）。
⚠️それ以前は`forward: false`が一律0.15で、中立値0.35を下回っていた
＝**「内で待つ」で8.1番手ぶん下がっていた**（§54）。値：0.75／0.5／0.35／0.15、
先頭は0.75／0.35／0.25／0.1。

### `CARD_MOVE_METERS` を 62 → 22 にした

⚠️62は目標差が逃げ3〜追込29mだったころの値。帯を0〜24mへ圧縮（§56）した後は
**「まくっていく」も「外に出す」も直線入口で1.0番手になり、2択の結果が同じだった**。
16通り（道中4×直線4）を全部試して22mに決めた：

| `CARD_MOVE_METERS` | 一番良い選び方 | 適当に選ぶ | 一番悪い選び方 |
|---|---|---|---|
| 18 | 66.0% | 49.5% | 21.0% |
| **22** | **68.0〜70.0%** | **50.9%** | **21.0%** |
| 26 | 75.0% | 50.3% | 17.0% |

⭐**目安（一番良い7〜8割／一番悪い2〜3割）の内側に入った。`TODO.md` #63を閉じた。**
⚠️`positionFactor`の30mと`effortMul`の1.2は据え置き（掃引したが22mだけで足りた）。

### 4択の効き（100レース中央値・中団6番手から）

| 選択肢 | 直線入口の順位 | 変化 | 進んだ距離 | 90%動くまで |
|---|---|---|---|---|
| まくっていく | 1.0番手 | −5.0 | +11.0m | 21.0秒 |
| 外に出す | 2.0番手 | −3.0 | +4.3m | 21.3秒 |
| 内で待つ | 6.0番手 | 0.0 | 0.0m | ― |
| 位置を下げる | 13.0番手 | +6.0 | −5.8m | 21.6秒 |

⭐**「内で待つ」で位置が動かなくなった**（8.1番手→0.0番手。71回中71回下がっていたのが、
52回中28回は1番手も動かない）。⭐4択が全部違う結果になり、`TODO.md` #46（道中の2組が
同じ結果）も道中については解消した。#50（`dropBack`が`forward: true`）も消えた。

### 実況

道中の8択ぶん24行を新規に書いた（前・中団・後方の4択は3つの位置から引かれるので4行ずつ、
先頭は2行ずつ）。平均13.8字・最短6字・12字以下が9行・丁寧語で終わるのが17%
（好かれた実況7本は平均15.9字・丁寧語14%）。
⚠️ユーザーが4箇所を直した：「前はふさがれていない」削除／「脚を残す判断」→「早いと見たか」／
「先頭◯◯が速くした」→「◯◯、これは大逃げの形」／「ペースは変えない」削除。

### 確認

選択肢16個すべてに実況があり、使われていない実況が0件であることを機械で確認。
`npm run build`が通る。Playwright（iPhone 13・×1）でJSエラー0件、
「ライトスター、まくって上がる。」が出て8番手→3番手へ上がる画面を目視した。

## §58 「第92回」を消し、レース番号を10R→11Rに直した（本筋1-C・2026-09-08）

ユーザー決定：**案A（消すだけ）**。3箇所。

| 場所 | 前 | 後 |
|---|---|---|
| レース前の画面（`DreamDerbyScreen.jsx`） | 第92回 日本ダービー。 | 日本ダービー。 |
| 実況`intro[0]` | 東京競馬場、第10レース。第92回日本ダービー、芝2400メートル。 | 東京競馬場、**第11レース**。日本ダービー、芝2400メートル。 |
| 実況`intro[3]` | 第92回日本ダービー。出走{field}頭。良馬場。 | 日本ダービー。出走{field}頭。良馬場。 |

### ⚠️レース番号の誤り（ユーザーが指摘）

ユーザー：「基本メインって11レースじゃないか？」→ **正しい。** 調べた結果：

- 2024年・2025年の日本ダービーはどちらも**東京11R**（JRA公式の結果ページ
  `pw01sde1005202402121120240526`・`pw01sde1005202502121120250601`のURLに「11レース」）
- 2010年は東京10R（競馬ラボ `201005300510`）

⭐**実装当初の「第10レース」は古い年の値だった。** 画面の見出し（`intro-eyebrow`）も
「東京競馬場　第11レース」へ直した。
⚠️タイトル画面で1974年などを選んだ場合は当時の番号と違う可能性があるが、⭐これは夢の場面
なので年に厳密には合わせない（「第92回」を消したのと同じ理由）。
