# 第3弾｜初めての人間の通しプレイと、そこで出た9件

⚠️`devlog/wave02.md`が198,573バイトで読み切れないため、`TODO.md` #25の決定に従い
この弾から新しいファイルに分けた（過去の記録は動かさない）。

## §0 初めての人間の通しプレイ（2026-09-04・ユーザー）

**背景**：CLAUDE.md §0-4が「第3弾までに1回、必ず人間が最小ループを通す」と定めていた分の、
**1回目**。第2弾の実装を終えたあと、単一HTML出力をアーティファクトとして公開し、
ユーザーがスマホのブラウザで通した。

⚠️**出た9件のうち7件は、ビルド・ヘッドレス計測・Playwrightのいずれでも検出できない種類の
指摘だった**（テンポ・言い回し・「人が作った感じがない」）。§0-4「simは退屈を検知できない。
人間の通しプレイに勝る検証はない」が、本作でも実証された。

⚠️**そこへ至る前に、ファイルの渡し方で1度つまずいている**——単一HTMLをファイルとして送ったが、
スマホでは白い画面になった。原因はファイルではなく開き方（アプリ内プレビュー・iOSの「ファイル」
アプリはJavaScriptを実行しない）。⭐**手元のPlaywrightでiPhoneの画面サイズとタップ操作を
再現して、ファイル自体は動くことを先に確認してから、アーティファクトとして公開してURLで渡した。**
以後、人間の通しプレイを頼むときは最初からURLで渡す。

### 指摘（ユーザーの原文）

1. 夢のダービーが始まる前に画面遷移アニメーションが欲しい。急に始まる感が強すぎる。
2. チュートリアルのテンポ感が悪すぎるね。何もしないで見ている時間が長すぎるので、矢継ぎ早に出る感じにしたい。
3. かつ、途中でスキップできちゃうのはいいのかな？
4. ゴール後の着差表示とか辺りの下の戦闘でゴール板を通過したみたいな文言は本当にいらない。
5. 夢での乗り方を反映するかどうかの確認で、あの乗り方でなく夢の乗り方みたいに言うべきだと思う。
6. 教官の卒業の際にステータスのアルファベットに言及してるのが違和感。世界の中の人間なんだから〇〇は得意になったようだな、みたいに言うべきだよね。
7. その他もろもろ全てにおいてなんだけど、言い回し、テキストなどなどが全てAI臭すぎる。
8. 後はアニメーションとか見た目全体とかもなんと言うか均一すぎると言うか、、、人が作った感じがない。
9. 名前は制限をつけるべき。文字数とか、フォントに対応している字かどうかとか。

### 指摘が指している実装上の現物

| # | 指摘 | 現物 |
|---|---|---|
| 1 | 急に始まる | `src/app.jsx`——`TitleScreen`から`DreamDerbyScreen`へstateの切り替えだけで遷移。中間の演出が無い（`TODO.md` #33） |
| 2 | 見ている時間が長い | チュートリアル3つの発火が`src/data/dreamDerbyCourse.js`の`T_TUT_CAMERA=10`／`T_TUT_DISPLAY=26`／`T_TUT_SPEED=40`秒。⭐**10秒→26秒の間に16秒、26秒→40秒の間に14秒、プレイヤーの操作が無い**。レース全体は`T_FINISH=144`秒（×1のとき実時間で2分24秒） |
| 3 | スキップの是非 | `dreamDerbyEngine.js`の`skip()`。⚠️**道中の判断カードを飛ばしてしまう**（既知・未修正）。⭐その判断カードは卒業式の脚質適性を決める入力なので、**キャラクターを決める操作そのものを飛ばせる**状態 |
| 4 | ゴール後の一言 | `dreamDerbyEngine.js:550`の`flavor`——勝ち「先頭でゴール板を過ぎた。」／負け「先頭の馬を、最後まで捉えられなかった。」。着差表の直下に出る |
| 5 | 「あの乗り方」 | `src/data/graduationText.js`の`dreamRecordChoices`——「あの乗り方を信じる」／「一から覚え直す」 |
| 6 | 教官がアルファベットを言う | `src/data/graduationText.js`の`reportLines`——「距離は◯◯がD。ほかは、それより下だ」「馬場は◯がD。もう片方は、Dにも届いていない」「B、A、Sは一つもない。」 |
| 7 | AI臭い | 文言全体。`data/dreamDerbyCommentary.js`（実況の全コーパス）・`data/graduationText.js`・各画面のUI文言 |
| 8 | 均一すぎる | 演出・見た目全体。⚠️具体的にどこを指しているかは未特定（本人に聞く） |
| 9 | 名前の制限 | `src/screens/GraduationScreen.jsx:265,276`——`maxLength={10}`のみ。文字種の検査も、フォントで表示できるかの検査も無い |

### ⚠️7・8は、CLAUDE.md §8が防ぐために書かれた失敗そのもの

§8は前作でユーザーが「めちゃめちゃAI臭い」と評したことを受けて作った節で、
**「作る前に候補を見せる」手順**を定めている。本作では夢のダービーも卒業式も
モックを見せて合意を取ってから実装したが、**それでも同じ評価が出た**。
⚠️**手順を踏んだのに同じ結果になった以上、原因は手順の不履行ではなく、
候補の作り方そのものにある可能性が高い。** ここは切り分けから始める。

## §1 「卒業式の後に何も起こらなくなった」の調査（2026-09-04）

**結論：クラッシュではない。動いているが、見えるものが何も無い。**

iPhone 13の画面サイズとタップ操作を再現し、タイトルから週の進行まで通した（`dist/index.html`を
`file://`で開き、Playwrightで操作）。結果：

- 卒業式→週の画面への遷移は**動く**。JSエラーは**0件**
- 週を5回進めた。年・週の表示は正しく進み、所持金は100,000円→620,000円、
  信頼の変動通知も出た。**エンジンは正常に回っている**

⚠️**では何が起きていないか。`WeekScreen.jsx`でプレイヤーができる操作は、
「行く競馬場を選ぶ」ラジオボタンと「この週を進める」ボタンの2つだけ。**

- **どの馬に乗るかを選べない**（`weekLoop.js`が内部で自動的に決めている）
- **レースが1つも見えない**（着順も、相手も、レース画面も無い）
- 結果は「特になし。」「信頼が大きく動いた：白石勇（+4）」という1行の通知だけ

⭐**つまり、押すと数字が増えるだけの画面である。** 夢のダービーという2分24秒の
レース画面を見せた直後にこれが来るので、「何も起こらなくなった」は
体験の記述として正確。⚠️バグとして直す対象ではなく、**第2弾の範囲設定
（週の進行は見た目が仮）の帰結**であり、鞍の選択とレースの実体化は
`TODO.md` #23（第3弾）・#24（第4弾）にある。

⚠️**もう1つ、見た目の断絶がある**：夢のダービーも卒業式も暗い画面なのに、
週の画面だけ**白地・ブラウザ既定のフォントとラジオボタン**のまま。
ゲームの外に放り出されたように見える。

## §2 実況の声——ユーザーが挙げた2人は、既にゲームの中にいた

ユーザーが実況のイメージとして**杉本清さん・馬場鉄志さん**（いずれも関西テレビ）を挙げた。
⚠️**`design/meijikkyou-sources.md`に収蔵済みの名実況12本のうち7本が、この2人のもの**で、
そのもじりは既に実装のコーパスに入っている。**それでも「AI臭い」と言われた。**

### 何が違うのかを数字で出した

| | この2人の名実況7本 | 実装の169行 |
|---|---|---|
| 平均の長さ | **15.9字** | **34.5字** |
| 最短 | **7字**（「弟は大丈夫だ！」） | **13字** |
| 丁寧語で終わる | **1/7（14%）** | **107/169（63%）** |

⭐**好きだと言われた実況の形と、私が書いた行の形が正反対だった。**
名台詞を7本混ぜても声にはならない。声は「どの一言を言うか」ではなく、
**言葉がどこで短くなり、どこで丁寧さが消えるか**という分布に宿っている。

### Fableサブエージェントの発見（CLAUDE.md §2の「自発的に使う」に従って発注）

⚠️デビルズアドボケートが「候補を作るのが同じ書き手なら同じ臭いのものが並ぶだけ」と
突いたため、**書き手そのものを変えた**。Fableが収蔵済みの表から見つけた事実：

⭐**杉本・馬場の7本には、評価の形容詞が1つも無い。**「強い」「速い」「すごい」
「素晴らしい」「見事」が1本も出てこない。情緒は**一人称（私の夢）・呼びかけ（見てくれ）・
断定（結晶だ・大丈夫だ）・事実の言い換え（何にも来ない＝大差）**の4つで乗せている。

⚠️**一方、収蔵済みの関東側（白川次郎・佐藤泉）には評価語が入っている**——
「見事中野騎手！」「これは強い！とてつもない強さ！」「鮮やかな逆転劇」。
**実装のコーパスで評価語を含む4行は、全部この関東側のもじりだった**（実測：169行中4行）。
⭐**2つの流派の声が混ざっていた。**

| 実装の行 | 元ネタ | 誰 |
|---|---|---|
| 独走に入った。これは強い、とてつもない強さだ！ | 9 | 白川次郎 |
| 堂々と抜け出した！新人騎手、見事にダービー制覇！ | 8 | 白川次郎 |
| 鮮やかな逆転劇、鮮やかな差し切り、{self}！ | 10 | 佐藤泉 |
| 独走に入った。これは強い、とてつもない強さ！{self}、圧勝！ | 9 | 白川次郎 |

### Fableが出した5つの規則（採否は未決定）

1. 発走前と道中は「です・ます」。**直線に入ったら「た・だ・体言」だけ**
2. 残り200mからゴールは**1本の平均を10字以下**にし、句点で終わらない行を半分以上に
3. **評価の形容詞を禁止語にする**
4. 決め台詞は**1着が確定してから1本だけ**（直線や残り200mに置かない）
5. **黙る区間を作る**

⭐**ゴールは1行では成立しない**という指摘も出た——「馬名だけの行 → 沈黙 →
確定と情緒の行」の2段になるので、**乱数で選ぶ単位を1行ではなく「2行の組」にする**。

⚠️Fableが「知らない」と明示した点：2人の実況の音声・テンポの実測値、
道中（決定的でない場面）の喋り方、馬場鉄志と杉本清の差。
⭐**杉本清が東京の日本ダービー本番を実況した記録は無い可能性が高い**
（関西テレビの担当は関西開催が基本）——真似ではなく喋り方の質の借用にしかなり得ない。

## §3 この弾で入れた直し

- **ゴール後の一言を削除**（指摘4）。`dreamDerbyEngine.js`の`flavor`
  （勝ち「先頭でゴール板を過ぎた。」／負け「先頭の馬を、最後まで捉えられなかった。」）と、
  `DreamDerbyScreen.jsx`の`.result-flavor`の描画、CSSの該当規則を削除した。

## §4 2回目の通しプレイで出たバグ3件（2026-09-04・実装はSonnetサブエージェント）

### バグ1：ゴール後にスキップを連打すると壊れる

**症状**：着順表が出ない／暗転して卒業式に行かない／ゴールの実況が無限に増える。

**原因**：`dreamDerbyEngine.js`の`skip()`が**先頭で無条件に`clearPendingTimers()`を呼ぶ**。
`doFinish()`は着順表（1100ms後）と暗転（そのさらに3200ms後）を`raceTimeout`で予約するので、
ゴール後にskipを押すと**その予約が毎回消え、同時に`doFinish()`が再実行されて実況だけが増える**。

**直し**：クロージャに`finished`フラグを追加し、`doFinish()`と`skip()`の先頭で早期return。
`updateSkipAvailability()`に`&& !finished`を足してゴール後はボタン自体を無効化した。

⭐**教訓：「予約を消してからやり直す」形の関数は、同じ関数を2回呼べる経路があると壊れる。**
`skip()`は発走前・道中・直線・ゴール後の4つの状態から呼ばれるのに、状態の区別が無かった。

### バグ2：実況の並びが逆（ユーザーの指示）

新しい行が下に積まれていた。`appendMessage`を先頭追加に変え、`is-latest`の判定を`i === 0`に、
スクロール先を先頭に変えた。⚠️目覚めの5行（`wakeLines`）は別の仕組みなので触っていない。

### バグ3：スマホで成績表が見切れる

**原因**：`GraduationScreen.css`と`DreamDerbyScreen.css`が**高さ720px固定**だった
（使い捨てモックの固定枠をそのまま移植したもの）。iPhone 13の表示領域は678pxしかなく、
`.scoreboard`の上端が`top: -50`＝画面の外に50px出ていた。

**直し**：`height: min(720px, 100dvh)`（レース画面は縦マージン64pxを引いた
`calc(100dvh - 64px)`）。`.grad-top`を`flex-shrink: 1` + `min-height: 0`にして縮むようにした。
⚠️`vh`ではなく`dvh`を使うのは、スマホのアドレスバーで表示領域が変わるため。
⚠️`TRACK_W = 390`等のレース座標の定数は**触っていない**。

**実測（修正後・iPhone 13の表示領域678px）**：`.scoreboard`が`top: 8, bottom: 308`。
iPhone SE（320×568）でもレース画面のフレームが収まることを確認。

### 文言（ユーザーが確定）
「あの乗り方を信じる」→**「夢の乗り方を信じてみる」**（指摘5）。

### 検証
`npm run build`成功。iPhone 13エミュレートで**スキップ10連打→着順表が出る・実況のゴール行は
2件（`finish`と`finishSelfWin`の設計どおり）・「卒業式へ」到達・JSエラー0件**を、
サブエージェントとは別に本体でも独立に確認した。

## §5 YouTubeの自動字幕は実況の資料として使えない（2026-09-04・実測）

ユーザーの提案「YouTubeの名実況集から文字起こしを持ってくる」を実際に試した。
`yt-dlp`をこの環境に入れ、杉本清アナの動画3本の**日本語自動字幕の取得に成功**した。
⚠️**しかし資料としては使えない。** 理由：

| 調べたこと | 結果 |
|---|---|
| 字幕が取れるか | **取れる**（3本とも成功） |
| **文の長さが測れるか** | ⚠️**測れない**。自動字幕は発話の区切りではなく**最大19字の固定幅の窓**。3本とも最長19字・中央16〜18字で頭打ち。⭐**今いちばん欲しい数字がこれ** |
| 語が正確か | ⚠️不正確（「サクラスターオー」→「さくら」、「京都競馬場」→「対位法の車京都競馬場」） |
| 間（無音）が測れるか | ⚠️測れない。3本とも**ファン編集の総集編でBGM入り**（[音楽]タグ6〜14個）。3秒以上の空白が2回・4回・12回とばらつき、**編集の産物と本物の間を区別できない** |
| 直線〜ゴール | ⚠️**1本で20.6秒まるごと字幕が落ちた**。歓声と絶叫でASRが効かない。**欲しいところだけ取れない** |

⭐**代案として残っているのは、音声認識（Whisper系）を自前で走らせること。**
固定幅19字の問題は消えるが、歓声の中の絶叫は同じく苦手で、権利の扱いも別途要検討。未着手。

### §5-2 Whisperでの自前の書き起こしは、音声が取れずに実行できなかった（2026-09-04）

ユーザーが「やってみる」と判断したので着手した。`faster-whisper`の導入は成功したが、
⚠️**YouTubeから音声そのものを落とせない。** `player_client`を5通り
（`tv`／`android_vr`／`web_safari`／`mweb`／`ios`）試して全部失敗した。

| 経路 | 結果 |
|---|---|
| tv | The page needs to be reloaded. |
| android_vr / ios | **Sign in to confirm you're not a bot.** |
| web_safari / mweb | Requested format is not available. |

⭐**字幕（timedtextのエンドポイント）は通るのに、音声・動画の実体は拒否される。**
データセンターのIPからのメディア取得がボット判定で止められているため。この環境では
Whisperを使う経路そのものが塞がっている。⚠️ffmpegもこの環境に無い（導入は試みた）。

**残っている手段**：①ユーザーが手元で音声を用意して渡す（権利の扱いを別途決める）
②ユーザーが「この実況のこの喋り方」を言葉で教える ③人が書き起こしたテキストをもっと探す
（ただし名実況集にあるのは決定的な1文だけで、**道中の普段の喋り方は載っていない**）。

## §6 級数の1回目の案は却下された——「畳む」ことと「階層を作る」ことは別（2026-09-04）

`design/mocks/`ではなくアーティファクトとして、**ゲームの本物のCSSを読み込み、実物の文言で
同じ画面をボタンで切り替えられる比較ページ**を作って見せた（CLAUDE.md §8の手順1）。

**1回目の案＝17種類を5段（10.5/13/16/20/44）に畳む。ユーザーの評価は「変化が小さくて
あまり効果がないように見える」。**

⚠️**正しい指摘だった。5段に畳んでも、その5段が10〜20pxの中に収まったままなので、
大きさの幅は変わらない。** 種類が17から5に減っても、画面の階層は1つも増えない。
⭐**問題は「種類が多いこと」ではなく「差が無いこと」だった。**

**2回目の案＝各画面で主役を1つ決めて大きくし、残りを2段（本文14px・補足11px）に落とす。**

| 画面 | 主役 | 落とすもの |
|---|---|---|
| 判断カード | 状況 **26px** | 選択肢14・ヒント11 |
| 実況欄 | **最新の1行だけ 19px** | 過去の行は**11pxに沈める**（今は4行とも13pxで色しか違わない） |
| 着順表 | 着順の数字 44px | 表は全部11 |
| 成績表 | **記号 19px** | 項目名11 |
| HUD | **残り距離 19px** | 局面・時計11 |

## §7 判断カードの文言（ユーザー指摘「冗長かつ変」）

今の4本のヒントは**全部が「良いこと。悪いこと」という同じ型**で書かれている（＝§2で測った
均一さが、実況以外にも同じ形で出ている）。加えて：

- 「内で我慢して進路を待つ」——**我慢と待つが同じことを言っている**（重複）
- 「外に持ち出す」「一列下げて外へ回す」「外へ持ち出して一気に」——**紛らわしい**
- **競馬用語が説明なしに並ぶ**（進路／持ち出す／馬群を割る／一列下げる／脚を溜める／1ハロン）。
  CLAUDE.md §7「競馬を知らない人が読んで分かる言葉だけ」に違反

**案：** 状況「馬群の中。前が壁」→「前が壁」／選択肢「内で待つ」「外へ出す」「下げて外へ」
「間を割る」／ヒント「開けば伸びる。開かなければ終わり」（8〜16字）。
⚠️**ヒントを全部消す案も並べたが、何を賭けているか分からないまま押すことになり
§0-1「意味のある判断」から遠のく**ので、消さない案を推した。

## §8 教官のセリフ：「今日で終わりだ」が却下された理由

⭐**卒業式は生徒にとって始まりで、教官がわざわざ「終わり」と言う理由が無い。**
突き放す言い方なので、直前の「三年前、お前は馬に触るのも怖がっていたな」という記憶とも
繋がらない。⚠️**私が「区切りをつける演出」が欲しくて置いた行で、教官が言う必然性がゼロだった。**

## §9 級数「差をつける」と文言3件の実装（2026-09-04・実装はSonnetサブエージェント）

### 入れたもの

**(1) 文字サイズのトークン化**——`src/styles/type.css`を新設し、`app.jsx`で読む。
`--fs-caption:11 / --fs-body:14 / --fs-lead:19 / --fs-title:26 / --fs-display:44`、
走路の上の文字だけ別に`--fs-world-sm:8 / --fs-world-md:9.5`。
⭐**`DreamDerbyScreen.css`と`GraduationScreen.css`の生の`px`は0件**（grepで検算済み）。
⚠️`--fs-input:16px`だけ例外——**iOS Safariは16px未満の入力欄にフォーカスすると画面を勝手に
拡大する**ため、これ以上小さくできない。トークンにその理由を書いた。
⚠️`TitleScreen.css`は**触っていない**（比較ページで見せておらず合意が無い。§8）。

**割り当て（各画面の主役）**：判断カードの状況26／実況の最新の1行19（過去の行は11＋不透明度.62）
／着順の数字44／成績表の記号19／HUDの残り距離19。押すもの・本文は14、それ以外は全部11。

**(2) 判断カードのヒントを画面から消し、選択肢を短くした**（ユーザーの判断）。
「内で我慢して進路を待つ」→「内で待つ」等8件。状況「馬群の中。前が壁」→「前が壁」。
⚠️`hint`のデータ自体は残した（将来の通常レースで使う可能性）。

**(3) 卒業式の文言**——式典の「「おめでとう。今日から、君は騎手だ」」から**おめでとうを外し**、
教官のセリフへ移した（10秒のあいだに2回言わせない）。教官の4行は成績表の読み上げをやめ、
「卒業、おめでとう。」「三年前、お前は馬に触るのも怖がっていたな」「ここから先は、俺は何も
言えん」「声をかけてきた厩舎がある。自分で選べ」に差し替えた。

### ⚠️ 検証で見つかった不具合2件（どちらも同じ弾の中で直した）

**(a) 判断カードの3つ目・4つ目が枠の外にあった。**
`.tab-panels`（実況欄）と`.card-panel`が**どちらも`flex:1`で`.race-bottom`（300px）を
分け合っており**、カードの取り分が147pxしかなかった。中身は約300px要るため、
`overflow-y:auto`で**パネル内スクロールになり、最後の選択肢の下端は766px**（枠の下端632pxの
134px下）にあった。⭐**押す前に4つを見比べられない＝判断が成立していない。**

**直し**：判断カードが出ている間はタブと実況欄を隠し、`.race-bottom`の300pxを全部カードに渡す。
⚠️`.tabbar`と`.tab-panels`には`style={{display: wakeActive ? "none" : ""}}`という
**インラインスタイルが付いており、CSSより強い**。JSX側の条件に`card`を足して確実に効かせた。
併せて余白を4か所詰めた（状況の下16→10／選択肢の間隔8→6／選択肢の内側11→9／パネル12）。

⚠️**この不具合は今回の変更で生まれたものではない可能性が高い**——以前は選択肢4つそれぞれに
40字前後のヒントが付いており、中身はもっと縦に長かった。⚠️ただし**旧版を実測してはいない**。

**(b) それでもiPhone SE（320×568）では44px足りなかった。**
`.race-top`が`height:300px`固定で、枠504pxのうち**カードの取り分が204px**しか残らないため。
**直し**：`height: min(300px, calc(100dvh - 330px))`。330の内訳は枠の縦マージン64＋カードに
必要な約262＋余裕4。⭐**iPhone 13では計算結果が334pxになるので`min`が300pxを選び、
今までと1pxも変わらない。** SEでは238pxに縮む。
⚠️**カードが出ている間だけ縮める作りにはしなかった**——カードが出た瞬間にレース画面の大きさが
変わると馬の位置が動いて見えるため、常に同じ式にした。
⚠️**330pxはカードの中身の高さに依存する。選択肢の文言や個数を増やしたら再計算が要る**（`TODO.md`）。

## §10 判断カードを位置で分ける（2026-09-04・ユーザー合意）

⚠️**発端**：`dreamDerbyEngine.js`は位置に関係なく必ず「馬群の中。前が壁」を出していた。
ユーザーの指摘——**「毎回前が壁なわけではないだろ。逃げてれば前に壁はないし、
大外を回してれば前壁ないはずだしさ」**。

### 位置の区切り（頭数Nに対して。⚠️18頭立て固定ではない）

| 位置 | 条件 | 18頭のとき |
|---|---|---|
| 先頭 | 1番手 | 1 |
| 前 | 2〜4番手 | 2〜4 |
| 中団 | 5番手〜後ろから5頭目 | 5〜14 |
| 後方 | それより後ろ | 15〜18 |

⚠️**10頭未満は中団が消えるので狭める**（ユーザー合意）：「前」を2〜3番手、「後方」を最後の2頭。
8頭立てなら中団が4〜6番手になる。

### 状況の文は書かない。順位と内外の事実だけを出す

⚠️**「前が壁」「先頭を捉えにいく」は、そうなっているかを確かめずに書いた決めつけだった。**
⭐**「7番手 内」「3番手 外」のように、画面の隊列と必ず一致する事実だけを出す。**
⚠️内外は表示用のレーン値から出せる見込みだが**未確認**。

### 選択肢（8通り＝位置4×場面2）

| 位置 | 道中（残り1200m） | 直線 |
|---|---|---|
| 先頭 | このまま行く／少し緩める／ペースを上げる／後ろを見る | すぐ追い出す／もう少し持つ／内に寄せる／外に出す |
| 前 | このまま／並びかける／外に出す／内で待つ | 今並びかける／外へ出す／内から抜く／もう少し待つ |
| 中団 | 内で待つ／外へ出す／下げて外へ／間を割る | ここで追い出す／もう少し待つ／外から一気に／内を突く |
| 後方 | 外から上がる／内で待つ／早めに動く／最後まで待つ | 大外から／内を突く／今すぐ追い出す／最後まで脚を残す |

⚠️初案から3つ直した：先頭の「突き放す」→「ペースを上げる」（突き放せる保証がない）／
後方の「今から動く」→「早めに動く」（「外から上がる」と重複）／
直線先頭の「後ろを確かめる」→「外に出す」（行動になっていなかった）。

### 脚質の決め方を案Bへ変更（ユーザー合意）

⚠️**旧：選択の2軸だけで決めていた**（`MID_CHOICE_AXIS`／`STRETCH_CHOICE_AXIS`／
`STRATEGY_BY_AXES`）。**位置を見ていないので、中団で「間を割る」→直線「ここで追い出す」が
逃げになる**という意味のずれがあった。

⭐**新（案B）：残り1200m時点の位置を基準段にし、選択で1段ずつずらす。**

- 基準段：先頭＝0（逃げ）／前＝1（先行）／中団＝2（差し）／後方＝3（追込）
- 道中の選択が「前へ行く」なら−1、「動かない」なら+1
- 直線の選択が「早く仕掛ける」なら−1、「溜める」なら+1
- 合計を0〜3にクランプ

⚠️**選択肢のデータに2軸のタグを持たせる**（`forward` / `early`）。そうしないと、
どの状況のカードが出たかに関わらず脚質を決められない。

⚠️**残る問題**：案Bだと中団（基準2）で両方前向きだと合計0＝逃げになる。ユーザーはこの挙動を
理解した上で案Bを選んだが、⭐**「中団から積極的に動いた乗り方」が逃げになるのは筋が通らない**。
直すなら直線の選択を±0.5にして四捨五入する（中団＋両方前向き＝0.5→1＝先行）。**未決定。**

## §11 道中実況の作り方（2026-09-04・ユーザーが教えた）

⚠️**私が「資料が無い」と言っていた「道中の普段の喋り方」を、ユーザーが直接教えた。**

> 道中実況は大体前から馬番と馬名を呼んでって、たまに鞍上の名前とかも呼ぶかな。
> 後は1000m通過タイム言ってペースの話したり。

⭐加えて**「隊列は、動きません」のような「動きがないこと」は言わない。動いた馬を名前で言う**
（まくっていった馬がいれば「◯番◯◯が上がっていきます」）。**読点を打ちすぎない。**
→ CLAUDE.md §7に記録済み。

### 実装に足りないもの2つ

1. ⚠️**夢のダービーの17頭に騎手がいない**（grepで確認。`dreamDerby.js`にも画面にも騎手の
   概念が無い）。⭐`pickHumanName`があるので`saveSeed`から18人ぶん決められる。
2. ⚠️**1000m通過タイムが毎回同じ**。`timeAtDistance(1000)`は固定のチェックポイント表から
   出るため。⭐案：`saveSeed`から±1.5秒揺らし、その値からペースの評価を出す。
   ⚠️**東京芝2400mの基準値（61秒前後）は私の推定で、実データを持っていない。**

## §12 作業の順番（2026-09-04・ユーザー合意）

ユーザーの「今本筋見失ってないよね？」に対して全体の状態を出し、**実況169行の書き直しの前に、
未着手のまま残っていた3件を先に片付ける**ことで合意した。

- 指摘1：画面遷移アニメーション（未着手）
- 指摘2：チュートリアルのテンポ（未着手）
- 指摘9：名前の入力制限（未着手）

⚠️**判断カードの位置分岐と道中実況は、「文言を直す」から一歩出てレースの仕組みに入る。**
第3弾（`TODO.md` #23 レースの実体化）と重なるので、**ここで作る仕組みは本番のレースでも
使える形にする**こと。夢のダービー専用に作ると二度手間になる。

## §13 チュートリアルの連鎖とスキップの削除（2026-09-04・ユーザー合意・実装はSonnet）

### チュートリアルを連鎖させた（案A）

⚠️**旧**：レース内の時計で10秒・26秒・40秒に出していた。**10→26の間に16秒、26→40の間に14秒、
プレイヤーの操作が無かった**（指摘2「何もしないで見ている時間が長すぎる」の実体）。

⭐**新**：発走3秒後に1つ目、**1つ前が終わってから1.5秒後に次**。3つ目（速度）も
「押させる」形に変えた（旧は閉じるだけ）。

**実測（本体で独立に確認・iPhone 13）**：1つ目→2つ目 **1.3秒**／2つ目→3つ目 **1.8秒**／
チュートリアル3つの合計 **17.5秒**（タイトル操作を含む）。JSエラー0件。

⚠️⚠️**罠**：`T_TUT_CAMERA=10`／`T_TUT_DISPLAY=26`／`T_TUT_SPEED=40`は、チュートリアルの
発火時刻であると同時に**`GAP_T`（隊列の演出の補間時刻）にも使われている**。
⭐**チュートリアルから外すからといって定数を消すとレースの見た目が壊れる。** 使うのをやめるだけにした。

### スキップを外した

**理由**：スキップは道中の判断カードごと飛ばしてしまい、⭐**プレイヤーが自分の脚質を決める
操作を、気づかないまま飛ばせる**状態だった。⚠️既知の不具合（スキップで道中カードが出ない）も
これで消えた。速度の×2・×4は残す——自分で速くするのは操作だが、飛ばすのは体験の放棄。

⚠️`skip()`・`updateSkipAvailability()`・`setSkipEnabled`とその呼び出し6箇所を削除。
`finished`フラグ（ゴール処理の二重実行防止）は残した。

### 文言（4本）

⭐**説明してから押させるのをやめ、押させてから結果を見せる。** 何が起きるかは押せば分かる。

| 場所 | 旧 | 新 |
|---|---|---|
| カメラ | このボタンで、画面が追いかける馬を切り替えられます。一度押してみてください。(35字) | **押してみてください**(9字) |
| 表示 | このボタンで、馬の上に出す表示を切り替えられます。3回押すと一巡します。(36字) | **3回押してみてください**(11字) |
| 速度 | このボタンで、レースの速さを変えられます。長い道中は右上の「スキップ」で直線まで飛ばせます。(45字) | **押すと速くなります**(9字) |
| 判断カード | ここで選んだ行動で、レースの流れが変わります。(23字) | **選んだ行動で、レースが変わります**(16字) |

### ⚠️ 残った未決定

**レースの長さ**。×1で144秒、×4で36秒。スキップが無くなったので、速度を上げない人は
2分24秒を最後まで見ることになる。⭐**長すぎるかは人が通してみないと分からない。**
短くするなら`T_FINISH`と`DIST_CHECKPOINTS`を縮める。

**サブエージェントが報告した既存の見た目の問題2件**（今回の対象外・未修正）：
判断カードのチュートリアルの吹き出しが3番目の選択肢に軽くかぶる／着順表の背景が半透明で
下のタブバーの文字が薄く透ける。

## §14 画面遷移（指摘1）——タイトル→夢のダービーの3案（2026-09-04）

### ⚠️ その前に：私が比喩で誤魔化していた

第2弾のMAGIで「動きを**中継車・紙・生き物**の3系統に分ける」と書いた。⚠️**ユーザーに
「中継車とか生き物は何の話？」と聞かれた。** ⭐**比喩が中身を1つも足していなかった。**
CLAUDE.md §7の「誤魔化して抽象的にまとめない（ユーザーへの報告文にも同じく適用する）」に
私自身が違反していた。

**実測した事実だけを書けば足りた**：アニメーションの加減速の指定が**2種類しかない**
（`linear` 7回・`ease` 5回、`cubic-bezier`は**0回**）。`linear`と`ease`はブラウザの初期値
そのもので、**動きの設計を一度もしていない**という意味。言いたかったのは
「要素ごとに違う加減速を与える」だけだった。

### 3案（アーティファクトで実際に再生できる形にして提示）

⚠️**範囲はタイトル→夢のダービーだけ**（ユーザーが決定）。⭐**ここで決めた形をそのまま
他の遷移に使うわけではない**ことも、ユーザーが明示した。

| 案 | 中身 | 長さ |
|---|---|---|
| A 暗転 | タイトルが0.4秒で黒くなる→黒のまま0.3秒→レース画面が0.7秒で現れる | 1.4秒 |
| B 沈める | タイトルが下へ46px沈みながら消え、同時にレース画面が現れる。暗転しない | 0.62秒 |
| C 声が先 | 0.5秒で暗転→黒い画面に実況の1行目だけ→1.6秒読ませる→消える→レース画面 | 約4.1秒 |

⚠️Cは「夢に入る」感じを狙ったが**4秒は長い**。毎回見せられるとくどい可能性がある。

**加減速を案ごとに変えた**（これが「`linear`と`ease`しかない」への直接の答え）：
暗転していくとき`cubic-bezier(.4,0,1,1)`（最初ゆっくり・後で一気に）／
明けていくとき`cubic-bezier(0,0,.2,1)`（最初に一気に・後はゆっくり）／
タイトルが沈むとき`cubic-bezier(.6,0,.9,.4)`（落ちるように加速）。

⚠️デモの遷移先は**簡略な絵**（空・芝・実況1行）。本物の走路SVGは重いので入れていない。
**見せたいのは遷移の動きであって遷移先の絵ではない**という前提で作った。

## §15 名前の入力制限（指摘9・2026-09-04・実装はSonnet）

**ユーザーの決定**：Unicodeの範囲だけで判定する案A／上限は姓4文字・名4文字。

⚠️⚠️**その前に、私が確かめずに「確認済み」と書いた件を訂正した。**
「フォントに無い字はcanvasで幅を測れば弾ける」と報告したが、**検証していなかった**。
実際に試すと：

- ⚠️**フォントに無い字は豆腐にならず、システムの別のフォントに落ちて普通に描かれる**
- ⚠️この環境には**日本語フォントが1つも入っていない**（fc-listで59個すべて欧文）ため、
  「予備フォントとの幅の違い」で判定する方法を**検証できなかった**
- ⚠️`document.fonts.check(font, text)`は**文字の有無を見ていない**（全部trueを返した）
- ⭐そもそも**端末ごとに入っているフォントが違うので、この方法は当てにならない**

代わりに**フォントの中身を直接調べた**（`fontTools`で`cmap`を読んだ）：

| M PLUS 1p | 持っている数 |
|---|---|
| 総文字数 | **8,331字** |
| ひらがな | 93/96（96.9%） |
| カタカナ | 96/96（100%） |
| **CJK統合漢字** | **5,082/20,992（24.2%）** |
| 互換漢字 | 2/110（1.8%） |

実例：山・田・悠・真・髙・﨑・𠮷は**ある**／彅（なぎ）・昱は**無い**。
⭐**「漢字なら大丈夫」とは言えない**が、常用漢字・人名用漢字はほぼ入っている。

### 許可する範囲（`src/data/nameRules.js`）

| 範囲 | 中身 | なぜ要るか |
|---|---|---|
| U+3005 | 々 | ⚠️**「佐々木」。CJK統合漢字の範囲外なので個別に足さないと弾かれる** |
| U+3041–U+309F | ひらがな | |
| U+30A1–U+30FF | カタカナ | ー（U+30FC）・ヶ（U+30F6）を含む |
| U+4E00–U+9FFF | CJK統合漢字 | |
| U+FA00–U+FA6D | 互換漢字 | 﨑（U+FA11）など |

⚠️**サロゲートペア（U+FFFF超）は弾く。** JavaScriptの文字列はUTF-16なので、
`maxLength={4}`だけでは`𠮷`のような字を2文字と数えてしまう。
⭐**`[...str]`でコードポイント単位に分けてから数える。**

⚠️案内の行「使える文字は、漢字・ひらがな・カタカナです」は**弾いたときだけ**出す。
**4文字の上限に当たっただけのときは出さない**（文字種の問題ではないため）。

### 実測（本体で独立に確認・10通り）

佐々木→佐々木（案内なし）／山﨑→山﨑（なし）／あいうえお→**あいうえ**（⭐**案内なし**）／
ヶ丘→ヶ丘（なし）／abc→**空**（案内あり）／😀山→**山**（あり）／뷁→**空**（あり）／
𠮷田→**田**（あり）／ー→ー（なし）／さとう→さとう（なし）。

## §16 説明の具体性を毎回サブエージェントで検査する（2026-09-04・ユーザーが明示）

**ユーザーの指示**：
> 今後、毎回サブエージェントを使い、あなたの判断やユーザーへの説明がアバウトであったり
> 抽象的であったり造語が含まれていないか必ずチェックしなさい。どんな人間が見ても理解できる
> 説明や判断をしなさい。これは仕組み化すること。

⚠️**きっかけ**：「動きを中継車・紙・生き物の3系統に分ける」と書き、ユーザーに
「中継車とか生き物は何の話？」と聞かれた（§14）。⭐**比喩が中身を1つも足していなかった。**
CLAUDE.md §7に既にあった規律（「誤魔化して抽象的にまとめない。ユーザーへの報告文にも
同じく適用する」）を、私自身が破った。**規律があるだけでは守れないことが実証された**ので、
検査を仕組みにする。

### 検査項目（サブエージェントに渡すもの）

1. **比喩・抽象語で言い換えていないか**——「刺さる」「解像度」「次元が違う」「構造的に」
   「レバレッジ」など、具体語に置き換えられる表現。⭐**中継車・紙・生き物**がこの型
2. **このプロジェクト内だけの造語が無いか**——初めて読む人に通じない言い方
3. **一読して意味が確定しない文が無いか**——「つまりどういうこと？」と聞き返せる文
4. **根拠の無い断定が無いか**——「重要です」「大きく変わります」で止まり、数字・期間・
   具体例のどれも無いもの
5. **確認していないことを断定していないか**——⚠️実例：「フォントに無い字を弾けることは
   確認済み」と書いたが、検証していなかった（§15）
6. **まとめで抽象度を上げていないか**——結論ほど具体的に書く。「〜を意識しましょう」
   「〜が鍵です」で締めない

### 運用

- ユーザーへ送る**下書きをそのまま**サブエージェントに渡し、上の6項目で指摘を出させる
- ⚠️サブエージェントは会話の文脈を持たないので、**「これはゲーム開発の会話の返信である」
  という前提と、上の6項目を毎回プロンプトに書き切る**（CLAUDE.md §2）
- 指摘を直してから送る。⚠️**直さずに送るなら、なぜ直さないかを自分の言葉で言えること**

## §17 画面遷移6箇所の実装と検証（2026-09-04・実装はSonnet・検証は本体）

`src/styles/motion.css`に秒数と加減速を集め、`src/screens/motionTiming.js`が`getComputedStyle`で
読む形にした（`type.css`と同じ。生の数値をJS/JSXに書かない）。`src/app.jsx`が
「いまどの画面か」に加えて「いまどの遷移の途中か」を持つ。

⚠️**視差効果を減らす設定の端末では全部0秒**にした（`prefers-reduced-motion: reduce`で
トークンを0msに上書き）。実測でも0.3秒でレース画面に到達することを確認。

### 実測（iPhone 13・本体で独立に確認）

| # | 実測 |
|---|---|
| ① | 黒0→1が0.5秒／文字が1.4秒に出そろい3.0秒まで／レース画面3.4秒／黒が4.3秒で明ける。⭐**黒画面の文と実況欄の1行目が一致**（同じ計算を2箇所に書いているので要確認だった） |
| ② | 2枚に`pane-m2-exit`と`pane-m2-enter-active`が同時に付き縦に動いた |
| ③ | 上半分の2枚が0.69と0.31で入れ替わり中。⭐**下半分は`matrix(1,0,0,1,0,0)`＝動いていない** |
| ④ | 下半分が364px→121px→64.7pxと下から上がった。⭐**上半分は`none`＝動いていない** |
| ⑤ | 出る側0→−9px、入る側390px→380px。上半分は動かず |
| ⑥ | 覆いが不透明度1まで上がり、週の画面に到達 |

JSエラーは全工程で0件。

### ⚠️ 検証で私が3回続けて取り違えたこと

**②③④が「動いていない」と3回出たが、3回とも私の測り方が原因だった。**

1. **1回目**：速度ボタンは×1→×2→×4→×1と循環するのに2回押して×1に戻していた。
   レースが144秒かかり時間切れ
2. **2回目**：チュートリアルは**指定されたボタン以外を押しても進まない**のに、
   私のスクリプトは常に最初に見つかったボタン（カメラ）を押していた。
   表示のチュートリアル中にカメラを押しても進まず、**時計が3.9秒で止まったまま**になった
3. **3回目**：③は`.grad-top`ではなく**`.grad-top-layer`**という別の要素で動いていた。
   存在しない場所を見ていた

⭐**`transform`の中間値を狙うのをやめ、要素に付いたクラス名をそのまま記録する形にしたら
1回で確定した。** 動いているかを見るには、状態を表すクラス名を読むほうが確実。

### ⚠️ 指定と違う点が1つ

⑥の覆いの色を「白」と決めたが、実装は`rgb(242, 237, 224)`（クリーム色）。
他の画面の文字色と揃えたものと思われる。**未確認・ユーザーの判断待ち。**

### ⚠️ 検証の過程で分かった、作業に効く事実

**画面の一番奥（週の画面）にたどり着くまで毎回1分近くかかる。**
遷移4秒＋チュートリアル17秒＋レース36秒（×4に上げた場合）。
⚠️**セーブ機能が無い**（`TODO.md` #13）ためリロードすると必ず最初からになり、
卒業式だけを見たいときも毎回レースを走らせることになる。
⭐**人が何度も見直す場所ほど、そこへ行くまでが長いと確認が雑になる。**
