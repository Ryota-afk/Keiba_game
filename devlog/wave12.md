# wave12 — 通しプレイ①の指摘への対応（続き・2026-09-24〜）

⚠️`devlog/wave11.md`が60,863バイトになり上限（実質64,000バイト）に近づいたので、ここから分けた
（CLAUDE.md §4）。経緯は`devlog/wave11.md`§1〜§16。

## §1 スタミナのつまみを1.0に決めた（2026-09-24・ユーザー決定）

`AskUserQuestion`で0／0.5／0.75／1.0の実測（`devlog/wave11.md`§16）を並べ、ユーザーが**1.0**を選んだ。
`src/domain/horse.js`の`STAMINA_PROGRAM_FIT`を0.0→1.0にした（確定した値を入れるだけの1行の変更。
CLAUDE.md §2の例外の範囲）。

| つまみ1.0の実測（初期値3通り・1974年開始） | 値 |
|---|---|
| 全段の幅（新馬−オープンのスタミナ中央値） | 0（直す前28.5） |
| 1974年の菊花賞 | 18／18／16頭 |
| 1974年のオークス | 10／9／5頭（0.75なら11／14／7頭——0.75のほうが多い唯一の項目） |

`TODO.md` #108（距離の好みと番組表の食い違い）は、これで直ったので削除した。
残りの重賞の問題（オークスの頭数・1972年のクラシック不成立・「除天皇賞1着馬」）は#111に残した。

`TODO.md` #106（3歳以上の41.1%が一度も走っていない）も削除した。つまみ1.0の実測で、助走後に
一度も走っていない3歳以上は**0%**（初期値3通りとも。`devlog/wave11.md`§16の計測）。
本筋5の指摘②（未勝利・新馬の割合が高すぎる）はこれで完了とした。

## §2 人気の材料「厩舎の強さ」を厩舎の成績へ差し替えた（`TODO.md` #109・2026-09-24・`claude-sonnet-5`）

⭐ユーザー決定（2026-09-22）：画面に出ない`stable.abilities`をやめ、厩舎の過去の成績にする。
仕様の正本は`ARCHITECTURE.md`§2「人気」。

- `computeStableStrengthById(horses, stableById)`：厩舎の現役馬の`record`を合計し、勝率0.5＋連対率0.5。
  出走の少ない厩舎は全体平均へ寄せる（`(勝ち + 30×全体平均) / (出走 + 30)`）。**週に1回だけ**作り、
  その週の全レースで使い回す。重み0.08はそのまま。
- **着順は全86,311走で差分0件**（直す前と後を突き合わせた）。

| 1番人気（つまみ1.0の世界・初期値3通り・助走104週＋本編52週） | 直す前 | 直した後 |
|---|---|---|
| 勝率 | 29.1% | **29.8%** |
| 3着内率 | 58.1% | 58.4% |
| 新馬戦だけの勝率 | 15.6% | 13.1% |

⚠️**目標（実データ32.3%）に2.5点届いていない。** つまみを1.0にしたことで世界の中身が変わり、
前回の30.4%から下がった。重みは変えていない（発注どおり）。⚠️厩舎の材料を差し替えても的中率は
ほぼ横ばい（新馬戦ではむしろ下がった）。`TODO.md` #113へ。

## §3 ユーザー決定：Fableが使えないときはOpus 5.5がデザイン面を担当してよい（2026-09-25）
CLAUDE.md §2と`arch/ui-fable.md`§2に追記した。§8の手順（候補を複数→選ぶ→合意→実装）と
`arch/ui-fable.md`§4の検査はそのまま守る。Opus 5.5で行ったデザインの仕事は報告と記録に明記する。
⭐これで、Fableの残量切れで止めていた本筋5の指摘④（レース結果の画面）に着手できる。

## §4 本筋5の指摘④「世界が動いている感じがしない」→ 週の結果の画面（2026-09-25）

**ユーザーが決めたこと**：「この週を進める」の後に結果だけの画面を1枚挟む。中身は5つ——
乗った鞍の結果／稼いだお金と所持金／主戦の進み具合／調教師からの信頼の変化／その週の重賞の勝ち馬。
見た目は**案C 掲示板**（候補3案×3状態：`design/mocks/week-result-v1.html`と`-A/-B/-C.png`）。
信頼は**言葉で出す**（数字は出さない）。

**分担**：画面（`src/screens/WeekResultScreen.jsx`・`.css`）はOpus 5.5（§3の決まりごと・Fableの残量切れ）。
データを組む純関数`src/domain/weekResultSummary.js`の`buildWeekResultSummary`と`WeekScreen.jsx`への
つなぎ込み・`ARCHITECTURE.md`§2の追記はSonnetサブエージェント。
本体で1か所足した：結果画面を出すときも先頭へスクロールする（「この週を進める」は一覧の最下部にあるため）。

- 信頼の言葉の境目`TRUST_BIG_CHANGE_WORD = 5`は**根拠なしの仮の値**（コードにも仮と書いた）。
  ±5以上で「大きく上がりました／大きく下がりました」。
- 重賞の騎手名は、NPCの重賞を実際に走らせた`runNpcGradedRaces`と同じ2関数
  （`assignStablePrimaryJockeys`→`jockeyIdForHorse`）で引く。サブエージェントが別のスクリプトで
  1974年12月1週のクモハタ記念を走らせ直し、勝ち馬の騎手（早坂章）が一致することを確かめた。

**確認（iPhone 13の画面サイズ・ビルドした単一HTML・1974年）**：夢のダービー→卒業式→週の画面→
毎週土日1頭ずつ乗って8週進めた。8週とも結果画面が出て「次の週へ」で週の画面へ戻った。JSエラー0件。
スクリーンショットで絵を見た（1月1週：12着と1着の2枚、重賞2本。2月1週：2着と17着）。

**§10 測ったもの**：この画面は数値を動かさない（表示だけ）。代わりに画面に出る量を測った。
- **重賞の欄**：52週で予定84本のうち、1着馬が出た（＝行われた）のは68本。16本は出走馬が集まらず
  行われていない（`TODO.md` #111の続き）。行われなかった重賞は画面に出さない。
- **お金**：8週のうち7週が「+55,000円」（騎乗手当27,500円×2鞍）。2着でも手当だけしか動かない
  （`TODO.md` #114）。
- **主戦の欄**：乗った馬が毎回ほぼすべて「あとN回乗って、1勝すれば主戦」で並ぶ（8週で「主戦に
  なりました」は画面の文字を読んだ範囲で0回）。行が毎週2行並ぶ（1週2鞍の乗り方で）。多すぎるかはユーザーの通しプレイで確かめる。

⚠️**サブエージェントの報告で1つ食い違い**：見本に入れた1974年12月1週の数字（4鞍2勝・セイウンテンペストが
主戦になる等）は、サブエージェントが同じ週を再現した結果（4鞍1勝）と合わなかった。1鞍（1着・12頭立て・
3番人気・177,500円）だけが数字まで一致し、競馬場が違った。見本は乗る馬の選び方が違う実行から
取った可能性がある。**見本は見た目を決めるためのもので、実装の正しさはこの節の確認で見ている。**

**追加の直し（本体・同日）：主戦を断って下がった信頼も結果画面に出す**。サブエージェントの実装は
「乗った鞍の厩舎」だけを信頼の欄の対象にしていたため、主戦を断ったとき（乗っていない厩舎で
信頼が2下がる・`weekLoop.js`の`DECLINE_MAIN_MOUNT_TRUST_LOSS`）が出なかった。これは`TODO.md` #100
（断った後に下がったことを見せる経路が無い・通知の閾値4に減点2が届かない）と同じ穴。
対象を「進める前か後に信頼を持つ全厩舎」へ広げ、変化0の厩舎は出さないまま。並びは乗った厩舎が先。
信頼が動くのは騎乗の結果（`raceOutcome`側）と断ったとき（`weekLoop.js`176〜179行）だけなので、
行が増えすぎることはない（grepで確認）。仮のデータで確かめた：乗った厩舎+1は「少し上がりました」、
乗っていない厩舎−2は「下がりました」と出る。⭐#100の直し方②（通知に頼らず信頼の増減を毎週見せる）
がこれで入ったので#100は消した。

## §5 本筋5：通しプレイ②を渡した（2026-09-25）
前回と同じURL（`https://claude.ai/artifact/BDn8axSzqeLSrEsoP5stQx`・版2）へ公開し直した。
公開前に見せる画面の名前で`TODO.md`を照合した（CLAUDE.md §14）。残っている記録済みの指摘：
#10（夢のダービーの見た目）・#44（判断カードに内外が出ない）・#68（実況の「府中」）・#91（重賞に
乗れる判定が1年目に出ない）・#104（怪我の知らせが2回）・#105（依頼の7〜8割が所属厩舎から）。
⚠️#78（目覚め〜卒業式の絵5件）は2026-09-22に3場面を描き直した後も`TODO.md`に残っている。
描き直した絵では寝ている人物・壁の絵が無くなり、窓の外と校門に絵が入っているが、5件を1件ずつ
ユーザーに確かめてはいない。通しプレイ②の答えで消すか決める。

## §6 通しプレイ②の指摘6件（2026-09-25・ユーザー）
別件の依頼（大学の英語の授業の自己紹介プレゼン用の情報集め）と一緒に届いた。原文の要旨：
1. スタートで「騎手になる」ボタンが連打できてしまい、連打するとバグる気がする
2. チュートリアルで、馬番・馬名が表示されない（時間が止まっているから？）
3. 夢の乗り方を信じるかどうかで「4つとも同じになる」という文言があるが、ユーザーはその4つを
   把握していないので変える必要がある
4. 自分の能力が把握できるタブは作ったほうが良い。どの距離・馬場・乗り方が得意か分からなくなる
5. レースの中身自体がなくても割と面白いと思ったので、レースを完全にオフにする（シミュレーション
   のみにする）ボタンかモードがあっても良いかも
6. 自分が乗った馬の結果は分かるが、競馬界全体がどうなっているかが分からない。番付があったら
   面白いかも（3歳牡馬クラシック番付、2歳馬番付、古馬王道路線番付、古馬短距離番付、古馬牝馬番付など）
`TODO.md`本筋5の⑤〜⑩に書いた。まだどれも調べていない・直していない。
⭐5は「レースを見ないでも面白い」という、週の判断そのものが遊びとして立っていることの証言でもある。
