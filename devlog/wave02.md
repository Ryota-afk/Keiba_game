# 第2弾：実装（進行中）

`TODO.md` #1「第2弾：実装」の詳細記録。仕様の正本は`ARCHITECTURE.md`。作る順序は
`TODO.md` #1末尾（①data ②core ③domain ④週の進行(仮sim) ⑤導入 ⑥tools/計測 ⑦sim本実装）。

## 着手前の確認（2026-09-02）

- モデルを`claude-sonnet-5`に切り替えて着手（CLAUDE.md §2）。`src/sim/`のみ`claude-opus-5`。
- 「この書類だけで実装できるか」のチェックは第1弾-Eの8回目の監査で通した（`wave01e.md`末尾）。
  新たな抜け漏れは着手前チェックとしては再実施していない——①の実装中に見つかったものは
  下の「①で見つかったこと」に書く。

## ① `data/`（静的データのみ・ロジック禁止……ではなく、引数のみを参照する自己完結の
   小さな純関数は許容。CLAUDE.md §5・Roadrace_Game CLAUDE.md §5の precedent）

作った5ファイル：

| ファイル | 内容 | 実データか雛形か |
|---|---|---|
| `data/courses.js` | JRA10場（名称・回り方向）＋地方10場（代表のみ・回り方向は未検証）＋海外4 | JRA10場の名称・回り方向のみ確定事実。コースプロファイル（距離・直線長・坂）は`profile: null`＝未取得（§15） |
| `data/classes.js` | クラス9段のラダーと`isGraded`等の述語 | 確定（ARCHITECTURE §3） |
| `data/names.js` | 冠名12語・語幹20語＋`generateHorseName`（純関数・rngは引数で受け取る） | **完全に架空**（このゲームのために作った語。実在の馬主・馬名の丸写しではない） |
| `data/calendar.js` | 52週の雛形（`buildCalendarSkeleton`）。各週`{weekNo, month, weekOfMonth, meetings:[]}` | **雛形のみ**。実カレンダーの月ごとの週数（4〜5週）は仮に3/6/9月を5週とした。実データ取得後に対象年ごとの実際の並びへ差し替える |
| `data/gradedRaces.js` | 重賞出走表のスキーマ（`GradedRaceEntry`）＋`GRADED_RACES`（空配列）＋検索関数 | **中身ゼロ・スキーマのみ**。⚠️創設年・開催月週を記憶だけで埋めなかった（下記） |

### ⚠️ 判断：重賞出走表・カレンダーの中身を記憶から埋めなかった理由

年約130重賞×30年（約3,900レース）は、JRA公式HTMLとWikipediaから取得する計画
（`ARCHITECTURE.md`§3データ源表・`design/rights-check.md`§6）だが、このセッションでは
まだ実データの取得（fetch）をしていない。⚠️**日本ダービーが5月・有馬記念が12月といった
広く知られた事実でも、創設年や正確な開催月週を記憶だけで書くと、CLAUDE.md §0.5の
「データの丸写しはしない」の逆側——**検証していない値を史実として書く**——を犯す。
一次資料と突き合わせるまでは`GRADED_RACES`を空のまま、スキーマだけを確定させた。

これにより②③④は「重賞データの形」を前提に進められる（`courseId`・`grade`・`slot`の
フィールドが決まっている）が、④の仮simで実際に重賞を発生させる場合は、当面は
架空の重賞（`data/names.js`の語彙＋`data/courses.js`の競馬場から仮生成）で代替する。
⑦で実データに差し替える。

### 検証

- 各ファイルをNodeで直接import・実行して確認（純ロジックなのでJSX無し・軽量）：
  10場・9クラス・名前生成3件・52週丁度・重賞0件、すべて期待どおり。
- `npm run build` → `dist/index.html`単一ファイル生成を確認（141KB）。既存の配信経路を壊していない。

### 次

② `core/`——週×馬シードと用途別ストリーム（C4。代打・天候は別ストリーム）。

## ② `core/`（乱数・共通の小関数）

作った1ファイル：`core/rng.js`。

| 関数 | 役割 |
|---|---|
| `streamRandom(saveSeed, stream, ...keyParts)` | (用途, キー) から独立した乱数生成器を作る |
| `RNG_STREAMS` | 用途名の定数（`fall`落馬／`reveal`開示／`standIn`代打／`weather`天候／`sim`レースsim／`rival`他の騎手の動き） |
| `pick(rand01, list)` / `chance(rand01, p)` | 小さな純ヘルパー |

### 設計判断：「1本の列を順に消費する」方式を採らなかった

`devlog/wave01.md` §15-4は3つの必須点を挙げる：①種は週×馬から導出②用途ごとに
ストリームを分ける（1本の列を順に消費すると呼び出し順序で運がずれる）③週の種をセーブに含める。

⭐**②の問題を作り込まない設計にした。** 呼び出しごとに`(saveSeed, stream, ...keyParts)`
からハッシュ種を作り直す方式のため、「列を消費する」という概念自体が存在しない。
ある呼び出しの有無・順序は、他のどの呼び出しの結果にも一切影響しない
（無関係な呼び出しを挟んでも同じキーの結果は変わらないことをテストで確認済み）。
結果として③も単純になる：セーブには`saveSeed`（キャリア開始時に1回引く基底シード）
だけを持てばよく、週ごとの状態を保存する必要がない。

### 検証

Nodeで直接実行し6点を確認：①同キーで決定的②ストリームが違えば結果も違う
③無関係な呼び出しを挟んでも結果が変わらない（順序非依存）④週だけでなく馬でも変わる
⑤saveSeedが違えば別キャリアになる⑥`chance()`が基準確率p=0.008に対し20万試行で0.0079
と近い値を返す。`npm run build`も引き続き成功。

### 次

③ `domain/`——馬・騎手・信頼・週の進行の純ロジック。

## ③ `domain/`（純ロジック）——まず馬（`domain/horse.js`）

⚠️③の範囲は「馬・厩舎・馬主・NPC騎手の生成と世代管理／次走の決定（H）／クラス昇降（J）」
（`TODO.md` #1の作る順序）と広い。CLAUDE.md §0-1が求める「指示を細分化し、正確性を
重視して段階を分ける」に従い、まず**馬**（他の3種の生成が下敷きにするパターン）を実装した。
厩舎・馬主・NPC騎手の生成と世代管理は③の続きとして次に回す。

### 実装した内容（`domain/horse.js`）

| 関数/定数 | 役割 |
|---|---|
| `generateHorse(saveSeed, key, opts)` | 架空馬を1頭生成。9軸能力・性別・成長型4種・血統の系統・名前を持つ |
| `GRADE_SCALE` | 記号評価G〜Sの抽象スケール。⚠️ウイポの表示形式（枠組み）のみ参照——実データの数値は使っていない |
| `growAdaptability(adaptability, raceTrendKey, amount)` | 適応能力を育てる純関数 |
| `classAfterWin` / `classAfterDebutLoss` | クラス昇降（J）。オープンで頭打ち——G3以上は勝利数だけで上がらない仕様（§15未定）を反映し、暫定的にオープンで止める |
| `isDueForNextRace` / `RACE_INTERVAL_WEEKS=8` | 出走馬の決定（H）の下地。年6.3走≒8週間隔の簡易版 |

`core/rng.js`に`RNG_STREAMS.GENERATION`（エンティティ生成用ストリーム）を追加した
（既存の6用途と衝突しない新規追加。既存呼び出しへの影響なし）。

### 判断：適応能力の初期値

血統の系統（日本型/欧州型/米国型）1つにつき対応する適応能力（瞬発/持久/消耗）を1に、
残り2つを0にする**単純な初期化**にした。⚠️本来は父・母それぞれの系統を辿る血統表が
必要（ARCHITECTURE.md §3「血統」）だが、血統表そのものの実装は別項目のため、
このファイルでは`bloodlineFamily`という単一フィールドで仮に代表させている。
血統表を実装する際にここを差し替える。

### 検証

Nodeで直接実行：①同じ(saveSeed,key)から同一の馬が生成される（決定性）②冠名ありの
名前生成が動く③クラス昇降が新馬→未勝利→1勝→…→オープンで頭打ちになる④適応能力の
成長が純関数として元の値を書き換えない⑤出走間隔（8週）の判定が期待どおり⑥2,000頭
生成して血統の系統と対応する適応能力の初期値が必ず一致する（不一致0件）。
`npm run build`も引き続き成功。

### 次

③の続き——厩舎（190）・馬主（100）・NPC騎手（160）の生成。その後④週の進行（仮sim）。

## ③の続き（2/4）——厩舎（調教師）と馬主

### リファクタ：`GRADE_SCALE`を`data/grades.js`へ切り出し

厩舎の能力（4軸のうち3軸）も馬と同じG〜S記号スケールを使うため、`domain/horse.js`に
置いていた`GRADE_SCALE`/`pickGrade`を`data/grades.js`（リテラル定数＋自己完結の純関数）へ
移した。`horse.js`は`GRADE_SCALE`を re-export して既存の呼び出し元との互換を保つ。
CLAUDE.md §5「特定の1ファイルが肥大化の受け皿になり続けていないか都度チェック」に沿った判断。

同様に、調教師・馬主・（次に作るNPC騎手も含む）「人の名前をもじる」処理は馬の名前
（`data/names.js`）と語彙の性質が異なるため、新しく`data/humanNames.js`を作って分離した。

### 実装した内容

| ファイル | 内容 |
|---|---|
| `data/grades.js` | `GRADE_SCALE`（G〜S）と`pickGrade` |
| `data/humanNames.js` | もじった人名の生成（`generateModeledName`。音節16種の組み合わせ） |
| `domain/stable.js` | `generateStable`（厩舎1件：得意分野・4軸のうち3軸・管理頭数）／`SPECIALTIES`6種／`TRUST_DISCLOSURE_TIERS`（信頼値の見せ方3段） |
| `domain/owner.js` | `generateOwner`（馬主1人：もじった名前・大口なら冠名）／`isMajorOwner`（暫定閾値5頭） |

### 検証

Nodeで確認：①厩舎生成の決定性②190件生成して得意分野6種が全て出現・名前の重複は
139/190に留まる（⚠️音節16種×16種＝256通りのため、実装が進んだら`data/humanNames.js`の
語彙を増やす）③馬主：大口のみ冠名を持つ・小口は持たない・100人生成して85件が別名④
`horse.js`のリファクタ後も既存の生成が壊れていないことを再確認。`npm run build`も継続して成功。

### 次

③の続き（3/4・4/4）——NPC騎手（160人・§4の適性10＋基礎能力2と同型）の生成、
および世代管理（引退・新規デビュー）。その後④週の進行（仮sim）。

## ③の続き（3/4・4/4）——NPC騎手の生成と世代管理。これで③完了

### 実装した内容

| ファイル | 内容 |
|---|---|
| `data/ranks.js` | 騎手ランク6段のラダーと仕様（昇格条件・平日の枠・スキル枠・適性のS上限。ARCHITECTURE.md §8の表をそのまま定数化。⚠️暫定値） |
| `data/aptitudeCategories.js` | 騎手の適性10個の内訳（戦法4・距離4・馬場2）。金曜の作戦・レースsimでも再利用する前提で共有部品にした |
| `domain/jockey.js` | `generateJockey`（NPC騎手／プレイヤー騎手共通。プレイヤー専用の分岐を作らない——§6「NPC騎手」の要求）／`learnSkill`（スキル枠を機械的に絞る）／`growAptitude`（Sにできる数の上限を守る）／`retireJockey` |
| `data/grades.js`に`nextGrade`追加 | 等級を1段上げる純関数（適性の成長に使う） |
| `domain/horse.js`に`isRetired`・`retireHorse`追加 | 世代管理の最小形 |
| `domain/stable.js`に`retireStable`追加 | 同上 |

### ⚠️⚠️ 前作の実測バグの再発防止を最初から組み込んだ

ARCHITECTURE.md §4「スキルの枠（実装時の必須確認）」が名指しした前作のバグ
（`maxSlots`が`sim/`から参照されておらず、枠の数と効果の発動が無関係だった）に対し、
`learnSkill`は**枠を超えたら習得自体をしない**形にした。効果側（将来の`sim/`）は
`skills`配列の中身だけを見ればよく、枠を別途チェックする必要が無い——
チェック漏れが起きようがない設計にした。適性の「Sにできる数」の上限（`growAptitude`）も
同じ形で実装した。Nodeでの実測で、新人（枠2）は3つ目のスキルを習得できないこと、
Sへの成長も2個目までしかSにならないことを確認済み。

### 気付いたこと（次に触るときのための記録）

`generateJockey`はランダム生成時に成長上限のチェックを経由しない
（`pickGrade`が直接G〜Sを引くため）。理論上は生成時点で3つ以上の適性がSになりうる
——「Sにできる数」の上限は**プレイ中の成長にだけ効き、初期生成には効かない**。
ARCHITECTURE.md はこれを明示的に禁じてはいないが、初期生成でSが偏って強い/弱い
騎手が生まれうる点は計測（§13）で確認する価値がある。

### 世代管理は「仮の最小形」に留めた

厩舎・馬主・NPC騎手・馬はいずれも`isActive`/`isRetired`フラグと引退用の純関数を持つに
留め、**いつ引退させるか**（年齢・成績・怪我などの判定と、年の区切りでの実行）は
実装しなかった。ARCHITECTURE.md §15が「年の区切りの処理順と時点：難易度変更・
引退勧告・馬の引退／デビュー・世代交代・ランク判定」を明示的に「未定義のまま実装に
入ると詰まる箇所」として挙げており、**週の進行（カレンダー）の仕組みが無いと
「年の区切り」自体が定義できない**ため。④週の進行で年の区切りを実装するときに
ここへ戻る。

### 検証

Nodeで確認：①騎手の決定的生成・160人生成して名前125種（音節16種の組み合わせの限界。
`data/humanNames.js`は厩舎・馬主と共有しているため、実装が進んだら語彙を増やす）
②スキル枠が新人=2件で頭打ちになり、重複習得は無視される③適性のS上限が2個で
頭打ちになる④馬・厩舎・騎手いずれの引退関数も元のオブジェクトを書き換えない純関数。
`npm run build`も継続して成功。

### ③完了。次は④

④週の進行（仮sim）——月曜の依頼一覧→平日7行動→金曜（競馬場→鞍→脚質→予報）→
仮のsim→結果（信頼・金・主戦判定・開示・疲労・落馬・6項目の通知）。

## ④ 週の進行（仮sim）——まず初期ロースターの生成（`domain/career.js`）

⚠️④の範囲（月曜の依頼一覧→平日7行動→金曜確定→仮sim→結果）も広いため、まず土台となる
「新しいキャリアの初期ロースター生成」を実装した。厩舎・馬主・馬・NPC騎手は③で個別の
生成関数ができているが、実際にJRAの実数規模で生成し、所属関係（どの馬がどの厩舎・
馬主に属するか）を結ぶ合成レイヤーが無いと、依頼一覧の生成（「その厩舎に属する馬」を
引く）が書けない。

### 実装した内容（`domain/career.js`）

`createInitialRoster(saveSeed)`：厩舎190・馬主100・馬7,600・NPC騎手160を生成し、
所属関係（`horseIds`）を結んで返す。

- 馬の所属厩舎：round-robin（`i % 190`）で厳密に40頭ずつ均等に埋まる（190×40=7,600と
  ちょうど一致するため）
- 馬の所属馬主：重み付き乱数（`rand01^2`）で歪ませ、少数の馬主に多くの馬が集まる
  （§3「馬主を実数の1/20に減らした…実態より大口になる」を反映）
- 大口馬主の判定→冠名の割り当て→馬の名前生成、の順で依存を解決（冠名は馬主が先に
  決まっていないと馬の名前に渡せないため）

### ⚠️ 計測：大口馬主の閾値と、冠名を持つ馬の割合（CLAUDE.md §10）

`isMajorOwner`の閾値（旧暫定値：持ち馬5頭以上）をこの規模の分布にそのまま使うと、
馬主の**91%**（100人中91人）が大口になった。⚠️§3「全馬主に冠名を与えると全部の馬名が
同じ形式になり単調になる」という設計意図に反する。

**対処（before/after）**：閾値を固定値5から**平均持ち馬数（76頭）**へ変更。
→ 大口の馬主は100人中**42人**に減った。

**それでも解決しなかったこと**：大口馬主は元々持ち馬が多い（分布が歪んでいるため）ので、
馬主の人数比を42%に抑えても、**馬全体の78.7%**は依然として冠名付きの名前になった。
⚠️**数値が動かなかった／不十分だった場合として記録する**（CLAUDE.md §10）。
`TODO.md` #9へ上げた（対策候補3つ）。実装の弾で決める。

### 検証

Node実行で確認：①7,600頭の生成が約30msで完了（性能上の懸念なし）②決定性（同じ
saveSeedで馬・馬主とも完全一致）③190厩舎すべてがちょうど40頭を持つ④馬主の持ち馬数
合計が7,600と一致する⑤持ち馬数の分布が歪んでいる（最小1・中央値64・最大230）⑥
全ての馬が実在する厩舎・馬主のIDを参照している⑦大口馬主の冠名が実際にその馬主の
馬の名前に使われている⑧全NPC騎手が実在する厩舎のIDを参照している。
`npm run build`も継続して成功。

### 次

④の続き——月曜の依頼一覧生成（依頼の質＝調教師の信頼×ランク×厩舎の強さ）から。
その前提として、信頼値（調教師・馬主・評判）を持つプレイヤー状態の形も必要になる。

## ④の続き——月曜の依頼一覧生成（`domain/player.js`・`domain/weeklyRequests.js`）

### 実装した内容

| ファイル | 内容 |
|---|---|
| `domain/player.js` | `createPlayer`（新人騎手のプレイヤー状態：所持金・信頼マップ2種・評判・主戦の座の進捗・現在週/年）／`trustFor`・`adjustTrust` |
| `domain/weeklyRequests.js` | `horsesDueThisWeek`（H：出走馬の決定の入口）／`requestQuality`（依頼の質＝調教師の信頼×ランク×厩舎の強さ）／`generateWeeklyRequests`（所属厩舎の馬は優先的に含み、残りは質の高い順に埋める。乗れる鞍数より多く来る） |
| `data/grades.js`に`gradeToNumber`追加 | 厩舎の強さ計算用（G〜Sを0〜7の数値に変換） |

### ⚠️ 実測で見つけた不具合と修正：初週に全馬が一斉に出走候補になる

`generateWeeklyRequests`をテストしたところ、新規キャリアの第1週で**所属厩舎40頭中
40頭全てが出走候補**になった。原因は`createInitialRoster`が全頭`lastRaceWeek: null`
で生成していたため——「まだ一度も出走していない」という初期値は新馬デビュー前の
架空馬なら正しいが、**30年続く世界では大半の馬が既にキャリア途中**であるべきで、
出走間隔がばらけていなければならない。

**修正**：`horse.js`の`generateHorse`が`opts.lastRaceWeek`を受け取れるようにし、
`career.js`で各馬に`1 - RACE_INTERVAL_WEEKS`〜`0`週の範囲で均等に散らした初期値を
与えた。修正後、週ごとの出走候補数は986→1,925→…→7,600と1/8ずつ滑らかに増える形になり、
第1週の依頼数も40件から**6件**（乗れる鞍数3×倍率2）に収まることを確認した。

### 検証

Nodeで確認：①決定性（同じsaveSeedで依頼一覧が完全一致）②所属厩舎の出走候補馬は
全て依頼一覧に含まれる③週が変われば依頼一覧の中身も変わる④出走間隔の階段状の
増え方が期待どおり（8週で母集団全体に到達）。`npm run build`も継続して成功。

### 次

④の続き——平日7行動（朝の調教で乗る／自分から売り込む／牧場・セリを見に行く／
人に会う／体を作る／休む／買い物に行く）。その後、金曜の確定（競馬場→鞍→脚質→予報）
→仮sim→結果処理（信頼・金・主戦判定・開示・疲労・落馬・6項目の通知）。

## ④の続き——平日7行動（`data/weekdayActions.js`・`domain/weekdayActions.js`）

### 実装した内容

| ファイル | 内容 |
|---|---|
| `data/weekdayActions.js` | 行動7種のID・ランクごとの解禁表（§8の表をそのまま定数化）・相手を要らない3行動 |
| `domain/weekdayActions.js` | `isActionUnlocked`／`trainAtOwnStable`（信頼+1）／`pitchForRide`（成功率＝基準0.3＋ランク×0.1＋評判/100、上限0.9）／`meetOwner`（金が無いと行動そのものが不成立。払っただけでは信頼は上がらず、行動が成立して初めて+1） |

### ⚠️ 3つはプレースホルダーのまま実装した

7行動のうち**体を作る・買い物に行く・牧場・セリを見に行く**は、ARCHITECTURE.md §15が
効果を明示的に「未定」としている（買い物＝ショップの価格、牧場・セリ＝デビュー前の馬を
押さえる効果、体を作るは記載自体が無い）。ここで架空の数値を割り当てると、
CLAUDE.md §0.5「データの丸写しはしない」の逆側——**検証していない仕様を史実ならぬ
"確定仕様"として書いてしまう**——ことになるため、**プレイヤーを書き換えずそのまま
返す明示的なプレースホルダー関数**として実装した。行動枠を消費するという構造は
（呼び出し側で）成立させられるが、数値効果は実装の弾で置く。

### 検証

Nodeで確認：①ランクごとの解禁（新人は調教・売り込む・休むの3つだけ、トップは全解禁）
②調教で信頼が+1されること・元のplayerが書き換わらないこと③売り込みの成功率が
決定的（同じ週×相手厩舎で同じ結果）で、新人は実測0.298（基準0.3と一致）、
トップ+評判100は実測0.896（上限0.9と一致）④人に会うが所持金不足だと不成立になり
playerが完全に元のまま返ること（参照レベルで同一）⑤3つのプレースホルダーが
本当に何もしない（`===`で同一オブジェクトが返る）。`npm run build`も継続して成功。

### 次

④の続き——金曜の確定（競馬場→鞍→脚質の指定→天候予報）。その後、仮simと結果処理
（信頼・金・主戦判定・開示・疲労・落馬・6項目の通知）。
