# 番組表（1年のレースをどう並べるか）

⭐**正本。** `ARCHITECTURE.md`§15の「クラスごとのレース数の配分」の答え。
決定A・質問8〜11（2026-09-12〜14にユーザーが決定）の結果を置く。経緯は`devlog/wave06.md`。

## 1. 重賞は年ごとの実データ（質問8＝(ア)）

⭐**その年に実在した重賞を、週・競馬場・距離・名前ごとそのまま置く。** 30年ぶん持つ。
出典は優駿達の蹄跡の結果表（`data-src/ahonoora/`）。1974年は100本（平地サラブレッド84・障害8・アラブ8）。
- **障害とアラブはゲームに出さない**（質問5）。1974年に番組表へ並ぶ重賞は**84本**。
- **グレード（G1/G2/G3）は1984年から出す。** 障害のグレードは1999年から。
- ⚠️**リステッド（L）は置かない**——JRAが2019年に付け始めた格付けで、1974〜2003年には無い。
- ⭐**開催の並び（どの週にどの競馬場が開くか）は今のJRAの52週の並びを毎年固定して使う**
  （質問20＝(イ)・2026-09-15にユーザー決定。`design/jra-race-calendar.md`§3の並び＝関東13・関西13・
  第3場10開催、288日）。⚠️Claudeは「重賞がその週に開いていない競馬場で行われる矛盾が出る
  （1974年の皐月賞は東京、今の並びでは第16週は中山）」を示したうえでの決定。
  ⭐**矛盾の処理（質問21＝(ア)・2026-09-15）：重賞は、並びの上でその週に開いている同じ地区の競馬場へ移す。**
  地区＝関東（東京・中山・福島・新潟）／関西（京都・阪神・中京・小倉）／北海道（札幌・函館）。
  並びに週があるG1（平地24本）は並びの週・並びの競馬場。それ以外の重賞はその年の実データの週を使い、
  競馬場だけを移す。⚠️1974年は84本中25本（30%）が移る（実測）。名前・距離・週は史実、競馬場は並び優先。

## 2. 1日は11レース（1997年まで）／12レース（1998年から）

根拠：Wikipedia「中央競馬」。⚠️**1974年の開催日数は未確認**（288日は今の開催の並びから出した数）。
288日×11＝年3,168本。

## 3. クラスの配り方（決定A）

⭐**クラスは1勝・2勝・3勝クラスの3段。昇級は勝った回数**（質問10＝(イ)）。
⚠️1974年の史実は賞金別11段（200〜1300万下）だが**採らない**。
⚠️**収得賞金は別に持つ**——重賞の出走順はJRAの規則どおり収得賞金の多い順（`design/jra-entry-rules.md`）。

**1974年の場合**（年3,168本＝288日×11）：

| 種別 | 1年の本数 | 1日あたり | 出どころ |
|---|---|---|---|
| 新馬 | 305 | 1.06 | 残り2,994本の10.2%（2026年の平地の割合） |
| 未勝利 | 1,138 | 3.95 | 同 38.0% |
| 1勝クラス | 889 | 3.09 | 同 29.7% |
| 2勝クラス | 449 | 1.56 | 同 15.0% |
| 3勝クラス | 213 | 0.74 | 同 7.1% |
| オープン以上 | 174 | 0.60 | ⭐**先に固定する**（重賞84＋オープン特別90） |
| **計** | **3,168** | **11.00** | |

⚠️⚠️**オープン・リステッド・重賞は2026年の型で増やさない**（ユーザーの制約）。
⭐**手順**：①その年に実在した重賞の数を数える（1974年は84本＝平地サラブレッドのみ）
②オープン特別をその1.07倍と置く（84×140/130＝90本）③残りを上の割合で新馬〜3勝クラスに配る。
⚠️**オープン特別の1.07倍には根拠が無い**——2026年のオープン以上270本のうち重賞が約130本で
残りが約140本だったので、その比を借りただけ。馬ごとの戦績表を取り終えたら実測して置き換える。

**2026年の実測の元表**（42開催・289日・3,468本。42開催すべてで「種別の（計）の合計＝日数×12」が成立）：

| 年齢 | 新馬 | 未勝利 | 1勝 | 2勝 | 3勝 | オープン以上 | 計 |
|---|---|---|---|---|---|---|---|
| 2歳 | 270 | 625 | 118 | — | — | 29 | 1,042 |
| 3歳 | 45 | 534 | 223 | — | — | 45 | 847 |
| 3歳以上 | — | 10 | 390 | 260 | 122 | 113 | 895 |
| 4歳以上 | — | — | 182 | 202 | 96 | 83 | 563 |
| 障害 | — | 80 | — | — | — | 41 | 121 |

## 4. 芝とダート（質問11＝(B)）

⭐**芝50.2%・ダート49.8%。** 1日11レースなら芝5.5本・ダート5.5本。
⚠️**1974年の実測（24頭の重賞以外304走で芝84.4%）は採らない**——Claudeが「1974年にダートが
1日5〜6本あったとは考えにくい」とリスクを示したうえでのユーザー決定（2026-09-14）。
根拠：2026年の番組表から距離のセルを2,694本（あるべき3,060本の88%）拾い、障害の距離124本を
除いた平地2,570本のうち芝1,289・ダート1,281。

## 5. 距離（2026年の実測・平地のみ）

| 芝 | % | | ダート | % |
|---|---|---|---|---|
| 1,000 | 1.6 | | 1,000 | 3.4 |
| 1,200 | 18.1 | | 1,150 | 2.8 |
| 1,400 | 9.6 | | 1,200 | 19.4 |
| 1,500 | 1.6 | | 1,300 | 1.2 |
| 1,600 | 17.7 | | 1,400 | 16.2 |
| 1,800 | 16.4 | | 1,600 | 5.8 |
| 2,000 | 22.3 | | 1,700 | 12.6 |
| 2,200 | 4.3 | | 1,800 | 31.3 |
| 2,400 | 4.0 | | 1,900 | 2.5 |
| 2,500 | 0.9 | | 2,000 | 1.2 |
| 2,600 | 2.6 | | 2,100 | 2.3 |
| 3,000以上 | 0.8 | | 2,400以上 | 1.3 |

⚠️**競馬場ごとに使える距離は違う。** 実際に置くときは`design/jra-course-geometry.md`の
10場のコースデータで絞り込んでから、この割合で選ぶ。

## 6. 年齢と牝馬限定

- **2歳戦は6月から**（根拠：史実の馬24頭のデビュー月の実測——6月1・7月7・8月6・9〜11月9・
  翌1月1。2026年も2歳戦がある最も早い開催は6月6日からの第3回東京）。
- **牝馬だけのレースは11.0%**（1日11レースなら1.2本。根拠：2026年の番組表の実測3,076本中339本）。

### ⭐ 重賞の年齢・性別条件（`condition`）を読む（第11弾・`devlog/wave11.md`§12）

⚠️⚠️**それまでコードのどこも重賞の実データが持つ`condition`（例："3歳牡牝 定量"）を
読んでいなかった。** 4歳以上の馬が日本ダービーに出られる、という実データと矛盾した状態が
残っていた。

- **読む文字列の形**：1972〜1987年の全JSON（`src/data/generated/gradedRaces.*.json`）
  1,400本を走査した実測（`condition`が`null`の11本を除く1,389本）。
  - 年齢：「N歳」＝ちょうどN歳（例："3歳"）・「N歳以上」＝N歳以上（例："4歳以上"）
  - 性別：「牡牝」＝牡と牝だけ（セン不可）／「牝」＝牝だけ／どちらも無ければ制限なし
  - ⚠️**読まない（通す）**：「除◯◯1着馬」（実測18件・すべて「4歳以上牡牝（除天皇賞1着馬）」）・
    「父内国産」（実測28件）・負担重量の種別（ハンデ／別定／定量／馬齢——着順の計算には
    関わるが出走資格には関わらない）
  - ⭐既存の`fillyOnly`（重賞オブジェクトが別に持つ真偽値）と`condition`から読んだ「牝」限定は
    **1,400本全件で食い違い無し**（実測で確認済み）。
- **置き場所**：文字列を読む純関数`parseRaceCondition(condition)`は`data/raceConditions.js`
  （`{ minAge, maxAge, sexes }`を返す）。馬を読む判定（`horse.bornYear`・`horse.gender`）は
  `domain/horse.js`の`isAgeSexEligible(horse, condition, year)`——`bornYear`が無い馬は
  `canDebutThisWeek`と同じ扱いで年齢条件を素通りさせる（性別条件は素通りさせない）。
- **年齢は「レースが行われる年」で数える**（`race.year - horse.bornYear`）。予定を立てる週の
  年ではない——年をまたいで翌年のレースを予定に入れることがあるため。
- **判定を入れた2箇所**：①走らせる側（`domain/npcGradedRace.js`の候補の絞り込み。優先出走権を
  持つ馬もここは免除しない）②予定を立てる側（`domain/rotation.js`の`collectCandidates`）。
  ⚠️**②を欠くと、4歳馬がダービーを予定に入れて枠（§7「定員」）を消費し、走る段階で弾かれて
  ダービーが埋まらない**という新しい壊れ方になる——両方に入れて初めて意味を持つ。
- **プレイヤーの相手馬**：`domain/weeklyRequests.js`が依頼に`condition`を乗せ、
  `domain/raceOutcome.js`の`assembleRealField`（`domain/entryListPreview.js`の
  `previewEntryField`もこれを使う）が相手馬を絞り込むときに読む——プレイヤーが重賞に乗る
  ときだけ効く（一般競走は`condition`を持たないため無関係）。
- **`buildWeeklyCard`が重賞オブジェクトに持たせるフィールド**：`condition`（実データの文字列
  そのまま）・`trialFor`（下記「優先出走権」参照）。一般競走・オープン特別はどちらも持たない
  （実データが無い）。

## 7. 出走頭数と、相手の選び方（決定3・質問14＝(A)・2026-09-15）

**出走頭数**は実測の分布から引く（史実の馬24頭・527走の実測）：

| 頭数 | 割合 |
|---|---|
| 5〜8 | 30.6% |
| 9〜12 | 38.0% |
| 13〜16 | 23.3% |
| 17〜18 | 3.2% |
| 19〜26 | 4.9% |

平均10.98・真ん中10。⚠️**18頭立ては3.2%しかない。出馬表の画面は10頭前後を前提に作る。**
⚠️`src/domain/raceOutcome.js`の`MIN_FIELD_SIZE=6`〜`MAX_FIELD_SIZE=18`の一様抽選は根拠が無い。差し替える。

**相手の選び方**：⭐**現役馬を全部持ち（1974年は5,800頭＝145厩舎×40）、毎週コンピュータの厩舎が
「次にどのレースへ出すか」を決める。** プレイヤーが乗るレースだけ`src/sim/`の本物の計算を回し、
他のレースは強さ比べで着順を決める。
⚠️⚠️**第11弾で「収得賞金の多い順」を撤去した**（一般競走＋オープン特別のみ。重賞は対象外・
下記参照。`devlog/wave11.md`§7）。3着までしか賞金が付かないため「走らないと賞金が付かない・
賞金が無いと走れない」の輪になっていた（同じ未勝利戦での通過率が賞金>0で76.3%・賞金=0で6.6%）。
⭐**今は「前走から空いた週数の多い順」（未出走は最優先）**——`domain/horse.js`の
`weeksSinceLastRace`・`domain/npcWeeklyRace.js`。⚠️**重賞（`domain/npcGradedRace.js`）は変えていない**
——重賞は収得賞金順のままが自然。

**定員（第11弾・`devlog/wave11.md`§7・§12）**：⭐**一般競走・オープン特別・重賞のすべてを
`domain/weeklyCard.js`が番組表を組む時点で`race.fieldSize`を持たせる**。一般競走・オープン特別は
`RNG_STREAMS.FIELD_SIZE`・`raceId`で固定した抽選値（`domain/npcWeeklyRace.js`は走る瞬間に
引き直さず、この値をそのまま使う）。⭐**重賞は`GRADED_MAX_FIELD_SIZE`（=18・`data/raceProgram.js`。
`domain/npcGradedRace.js`の`MAX_FIELD_SIZE`と同じ値を共有）で固定**。
⚠️⚠️**§12で訂正**：当初（§7設計時点）は「重賞は対象外（`fieldSize`は`null`のまま）」としていた
——`domain/npcGradedRace.js`が実データの優先出走権と`MAX_FIELD_SIZE`（18）で別に定員を持つため
二重に絞ると衝突する、という判断だった。⚠️**しかしそれは「走らせる側」の定員で、「計画を
立てる側」（`domain/rotation.js`）には何も効いていなかった**——重賞の目標選びに定員という
考え方が無いまま残り、全馬が「年内で一番点数の高い1本」に集中する問題が温存されていた
（実例：3000mに適性が届く馬14頭全員が、菊花賞ではなく僅差で上回る天皇賞（春）・ダービーに
集中し、菊花賞の目標が0頭になっていた）。**重賞にも同じ`fieldSize`を持たせることで、
走らせる側と計画を立てる側が同じ定員を見るようになった**——二重に絞る心配は無い
（両方とも同じ18という同じ値を見ているだけで、走る段階での絞り込み方自体（優先出走権→
収得賞金順で18頭まで）は変えていない）。
⭐**`domain/rotation.js`の`replanStaleHorses`が、この定員を見ながらその週のぶんをまとめて配る**
（目標も前哨戦も同じ「残り枠」の表を消費する。重賞もこの対象に含む）——これが無いと、条件の
似た馬が全員同じ1本を選び、他のレースが空になる（before実測：登録0頭のレースが**46.3%**）。

⚠️⚠️**`tools/bench-weekloop.mjs`の「1週0.98ms」を、本物のエンジンの値として引用しないこと**
（2026-09-22にClaudeがユーザーへの報告で引用してしまった。`devlog/wave11.md`§10）。
あれは実際のドメイン関数を使わない簡易モデルで、**下限**である。

⭐**本物のエンジンでの実測（2026-09-22・同じ機械で直す前と後）**：ゲーム開始時にブラウザで走る
助走104週が**7,002ms（1週67.3ms）→ 定員を入れた後 11,482ms（1週110.4ms・1.64倍）**。
⚠️助走は夢のダービーを見ている裏で`bootstrapRosterAsync`がコマ切れに進めるので、
プレイヤーを待たせる時間としては表に出ない。⚠️**ここをさらに重くするなら、
夢のダービーの尺（実況＋判断カード2回）に収まるかを実機で確かめること。**

## 8. 1着賞金（2026-09-15・⚠️条件戦は仮の値）

| 種別 | 1974年（仮） | 出どころ |
|---|---|---|
| 新馬 | 87万円 | 2026年の中央値650万円÷7.5 |
| 未勝利 | 77万円 | 同 580÷7.5 |
| 1勝クラス | 109万円 | 同 820÷7.5 |
| 2勝クラス | 211万円 | 同 1,580÷7.5 |
| 3勝クラス | 249万円 | 同 1,870÷7.5 |
| 重賞 | **その年の実データ** | 結果表の本賞金（1974年は1,400〜4,000万円） |
| オープン特別 | 重賞の最低額の0.8倍（仮） | 根拠なし |

7.5＝日本ダービー1着の2026年（3億円・Wikipedia「東京優駿」）÷1974年（4,000万円・結果表）。
2026年の中央値は`data-src/jra-bangumi/`の番組表PDFの本賞金の列の実測。⚠️他の年は、その年の
日本ダービーの1着賞金（結果表）で同じ比を作って割る。**収得賞金の順番付けにしか使わない。**

## 9. 年の区切りの処理順（2026-09-15）
第52週の結果処理の直後：①馬の引退判定 →②繁殖入り →③繁殖に上がらなかった引退馬を捨てる
（プレイヤーが乗った馬は残す）→④新しい2歳の生成（6月まで出走しない）→⑤調教師の引退と開業
→⑥騎手の引退と新人 →⑦主戦の付け直し →⑧既存の年次処理（難易度・引退勧告・ランク判定）。

## 10. 週の番組表と依頼の結びつけ（2026-09-15・通しプレイ①の指摘で決定）

⭐**週のレースは`domain/weeklyCard.js`の`buildWeeklyCard(saveSeed, week, year)`が1回だけ組み、
依頼（`weeklyRequests.js`）・NPCのレース（`npcWeeklyRace.js`／`npcGradedRace.js`）・出馬表
（`entryListPreview.js`）はすべてその中の1レース（`raceId`）を指す。**
- 中身：その週の重賞（実データ）＋オープン特別（年の推定本数を52週へ確率配分・クラス`open`）＋
  一般競走（`coursesOpenInWeek`の各場×2日×`racesPerDay(year)`から重賞・オープン特別の本数を引いた残り）。
- 依頼は「クラス一致・馬場に出られる・牝馬限定の条件が合う」レースを1つ選ぶ。合う物が無い馬は依頼にならない。
  ⚠️**第11弾・案B-1で例外を1つ追加**（`devlog/wave11.md`§7）：**新馬クラスの馬は新馬戦「と」
  未勝利戦の両方に出られる**（`data/classes.js`の`isEligibleForRaceClass`・
  `domain/weeklyCard.js`の`eligibleBucketsForHorseClass`）。逆（未勝利の馬が新馬戦に出る）はしない。
  ⭐**第11弾§12（`devlog/wave11.md`§12）**：重賞は`condition`（年齢・性別条件）も合う馬にしか
  依頼が出ない——`domain/rotation.js`が`horse.plan`を立てる段階で`isAgeSexEligible`を通すため
  （§6「重賞の年齢・性別条件」）。一般競走・オープン特別は`condition`を持たないため対象外。
- 競馬場・馬場・距離・クラスは必ずレースから引く。⚠️`fridayConfirmation.js`の`resolveRaceContext`
  （無作為の割り当て）は廃止。
- 2歳は第23週（`TWO_YEAR_OLD_DEBUT_WEEK`）より前に出走候補にしない。
- 乱数ストリームは`RNG_STREAMS.CARD`（`(saveSeed, week)`）。月曜の一覧と週末のレースが一致する。

## 11. 開始前の事前シミュレーション（質問22の一般化・2026-09-15設計・2026-09-16実装）

`domain/bootstrap.js`の`bootstrapRoster(saveSeed, startYear)`が、開始年Yの2年前から104週を
プレイヤー無しで回し（各年末に`processYearBoundary`）、クラス・戦績・収得賞金・年齢・繁殖プールを作る。
- 生年：Y−2時点の馬齢を`INITIAL_AGE_SHARE`（0歳13%・1歳45%・2歳22%・3歳14%・4歳6%・5歳1%）で引く。
  ⚠️**当初は「開始年Yでの実測分布（2歳13%・3歳45%・4歳22%・5歳14%・6歳5%・7歳以上1%）を
  そのままY−2時点の割合として使う」という誤りがあった**——2年間歳を取らせるとそのまま
  4〜8歳へずれてしまい、実測の主力である2・3歳（合計58%）が開始時点に1頭も存在しない
  不整合が実測で判明した（`devlog/wave07.md`「計測」）。**実測の年齢からそのまま2を引いた
  値をY−2時点の割合とする**のが正しい対応で、直した後は開始年Yの分布が2歳14.0%・3歳46.1%・
  4歳20.9%・5歳12.8%・6歳5.4%・7歳0.9%（実測）となり、1974年の実測に近づいた。
- 前提：Y−2〜Yの重賞データ（`hasGradedRaceData`）。無い年は`assertStartYearData`が開始年の選択を拒否する。
- 実行：夢のダービーの間に裏で回す（実測12.26ms/週×104週≒1.3秒。`domain/weeklyCard.js`化で
  §10と同じ副次効果により、当初見積り3.9秒より速い）。ブラウザではコマ切れ実行版
  `bootstrapRosterAsync`（`weeksPerChunk`週ごとに制御を返す）を使う。
- ⚠️**開始直後は種牡馬がほぼ0頭になる**（重賞に勝った5歳牡馬しか種牡馬にならないため、
  出走歴の無い状態から2年では育たない）。population自体は減らない方向で自己修復する
  （実測：開始2年後には種牡馬が2桁に回復し、現役頭数は7,286頭で安定。`devlog/wave07.md`）。
- ⚠️⚠️**2026-09-17まで、事前シミュレーションの週番号（1〜104）が本編の週番号（1から開始）と
  合っておらず、事前に走った馬が本編のおよそ2年目まで出走候補にならないバグがあった**
  （`isDueForNextRace`の`currentWeek - lastRaceWeek`が本編の週では負に近い値になり続けたため。
  実測：修正前は本編1週目・12週目の出走予定が新馬のみ。`devlog/wave09.md`§13）。
  ⭐**`bootstrapRoster`／`bootstrapRosterAsync`の返り値の直前で、全馬の`lastRaceWeek`から
  `BOOTSTRAP_WEEKS`(104)を引いて本編の週番号に揃え直す**（`rebaseLastRaceWeekToMainTimeline`）。
  修正後は本編12週目で現役馬7,262頭全頭が出走候補になる。


## ⭐ 競馬場ごとの施行距離（2026-09-16に追加）

`data/courses.js`のJRA10場が`profile: { turf: [距離], dirt: [距離] }`を持つ。
`domain/weeklyCard.js`は**競馬場を先に決めてから**`drawShapeAtCourse`で馬場×距離を引く。

⚠️**これが無いと札幌で芝3600mのレースが組まれる**（競馬場と距離を別々に抽選していたため。
実測：直した後は104週6,336本のうち、その競馬場で行われない組み合わせは0本）。

- 割合は全国の実測表（`SURFACE_SHARE`・`DISTANCE_SHARE_TURF`／`_DIRT`）をそのまま使い、
  **その競馬場に無い距離を取り除いてから正規化**する。
- ⚠️割合の表に載っていない距離（中山の芝4000mなど）には`MIN_DISTANCE_WEIGHT`(0.3)の
  下駄を履かせる。履かせないと一生選ばれない。
- ⚠️`profile`を持たない競馬場（地方・海外）は従来どおり全国の割合から引く。
- ⚠️**距離の一覧は今のJRAの表**（`design/jra-course-geometry.md`）＋史実の重賞データ
  （1972〜1989年）の和集合。当時は行われていない距離が混じる。福島のダートは出典から
  取れておらず仮の値。
- 芝とダートの割合は50.2／49.8から**51.3／48.7**へ動いた（開催している競馬場の偏りぶん）。

## ⭐ レースの土曜・日曜割り当てと1日1場（2026-09-17に追加）

`domain/weeklyCard.js`が組む各レースは`day: "sat"|"sun"`を持つ（`data/weekDays.js`）。
⚠️**これが無いと1日1場（`ARCHITECTURE.md`§2）が成立しない**——それまでレースが土曜か
日曜かというデータそのものが無かった。

- **重賞**：史実の`historicalDate`から曜日を計算する（推測しない）。実測（1972〜1989年の
  重賞1,400本・日付の欠けは0本）：日曜92.4%・土曜3.9%・平日3.7%。⚠️**平日の3.7%だけは
  実データが無い決定**——本作は土日しか持たないので日曜へ寄せる（`dayOfGradedRace`）。
- **オープン特別・一般競走**：史実の日付が無いので、1レースごとに`rand01`で50/50に振る。
  実測（52週・3,168本）：土曜49.4%・日曜50.6%。

**金曜の確定**（`domain/fridayConfirmation.js`）は`courseByDay: { sat, sun }`を受け取る形。
⚠️⚠️**同じレースには1頭にしか乗れない**（2026-09-17に追加）——依頼の41%は別の依頼と
同じレースに重なる（`devlog/wave09.md`§3の実測）ので、`confirmMounts`は同じ`raceId`の
依頼が複数あれば質の高いほうだけを残してから鞍を選ぶ。

**重賞に乗れるかの判定**（`domain/rideEligibility.js`の`canRideGradedRace`）は
`weeklyRequests.js`の`matchRaceForHorse`に組み込んだ——プレイヤーが乗れない重賞の依頼を
月曜に見せない。実測・較正の経緯は`devlog/wave09.md`§9。
