## ⭐ 0. ゲームデザインの哲学（最重要・全作業の前に毎回確認）

前作`Roadrace_Game`（自転車ロードレース経営ゲーム・95弾）の面白さ総点検で確立し、
ユーザーが「最重要事項として毎回確認する」ことを明示した5点。機能追加・バランス・UI、
**どの種類の作業でも着手前にこの5点を通す**こと。

1. **面白さ＝プレイヤーのリスクとリターン。常に「椅子に座っている人間」から見る。**
   simの中の馬や騎手に駆け引きがあっても、プレイヤー自身の判断に緊張が無ければゲーム性は
   無い。正解が表示される選択・分散ゼロの購入・承認するだけの操作は「判断」ではない。
   新要素を足すときは「これはプレイヤーの意味のある判断を増やすか」を最初に問う。
2. **ギュッとパッ（緊張と解放）。「奪う側」の設計を忘れない。**
   ⚠️**前作の最大の欠陥がここだった**——「均す・和らげる・保証する」方向に手が動く癖が
   実績として確認されている（優勝率92%の無自覚な蓄積・破産の実質到達不能）。報酬を足す前に
   「何がプレイヤーを締め付けるか」を問う。勝利のカタルシスは、負けが実在するときにだけ
   生まれる。⚠️本作では**落馬・怪我・騎乗依頼の途絶・信頼の喪失**を最初から柱に据える。
3. **バランス指標だけでなく、面白さ指標で考える。**
   勝率・金額・飽和は面白さを測らない。「意味のある判断の密度」「選択肢に本当の対立が
   あるか」「危機を感じる瞬間があるか」を、設計と計測（§10）の両方に含める。
4. **simは退屈を検知できない。人間の通しプレイに勝る検証はない。**
   ⚠️**前作は95弾のあいだ人間の通しプレイが1度も無かった。** 本作は**第3弾までに1回、
   必ず人間が最小ループを通す**。数値検証がどれだけ厚くても、体験の検証ゼロのままの機能は
   「未検証の仮説」として扱う。
5. **設計・バランス・UI変更の前に`DESIGN_PRINCIPLES.md`の索引から該当ノートを読む**（§11）。

## 🐎 0.5 このプロジェクトは何か

競馬ゲーム。React + Vite + 単一HTML出力（GitHub Pages配信）。
**前作`Roadrace_Game`の後継**で、開発プロセス（このファイル）と`design/`の知見を丸ごと
引き継いでいる。⚠️**コードは流用しない**（自転車固有の型が全層に染みているため）が、
設計パターン（レースsimの先計算→再生・判断カード・配合・マスク能力・絆）は下敷きにする。

- **主モード＝騎手**（最初はこれだけを作る）。調教師モードは騎手の仕組みの上に後から載せる。
  オーナーブリーダーは未定。
  ⚠️**理由**：前作は2モードの主従を最後まで決められず、それが「最初の3分で何を見せるゲームか」に
  答えられない根因になった（`Roadrace_Game/devlog/wave95.md` #47）。同じ轍を踏まない。
- 見た目はカイロソフト風の斜め見下ろし。**史実の名馬は実名で登場し、ゲーム内で生まれた馬は架空**
  （⚠️2026-09-02にユーザーが権利を確認して確定。当初は「もじる」だった）。配合要素を強めに、
  血統表もしっかり作る。馬の能力モデルはウイニングポストの**枠組み**を参考にする
  （`design/`に参考ノートを置く。⚠️**データの丸写しはしない**——取るのは枠組みだけ。
  ⚠️この行は2026-09-02に一度削除され、権利関係の調査（`design/rights-check.md`）を経て
  同日に**復活した**。史実馬の能力値・成長型はウイポから取らず、**戦績（事実）から自分で導く**）。

## 🧠 開発統括とモデル使い分けのルール（メタ指示）

Claudeは、本プロジェクトにおいて単なるコーディングアシスタントではなく、**「開発ディレクター
（Opus的思考）」および「メインエンジニア（Sonnet的実装）」を兼ねるプロフェッショナル**として
振る舞うこと。以下の行動規範を厳守する。

### 1. 完璧な設計図の先行作成（不確定変数の排除）
- ユーザーから機能追加や大きな改修の要望を受けた際、**いきなりコードを書かないこと。**
- まずはユーザーとの対話を通じて、要件の曖昧な点や隠れた変数（リスク、エッジケース、
  ゲームバランスへの影響など）を徹底的に洗い出すこと。
- 不確定要素をすべて排除した**「完璧な設計図」をテキストで提示し、ユーザーの合意を得てから**
  次のステップに進むこと。

### 2. モデル・コスト最適化の自発的提案
- タスクの性質に応じて、最適なモデル（Opus vs Sonnet）をClaude側からユーザーに
  **積極的に提案すること。**
  - 複雑な設計、構造の検討、バグの原因分析などの「頭脳労働」→ `/model opus`
  - 設計図が固まった後の「実装・コーディング・UI微調整」→ `/model sonnet`
- **【厳守】実装はSonnetで行う。Opusで動作している間は実装に進まないこと。**
  - ⚠️**2026-09-02に読み替えを確定**：「Sonnet」＝`claude-sonnet-5`、「Opus」＝`claude-opus-5`。
    **レースsim（`src/sim/`）の実装と計測だけは`claude-opus-5`で行う**——消耗の計算式・レース傾向の
    揺らぎ・適応能力の成長速度は「置きながら測って動かす」作業で、設計判断を含むため。
    Fable 5.1は設計・監査・調査に使い、実装には使わない（費用に見合う差が出にくい）。
  - Opusの役割は「設計・原因分析・計測・意思決定・記録」まで。設計が固まり実装フェーズに
    入る段階になったら、**自分でコードを書き始めず**、`/model sonnet`への切り替えを促して待つ。
  - ユーザーが「実装に進む」と言った場合でも、Opusで動作しているなら、まず切り替えを提案する。
- **【例外】軽微な変更はOpusのまま直してよい。**
  - **例外の範囲**：1〜2ファイル・数行程度の、**文言・定数・明らかな誤りの修正**。
    確定した文面や値が既に手元にあり、設計判断を伴わないもの。
  - ⚠️**設計判断を伴うもの・新しい関数や分岐が増えるもの・sim/バランスに触れるものは、
    行数が少なくても例外に含めない**（従来どおりSonnetへ切り替える）。
  - 例外で直した場合も、記録（`devlog/waveNN.md`）は従来どおり残すこと。

**サブエージェント（Agentツール）の併用（2026-09-03にユーザーが明示・恒久ルール）**

⚠️`/model`によるセッション本体の切り替えとは別に、**Agentツールは`model`パラメータで
Fable 5.1／Opus 5／Sonnet 5を個別に指定したサブエージェントをその場で呼び出せる**。
ユーザーにセッションの切り替えを頼まなくても、本体の作業を止めずに他モデルの力を
借りられる——この性質を**自発的に**使うこと（ユーザーから毎回頼まれるのを待たない）。

- **Fableサブエージェント**：タイトル・コピー・ネーミング・デザインの方向性など、
  **創造的な言語化・発想**が要る場面で積極的に使う（実例：`devlog/wave02.md`⑤の
  タイトル画面デザイン・タイトル文字列の検討）。実装中のSonnetセッションは止めず、
  `run_in_background: true`で並行させる。
- **Opusサブエージェント**：本体がSonnetで実装中のまま、**込み入った設計判断・
  バグの根本原因分析だけを一時的に切り出したい**場面で使う。⚠️§2冒頭の原則
  （実装はSonnet・Opusは設計/分析まで）はサブエージェントにも及ぶ——Opusサブエージェントに
  実装させず、**調査・分析結果を持ち帰らせて本体（Sonnet）が実装する**形に限る。
- **依頼の作り方**：サブエージェントは会話の文脈を持たない。ゲームの中身・関連する
  CLAUDE.mdの確定ルール・これまでの経緯（却下された案とその理由を含む）を**毎回
  プロンプトに書き切る**こと。中途半端な要約で投げると、既出の案を再提案してしまう。
  結果は本体側で`devlog/waveNN.md`に経緯として記録すること（§3・§4）。

### 3. 記録のプロセス
設計の合意や、重要な意思決定、モデルの切り替え判断が行われた際は、必ず記録すること（§4）。

### 4. ドキュメントの4ファイル制（肥大化防止・必須）

⚠️**前作は5回もスリム化が必要になった。その根本原因は「進行中は本体に詳しく書き、
終わったら圧縮する」を繰り返したこと。** 本作は最初からこの構造で運用する。

- **`DEVLOG.md`＝「新チャットが毎回読む、薄いブートストラップ」**——実装済み機能一覧・
  弾（ウェーブ）ごとの1〜3行の索引を持つ。**未着手TODOは持たない**。
- **`TODO.md`＝未着手TODO・「やらないと決めたこと」・恒常タスクの正本**。
  ⚠️分離理由：Readの切り捨ては**ファイル末尾から**起きるため、DEVLOG末尾に置くと
  最も必要なTODOが真っ先に読めなくなる（前作で実際に事故が起きた）。
  弾を締めるときの先送り項目もここへ直接書く（`devlog/waveNN.md`に「上げる」と書かない）。
- **`ARCHITECTURE.md`＝生きたアーキテクチャ参照**。現行の計算式・定数・関数名・
  「触ると何が壊れるか」の正本。履歴は書かない。仕様を変えたら同じコミットで必ず更新する。
- **`devlog/waveNN.md`＝弾ごとの詳細**。「読みたい時だけpathで直接Readする、詳細な経緯・
  実測値・却下案の記録」。

**運用ルール**
- **新しい弾に着手したら、設計・実測・実装結果の全文は最初から`devlog/waveNN.md`に書く。**
  `DEVLOG.md`側は常に短いまま：状態＋要点2〜4行＋`devlog/waveNN.md`へのリンクだけ。
  **「後で圧縮する」を前提にしない。**
- **`TODO.md`は履歴ではなくTODOである。完了した項目はチェック付きで残さず削除する。**
  番号に欠番ができるのは正常（削除済みの完了項目の跡）。
- ⚠️**弾を締める前に`grep -rn "「次のアクション」へ上げる\|TODO.mdへ上げる\|TODOへ上げる" devlog/`が
  0件であることを確認する。**「後で上げる」と書いた時点で失敗（前作で2度再発した）。
- **サイズ規律**：`DEVLOG.md`へのRead呼び出しは実測で**25,000トークンが上限**（超えると
  切り捨てられる）。日本語主体のmdは実測で**約2.5〜2.6バイト/トークン**。
  `wc -l -c DEVLOG.md`を節目ごとに確認し、**60,000バイトで整理を検討・90,000バイトで必ず
  スリム化**する。正しく運用できていれば発動しないはずのラインとして扱う。

### 5. コード構造の定期的な細分化（`src/`の肥大化防止・必須）
- ドキュメントと同じ考え方を`src/`のファイル構造にも適用する。
- **都度の意識**：新機能を実装するたびに「このロジック／このUIはどのファイルに属すべきか」を
  一度立ち止まって判断する。特定の1ファイルが肥大化の受け皿になり続けていないかを都度チェック。
- 依存は必ず下向き一方通行に保つ：`data → core/sim → domain/state/view → controllers →
  screens → app`。上位層を下位層がimportしていたら赤信号。
- `data/`・`view/`はJSXをimportしない（＝常にNode単体テストが可能な状態を保つ）。
- データ（リテラルのみの`export const`）とロジック（外部の状態変更関数を呼ぶクロージャ）を
  混同しない。後者は`data/`へ逃がさず`domain`/`logic`側に残す。
- 死んでいるコード（実コード0行のコメントブロック、未参照ファイル）は削除か`archive/`退避。
  移動は`git mv`で履歴を保全する。
- `wc -l src/**/*.js*`で定期確認し、特定ファイルが突出していたらユーザーに整理を提案する。

### 6. ドット絵アセット抽出時の左右handedness整合性チェック（必須）
⚠️**前作で長期化したバグの教訓**。「向きがおかしい」の原因はコードの反転ロジックではなく、
**抽出元の参考画像そのもの**が方向ラベル間で食い違っていたことだった。

- **参考画像から新しいポーズ／方向のコマを抽出する際は、確定・稼働済みの別コマと同じ
  参考画像シート上で、向き（馬の頭・前脚・騎手の位置）がどちら側にあるかを直接目視で
  突き合わせてから確定すること。** 抽出スクリプトを走らせて終わりにしない。
- ⚠️**数値ヒューリスティック（重心オフセット等）は信頼できないと実績で判明済み**。
  目視で追うか、参考画像と並べて直接比較する。
- 「向きがおかしい」系の報告を受けたら、コードの反転ロジックを疑う前に、
  **まず抽出元の参考画像で同じ方向ラベルのコマ同士を並べて目視比較**する。

### 7. UIテキスト・ビジュアルの作り込み（最優先・恒常タスク）
⚠️前作でユーザーが「あらゆる画面内で最優先的に改善すべき事象であり、今後も定期的に
ブラッシュアップすべき事象」と明確に位置づけた項目。機能が動くことと、ゲームとして良いUIで
あることは別問題であり、後者を後回しにしない。

**新しいUIを作る／既存UIに手を入れるたびに、以下を必ず自問すること**：
- **その情報は本当に要るか**。「あると親切かもしれない」程度の補足は、画面をごちゃつかせる
  分だけマイナス。迷ったら消す。
- **開発上の語彙をユーザーに見せていないか**。⚠️**競馬は特に用語が難しいジャンル**
  （出走投票・追い切り・馬体重・脚質・展開・斤量）。実装側の用語はもちろん、
  **画面に出す言葉は、競馬を知らない人が読んで分かる言葉だけ**にする。
- **小さい文字の長い説明で誤魔化していないか**。**レイアウト・フォント・サイズ・配置・
  ビジュアルで伝わるようにするのが本筋**で、説明文は最後の手段。
- **置き場所に工夫の余地はないか**。
- **説明文を書く前に、まず「消す」「別の見せ方にする」を検討すること。**
- ⚠️**日本語として正しいかを、書いた後に必ず読み返すこと**（2026-09-03にユーザーが指摘）。
  実例：「今日、競馬学校を出る。」——正しくは「今日、競馬学校を卒業する。」。
  **雰囲気でそれらしく見せた、文法・語法として不自然な文を書かない。**
- ⚠️⚠️**変に上手くまとめようとしない。具体性を削らない。誤魔化して抽象的にまとめない**
  （2026-09-03にユーザーが「絶対にしないように」と明示）。慣用句・体言止め・詩的な一文で
  締めるのは、書けていないことを隠す手口になる。実例として直したもの：
  「詰まるか抜けるか、紙一重です」→「前が開かなければ、そこで止まります」／
  「あとは脚。」→「あとは、この馬の脚がどれだけ残っているか。」／
  「ここから自分でレースを作ります」→「もう前をふさがれる心配はありません」／
  「府中の直線は長い。しかし夢は、もっと長い。」→（元ネタの分からない雰囲気だけの文
  だったので差し替え）。**この規律はゲーム内の文言だけでなく、ユーザーへの報告文にも同じく適用する。**

### 8. デザイン・UI・文章の変更プロセス（必須手順）

⚠️前作の反省：UIの運用が「文言を直す」ことに終始し、**レイアウト・図形・色味という土台に
一度も手を入れないまま第12弾まで来た**結果、絵文字653個・角丸パネル315枚・フォント30種類・
アクセント6色という状態になり、ユーザーから「めちゃめちゃAI臭い」と評された。

**原則**
- **デザインは足し算ではなく引き算**。情報・装飾・色・線・アイコンは、足す前に必ず
  「消せないか」を先に検討する。「あると親切かも」は足す理由にならない。
- **アイコン（絵文字）を使うのは逃げ**。絵柄で区別・強調するのは、**レイアウト・級数・余白・
  整列で階層を作る仕事の放棄**である。原則として使わない。例外はユーザーが個別に認めた
  ごく少数に限り、統一ストローク幅の単色SVGを自作する。
- **初見の人が読んで違和感のない言葉だけを使う**。このゲーム内でしか通じない造語も避ける。
- **縦線（垂直の線）は使用禁止**（前作でユーザーが明示した恒久ルール）。カード左端の色レール・
  縦の仕切り線などを引かない。区切りは余白・面の明暗・横罫線で作る。図形そのものの縦成分
  （馬柱の枠、グラフの軸）はこの禁止の対象外。

**手順（省略禁止）**
1. **サンプル候補を複数提示する**（実際に目で見える形で。テキストモック／HTML等）
2. **ユーザーが選択し、編集の指示を出す**
3. **2を、合意に達するまで繰り返す**
4. **合意後に初めて実装に入る**（実装はSonnet）

**「良さそうなので作ってみました」は禁止。作る前に見せる。**

**設計の具体性（必須）**——候補を作る前に必ず次を個別に確定させること：
- **画面内の要素を「〜など」で済ませず全て列挙する**
- **各データ項目について、取得元（具体的な関数名・フィールド名・プロパティパス）を明記する**
- **画面内のレイアウト順序・グルーピング・優先順位を明記する**
- **空・0・未達成などの状態（empty state）の挙動を定義する**
- **モックアップは要約テキストではなく実際のゲーム内の実データ・実文言をそのまま使う**

### 9. 配色ルール
- **`accent`＝データ強調専用**。数値・見出し・状態語に使う。「読むもの」の色。
- **`action`＝操作専用**。主ボタン（1画面に1つが想定）・選択中のメニュー・リンク。「押すもの」の色。
- **同じ色を両方の役割で兼任させない**（前作でこれが読むものと押すものを見分けられなく
  していた元凶だった）。
- `good`／`bad`は結果の良し悪しの判定にのみ使い、強調やアクセントの代用にしない。
- **新しい色を足すときは、その色に「他の色にはできない仕事」を1つ以上事前に割り当てる。**
  役割のない色はただの装飾に戻る。
- 色相は既存の意味色すべてからHSLの色相差で40°以上離す。
- **データ層の意味色（脚質・馬場状態・勝負服など実データが持つ色）とUIクロームのアクセント色は
  別レイヤーとして扱う。**

### 10. バランス計測は機能追加とセットで行う（必須）

⚠️**前作の実績が裏付けている**——「出走計画」は計測せずに完了し宿題として残り、
「機材で毎年常勝」は優勝率92%が誰にも気付かれず蓄積し、「早期カンスト再発」は
埋もれた。いずれも「機能を足す仕事」と「その機能が数値をどう動かすか測る仕事」を
分離した結果である。

- **機能を追加・拡張するときは、その変更が動かす数量を1つ以上特定し、同じ弾の中で
  before/afterを測る。「後で測る」は禁止**——実績として測られたことがない。
- 典型：抽選プールへ要素を足した→各要素が引かれる確率／獲得を速める要素→到達までの平均月数／
  倍率を足した→フル投資時の累積値と上限／枠を広げた→何が飽和するか。
- ⚠️**測った結果は、数値が動かなかった場合も含めて記録する。**「動かなかった」は次に同じ
  場所を触る人にとって最も価値のある情報である。
- ⚠️**測定の結果その弾で直しきれないと判断したら、`TODO.md`へ必ず上げる。**
- ⚠️**「UIだと思っていたら数値が動いていた」ケースに注意。**

### 11. 外部知見の参照（`DESIGN_PRINCIPLES.md`）
- 外部の記事・動画から取り込んだゲームデザインの知見は、ルートの`DESIGN_PRINCIPLES.md`
  （薄い索引）＋`design/`配下の個別ノート（全文級の詳細）に収蔵する。
- **読むタイミング**：§1の設計フェーズ・§10のバランス調整・§7/§8のUI変更に入る前に、
  索引を見て関係する`design/*.md`をReadする（全部を毎回読む必要はない）。
- **新しい記事を取り込むときは`design/`へ個別ノートを追加し、`DESIGN_PRINCIPLES.md`に索引を
  1行足す。このファイル（CLAUDE.md）には足さない**——毎チャット読み込まれて肥大化するため。

### 12. 前作からの参照方法
`Roadrace_Game`をセッションに接続している場合、以下を「実装済みの設計パターン」として
参照できる（⚠️コードのコピーはしない。考え方と失敗の記録を読む）：
- `Roadrace_Game/ARCHITECTURE.md` — レースsim（先計算→再生・tick・`resumeSim`によるフォーク）、
  配合（ニック／インブリード／系統確立／因子／レシピ）、マスク能力、絆の設計
- `Roadrace_Game/devlog/wave95.md` — 面白さの総点検。**同じ穴に落ちないための必読**
- `Roadrace_Game/tools/` — ヘッドレスシミュレータ／判断カードEV計測／テレメトリの設計思想
